<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>NarrowGate：从 Maker Quote EV 到 C++ Tick Replay 的低延迟研究框架</title>
    <link href="/2026/06/19/NarrowGate-Cpp-Low-Latency-Market-Making/"/>
    <url>/2026/06/19/NarrowGate-Cpp-Low-Latency-Market-Making/</url>
    
    <content type="html"><![CDATA[<h2 id="TL-DR：读者收益摘要"><a href="#TL-DR：读者收益摘要" class="headerlink" title="TL;DR：读者收益摘要"></a>TL;DR：读者收益摘要</h2><p><strong>NarrowGate</strong> 不是一个“C++ 高频做市系统已经跑通并盈利”的项目，而是一个用于验证 maker quote EV 的研究框架。它的核心不是证明某个做市策略有效，而是建立一套让错误结论更难通过的 maker 研究系统。</p><p>读完这篇文章，至少可以得到五个结论：</p><ol><li>maker fill 不能简单等价于 spread 收益，必须用 fill 后 markout 和 inventory exposure 衡量；</li><li>多市场 reference 有信息量，但不等于统一打开 <code>multi_market</code> 就有正 EV；</li><li>tick replay、queue ahead、latency、TTL 和 maker fill gate 会显著改变 bar 回测结论；</li><li>C++ 的价值不是替代 Python 写策略，而是把 replay、feature state、quote core 和 live hot path 中稳定、高频、低基数的部分收紧边界；</li><li>当前项目没有宣称“搞定做市终局”：动态到达率、主动对冲、多 venue lead-lag 和 HFT 级线程模型仍然是明确的未完成边界。</li></ol><p>换句话说，C++ 没有替项目找到策略答案。它做的是更有用的事：让错误答案更快暴露，让好看的数字更难逃过验证。</p><p><strong>NarrowGate</strong> 是我最近一段时间一直在做的 maker/被动做市研究项目。它不是从“我要写一个 C++ 高频系统”开始的，而是从一个更朴素的问题开始：<strong>当市场有人急于成交时，站在被动一侧提供流动性，究竟是在收取 spread，还是在替更快的信息交易者接风险？</strong></p><p>最初的实现只是一个基于 Avellaneda-Stoikov 模型的 Python 研究框架。随后，为了检验这个问题，项目才逐步加入逐笔成交、原生 L2 盘口、LightGBM、BTCUSDT 永续 reference、BTCUSDC/BTCUSDT spot anchor、逐侧 quote EV、库存时间风险，以及 Python/C++ 双引擎回放。也就是说，C++ 是研究越做越深之后出现的工程结果，不是项目的前提。</p><p>先把阅读边界说清楚：本文只从 C++ 低延时工程、市场微观结构、价格行为学、数据质量和回测方法的角度分析 NarrowGate，不讨论也不建议任何真实交易行为。文中的 PnL、fill、markout、A/B 都是研究指标，用来验证模型假设、执行口径和系统性能，而不是收益承诺。</p><p>一路做下来，真正困难的部分并不是把某个公式写出来，而是连续回答几个会推翻直觉的问题：</p><ul><li>一笔被动挂单获得的究竟是 spread 补偿，还是承担信息风险后的偶然结果？</li><li>新增一个数据源，为什么可能让策略更差，而不是更健壮？</li><li>历史数据中缺一小时、缺一次 orderbook snapshot，会怎样污染 rolling feature、label 和回测？</li><li>回测比事件引擎“更聪明”时，漂亮的结果还有多少可信度？</li><li>C++ 到底应该负责什么，才能既加快研究，又不制造第二套策略？</li><li>为什么 live 路径里“把单个公式搬到 C++”反而会变慢，而把 quote、policy、routing 的数据边界收紧后才开始变快？</li><li>为什么 WebSocket、REST、日志和健康检查不该急着迁到 C++，真正的热路径反而是 WebSocket 上方的 rolling state 和决策包？</li></ul><p>这篇文章想把这些前因后果完整记录下来，但不把算法和工程混在一起讲。后文按五层展开：</p><ol><li><strong>Maker 量化算法与数据假设</strong>：先解释 NarrowGate 在研究什么，AS/GLFT/quote EV 如何组成报价框架，以及为什么数据源和 label 连续性会决定结论可信度；</li><li><strong>从算法验证到工程问题</strong>：再解释为什么更严格的 tick replay、A/B 和 parity 会把问题自然推到系统吞吐量上；</li><li><strong>Python/C++ 技术实现</strong>：然后进入 C++，讨论哪些状态机、批处理和 live hot path 值得迁移，哪些不该迁；</li><li><strong>从原理到实操的桥</strong>：把参数映射、queue ahead、shadow gate、Python/C++ 胶水层、fixed-array state 和 live 容错讲成可执行规则；</li><li><strong>已完成与未完成边界</strong>：最后把金融 Alpha 终局和低延迟系统终局的问题单独摊开，说明哪些已经完成、哪些只是部分完成、哪些还没有完成。</li></ol><p>这样读下来，C++ 不是突然闯进来的“性能炫技”，而是前面算法验证不断收紧之后产生的工程压力。</p><h2 id="阅读路线：先算法，再工程"><a href="#阅读路线：先算法，再工程" class="headerlink" title="阅读路线：先算法，再工程"></a>阅读路线：先算法，再工程</h2><p>如果只看现在的代码目录，很容易以为 NarrowGate 是一个“AS + LightGBM + C++”的技术拼盘。实际演进顺序恰好相反：每一层都是上一层暴露问题后的修正。</p><div class="table-container"><table><thead><tr><th>阶段</th><th>最初假设</th><th>真实问题</th><th>后续修正</th></tr></thead><tbody><tr><td>AS 与 1s-bar 原型</td><td>spread、波动和库存足以描述报价</td><td>bar touch 无法表示 maker 排队、延迟和逆向选择</td><td>引入逐笔回放、BBO/L2、queue ahead 和 maker fill gate</td></tr><tr><td>原生盘口接入</td><td>有文件就能重建盘口</td><td>CryptoHFTData 存在缺小时、低 snapshot coverage；gap 会污染 rolling 和未来 label</td><td>audit 成为硬边界，物理清理坏数据，并统一 continuous segment / horizon guard</td></tr><tr><td>BTCUSDT reference</td><td>高流动性参考市场应提升收益</td><td>reference 同时包含信息冲击和已被本地吸收的流动性冲击，统一开关会误杀机会</td><td>由布尔开关转向 shock attribution 和逐侧 quote EV</td></tr><tr><td>enhanced spot</td><td>更多市场能让模型更健壮</td><td>特征有信息不等于 policy 有正 EV，bid/ask 响应还不对称</td><td>重建 spot features、逐月 A/B、按 side/regime 验证</td></tr><tr><td>quote EV</td><td>ask 侧 raw PnL 改善即可晋级</td><td>有效 fill 太少，校准和 30s markout gate 不通过</td><td>strict promotion gates、walk-forward 扩样、shadow/research 定位</td></tr><tr><td>多臂 tick A/B</td><td>更严格的验证只要多跑几次</td><td>Python replay 与逐行 quote context 让完整月份实验过慢</td><td>C++ batch quote core、tick replay 状态机、window cache 与双引擎 parity</td></tr><tr><td>live C++ 优化</td><td>native 代码一定比 Python 快</td><td>每 tick <code>dict/dataclass/asdict</code> 往返比 quote 数学还贵，WebSocket/REST 不在 CPU 热点</td><td>cached config、fixed tuple、compact context、routing bridge、fixed-array signal</td></tr></tbody></table></div><p>整个研究链路可以概括成：</p><p><img src="/images/narrowgate/maker-hot-path.svg" alt="NarrowGate maker live hot path"></p><div class="code-wrapper"><pre><code class="hljs text">Binance Vision / CryptoHFTData        │        ▼data audit + physical cleanup + gap/horizon guard        │        ▼1s bars + BBO/L2 + reference/spot features        │        ├──► 主 LightGBM：ret / vol / toxicity 等状态预测        │        ▼AS quote core + side policy + inventory controls        │        ▼tick replay：queue / latency / TTL / maker fill gate        │        ▼quote trace + fill markout + cross-market shock attribution        │        ▼quote EV labels/model + monthly A/B + strict gates</code></pre></div><p>这条链路很重要，因为 quote EV 并不是从原始行情直接训练出来的万能模型。它依赖上游报价轨迹、成交路径、未来 markout 和 cross-market context；任何一层的数据边界或执行口径出错，最后得到的 AUC 和 PnL 都可能只是被污染后的精确数字。</p><p>这也是为什么数据源需要先分层，而不是全部丢进一个 dataframe。NarrowGate 里至少有四类数据：</p><p><img src="/images/narrowgate/data-lineage.svg" alt="NarrowGate research data lineage"></p><div class="table-container"><table><thead><tr><th>数据层</th><th>主要来源</th><th>在项目中的角色</th><th>最容易出错的地方</th></tr></thead><tbody><tr><td>execution trades</td><td>Binance Vision <code>daily/aggTrades</code>，perp 与 spot</td><td>构造 1s/10s flow、taker tempo、spot/reference return</td><td>月度容器混入坏日期；日度缺口会污染 rolling 和 future label</td></tr><tr><td>execution orderbook</td><td>CryptoHFTData 原生 hourly orderbook，重放为 daily <code>bbo/l2</code></td><td>tick replay 的 BBO、top-N L2、queue ahead 和执行层 microprice</td><td>小时文件缺失，或文件存在但 snapshot coverage 太低</td></tr><tr><td>slow market context</td><td>Binance OI、long/short ratio、旧 <code>bookDepth</code> 百分比桶</td><td>慢变量和历史 proxy，不作为精确 L2 撮合依据</td><td><code>bookDepth</code> 是百分比聚合桶，不等价于运行时 top-20 depth</td></tr><tr><td>cross-market anchors</td><td>BTCUSDT perp、BTCUSDT spot、BTCUSDC spot</td><td>区分本地流动性冲击和更广泛的信息重定价</td><td>多个 Binance 市场高度相关，不能伪装成独立 venue consensus</td></tr></tbody></table></div><p>因此本文里的“数据清理”不是普通 ETL 卫生问题，而是模型定义的一部分。maker 的 label 常常是条件事件：先有候选 quote，再看是否 fill，fill 后再看 1s/5s/30s mid。只要 orderbook 缺口或长 gap 横跨这个链条，<code>P(fill)</code>、<code>E(markout | fill)</code> 和库存风险都会被误标。</p><p>为了避免后面术语来回跳，这里先给一个小词典。它不是交易教材，只是本文后续代码和公式里的统一含义：</p><div class="table-container"><table><thead><tr><th>术语</th><th>在 NarrowGate 里的含义</th></tr></thead><tbody><tr><td>maker</td><td>挂被动限价单，等待别人主动成交；不是追着价格打单，而是在盘口提供流动性</td></tr><tr><td>taker</td><td>主动吃掉盘口流动性的人；他的成交方向常被用来估计短期 flow pressure</td></tr><tr><td>BBO</td><td>best bid / best ask，即当前最优买一和卖一</td></tr><tr><td>mid</td><td><code>(best_bid + best_ask) / 2</code>，最简单的中间价</td></tr><tr><td>microprice</td><td>用买卖一深度加权后的中间价；它不是预测模型，只是表达盘口哪侧更薄</td></tr><tr><td>spread edge</td><td>maker 买在 bid、卖在 ask 所获得的价格让步；这是毛收益来源，不等于最终收益</td></tr><tr><td>queue ahead</td><td>自己挂单前方同价位或更优价位的可见数量；价格碰到不代表一定轮到自己成交</td></tr><tr><td>markout</td><td>fill 后若干秒的价格变化；用来问“这笔成交后来是不是变成了坏成交”</td></tr><tr><td>maker-signed markout</td><td>按 maker 方向归一后的 markout；买入后上涨为正，卖出后下跌为正</td></tr><tr><td>adverse selection</td><td>成交后价格向不利方向移动；直觉上就是“我被更快或更有信息的一方打到了”</td></tr><tr><td>toxicity</td><td>某侧成交后出现 adverse markout 的概率或强度估计</td></tr><tr><td>quote EV</td><td>不是预测价格涨跌，而是问“这一侧、这个价格、这一笔被动挂单，成交之后是否值得”</td></tr><tr><td>TTL / cooldown</td><td>挂单最长存活时间 / 成交后短时间限制同侧继续暴露</td></tr></tbody></table></div><h2 id="第一部分：Maker-量化算法与数据假设"><a href="#第一部分：Maker-量化算法与数据假设" class="headerlink" title="第一部分：Maker 量化算法与数据假设"></a>第一部分：Maker 量化算法与数据假设</h2><p>这一部分只讨论策略研究本身：maker 订单为什么有条件 EV，AS/GLFT 公式如何提供报价坐标系，数据源和 quote labels 为什么必须经过连续性校验。暂时不谈 C++，因为如果算法假设和数据边界没立住，任何优化都只是更快地产生错误结论。</p><h3 id="1-1-为什么叫-NarrowGate"><a href="#1-1-为什么叫-NarrowGate" class="headerlink" title="1.1 为什么叫 NarrowGate"></a>1.1 为什么叫 NarrowGate</h3><p>NarrowGate 取自“窄门”的意象。对 maker 策略来说，市场里的机会并不是越多越好。每一个 tick 都可以触发报价，但真正值得暴露的窗口必须经过几道门：</p><ol><li>当前 spread 是否提供了足够的流动性补偿；</li><li>报价相对 mid/BBO 的位置是否合理；</li><li>主动成交和盘口压力是短期流动性冲击，还是信息驱动的重定价；</li><li>reference perp 与 spot 是否确认了这个变化；</li><li>库存、挂单 TTL 和撤单频率是否仍在风险预算内。</li></ol><p>因此 NarrowGate 不是“预测涨跌然后追单”的趋势策略。它研究的是被动限价单：当别人为了立刻成交而付出成本时，maker 是否值得站在另一边接住这笔流动性。</p><p>这里最关键的分界是：</p><ul><li><strong>流动性冲击</strong>：某一侧突然有主动成交，但价格影响短暂，其他市场没有持续确认，本地盘口可能很快恢复；</li><li><strong>信息冲击</strong>：BTCUSDT perp、spot 或更广泛的盘口同时重定价，此时在旧价格继续挂单，往往是在给更快的信息交易者提供退出流动性。</li></ul><p>所以每一侧 quote 都应该被看作一个条件 EV 问题：</p><script type="math/tex; mode=display">\operatorname{EV}_{\text{side}}(x)\approxP(\operatorname{fill}\mid x)\left(  \operatorname{spread}_{\text{edge}}  + \mathbb{E}[\operatorname{markout}\mid \operatorname{fill}, x]\right)- \operatorname{adverse}_{\text{tail}}(x)- \operatorname{inventory}_{\text{cost}}(q, x)</script><p><code>P(fill)</code> 高不一定是好事。一个几乎必然成交、但成交后价格立刻向不利方向移动的报价，可能比完全不成交更糟。</p><p>把 maker 的一次决策写成伪代码，大概是下面这样。真实系统当然有更多异常处理、日志和 REST 细节，但热路径上的逻辑并不神秘：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">on_requote_tick</span>(<span class="hljs-params">state, market, models, cfg</span>):</span>    <span class="hljs-comment"># 1. 更新短周期市场状态：mid、BBO、depth、trade flow、reference/spot</span>    signal = signal_engine.compute(        exec_trades=market.btcusdc_trades,        exec_book=market.btcusdc_l2,        ref_perp=market.btcusdt_perp,        exec_spot=market.btcusdc_spot,        ref_spot=market.btcusdt_spot,    )    <span class="hljs-comment"># 2. 主模型只回答状态问题：短期 ret / vol / toxicity</span>    pred = models.main.predict(signal.feature_vector)    <span class="hljs-comment"># 3. AS quote core 给出报价坐标系</span>    quote = compute_quote_core(        mid=market.mid,        inventory=state.position,        sigma_sq=pred.vol_10s,        ret_10s=pred.ret_10s,        tox_bid=pred.tox_bid,        tox_ask=pred.tox_ask,        depth=market.l2_topN,        cfg=cfg.quote,    )    <span class="hljs-comment"># 4. side policy 决定每一侧是否值得暴露</span>    bid_policy = build_side_policy(        side=<span class="hljs-string">&quot;bid&quot;</span>,        quote_ctx=quote.bid_ctx,        inventory=state.position,        fill_cooldown=state.fill_cooldown,        quote_ev=models.bid_quote_ev,    )    ask_policy = build_side_policy(        side=<span class="hljs-string">&quot;ask&quot;</span>,        quote_ctx=quote.ask_ctx,        inventory=state.position,        fill_cooldown=state.fill_cooldown,        quote_ev=models.ask_quote_ev,    )    <span class="hljs-comment"># 5. routing 只做本次订单动作：保留、撤单、换价、新挂</span>    decision = route_orders(        quote=quote,        bid_policy=bid_policy,        ask_policy=ask_policy,        live_orders=state.live_orders,        filters=market.exchange_filters,    )    <span class="hljs-keyword">return</span> decision</code></pre></div><p>这段伪代码也解释了为什么 live C++ 优化不能只盯着某一个公式。<code>compute_quote_core()</code> 的浮点数学很短；真正反复发生的是 feature state、quote context、side policy 和 routing decision 在 Python 对象之间来回搬运。C++ 如果只接住第 3 步，却每次都要复制完整 dataclass/dict，就会被边界成本反噬；如果把第 3 到第 5 步中稳定的计算和固定字段打包，才可能降低尾延迟。</p><h3 id="1-2-从-AS-报价骨架开始"><a href="#1-2-从-AS-报价骨架开始" class="headerlink" title="1.2 从 AS 报价骨架开始"></a>1.2 从 AS 报价骨架开始</h3><p>项目最早使用经典 Avellaneda-Stoikov 做市模型。它给出了两个很有用的结构：reservation price 和最优 spread。</p><script type="math/tex; mode=display">r(s,q,t)=s-q\gamma\sigma^2(T-t)</script><script type="math/tex; mode=display">\delta=\gamma\sigma^2(T-t)+\frac{2}{\gamma}\ln\left(1+\frac{\gamma}{\kappa}\right)</script><script type="math/tex; mode=display">\operatorname{bid}=r-\frac{\delta}{2},\qquad\operatorname{ask}=r+\frac{\delta}{2}</script><p>其中 <code>q</code> 是库存，<code>sigma</code> 是短期波动，<code>kappa</code> 描述成交概率随报价距离衰减的速度。直觉上，库存偏多时 reservation price 下移，报价会更愿意卖出、更不愿继续买入；波动上升时 spread 变宽，以补偿更高的逆向选择风险。</p><p>这几个式子背后的论文假设可以压缩成三步。</p><p>第一步，Avellaneda-Stoikov 假设中间价近似服从扩散过程：</p><script type="math/tex; mode=display">dS_t=\sigma\,dW_t</script><p>做市商持有库存 <code>q</code>，财富受现金和库存市值共同影响，并使用 CARA utility：</p><script type="math/tex; mode=display">U(x)=-\exp(-\gamma x)</script><p>在这个设定下，多持有一单位库存会暴露未来价格方差，CARA + Brownian price 的风险惩罚项近似为：</p><script type="math/tex; mode=display">\operatorname{inventory\ penalty}\approx q\gamma\sigma^2(T-t)</script><p>于是 reservation price 可以理解成“愿意用来给库存做无差异估值的价格”：</p><script type="math/tex; mode=display">r=s-q\gamma\sigma^2(T-t)</script><p>第二步，论文用一个很简洁的到达率假设描述“报价离中间价越远，越不容易成交”：</p><script type="math/tex; mode=display">\lambda(\delta)=A\exp(-\kappa\delta)</script><p>在 CARA utility 下最大化一次被动成交的效用增量，会得到每一侧报价距离中的 log 项：</p><script type="math/tex; mode=display">\operatorname{edge\ term}=\frac{1}{\gamma}\ln\left(1+\frac{\gamma}{\kappa}\right)</script><p>再叠加库存持有风险，得到总 spread：</p><script type="math/tex; mode=display">\operatorname{total\ spread}=\gamma\sigma^2(T-t)+\frac{2}{\gamma}\ln\left(1+\frac{\gamma}{\kappa}\right)</script><p>第三步，GLFT/Guéant 体系把“指数到达率”推广成更一般的强度函数 <code>Lambda(delta)</code>。这时不必假设所有市场都满足 <code>A exp(-kappa delta)</code>，而是通过 Hamiltonian 写成：</p><script type="math/tex; mode=display">H(p)=\sup_{\delta}\Lambda(\delta)\left(1-\exp\left[-\gamma(\delta-p)\right]\right)</script><p>指数强度只是一个特例；如果真实 order arrival 对报价距离的衰减不是纯指数，就应该校准 <code>Lambda</code> 或等价的 <code>kappa_eff</code>，而不是迷信一个固定 <code>kappa</code>。NarrowGate 里保留 AS 的骨架，是因为它给出了稳定、可解释的库存坐标系；但具体的 <code>sigma</code>、<code>kappa</code>、depth half-spread、adverse widening 和 quote EV gate 都要由数据重新验证。</p><p>实际系统里，AS 公式只是报价骨架。NarrowGate 的 quote core 更接近下面这个组合：</p><p><img src="/images/narrowgate/quote-ladder.svg" alt="NarrowGate quote ladder"></p><script type="math/tex; mode=display">\operatorname{mid}=\frac{b_{\text{best}}+a_{\text{best}}}{2}</script><script type="math/tex; mode=display">\operatorname{microprice}=\frac{a_{\text{best}}Q_b+b_{\text{best}}Q_a}{Q_b+Q_a}</script><script type="math/tex; mode=display">\operatorname{fair}=\operatorname{mid}+\Delta_{\text{microprice}}+\Delta_{\text{model ret}}+\Delta_{\text{cross-market}}</script><script type="math/tex; mode=display">\operatorname{inventory\ cost}=q\gamma\sigma^2\tau,\qquadr=\operatorname{fair}-\operatorname{inventory\ cost}</script><script type="math/tex; mode=display">h_{\text{AS}}=\frac{1}{2}\left[\gamma\sigma^2\tau+\frac{2}{\gamma}\ln\left(1+\frac{\gamma}{\kappa}\right)\right]</script><script type="math/tex; mode=display">h=\max(h_{\text{AS}},h_{\text{depth}},h_{\text{fee floor}})\cdot m_{\text{regime}}\cdot m_{\text{adverse/defense}}</script><script type="math/tex; mode=display">\begin{aligned}\operatorname{bid}_{\text{candidate}} &= r-h+\Delta_{\text{bid side}},\\\operatorname{ask}_{\text{candidate}} &= r+h+\Delta_{\text{ask side}}.\end{aligned}</script><script type="math/tex; mode=display">\begin{aligned}\operatorname{final\ bid}&=\operatorname{floor\_to\_tick}\left(\min(\operatorname{bid}_{\text{candidate}},b_{\text{best}})\right),\\\operatorname{final\ ask}&=\operatorname{ceil\_to\_tick}\left(\max(\operatorname{ask}_{\text{candidate}},a_{\text{best}})\right).\end{aligned}</script><p>这里最容易误解的是 <code>microprice</code>。如果买一数量很厚、卖一很薄，<code>microprice</code> 会向卖一侧偏移，暗示向上打穿更容易；反过来则向买一侧偏移。它不是预测模型，而是盘口当前形状给出的局部压力。</p><p>把 microprice 写成这个形式，也可以从“下一笔成交更可能打哪一侧”来理解。若 <code>bid_qty</code> 远大于 <code>ask_qty</code>，卖一更薄，价格向上移动的局部概率更高，于是 microprice 靠近 <code>best_ask</code>：</p><script type="math/tex; mode=display">\operatorname{microprice}-\operatorname{mid}=\frac{1}{2}\operatorname{spread}\cdot\frac{Q_b-Q_a}{Q_b+Q_a}</script><p>这也是为什么它只能作为局部盘口压力，而不能替代未来收益模型。一个厚 bid 可能是支撑，也可能只是即将撤掉的虚假流动性；真正进入策略时，还必须经过 freshness、depth coverage、reference confirmation 和 fill 后 markout 验证。</p><p>side policy 则在候选价格之后再做一次门控：</p><div class="code-wrapper"><pre><code class="hljs text">allow_bid =    fresh_book    and not bid_adverse_pause    and not bid_local_extreme_pause    and inventory &lt; max_inventory    and not fill_cooldown_blocks_bidallow_ask =    fresh_book    and not ask_adverse_pause    and not ask_local_extreme_pause    and inventory &gt; -max_inventory    and not fill_cooldown_blocks_ask</code></pre></div><p>当库存偏多时，bid 是增加风险的一侧，ask 是降低风险的一侧；库存偏空时反过来。因此同一个 adverse guard 对 bid/ask 的含义并不对称，这也是 quote EV 必须按 side 训练和评估的原因。</p><p>但真实盘口不会只服从这几个变量。因此项目后来逐步加入：</p><ul><li>microprice 与盘口 imbalance；</li><li>动态 kappa 和近端深度；</li><li>fee-aware spread floor；</li><li>LightGBM 的波动率、收益和 toxicity 预测；</li><li>CJP 风格库存偏移和库存衰减；</li><li>adverse guard、defense guard 与逐侧暂停；</li><li>adaptive/fragile TTL 与 cooldown；</li><li>BTCUSDT reference perp、BTCUSDC/BTCUSDT spot；</li><li>quote EV 对 fill probability、markout bucket 和 extreme adverse 的建模。</li></ul><p>AS 在这里不是一个直接给答案的“万能公式”，而是稳定的报价坐标系。机器学习和策略 guard 不应该随意取代这个坐标系，而应该回答更局部的问题：这一侧现在是否值得挂、要离 BBO 多远、该暴露多久、库存代价是否已经过高。</p><p>主模型和 quote EV 在这里扮演不同角色。主 LightGBM 描述市场状态，例如短期收益、波动与 toxicity；quote EV 则站在某一笔候选报价的角度，估计“是否会成交、成交后是否有利、尾部是否危险”。前者回答市场正在发生什么，后者回答这一侧是否值得承担一次具体的 maker 暴露。把二者混成一个方向预测器，反而会丢掉 maker 最关心的条件性。</p><p>项目也因此从早期 1s-bar 回测逐渐转向 tick replay。bar 回测适合快速检查公式和参数方向，但只知道价格是否触及；maker 是否真的成交，还取决于挂单前方的队列、订单到达时间、撤单延迟和同价位主动成交量。没有这些状态，回测很容易把“价格碰到过”错误地当成“我成交了”。</p><p>简化后的 queue/fill 逻辑可以写成：</p><p><img src="/images/narrowgate/order-lifecycle.svg" alt="NarrowGate maker order lifecycle"></p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">process_aggressive_trade</span>(<span class="hljs-params">order, trade</span>):</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> order.is_live:        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span>    <span class="hljs-comment"># BUY maker order fills only when sell aggressor trades at or below bid.</span>    <span class="hljs-comment"># SELL maker order fills only when buy aggressor trades at or above ask.</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> trade.crosses(order.side, order.price):        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span>    remaining = trade.qty    <span class="hljs-comment"># Visible queue ahead must be consumed before our order can fill.</span>    eaten = <span class="hljs-built_in">min</span>(order.queue_left, remaining)    order.queue_left -= eaten    remaining -= eaten    <span class="hljs-keyword">if</span> remaining &lt;= <span class="hljs-number">0</span>:        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span>    fill_qty = <span class="hljs-built_in">min</span>(order.remaining_qty, remaining)    order.remaining_qty -= fill_qty    <span class="hljs-keyword">return</span> Fill(side=order.side, price=order.price, qty=fill_qty)</code></pre></div><p>成交后再按 maker 方向计算 markout：</p><script type="math/tex; mode=display">\operatorname{markout}_{\text{buy}}(h)=\operatorname{mid}(t+h)-p_{\text{fill}}</script><script type="math/tex; mode=display">\operatorname{markout}_{\text{sell}}(h)=p_{\text{fill}}-\operatorname{mid}(t+h)</script><p>这个定义让“对 maker 有利”的成交质量统一为正值。比如买入后价格上涨是正 markout，卖出后价格下跌也是正 markout。quote EV 和 adverse guard 关心的不是成交本身，而是 <code>fill</code> 条件下未来 markout 是否补偿了库存和尾部风险。</p><h3 id="1-3-第一场真正的麻烦来自数据，而不是模型"><a href="#1-3-第一场真正的麻烦来自数据，而不是模型" class="headerlink" title="1.3 第一场真正的麻烦来自数据，而不是模型"></a>1.3 第一场真正的麻烦来自数据，而不是模型</h3><p>项目的数据主要来自两类来源：</p><ul><li>Binance Vision 的 aggTrades、bookDepth、metrics，以及后续补充的 spot 数据；</li><li>CryptoHFTData 的原生 orderbook 数据，用于重建 BBO 和 top-N L2。</li></ul><p>最开始很容易产生一种错觉：文件存在，就代表这一小时的数据可用。实际并不是这样。CryptoHFTData 中出现过两种更隐蔽的问题：</p><ol><li>某些小时文件直接缺失；</li><li>文件存在，但内部缺少足够的 orderbook snapshot，无法重建可靠的盘口状态。</li></ol><p>如果这些日期继续进入训练和回测，问题不只是一段时间“没有成交”。maker 的 order lifetime 可能很短，日与日之间离散本身未必严重；真正危险的是 rolling feature 或未来 label 跨过了坏日期和长 gap。</p><p>例如一个 30 秒 markout label，如果第 30 秒对应的价格来自长时间缺口之后，那么这个 label 在数学上有值，在市场含义上却已经失效。rolling volatility、taker tempo、cross-market return 也可能把两个不连续的市场状态拼在一起。</p><p>因此后来做了三层修正。</p><h4 id="1-3-1-审计结果成为硬边界"><a href="#1-3-1-审计结果成为硬边界" class="headerlink" title="1.3.1 审计结果成为硬边界"></a>1.3.1 审计结果成为硬边界</h4><p>orderbook audit 中不可用或低 coverage 的日期不再只是报告里的警告，而是进入统一的 <code>data_quality.py</code>。训练、回测、quote trace、shock audit 和 quote EV 都必须二次过滤，不能依赖某一个上游脚本“碰巧已经删过”。</p><h4 id="1-3-2-物理数据也同步清理"><a href="#1-3-2-物理数据也同步清理" class="headerlink" title="1.3.2 物理数据也同步清理"></a>1.3.2 物理数据也同步清理</h4><p>坏日期对应的 CryptoHFTData 和 Binance raw 数据被物理删除。由于 Binance 月度压缩包混合了好日期与坏日期，不能只在月度容器里删掉某一天，所以改为：</p><ol><li>删除覆盖坏日期的月度 aggTrades 容器；</li><li>从 Binance Vision 按日重新下载 retained good days；</li><li>验证坏日期日度文件命中为 0，月度容器残留为 0。</li></ol><p>这样既避免磁盘继续保留不可用数据，也让物理层和逻辑层使用同一份质量边界。2026-06-06 的清理最终保留了 375 个好日期日度文件、删除 23 个月度容器，并验证坏日期命中与月度容器残留都为 0。</p><h4 id="1-3-3-rolling-与-label-使用同一套连续段定义"><a href="#1-3-3-rolling-与-label-使用同一套连续段定义" class="headerlink" title="1.3.3 rolling 与 label 使用同一套连续段定义"></a>1.3.3 rolling 与 label 使用同一套连续段定义</h4><p>项目增加了统一的：</p><div class="code-wrapper"><pre><code class="hljs python">continuous_segment_ids(index, max_gap_s=<span class="hljs-number">5</span>)mask_valid_horizon(index, horizon_s, max_gap_s=<span class="hljs-number">5</span>)</code></pre></div><p>这两个函数不是简单的 <code>dropna</code>。连续段要同时切断无效时间戳、时间倒退和超长间隔；future label 则先用 <code>searchsorted</code> 找到目标 horizon 的第一条观测，再验证它仍处于同一 segment，并且没有晚于目标太多。当前 Python fallback 的核心逻辑如下（省略时间戳类型兼容代码）：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">continuous_segment_ids</span>(<span class="hljs-params">index_like, max_gap_s=<span class="hljs-number">5.0</span></span>):</span>    ts = _coerce_utc_timestamps(index_like)    ns = ts.view(<span class="hljs-string">&quot;int64&quot;</span>)    invalid = np.asarray(ts.isna(), dtype=<span class="hljs-built_in">bool</span>)    breaks = np.zeros(<span class="hljs-built_in">len</span>(ts), dtype=<span class="hljs-built_in">bool</span>)    breaks[<span class="hljs-number">0</span>] = <span class="hljs-literal">True</span>    <span class="hljs-keyword">if</span> <span class="hljs-built_in">len</span>(ts) &gt; <span class="hljs-number">1</span>:        delta = ns[<span class="hljs-number">1</span>:].astype(<span class="hljs-built_in">float</span>) - ns[:-<span class="hljs-number">1</span>].astype(<span class="hljs-built_in">float</span>)        breaks[<span class="hljs-number">1</span>:] = (            invalid[:-<span class="hljs-number">1</span>]            | invalid[<span class="hljs-number">1</span>:]            | (delta &lt; <span class="hljs-number">0.0</span>)            | (delta &gt; max_gap_s * <span class="hljs-number">1e9</span>)        )    <span class="hljs-keyword">return</span> np.cumsum(breaks, dtype=np.int64) - <span class="hljs-number">1</span><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">mask_valid_horizon</span>(<span class="hljs-params">index_like, horizon_s, max_gap_s=<span class="hljs-number">5.0</span></span>):</span>    ts = _coerce_utc_timestamps(index_like)    ns = ts.view(<span class="hljs-string">&quot;int64&quot;</span>)    segments = continuous_segment_ids(ts, max_gap_s)    target_ns = ns.astype(<span class="hljs-built_in">float</span>) + horizon_s * <span class="hljs-number">1e9</span>    future_idx = np.searchsorted(ns, target_ns, side=<span class="hljs-string">&quot;left&quot;</span>)    valid = (~ts.isna()) &amp; (future_idx &lt; <span class="hljs-built_in">len</span>(ts))    rows = np.flatnonzero(valid)    future = future_idx[rows]    valid[rows] = (        (segments[future] == segments[rows])        &amp; ((ns[future] - target_ns[rows]) &lt;= max_gap_s * <span class="hljs-number">1e9</span>)    )    <span class="hljs-keyword">return</span> valid</code></pre></div><p>rolling feature 则按 segment 分组后再滚动，而不是算完 rolling 再把坏日期删掉：</p><div class="code-wrapper"><pre><code class="hljs python">segment = continuous_segment_ids(frame.index, max_gap_s=<span class="hljs-number">5</span>)frame[<span class="hljs-string">&quot;ret_std_60s&quot;</span>] = (    frame.groupby(segment, sort=<span class="hljs-literal">False</span>)[<span class="hljs-string">&quot;log_ret&quot;</span>]         .rolling(<span class="hljs-number">60</span>, min_periods=<span class="hljs-number">20</span>)         .std()         .reset_index(level=<span class="hljs-number">0</span>, drop=<span class="hljs-literal">True</span>))valid_30s = mask_valid_horizon(frame.index, horizon_s=<span class="hljs-number">30</span>, max_gap_s=<span class="hljs-number">5</span>)labels.loc[~valid_30s, <span class="hljs-string">&quot;markout_30s&quot;</span>] = np.nan</code></pre></div><p>在时间戳全部有效时，同一语义还能委托给 C++ 的 <code>continuous_segment_ids_ms</code> / <code>mask_valid_horizon_ms</code>；有非法值时保留 Python fallback，避免 native 快路径为了性能吞掉数据异常。</p><p><code>feature_engineer.py</code>、<code>cross_market_shock_audit.py</code> 和 <code>train_quote_ev.py</code> 共用同一口径。rolling feature 只在连续 segment 内计算，未来 horizon 跨 gap 时 label 直接失效。</p><p>这件事看起来不像模型升级，但它比多加几十个 feature 更重要。一个更复杂的模型只会更擅长拟合被污染的数据。</p><p>到这里，项目的研究顺序也被迫改变了：不是先训练一个更大的模型，再在结果异常时检查数据；而是先证明每个 feature 和 label 都来自连续、可用的市场片段，然后才允许它进入训练和回放。对于订单生命周期很短的 maker 来说，日与日之间不连续未必有问题，<strong>跨越不连续边界计算 rolling 或 future horizon 才是问题</strong>。</p><h3 id="1-4-为什么加入-BTCUSDT-和-spot-后，结果反而更差"><a href="#1-4-为什么加入-BTCUSDT-和-spot-后，结果反而更差" class="headerlink" title="1.4 为什么加入 BTCUSDT 和 spot 后，结果反而更差"></a>1.4 为什么加入 BTCUSDT 和 spot 后，结果反而更差</h3><p>BTCUSDC 的本地成交和盘口并不是孤立市场。项目先加入 BTCUSDT perpetual 作为 reference，后来又重建 enhanced spot features：</p><div class="code-wrapper"><pre><code class="hljs text">cv_ref_perp_*cv_exec_spot_*cv_ref_spot_*</code></pre></div><p>从直觉上说，多一个高流动性的 reference、多两个 spot anchor，算法应该更健壮。数据也确实带来了信息：cross-market shock audit 显示，很多 adverse fill 能被 BTCUSDT reference move 解释；补齐 spot 后，相关窗口的 <code>spot_available_rate</code> 达到 1.0。</p><p>但“数据有信息”不等于“把开关打开就能形成正 EV”。</p><p><code>multi_market.enabled=true/false</code> 的 Jan/Feb/Apr/May 月度 A/B 中，早期 minimal 口径下 true 的 raw PnL 四个月全部差于 false，合计约 <code>-14.05</code>。enhanced spot + audit-quality 重跑后，同样的整体开关仍然没有变成稳定收益来源，raw 差值合计约 <code>-18.14</code>。</p><p>这次 enhanced 主模型并不是只拿几天 2026 数据做 smoke test。它的 train/validation split 实际包含 2025-08/09 和 2025-11/12，补齐后的 <code>cv_exec_spot_*</code>、<code>cv_ref_spot_*</code> 在 train/val/test 中非零覆盖率均为 1.0，audit 坏日期残留为 0。换言之，“结果更差”不能简单归因于 spot 列全零或坏日期仍在训练；它更像是 policy 对新增信息的使用方式有问题。</p><p>这不是简单说明 reference 无效，而是说明当前使用方式太粗。cross-market 分组给出了更细的答案：</p><ul><li><code>reference_information_shock</code> 的 fill 后 markout 明显更差，说明 reference 确认了信息冲击；</li><li><code>reference_confirmed_absorbed</code> 多数反而为正，说明 reference 虽然移动，但本地流动性已经吸收冲击；</li><li>spot adverse return 有解释力，但整体弱于 BTCUSDT perp 的短期 adverse return；</li><li>bid 与 ask 两侧的响应并不对称。</li></ul><p>如果把 reference 信号直接变成统一 widen/pause 开关，就会把“应该避开的信息冲击”和“可以承接的已吸收流动性冲击”一起过滤掉。新增数据提高的是<strong>可分辨性</strong>，不是自动提供一条单调的收益规则。</p><p>这也解释了一个常见的机器学习误区：feature 更多、数据源更多，只会扩大模型可以表达的空间；它不会自动保证目标函数、时间对齐、样本覆盖和执行 policy 都正确。</p><h3 id="1-5-quote-EV：方向看起来对，但统计还不允许下结论"><a href="#1-5-quote-EV：方向看起来对，但统计还不允许下结论" class="headerlink" title="1.5 quote EV：方向看起来对，但统计还不允许下结论"></a>1.5 quote EV：方向看起来对，但统计还不允许下结论</h3><p>为了避免把 cross-market 信息硬编码成一个布尔开关，项目又训练了逐侧 quote EV 模型。它不直接预测 BTC 下一秒涨跌，而是分别估计：</p><ul><li><code>P(fill)</code>；</li><li>fill 后 1s/5s/30s markout bucket；</li><li>extreme adverse probability；</li><li>reference/spot 对冲击性质的确认。</li></ul><p>在 enhanced spot A/B 中，<code>ask_ev_soft_widen</code> 曾得到相对 baseline 的 raw PnL <code>+14.55</code>、InvAdj <code>+0.0049</code>。只看这两个研究指标，它很像一个值得继续做 shadow 验证的候选。</p><p>但再往下看，证据并不够：</p><ul><li>用于 baseline quote label 的真实 fill 只有 bid/ask 各约 334 条；</li><li>May validation 的有效 filled rows 只有 bid 58、ask 56；</li><li>strict promotion gate 要求至少 100 条 valid filled rows，且 fill calibration MAE 不高于 0.05；</li><li>实际 bid/ask calibration MAE 约为 0.078/0.243，均未通过；</li><li>30s expected-markout calibration MAE 也仍然很大。</li></ul><p>后来把质量合格的 2025-08/09/11/12 加入 walk-forward，训练侧 fill 增加到 bid 414、ask 415，但 May 可用 feature 当时只覆盖到 5 月 17 日，validation 只剩 bid 26、ask 25 条有效 fill。扩样改善了训练数量，却没有解决验证集覆盖和校准问题。</p><p>这里的“两万多行 labels”和“三百多条 fill”也不是矛盾。前者是被评估的候选 quote rows，绝大多数报价本来就不会成交；后者才是能够观测 fill 后 markout、用于校准条件结果的有效样本。<code>P(fill)</code> 可以从大量未成交报价中学习，但 <code>E(markout | fill)</code> 的统计强度最终仍受真实 filled rows 限制。这正是 maker quote-level 模型最容易出现“数据行很多、有效监督很少”的地方。</p><p>所以目前最诚实的结论是：ask-only 的方向值得继续研究，但仍是 shadow/research 候选，不能因为一次 raw PnL 改善就跳过样本质量与校准 gate。bid 侧在 enhanced shadow eval 中还出现过明显退化，更不能把 both-side 结果当作自然升级。</p><h3 id="1-6-只看-PnL-还不够：把库存风险写成积分"><a href="#1-6-只看-PnL-还不够：把库存风险写成积分" class="headerlink" title="1.6 只看 PnL 还不够：把库存风险写成积分"></a>1.6 只看 PnL 还不够：把库存风险写成积分</h3><p>maker 策略可能用更长时间、更大库存换取同样的 PnL。如果 A/B 报告只列 raw PnL 和最终库存，这种风险会被隐藏。</p><p>项目因此加入库存时间暴露：</p><script type="math/tex; mode=display">T_{\lvert q\rvert}=\int_0^T |q(t)|\,dt,\qquadT_q=\int_0^T q(t)\,dt</script><script type="math/tex; mode=display">T_{q^2}=\int_0^T q(t)^2\,dt,\qquadT_{\text{notional}}=\int_0^T |q(t)|\,\operatorname{mid}(t)\,dt</script><p>它表达的不是某一时刻仓位有多大，而是整个持仓期间承担了多少风险预算。两个 arm 即使最终都回到零库存，持仓 10 秒与持仓 2 小时也不应该被视为相同。</p><p>这些字段被接入 tick replay summary、事件引擎 HEALTH/metrics 和 A/B report，并进一步派生 <code>time_avg_abs_inventory</code> 与 <code>pnl_per_abs_inventory_hour</code>。从此比较 ask EV、multi-market 和 enhanced spot 时，不只问“raw PnL 改善多少”，还要问“为这点改善占用了多少库存时间”。</p><blockquote><p><strong>本节工程结论</strong></p><ul><li>maker 研究不能只看 <code>spread</code> 或 raw PnL，必须把 fill 后 markout、库存时间和校准质量放进同一张表。</li><li>多市场数据首先用于解释冲击来源，不应该直接变成一个全局开关。</li><li>数据质量不是 ETL 细节，而是模型定义的一部分；rolling feature 和 future label 必须共享连续段口径。</li><li>quote EV 的 promotion gate 应该先过样本量、Brier/calibration、bucket realized-vs-pred 和分侧稳定性，再谈上线。</li></ul></blockquote><h2 id="第二部分：从算法验证到工程问题"><a href="#第二部分：从算法验证到工程问题" class="headerlink" title="第二部分：从算法验证到工程问题"></a>第二部分：从算法验证到工程问题</h2><p>到这里，maker 算法部分已经形成了一个闭环：候选报价、排队成交、fill 后 markout、cross-market attribution、quote EV 校准和库存时间积分。但这个闭环一旦严格起来，工程问题就不再是“以后再说”的附属项。因为每一次反例验证都要重新跑真实窗口、真实队列和真实 policy state，系统吞吐量本身会决定研究能不能继续。</p><p>这一部分是算法和 C++ 之间的桥。它回答的不是“怎么写 native 代码”，而是“为什么验证流程会需要 native 状态机”。</p><h3 id="2-1-严格验证带来了新的工程问题"><a href="#2-1-严格验证带来了新的工程问题" class="headerlink" title="2.1 严格验证带来了新的工程问题"></a>2.1 严格验证带来了新的工程问题</h3><p>NarrowGate 的 tick replay 不是拿下一根 K 线 high/low 判断是否成交。为了尽量接近 maker 的执行过程，它需要模拟：</p><ul><li>历史 BBO 与 L2 best bid/ask；</li><li>exact-level queue ahead；</li><li>new order latency 与 cancel latency；</li><li>maker fill gate；</li><li>final spread cap；</li><li>adverse/defense guard；</li><li>flat、fragile、adaptive TTL 与 cooldown；</li><li>local extreme guard；</li><li>position timeout 和 emergency path；</li><li>库存时间积分与完整 trace。</li></ul><p>这些机制大多是高频、顺序相关、状态明确的循环。Python 写起来灵活，但每一个策略臂都重复读取数据、更新 queue、计算 quote、写 summary。做 Jan/Feb/Apr/May 多臂 A/B 时，真正耗时的并不是 LightGBM 训练，而是一次次 replay 和 quote context 生成。</p><p>还有一个比速度更重要的问题：如果某个 guard 只存在于回测，或回测拥有事件引擎没有的 queue/TTL 信息，那么回测会比实际策略“更聪明”。此时加速一套不一致的模拟，只会更快地产生错误信心。</p><p>前面的数据审计、逐月 A/B、strict calibration gate 都指向同一个现实：这类策略不能靠一两个漂亮窗口下结论，必须反复跑多个真实月份、多个 side policy 和多个风险口径。也正是在这里，Python 循环从“足够灵活”变成了研究吞吐量的瓶颈。</p><p>所以 C++ 迁移的第一目标不是炫耀速度，而是让更严格的验证在时间上可负担；第二目标才是建立同一批真实数据下的 Python/C++ parity，并借此把 policy 边界显式化。若两套引擎不能解释同一条成交路径，跑得再快也没有研究价值。</p><blockquote><p><strong>本节工程结论</strong></p><ul><li>严格 tick replay 的价值不是“更复杂”，而是阻止 bar 回测把触价误判成成交。</li><li>Python/C++ 双引擎先做 parity，再做 sweep 加速；路径不一致时，速度没有意义。</li><li>A/B 不能只比最终 PnL，还要比 fills、bid/ask split、final inventory、InvAdj、inventory time、spread/cap 和 trace 行数。</li><li>replay、queue、TTL、latency、guard 是强顺序状态机，适合一次 C++ 调用跑完整窗口；pandas IO 和训练编排继续留给 Python。</li></ul></blockquote><h2 id="第三部分：Python-C-技术实现"><a href="#第三部分：Python-C-技术实现" class="headerlink" title="第三部分：Python/C++ 技术实现"></a>第三部分：Python/C++ 技术实现</h2><p>从这里开始才进入 C++。前面的 maker 章节回答“应该验证什么”，这一部分回答“哪些计算形态适合迁移、迁移后如何保证和 Python 口径一致”。因此它不会把下载、pandas 清洗、LightGBM 训练和报告生成都重写，而只关注状态长期驻留、调用粒度足够大、语义相对稳定的路径。</p><p>如果从量化 C++ 面试展示角度看，这部分真正想展示的不是“我会写 pybind11”，而是这些工程判断：</p><ul><li>pybind11 边界成本和 object materialization 往往比公式本身更贵；</li><li>native path 必须 fail fast，不能静默 fallback 后仍把结果当 C++ benchmark；</li><li>parity test 用来区分策略差异、对象转换差异和 ABI 差异；</li><li>SoA view、fixed-array feature vector、ring buffer 和 O(1) rolling state 比“把函数搬到 C++”更重要；</li><li>PMR 只适合生命周期清楚的 replay scratch，不应该跨 Python 边界泄漏 arena 生命周期；</li><li>live scalar path 和 offline batch path 是两种问题：<strong>offline 关心吞吐，live 关心尾延迟；offline 可以 batch，live 只能 scalar；offline 释放 GIL 有意义，live 主路径通常没有</strong>；</li><li>benchmark 要看 p99/p99.9，而不是只看 mean；KVM 不暴露 PMU 时，要明确 wall-time soak 的局限。</li></ul><h3 id="3-1-为什么-C-是结果，而不是起点"><a href="#3-1-为什么-C-是结果，而不是起点" class="headerlink" title="3.1 为什么 C++ 是结果，而不是起点"></a>3.1 为什么 C++ 是结果，而不是起点</h3><p>Python 仍然很适合：</p><ul><li>数据下载、清洗和 parquet/CSV IO；</li><li>pandas/NumPy 特征验证；</li><li>LightGBM 训练和校准；</li><li>实验编排、图表和报告。</li></ul><p>真正适合 C++ 的是：</p><ul><li>每个 tick 都要更新的 streaming state；</li><li>quote core 的大批量数值计算；</li><li>queue、latency、TTL、guard、inventory 组成的 replay 状态机；</li><li>同一数据窗口被几十个参数臂反复执行的 sweep/A/B。</li></ul><p>这里有一个很关键的取舍：项目没有把数据下载、pandas 清洗、LightGBM 训练和报告生成全部重写。它们要么不是 CPU 热点，要么迭代频繁、Python 生态更合适。迁移对象集中在“状态长期驻留、每 tick 重复执行、分支语义相对稳定”的部分。这样 C++ 服务的是研究方法，而不是制造一个难以验证的新系统。</p><p>最终的分层大致是：</p><div class="code-wrapper"><pre><code class="hljs text">Python / pandas / LightGBM  - 数据质量、特征工程、模型训练、校准和报告C++ extension  - quote core batch  - tick replay state machine  - streaming feature state  - queue / latency / TTL / policy executionPython wrapper  - 加载数组和模型产物  - 调用 C++ batch/replay  - 写 trace、label 和 A/B summary</code></pre></div><p>C++ 目录由 pybind11 暴露给 Python：</p><div class="code-wrapper"><pre><code class="hljs text">cpp/  CMakeLists.txt  pyproject.toml  narrowgate_cpp/    bindings.cpp    common.hpp    quote_core.hpp / quote_core.cpp    tick_replay.hpp / tick_replay.cpp    streaming_features.hpp / streaming_features.cpptests/  test_cpp_quote_core_parity.py  test_cpp_tick_replay_parity.py  test_cpp_signal_features.pybench/  bench_live_path.py  bench_quote_core.py  bench_tick_replay.py</code></pre></div><p><code>bindings.cpp</code> 只处理边界，业务逻辑留在普通 C++ 类型和函数里。这样 quote core 和 replay 可以脱离 Python binding 单独测试，也更容易发现“是策略差异，还是对象转换差异”。</p><p>构建层使用 <code>scikit-build-core + CMake + pybind11</code>。extension 由当前虚拟环境的 CPython ABI 编译成 <code>.so</code>，并不是在运行时解释 C++：</p><div class="code-wrapper"><pre><code class="hljs toml"><span class="hljs-comment"># cpp/pyproject.toml</span><span class="hljs-section">[build-system]</span><span class="hljs-attr">requires</span> = [<span class="hljs-string">&quot;scikit-build-core&gt;=0.10&quot;</span>, <span class="hljs-string">&quot;pybind11&gt;=2.12&quot;</span>]<span class="hljs-attr">build-backend</span> = <span class="hljs-string">&quot;scikit_build_core.build&quot;</span></code></pre></div><div class="code-wrapper"><pre><code class="hljs cmake"><span class="hljs-comment"># cpp/CMakeLists.txt</span><span class="hljs-keyword">set</span>(CMAKE_CXX_STANDARD <span class="hljs-number">20</span>)<span class="hljs-keyword">set</span>(CMAKE_CXX_STANDARD_REQUIRED <span class="hljs-keyword">ON</span>)<span class="hljs-keyword">set</span>(CMAKE_CXX_EXTENSIONS <span class="hljs-keyword">OFF</span>)<span class="hljs-keyword">find_package</span>(Python COMPONENTS Interpreter Development.Module REQUIRED)<span class="hljs-keyword">find_package</span>(pybind11 CONFIG REQUIRED)pybind11_add_module(narrowgate_cpp    narrowgate_cpp/bindings.cpp    narrowgate_cpp/quote_core.cpp    narrowgate_cpp/tick_replay.cpp    narrowgate_cpp/streaming_features.cpp)<span class="hljs-keyword">install</span>(TARGETS narrowgate_cpp LIBRARY DESTINATION .)</code></pre></div><div class="code-wrapper"><pre><code class="hljs bash">python3.12 -m venv .venv.venv/bin/pip install -r requirements.txt.venv/bin/pip install -e cpp.venv/bin/python -c \  <span class="hljs-string">&#x27;import narrowgate_cpp; print(narrowgate_cpp.__file__)&#x27;</span></code></pre></div><p>最后一行很重要。BTCUSDC 和 BTCUSDT 两个仓库都暴露名为 <code>narrowgate_cpp</code> 的模块，共用一个环境时可能导入另一份 build。loader 因此检查模块路径和 editable-install 的 <code>direct_url.json</code> 是否指向当前 repo，也允许用 <code>NARROWGATE_CPP_EXPECT_MODULE_TOKEN</code> 显式约束；<code>NARROWGATE_CPP_STRICT=1</code> 下来源不符会直接失败，不允许悄悄 fallback 到 Python。</p><blockquote><p><strong>本节工程结论</strong></p><ul><li>不把 Python binding 和业务逻辑揉在一起：<code>bindings.cpp</code> 只做边界，quote/replay/signal core 用普通 C++ 类型实现。</li><li>C++ core 必须可以脱离 pybind 单测；否则无法区分策略差异、对象转换差异和 ABI 差异。</li><li><code>NARROWGATE_CPP_STRICT=1</code> 是开发期默认心态：模块来源、字段缺失、shape 错误必须 fail fast。</li><li>同名 extension 在多个 repo 之间很容易碰撞，import path 和 editable install 来源必须进入 sanity check。</li></ul></blockquote><h3 id="3-2-quote-core：真正慢的是“每-tick-全量物化”"><a href="#3-2-quote-core：真正慢的是“每-tick-全量物化”" class="headerlink" title="3.2 quote core：真正慢的是“每 tick 全量物化”"></a>3.2 quote core：真正慢的是“每 tick 全量物化”</h3><p>做市 quote core 包含 microprice、reservation price、volatility/kappa/depth spread、inventory skew、adverse/defense guard，以及最终 bid/ask 到 BBO 的距离。它的浮点计算并不重，早期 scalar C++ 路径却明显更慢：</p><div class="table-container"><table><thead><tr><th>路径</th><th style="text-align:right">Python</th><th style="text-align:right">旧 C++</th><th>结果</th></tr></thead><tbody><tr><td>quote + policy</td><td style="text-align:right">85.48 us</td><td style="text-align:right">136.33 us</td><td>C++ 慢约 59%</td></tr><tr><td><code>_compute_quotes</code></td><td style="text-align:right">54.93 us</td><td style="text-align:right">105.07 us</td><td>C++ 慢约 91%</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：单独把 quote 公式搬到 C++，在 pybind/dataclass/dict 往返很重时会负优化。</li><li>不能说明什么：不能说明 C++ quote core 算法本身慢，也不能说明所有 native path 都不值得做。</li><li>下一步验证：拆掉每 tick 全量物化后，重新测 compact ABI，并同时跑 quote-field parity。</li></ul></blockquote><p>profile 后发现，时间并没有花在 <code>quote_core.cpp</code> 的公式里，而是花在边界两端：每 tick 都要重新创建并复制约 70 个 config 字段、14 个 state 字段、5 个 prediction 字段和整本 depth levels；C++ 返回以后，Python 又把 native result 展开成两个完整 <code>quote_context</code>、quote flags、diagnostics 和 dataclass。一次报价所需的数学运算很少，对象搬运反而成了主任务。</p><h4 id="3-2-1-解决方法：热路径紧凑，完整对象按需物化"><a href="#3-2-1-解决方法：热路径紧凑，完整对象按需物化" class="headerlink" title="3.2.1 解决方法：热路径紧凑，完整对象按需物化"></a>3.2.1 解决方法：热路径紧凑，完整对象按需物化</h4><p>这次没有继续微调 C++ 公式，而是重新设计边界：</p><ol><li>Python wrapper 缓存已经物化的 native <code>QuoteCoreConfig</code>，配置对象热重载时失效；</li><li>state/pred 通过紧凑 tuple 传入，depth levels 由 C++ 直接读取 Python sequence，不再逐档创建 pybind <code>DepthLevel</code> 对象；</li><li>quote EV 关闭时，只返回 side policy 真正读取的 adverse、defense、TTL、local-extreme、near-depth 字段，以及周期诊断日志使用的小型 diagnostics；</li><li>任一侧 quote EV 开启时自动回到完整 context，因为模型确实需要完整特征，不能为了速度静默丢列。</li></ol><p>也就是说，完整 context 不是被删除，而是从“每 tick 固定税”变成“功能真正需要时才支付的成本”。</p><h4 id="3-2-2-把配置、状态和诊断分成三种生命周期"><a href="#3-2-2-把配置、状态和诊断分成三种生命周期" class="headerlink" title="3.2.2 把配置、状态和诊断分成三种生命周期"></a>3.2.2 把配置、状态和诊断分成三种生命周期</h4><p>原接口几乎把 Python dataclass 一比一复制成 pybind 对象。改造后，配置只在对象变化时同步；高频 state/pred 使用固定位置 tuple；L2 sequence 由 binding 直接读取。下面是实际 wrapper 的裁剪版：</p><div class="code-wrapper"><pre><code class="hljs python">_CPP_STATE_FIELDS = (    <span class="hljs-string">&quot;mid&quot;</span>, <span class="hljs-string">&quot;inventory&quot;</span>, <span class="hljs-string">&quot;sigma_sq&quot;</span>, <span class="hljs-string">&quot;trade_intensity&quot;</span>,    <span class="hljs-string">&quot;best_bid&quot;</span>, <span class="hljs-string">&quot;best_ask&quot;</span>, <span class="hljs-string">&quot;ber_active&quot;</span>,    <span class="hljs-string">&quot;mo_ema_all&quot;</span>, <span class="hljs-string">&quot;mo_ema_bid&quot;</span>, <span class="hljs-string">&quot;mo_ema_ask&quot;</span>, <span class="hljs-string">&quot;mo_ref&quot;</span>,    <span class="hljs-string">&quot;position_open&quot;</span>, <span class="hljs-string">&quot;hold_time_s&quot;</span>, <span class="hljs-string">&quot;unrealized_pnl&quot;</span>,)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">_cached_cpp_config</span>(<span class="hljs-params">cpp, cfg</span>):</span>    <span class="hljs-keyword">global</span> _CPP_CFG_CACHE_KEY, _CPP_CFG_CACHE_REF, _CPP_CFG_CACHE_VALUE    cached = _CPP_CFG_CACHE_REF() <span class="hljs-keyword">if</span> _CPP_CFG_CACHE_REF <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span>    <span class="hljs-keyword">if</span> _CPP_CFG_CACHE_KEY == <span class="hljs-built_in">id</span>(cfg) <span class="hljs-keyword">and</span> cached <span class="hljs-keyword">is</span> cfg:        <span class="hljs-keyword">return</span> _CPP_CFG_CACHE_VALUE    native = _copy_attrs(cfg, cpp.QuoteCoreConfig(), _CPP_CFG_FIELDS)    _CPP_CFG_CACHE_KEY = <span class="hljs-built_in">id</span>(cfg)    _CPP_CFG_CACHE_REF = weakref.ref(cfg)    _CPP_CFG_CACHE_VALUE = native    <span class="hljs-keyword">return</span> nativeresult = cpp.compute_quote_core_live(    <span class="hljs-built_in">tuple</span>(<span class="hljs-built_in">getattr</span>(state, name) <span class="hljs-keyword">for</span> name <span class="hljs-keyword">in</span> _CPP_STATE_FIELDS),    _cached_cpp_config(cpp, cfg),    (pred_dir, pred_vol, pred_ret, tox_bid, tox_ask),    depth.bids <span class="hljs-keyword">if</span> depth <span class="hljs-keyword">else</span> (),    depth.asks <span class="hljs-keyword">if</span> depth <span class="hljs-keyword">else</span> (),)</code></pre></div><p>C++ binding 则对 tuple 做长度检查并填充栈上的普通结构体，depth 不再逐档创建 <code>DepthLevel</code> Python wrapper：</p><div class="code-wrapper"><pre><code class="hljs cpp">m.<span class="hljs-built_in">def</span>(<span class="hljs-string">&quot;compute_quote_core_live&quot;</span>,    [](py::sequence state_values,       <span class="hljs-keyword">const</span> QuoteCoreConfig&amp; cfg,       py::sequence pred_values,       py::handle bids,       py::handle asks) &#123;        <span class="hljs-keyword">if</span> (py::<span class="hljs-built_in">len</span>(state_values) != <span class="hljs-number">14</span> || py::<span class="hljs-built_in">len</span>(pred_values) != <span class="hljs-number">5</span>)            <span class="hljs-keyword">throw</span> std::<span class="hljs-built_in">invalid_argument</span>(<span class="hljs-string">&quot;live quote state/pred length mismatch&quot;</span>);        QuoteState s;        s.mid             = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">0</span>]);        s.inventory       = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">1</span>]);        s.sigma_sq        = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">2</span>]);        s.trade_intensity = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">3</span>]);        s.best_bid        = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">4</span>]);        s.best_ask        = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(state_values[<span class="hljs-number">5</span>]);        <span class="hljs-comment">// ...其余状态按固定位置读取</span>        QuotePrediction p;        p.dir_10s = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(pred_values[<span class="hljs-number">0</span>]);        p.vol_10s = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(pred_values[<span class="hljs-number">1</span>]);        p.ret_10s = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(pred_values[<span class="hljs-number">2</span>]);        p.tox_bid = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(pred_values[<span class="hljs-number">3</span>]);        p.tox_ask = py::cast&lt;<span class="hljs-keyword">double</span>&gt;(pred_values[<span class="hljs-number">4</span>]);        <span class="hljs-keyword">return</span> <span class="hljs-built_in">compute_quote_core</span>(            s, cfg, p, <span class="hljs-built_in">depth_from_python_levels</span>(bids, asks));    &#125;);</code></pre></div><h4 id="3-2-3-diagnostics-不是全有或全无"><a href="#3-2-3-diagnostics-不是全有或全无" class="headerlink" title="3.2.3 diagnostics 不是全有或全无"></a>3.2.3 diagnostics 不是全有或全无</h4><p>side policy 每 tick 真正读取的字段远少于离线 quote EV。compact path 只物化 guard、距离、TTL 和少量 cap 诊断：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">compact_side_context</span>(<span class="hljs-params">side, ctx, result</span>):</span>    <span class="hljs-keyword">return</span> &#123;        <span class="hljs-string">&quot;near_depth_total&quot;</span>: <span class="hljs-built_in">float</span>(result.near_depth_total),        <span class="hljs-string">&quot;final_quote_delta_to_bbo&quot;</span>: <span class="hljs-built_in">float</span>(ctx.final_quote_delta_to_bbo),        <span class="hljs-string">&quot;side_adverse&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.side_adverse),        <span class="hljs-string">&quot;side_adverse_pause&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.side_adverse_pause),        <span class="hljs-string">&quot;defense_guard&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.defense_guard),        <span class="hljs-string">&quot;defense_pause&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.defense_pause),        <span class="hljs-string">&quot;mid_guard&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.mid_guard),        <span class="hljs-string">&quot;post_only&quot;</span>: <span class="hljs-built_in">bool</span>(ctx.post_only),        <span class="hljs-string">&quot;delta_cap&quot;</span>: <span class="hljs-built_in">bool</span>(result.flags.delta_cap),    &#125;<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">compute_quote_core_live</span>(<span class="hljs-params">..., require_full_context=<span class="hljs-literal">False</span></span>):</span>    <span class="hljs-keyword">if</span> cpp_enabled <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> require_full_context:        <span class="hljs-keyword">return</span> _compute_quote_core_cpp_compact(...)    <span class="hljs-keyword">return</span> compute_quote_core(...)       <span class="hljs-comment"># quote EV 需要完整 feature context</span></code></pre></div><p>这个方案的关键不是少返回几个字段，而是明确了三种生命周期：几乎不变的 config 被缓存，每 tick 变化的 state 用紧凑序列，只有模型训练、trace 或调试需要的长 diagnostics 才完整物化。可观测性仍在，只是不再无条件出现在最热路径。</p><p>固定环境复测使用 Python 3.12.13、AppleClang Release build、ML 关闭以隔离 quote/feature CPU，<code>--n 10000 --signal-n 1000</code>，每项跑三次取 mean 的中位数：</p><div class="table-container"><table><thead><tr><th>路径</th><th style="text-align:right">Python</th><th style="text-align:right">compact C++</th><th style="text-align:right">变化</th></tr></thead><tbody><tr><td>live <code>_compute_quotes</code></td><td style="text-align:right">42.85 us</td><td style="text-align:right">34.53 us</td><td style="text-align:right"><strong>快 19.4%</strong></td></tr><tr><td>quote + policy</td><td style="text-align:right">74.55 us</td><td style="text-align:right">62.32 us</td><td style="text-align:right"><strong>快 16.4%</strong></td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：边界收紧后，scalar native path 从负优化翻转为小幅正收益。</li><li>不能说明什么：不能把这组数字套到 quote EV 开启后的完整 context 路径，也不能推出端到端报单延迟改善 16%。</li><li>下一步验证：在目标 x86 live 机型上做 p99/p99.9 soak，并确认 strict 模式没有 fallback。</li></ul></blockquote><p>这不是数量级加速，但它完成了更重要的翻转：scalar native path 终于不再因为 Python 对象往返而负优化。它仍保持显式 opt-in，quote EV 开启后的完整 context 路径也不能套用这组数字。</p><p>离线 quote decomposition、shock audit 和 quote EV label 仍使用 depth-aware batch binding。batch 场景一次传连续数组，跨语言成本被大量样本摊薄，依然比 scalar 更适合 C++。<code>book_imb</code>、<code>near_depth_total</code>、kappa/depth、adverse/defense 等字段继续纳入 parity 审计；性能优化不能把 C++ 悄悄变成另一套策略。</p><h3 id="3-3-tick-replay：先迁完整状态机，再谈加速倍数"><a href="#3-3-tick-replay：先迁完整状态机，再谈加速倍数" class="headerlink" title="3.3 tick replay：先迁完整状态机，再谈加速倍数"></a>3.3 tick replay：先迁完整状态机，再谈加速倍数</h3><p>tick replay 是整个迁移中最适合 C++ 的部分，因为 queue、latency、TTL、inventory 和 fill path 都是顺序相关状态。若只把某个公式做成 scalar binding，Python 仍要在每个 tick 调度所有状态；把完整 replay loop 放进一次 C++ 调用，跨语言边界才只出现于窗口输入和 summary/trace 输出。</p><p>C++ 版本逐项迁入所有机制：</p><ul><li>historical BBO/L2；</li><li>exact-level queue ahead；</li><li>new/cancel latency；</li><li>maker fill gate；</li><li>inventory 与 inventory-time summary；</li><li>spread cap、adverse/defense guard；</li><li>quote EV policy；</li><li>local extreme、fragile/adaptive TTL/cooldown；</li><li>position timeout/emergency path；</li><li>完整 trace 和 summary 字段。</li></ul><h4 id="3-3-1-输入是只读-view，窗口数据不再逐-tick-穿过-Python"><a href="#3-3-1-输入是只读-view，窗口数据不再逐-tick-穿过-Python" class="headerlink" title="3.3.1 输入是只读 view，窗口数据不再逐 tick 穿过 Python"></a>3.3.1 输入是只读 view，窗口数据不再逐 tick 穿过 Python</h4><p>replay 的入口不是 <code>on_trade(dict)</code>，而是一组生命周期覆盖整个调用的连续数组 view。Python 完成 parquet/Arrow 加载和 dtype 归一化后，只跨一次 pybind 边界：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">TickReplayInput</span> &#123;</span>    ArrayView&lt;std::<span class="hljs-keyword">int64_t</span>&gt; trade_ts_ms;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; trade_price;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; trade_qty;    ArrayView&lt;std::<span class="hljs-keyword">uint8_t</span>&gt; is_buyer_maker;    ArrayView&lt;std::<span class="hljs-keyword">int64_t</span>&gt; bbo_ts_ms;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; bbo_best_bid;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; bbo_best_ask;    ArrayView&lt;std::<span class="hljs-keyword">int64_t</span>&gt; l2_ts_ms;    MatrixView&lt;<span class="hljs-keyword">double</span>&gt; l2_bid_px;    MatrixView&lt;<span class="hljs-keyword">double</span>&gt; l2_bid_qty;    MatrixView&lt;<span class="hljs-keyword">double</span>&gt; l2_ask_px;    MatrixView&lt;<span class="hljs-keyword">double</span>&gt; l2_ask_qty;    ArrayView&lt;std::<span class="hljs-keyword">int64_t</span>&gt; quote_ev_ts_ms;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; bid_quote_ev_30s;    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; ask_quote_ev_30s;&#125;;<span class="hljs-function">TickReplayResult <span class="hljs-title">simulate_tick_arrays</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> TickReplayInput&amp; input,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> TickReplayParams&amp; params)</span></span>;</code></pre></div><p><code>ArrayView/MatrixView</code> 只保存指针和 shape，不拥有数据。binding 在调用期间持有 NumPy array，C++ 内循环读取连续内存；结果结束后一次性返回 summary 和可选 trace。这样优化的是整个状态机，而不是把每个 tick 的 Python callback 换成一次 pybind callback。</p><h4 id="3-3-2-queue-policy-用模板固定-side-与模式"><a href="#3-3-2-queue-policy-用模板固定-side-与模式" class="headerlink" title="3.3.2 queue policy 用模板固定 side 与模式"></a>3.3.2 queue policy 用模板固定 side 与模式</h4><p>bid/ask 和 exact-level/through-level queue 的分支在循环内非常频繁。这里用模板把 side 和 queue mode 固定到编译期：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;Side S, QueueAheadMode Mode&gt;<span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">visible_queue_ahead</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; prices,</span></span><span class="hljs-params"><span class="hljs-function">    ArrayView&lt;<span class="hljs-keyword">double</span>&gt; quantities,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">double</span> quote_price,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">double</span> tolerance)</span> </span>&#123;    <span class="hljs-keyword">double</span> visible = <span class="hljs-number">0.0</span>;    <span class="hljs-keyword">for</span> (std::<span class="hljs-keyword">size_t</span> i = <span class="hljs-number">0</span>; i &lt; prices.<span class="hljs-built_in">size</span>(); ++i) &#123;        <span class="hljs-keyword">bool</span> include = <span class="hljs-literal">false</span>;        <span class="hljs-function"><span class="hljs-keyword">if</span> <span class="hljs-title">constexpr</span> <span class="hljs-params">(Mode == QueueAheadMode::ExactLevel)</span> </span>&#123;            include = std::<span class="hljs-built_in">abs</span>(prices.<span class="hljs-built_in">data</span>()[i] - quote_price) &lt;= tolerance;        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> <span class="hljs-built_in"><span class="hljs-keyword">constexpr</span></span> (is_buy_v&lt;S&gt;) &#123;            include = prices.<span class="hljs-built_in">data</span>()[i] &gt;= quote_price;        &#125; <span class="hljs-keyword">else</span> &#123;            include = prices.<span class="hljs-built_in">data</span>()[i] &lt;= quote_price;        &#125;        <span class="hljs-keyword">if</span> (include) visible += std::<span class="hljs-built_in">max</span>(<span class="hljs-number">0.0</span>, quantities.<span class="hljs-built_in">data</span>()[i]);    &#125;    <span class="hljs-keyword">return</span> visible;&#125;</code></pre></div><p>订单本身是一个显式状态机：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ReplayOrder</span> &#123;</span>    Side side;    <span class="hljs-keyword">double</span> price, quantity, remaining;    <span class="hljs-keyword">double</span> queue_left, queue_init;    std::<span class="hljs-keyword">int64_t</span> quote_ts, activate_ts, cancel_effective_ts;    std::<span class="hljs-keyword">int64_t</span> ttl_ms;    OrderState state;    <span class="hljs-keyword">bool</span> side_adverse, defense_guard, local_extreme_guard, quote_ev;    <span class="hljs-comment">// 默认不分配；只有打开 trace 时才指向冷诊断对象。</span>    TraceOrderPtr trace;&#125;;order.activate_ts = ts + sampled_new_latency_ms;order.state = sampled_new_latency_ms &gt; <span class="hljs-number">0</span> ? ORDER_PENDING_NEW : ORDER_OPEN;<span class="hljs-keyword">if</span> (order.ttl_ms &gt; <span class="hljs-number">0</span> &amp;&amp; ts - order.quote_ts &gt;= order.ttl_ms) &#123;    order.state = ORDER_PENDING_CANCEL;    order.cancel_effective_ts = ts + sampled_cancel_latency_ms;&#125;</code></pre></div><p><code>PENDING_CANCEL</code> 期间仍允许成交，因此 trace 会单独统计 <code>pending_cancel_fills</code>。这类语义如果只保留“请求撤单后立即删除订单”的简化实现，回测会系统性低估撤单延迟风险。</p><h4 id="3-3-3-fill-先消耗-queue，再改变-inventory"><a href="#3-3-3-fill-先消耗-queue，再改变-inventory" class="headerlink" title="3.3.3 fill 先消耗 queue，再改变 inventory"></a>3.3.3 fill 先消耗 queue，再改变 inventory</h4><p>maker fill 不由价格 touch 直接触发。主动成交必须跨过订单价格，并先消耗 <code>queue_left</code>：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span> (!trade_crosses_order&lt;S&gt;(trade_price, order.price))    <span class="hljs-keyword">continue</span>;<span class="hljs-keyword">if</span> (order.queue_left &gt; <span class="hljs-number">0.0</span>) &#123;    <span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> eaten = std::<span class="hljs-built_in">min</span>(order.queue_left, remaining_trade_qty);    order.queue_left -= eaten;    remaining_trade_qty -= eaten;&#125;<span class="hljs-keyword">double</span> fill_qty = <span class="hljs-built_in">floor_lot</span>(    std::<span class="hljs-built_in">min</span>(order.remaining, remaining_trade_qty), lot_size);<span class="hljs-keyword">if</span> (fill_qty &lt; lot_size)    <span class="hljs-keyword">continue</span>;cash += side_cash_delta&lt;S&gt;(order.price, fill_qty, maker_fee);inventory += inventory_delta_sign&lt;S&gt;() * fill_qty;order.remaining -= fill_qty;</code></pre></div><p>库存时间风险也在同一事件时钟中积分，不使用“最终仓位乘总时长”的粗略近似：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> dt_s = (ts[i] - ts[i - <span class="hljs-number">1</span>]) / <span class="hljs-number">1000.0</span>;<span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> mark = trade_price[i - <span class="hljs-number">1</span>];summary.signed_inventory_time_s += inventory * dt_s;summary.abs_inventory_time_s += std::<span class="hljs-built_in">abs</span>(inventory) * dt_s;summary.sq_inventory_time_s += inventory * inventory * dt_s;summary.notional_inventory_time_s += std::<span class="hljs-built_in">abs</span>(inventory) * mark * dt_s;</code></pre></div><p>迁移顺序刻意很慢：每增加一个机制，就在固定真实窗口同时跑 Python 和 C++，比较：</p><div class="code-wrapper"><pre><code class="hljs text">PnLfills / bid-ask splitfinal inventoryInvAdjinventory timespread&lt;100 / cap_hitSharpetrace rows</code></pre></div><p>golden window 覆盖正常、高波动、低成交/稀疏以及不同月份。只有差异能够被解释，C++ engine 才被允许进入 sweep、cap A/B、quote EV A/B 等批量 runner。默认行为仍保留 Python，显式 <code>--engine cpp</code> 才切换，strict 模式下 extension 不可用会直接报错，避免静默 fallback 让人误以为跑了 C++。</p><p>完整 CLI 的 wall time 还包含 parquet、模型和窗口加载，因此不能拿 C++ 内层循环的 microbenchmark 直接宣称整月回测有同样倍数。对回测来说，<strong>路径一致比一个夸张的加速倍数更重要</strong>。</p><h3 id="3-4-quote-EV-fast-screening：快筛不是最终裁决"><a href="#3-4-quote-EV-fast-screening：快筛不是最终裁决" class="headerlink" title="3.4 quote EV fast screening：快筛不是最终裁决"></a>3.4 quote EV fast screening：快筛不是最终裁决</h3><p>quote EV online inference 会对大量 quote 逐行构造 feature 并调用模型，完整月度 A/B 很慢。为此项目增加了一条研究快筛路径：</p><ol><li>在 baseline quote context 下预计算 bid/ask EV arrays；</li><li>C++ replay 在每个 tick 直接读取对应分数；</li><li>快速跑 baseline、bid、ask、both 四个 key arms；</li><li>淘汰明显差的 arm；</li><li>最终候选仍回到 Python online inference 全量确认。</li></ol><p>本机接电后的 wall-time 记录是：Jan/Feb/Apr/May arrays 预计算约 <code>57.84s</code>，C++ replay 跑四臂约 <code>7.84s</code>。</p><p>研究链路不是一句“打开 C++”就结束，而是把昂贵步骤和快速步骤拆开。下面是实际命令形态（路径缩短为变量）：</p><div class="code-wrapper"><pre><code class="hljs bash">RESULTS=~/MarketData/NarrowGate_BTCUSDC/backtest_results_btcusdcMODEL=models/saved_btcusdc_quote_ev_xmarket_cpp_context_20260618<span class="hljs-comment"># 1. baseline context 下只做一次 online inference，写成按时间对齐的 arrays</span>.venv/bin/python models/precompute_quote_ev_arrays.py \  --symbol BTCUSDC \  --months 2026-01 2026-02 2026-04 2026-05 \  --model-dir <span class="hljs-string">&quot;<span class="hljs-variable">$MODEL</span>&quot;</span> \  --orders <span class="hljs-string">&quot;<span class="hljs-variable">$RESULTS</span>/cross_market_shock_audit.orders_features.parquet&quot;</span> \  --tag cpp_fast_screen<span class="hljs-comment"># 2. 四个 policy arm 复用同一份窗口 cache 与 EV arrays</span>NARROWGATE_CPP_STRICT=1 .venv/bin/python models/quote_ev_ab_tick.py \  --symbol BTCUSDC \  --months 2026-01 2026-02 2026-04 2026-05 \  --arms baseline bid_ev_soft_widen ask_ev_soft_widen both_ev_soft_widen \  --engine cpp \  --cpp-quote-ev-data <span class="hljs-string">&quot;<span class="hljs-variable">$RESULTS</span>/quote_ev_arrays_cpp_fast_screen_btcusdc.parquet&quot;</span> \  --window-cache-dir /tmp/narrowgate_tick_window_cache \  --workers 1</code></pre></div><p>数组必须按 <code>quote_ev_ts_ms</code> 与 replay 时钟做 backward/as-of 对齐，并在 C++ 入口验证所有 bid/ask EV 列长度一致。<code>NARROWGATE_CPP_STRICT=1</code> 则保证模块不可用、字段缺失或 shape 错误时直接终止，而不是改走 Python 后仍输出一个看似成功的 A/B 文件。</p><p>这个数字不能被误读成“完整研究只需八秒”。EV score 被冻结在 baseline context；某个 arm 改变报价后，成交路径、库存和后续 context 都会漂移，而预计算数组不会重算。它适合 screening，不适合 promotion。</p><p>同样，C++ loop 变快以后，CLI wall time 可能仍受 parquet、模型和数据加载支配。后续优化重点因此转向 window cache、数组复用和批量 quote decomposition，而不是继续微调一个已经很快的循环。</p><h3 id="3-5-streaming-feature：迁状态，而不是反复搬列表"><a href="#3-5-streaming-feature：迁状态，而不是反复搬列表" class="headerlink" title="3.5 streaming feature：迁状态，而不是反复搬列表"></a>3.5 streaming feature：迁状态，而不是反复搬列表</h3><p>逐笔成交进入系统后，需要更新 1s bar、10s feature、taker tempo、rolling volume、L2 execution 和 cross-market state。</p><p>第一版 <code>SIGNAL_FEATURES=1</code> 恰好犯了 quote scalar 相同的错误：每十秒把最多 320 根 Python <code>Bar1s</code> 和全部 feature history 逐字段复制成 pybind 对象，C++ 算完 overlay 后再返回 dict。旧基准从 Python 约 1.40ms 变成 C++ 约 2.65ms，明显负优化。</p><p>修正后的 <code>SignalFeatureEngine</code> 长期持有最近 320 根 1s bars 和最多 60,480 条 10s history。Python 每完成一根 1s bar 增量推送一次，每十秒只推一条新的 history，再请求一个 feature snapshot：</p><div class="code-wrapper"><pre><code class="hljs text">on_trade(price, qty, side, ts)  -&gt; update current 1s bar  -&gt; close gap/completed bars  -&gt; update rolling state  -&gt; emit compact feature snapshot</code></pre></div><p>固定时间窗的基础统计用模板表达。每次 push 只追加新值并从 deque 左侧淘汰过期值，<code>mean/stddev</code> 直接使用累计的一阶、二阶矩，不再扫描整个窗口：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">int</span> WindowSeconds&gt;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RollingStats</span> &#123;</span><span class="hljs-keyword">public</span>:    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push</span><span class="hljs-params">(std::<span class="hljs-keyword">int64_t</span> ts_ms, <span class="hljs-keyword">double</span> value)</span> </span>&#123;        values_.<span class="hljs-built_in">push_back</span>(&#123;ts_ms, value&#125;);        sum_ += value;        sum_sq_ += value * value;        <span class="hljs-built_in">expire</span>(ts_ms);    &#125;    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">expire</span><span class="hljs-params">(std::<span class="hljs-keyword">int64_t</span> now_ms)</span> </span>&#123;        <span class="hljs-keyword">const</span> <span class="hljs-keyword">auto</span> cutoff = now_ms - WindowSeconds * <span class="hljs-number">1000LL</span>;        <span class="hljs-keyword">while</span> (!values_.<span class="hljs-built_in">empty</span>() &amp;&amp; values_.<span class="hljs-built_in">front</span>().first &lt;= cutoff) &#123;            <span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> old = values_.<span class="hljs-built_in">front</span>().second;            sum_ -= old;            sum_sq_ -= old * old;            values_.<span class="hljs-built_in">pop_front</span>();        &#125;    &#125;    <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">mean</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;        <span class="hljs-keyword">return</span> values_.<span class="hljs-built_in">empty</span>() ? <span class="hljs-number">0.0</span> : sum_ / values_.<span class="hljs-built_in">size</span>();    &#125;    <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">variance</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;        <span class="hljs-keyword">if</span> (values_.<span class="hljs-built_in">size</span>() &lt; <span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">0.0</span>;        <span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> mu = <span class="hljs-built_in">mean</span>();        <span class="hljs-keyword">return</span> std::<span class="hljs-built_in">max</span>(<span class="hljs-number">0.0</span>, sum_sq_ / values_.<span class="hljs-built_in">size</span>() - mu * mu);    &#125;<span class="hljs-keyword">private</span>:    std::deque&lt;std::pair&lt;std::<span class="hljs-keyword">int64_t</span>, <span class="hljs-keyword">double</span>&gt;&gt; values_;    <span class="hljs-keyword">double</span> sum_ = <span class="hljs-number">0.0</span>;    <span class="hljs-keyword">double</span> sum_sq_ = <span class="hljs-number">0.0</span>;&#125;;RollingStats&lt;<span class="hljs-number">3</span>&gt;  tick_sign_3s;RollingStats&lt;<span class="hljs-number">10</span>&gt; tick_sign_10s;RollingStats&lt;<span class="hljs-number">60</span>&gt; log_ret_60s;</code></pre></div><p>更高层的 engine 只暴露增量接口：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SignalFeatureEngine</span> &#123;</span><span class="hljs-keyword">public</span>:    <span class="hljs-built_in">SignalFeatureEngine</span>(std::<span class="hljs-keyword">size_t</span> max_bars = <span class="hljs-number">320</span>,                        std::<span class="hljs-keyword">size_t</span> max_history = <span class="hljs-number">60480</span>);    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push_bar</span><span class="hljs-params">(<span class="hljs-keyword">const</span> Bar1s&amp; bar)</span></span>;    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push_history</span><span class="hljs-params">(<span class="hljs-keyword">const</span> FeatureHistoryRow&amp; row)</span></span>;    [[nodiscard]] <span class="hljs-function">SignalFeatureVector <span class="hljs-title">compute</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">        <span class="hljs-keyword">const</span> Bar1s&amp; bar_10s)</span> <span class="hljs-keyword">const</span></span>;<span class="hljs-keyword">private</span>:    CircularBuffer&lt;Bar1s&gt; bars_;    CircularBuffer&lt;FeatureHistoryRow&gt; history_;&#125;;</code></pre></div><p>这里的返回值也不再是热路径上的 <code>std::map&lt;std::string, double&gt;</code>。80 个稳定字段在 C++ 内部使用 <code>enum class SignalFeatureId</code> 定位，值存进 <code>std::array&lt;double, 80&gt;</code>；只有跨过 pybind 边界返回 Python 时，才按 compile-time 字段表物化一次 dict。这样同时去掉了红黑树节点分配、字符串比较和字段拼写漂移。</p><p>Python 只在 warmup/reconnect 后 seed 一次，正常运行时逐根追加：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">_ensure_cpp_feature_engine</span>(<span class="hljs-params">self</span>):</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> self._cpp_feature_engine_seeded:        <span class="hljs-keyword">for</span> bar <span class="hljs-keyword">in</span> <span class="hljs-built_in">list</span>(self._bar_buffer)[-<span class="hljs-number">320</span>:]:            self._cpp_feature_engine.push_bar(self._bar_to_cpp(bar))        <span class="hljs-keyword">for</span> row <span class="hljs-keyword">in</span> self._feat_history:            self._cpp_feature_engine.push_history(self._history_to_cpp(row))        self._cpp_feature_engine_seeded = <span class="hljs-literal">True</span>    <span class="hljs-keyword">return</span> self._cpp_feature_engine<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">_finalize_bar</span>(<span class="hljs-params">self, bar</span>):</span>    self._bar_buffer.append(bar)    self._ensure_cpp_feature_engine().push_bar(self._bar_to_cpp(bar))overlay = self._ensure_cpp_feature_engine().compute(cpp_bar_10s)</code></pre></div><p>这里故意没有把所有 cross-market book/trade state 一次迁完。当前 C++ overlay 负责稳定、重复的 rolling 数值部分，Python 继续补 execution L2、cross-market、time 和 metrics features。这样的分层可以逐列做 parity；若一口气迁完整 82-feature pipeline，任何时间对齐差异都会很难定位。</p><p>Python 负责事件和研究编排，C++ 负责持续演化的数值状态。这个边界也更接近未来真正值得优化的“计算/决策层”，而不是去重写 WebSocket 和 IO。</p><p>同一组固定环境三轮中位数中，完整“喂入十秒事件并计算 features”的路径从 Python <code>803.20 us</code> 降到 C++ persistent state <code>563.26 us</code>，快约 <strong>29.9%</strong>。单独的 1s ingest 会从 <code>22.42 us</code> 增到约 <code>25.44 us</code>，但十秒整体仍有净收益，因为昂贵的窗口重拷贝被移除了。</p><p>另一个开关 <code>SIGNAL_STATE=1</code> 只把单条 trade aggregation 搬进 C++，Python 仍负责 event dict 解析、锁、回调和 bar 对象同步。它的 ingest 中位数是 <code>23.44 us</code>，比 Python <code>22.42 us</code> 慢约 4.5%，因此继续保持关闭。这里的结论很朴素：<strong>只有状态和足够长的计算链一起迁移才有意义，单独迁四则运算没有。</strong></p><h3 id="3-6-pybind11-的教训：不要估算边界，要-profile-边界"><a href="#3-6-pybind11-的教训：不要估算边界，要-profile-边界" class="headerlink" title="3.6 pybind11 的教训：不要估算边界，要 profile 边界"></a>3.6 pybind11 的教训：不要估算边界，要 profile 边界</h3><p>编译后的 extension 会被 CPython 作为 native module 加载。进入 C++ 后当然运行机器码，但调用前后的 tuple/dict 解包、属性访问、容器复制和返回对象构造依旧发生。NarrowGate 这次最有价值的经验不是某个通用的“pybind 调用耗时”，而是同一条路径在不同接口形状下会从负优化变成正收益：</p><div class="table-container"><table><thead><tr><th>接口形状</th><th>结果</th></tr></thead><tbody><tr><td>scalar quote + 每 tick 完整 dataclass/dict</td><td>C++ 明显慢于 Python</td></tr><tr><td>scalar quote + cached config + compact context</td><td>C++ quote+policy 快约 16.4%</td></tr><tr><td>每 10s 重传 320 bars/history</td><td>C++ features 明显变慢</td></tr><tr><td>C++ 常驻 rolling state + 增量更新</td><td>10s features 快约 29.9%</td></tr><tr><td>单条 trade aggregation 跨一次 pybind</td><td>基本打平且略慢</td></tr><tr><td>离线连续数组 batch / 完整 replay 状态机</td><td>最适合迁移</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：迁移对象应该按“状态是否能常驻、单次调用是否足够大”来选择。</li><li>不能说明什么：不能简单得出“Python 慢、C++ 快”；接口形状比语言标签更关键。</li><li>下一步验证：每个 native 开关都必须同时通过 parity、wall-time 和 strict import/source 检查。</li></ul></blockquote><p>因此现在的判断标准是：</p><ul><li>计算很少、对象很多时，保留 Python；</li><li>配置稳定时缓存 native representation；</li><li>rolling window 让 C++ 持有状态，不重复搬历史；</li><li>离线大数组用 batch，一次跨边界处理许多样本；</li><li>完整诊断对象按需要生成，不把可观测性变成每 tick 的固定税；</li><li>每个开关都必须同时通过 parity 和 wall-time，不能只证明“确实执行了 C++”。</li></ul><h4 id="3-6-1-一条-native-path-要过三层测试"><a href="#3-6-1-一条-native-path-要过三层测试" class="headerlink" title="3.6.1 一条 native path 要过三层测试"></a>3.6.1 一条 native path 要过三层测试</h4><p>第一层是小输入的字段 parity，用专门构造的 inventory、depth、adverse/defense 场景逐字段比较；第二层是 synthetic replay，检查成交路径、queue、latency、TTL 和 emergency 分支；第三层才是真实窗口 golden test：</p><div class="code-wrapper"><pre><code class="hljs bash"><span class="hljs-comment"># quote / signal / synthetic replay</span>.venv/bin/python -m pytest \  tests/test_cpp_quote_core_parity.py \  tests/test_cpp_signal_features.py \  tests/test_cpp_tick_replay_parity.py -q<span class="hljs-comment"># 正常、高波动、稀疏和跨月真实窗口</span>RUN_NARROWGATE_GOLDEN=1 NARROWGATE_CPP_STRICT=1 \  .venv/bin/python -m pytest \  tests/test_cpp_tick_replay_golden_parity.py -q</code></pre></div><p>golden test 不要求所有浮点数逐 bit 相同，但必须给每类差异设显式容忍度。尤其不能只比最终 PnL，因为两条不同成交路径可能偶然得到接近的 PnL。当前比较集合至少包括：</p><div class="code-wrapper"><pre><code class="hljs python">SUMMARY_KEYS = (    <span class="hljs-string">&quot;pnl&quot;</span>, <span class="hljs-string">&quot;inventory_adjusted_pnl&quot;</span>, <span class="hljs-string">&quot;inventory_pnl&quot;</span>,    <span class="hljs-string">&quot;fills_total&quot;</span>, <span class="hljs-string">&quot;fills_bid&quot;</span>, <span class="hljs-string">&quot;fills_ask&quot;</span>, <span class="hljs-string">&quot;final_inventory&quot;</span>,    <span class="hljs-string">&quot;avg_markout&quot;</span>, <span class="hljs-string">&quot;markout_count&quot;</span>,    <span class="hljs-string">&quot;abs_inventory_time_s&quot;</span>, <span class="hljs-string">&quot;signed_inventory_time_s&quot;</span>,    <span class="hljs-string">&quot;sq_inventory_time_s&quot;</span>, <span class="hljs-string">&quot;notional_inventory_time_s&quot;</span>,    <span class="hljs-string">&quot;avg_spread&quot;</span>, <span class="hljs-string">&quot;avg_final_spread&quot;</span>, <span class="hljs-string">&quot;n_requotes&quot;</span>,    <span class="hljs-string">&quot;quote_spread_lt_100_rate&quot;</span>, <span class="hljs-string">&quot;cap_hit_rate&quot;</span>,    <span class="hljs-string">&quot;sharpe&quot;</span>, <span class="hljs-string">&quot;max_drawdown&quot;</span>,)</code></pre></div><p>开发阶段还必须打开 strict 模式：</p><div class="code-wrapper"><pre><code class="hljs bash">NARROWGATE_CPP_QUOTE_CORE=1 \NARROWGATE_CPP_SIGNAL_FEATURES=1 \NARROWGATE_CPP_STRICT=1 \.venv/bin/python bench/bench_live_path.py \  --n 10000 --signal-n 1000 --engine cpp --strict-cpp</code></pre></div><p>如果 C++ import、模块来源检查或某个 native 调用失败，这条命令必须非零退出。否则 benchmark 很可能测到 fallback 后的 Python，却被错误记录为 C++ 性能。</p><h4 id="3-6-2-是否迁移，用“状态驻留-×-调用粒度”决定"><a href="#3-6-2-是否迁移，用“状态驻留-×-调用粒度”决定" class="headerlink" title="3.6.2 是否迁移，用“状态驻留 × 调用粒度”决定"></a>3.6.2 是否迁移，用“状态驻留 × 调用粒度”决定</h4><p>项目现在用下面这套决策矩阵，而不是看到 Python 函数就重写：</p><div class="table-container"><table><thead><tr><th>路径</th><th style="text-align:right">状态是否可常驻</th><th style="text-align:right">单次工作量</th><th>方案</th></tr></thead><tbody><tr><td>parquet/特征表 IO</td><td style="text-align:right">否</td><td style="text-align:right">大，但 Arrow 已优化</td><td>保留 Python/Arrow</td></tr><tr><td>LightGBM 训练</td><td style="text-align:right">模型库内部已 native</td><td style="text-align:right">大</td><td>保留 Python orchestration</td></tr><tr><td>scalar side policy</td><td style="text-align:right">少</td><td style="text-align:right">约几十微秒</td><td>与 quote/routing 合并后再迁</td></tr><tr><td>rolling signal</td><td style="text-align:right">是</td><td style="text-align:right">每 trade/每秒重复</td><td>C++ 长期持有窗口状态</td></tr><tr><td>quote decomposition</td><td style="text-align:right">否</td><td style="text-align:right">数万至数百万行</td><td>NumPy contiguous batch</td></tr><tr><td>tick replay</td><td style="text-align:right">是</td><td style="text-align:right">完整窗口顺序状态机</td><td>一次 C++ 调用跑完整窗口</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：迁移优先级来自计算形态，不来自文件名或语言偏好。</li><li>不能说明什么：不能说明 WebSocket、REST、日志这些冷路径没有工程价值，只是它们不是当前 CPU 热点。</li><li>下一步验证：对候选迁移点先做 flame/profile，再设计最小 native API，而不是照搬 Python 对象模型。</li></ul></blockquote><p>这张表也解释了为什么 WebSocket 层没有成为 C++ 迁移重点。网络等待、JSON 解码和 Python callback 不是当前最大的可控 CPU 成本；真正值得迁的是 WebSocket 上方的 rolling state、quote/policy bundle 和订单状态机。把 IO 也改写，只会扩大维护面，却不一定缩短决策尾延迟。</p><p>当前相关 quote、signal、tick replay 快速测试共 <code>20 passed</code>；显式开启的四个真实窗口 golden tests 也全部通过。这些 benchmark 是同机 synthetic CPU isolation，关闭 ML 以分离 feature/quote 成本；它们不代表网络延迟、模型推理或完整事件引擎的端到端耗时，也不能直接决定 live 默认配置。</p><h4 id="3-6-3-C-20-现代化：改的是数据运动，不只是标准号"><a href="#3-6-3-C-20-现代化：改的是数据运动，不只是标准号" class="headerlink" title="3.6.3 C++20 现代化：改的是数据运动，不只是标准号"></a>3.6.3 C++20 现代化：改的是数据运动，不只是标准号</h4><p>完成 Python/C++ parity 后，构建标准从 C++17 升到了 C++20：</p><div class="code-wrapper"><pre><code class="hljs cmake"><span class="hljs-keyword">set</span>(CMAKE_CXX_STANDARD <span class="hljs-number">20</span>)<span class="hljs-keyword">set</span>(CMAKE_CXX_STANDARD_REQUIRED <span class="hljs-keyword">ON</span>)<span class="hljs-keyword">set</span>(CMAKE_CXX_EXTENSIONS <span class="hljs-keyword">OFF</span>)<span class="hljs-keyword">target_compile_features</span>(narrowgate_cpp PRIVATE cxx_std_20)</code></pre></div><p>选择 C++20 并不是为了把所有新语法都塞进交易路径，而是优先使用几项收益明确、行为容易验证的能力。</p><p>第一项是用 <code>concept</code> 和 <code>std::span</code> 表达“不拥有、连续、只读的数值数组”。quote core、tick replay 和 streaming feature 不再各自传裸指针与长度：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt;<span class="hljs-keyword">concept</span> Arithmetic = std::is_arithmetic_v&lt;T&gt;;<span class="hljs-keyword">template</span> &lt;Arithmetic T&gt;<span class="hljs-keyword">using</span> ArrayView = std::span&lt;<span class="hljs-keyword">const</span> T&gt;;<span class="hljs-keyword">template</span> &lt;Arithmetic T&gt;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">MatrixView</span> &#123;</span>    ArrayView&lt;T&gt; values;    std::<span class="hljs-keyword">size_t</span> rows&#123;&#125;;    std::<span class="hljs-keyword">size_t</span> cols&#123;&#125;;    [[nodiscard]] <span class="hljs-function">T <span class="hljs-title">at</span><span class="hljs-params">(std::<span class="hljs-keyword">size_t</span> row,</span></span><span class="hljs-params"><span class="hljs-function">                       std::<span class="hljs-keyword">size_t</span> col)</span> <span class="hljs-keyword">const</span> <span class="hljs-keyword">noexcept</span> </span>&#123;        <span class="hljs-keyword">return</span> values[row * cols + col];    &#125;&#125;;</code></pre></div><p>这不会自动让循环变快，但它把 shape、ownership 和 constness 写进类型，减少 Python buffer 绑定和 replay 数组新增字段时的错误面。<code>[[nodiscard]]</code> 则放在 quote result、feature snapshot 和 replay result 上，避免调用方无意丢弃策略结果。</p><p>第二项是固定容量 ring buffer。旧的 60,480 行 history 使用 <code>vector.erase(begin())</code> 淘汰头部，每次满载 push 都会搬动后续元素；现在只覆盖 head：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CircularBuffer</span> &#123;</span><span class="hljs-keyword">public</span>:    <span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">CircularBuffer</span><span class="hljs-params">(std::<span class="hljs-keyword">size_t</span> capacity)</span></span><span class="hljs-function">        : storage_(capacity) &#123;</span>&#125;    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push</span><span class="hljs-params">(<span class="hljs-keyword">const</span> T&amp; value)</span> </span>&#123;        <span class="hljs-keyword">if</span> (size_ &lt; storage_.<span class="hljs-built_in">size</span>()) &#123;            storage_[(head_ + size_) % storage_.<span class="hljs-built_in">size</span>()] = value;            ++size_;            <span class="hljs-keyword">return</span>;        &#125;        storage_[head_] = value;        head_ = (head_ + <span class="hljs-number">1</span>) % storage_.<span class="hljs-built_in">size</span>();    &#125;    [[nodiscard]] <span class="hljs-keyword">const</span> T&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-keyword">size_t</span> i) <span class="hljs-keyword">const</span> &#123;        <span class="hljs-keyword">return</span> storage_[(head_ + i) % storage_.<span class="hljs-built_in">size</span>()];    &#125;<span class="hljs-keyword">private</span>:    std::vector&lt;T&gt; storage_;    std::<span class="hljs-keyword">size_t</span> head_&#123;&#125;;    std::<span class="hljs-keyword">size_t</span> size_&#123;&#125;;&#125;;</code></pre></div><p>对应的 80 维 feature 输出改成稳定布局；字段名只在边界出现：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-keyword">class</span> <span class="hljs-title">SignalFeatureId</span> :</span> std::<span class="hljs-keyword">size_t</span> &#123;    AggressionImbalance10s,    BookImbalance,    <span class="hljs-comment">// ... 其余稳定字段</span>    Count&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">SignalFeatureVector</span> &#123;</span>    std::array&lt;<span class="hljs-keyword">double</span>,        <span class="hljs-keyword">static_cast</span>&lt;std::<span class="hljs-keyword">size_t</span>&gt;(SignalFeatureId::Count)&gt; values&#123;&#125;;    <span class="hljs-keyword">double</span>&amp; <span class="hljs-keyword">operator</span>[](SignalFeatureId id) <span class="hljs-keyword">noexcept</span> &#123;        <span class="hljs-keyword">return</span> values[<span class="hljs-keyword">static_cast</span>&lt;std::<span class="hljs-keyword">size_t</span>&gt;(id)];    &#125;&#125;;</code></pre></div><p>名称诊断使用 <code>std::ranges::lower_bound</code>，失败路径附带 <code>std::source_location</code>；窗口常量使用 <code>std::to_array</code>。这些都在冷路径或编译期工作，不给每 tick 增加新分配。</p><p>第三项是让 replay 的状态和 trace 从字符串变成强类型枚举，并用 <code>std::pmr::monotonic_buffer_resource</code> 承担单次 replay 内部短生命周期容器：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-keyword">class</span> <span class="hljs-title">OrderState</span> :</span> std::<span class="hljs-keyword">uint8_t</span> &#123;    PendingNew, Active, PendingCancel, Filled, Cancelled&#125;;<span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-keyword">class</span> <span class="hljs-title">TraceOutcome</span> :</span> std::<span class="hljs-keyword">uint8_t</span> &#123;    None, Filled, Cancelled, Expired, Rejected&#125;;std::array&lt;std::byte, 64 * 1024&gt; replay_scratch&#123;&#125;;std::<span class="hljs-function">pmr::monotonic_buffer_resource <span class="hljs-title">arena</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">    replay_scratch.data(), replay_scratch.size())</span></span>;std::pmr::vector&lt;ReplayOrder&gt; bid_orders&#123;&amp;arena&#125;;std::pmr::vector&lt;ReplayOrder&gt; ask_orders&#123;&amp;arena&#125;;</code></pre></div><p>pybind property 仍返回旧的字符串值，所以 Python trace schema、parquet 报告和 A/B 脚本无需改变。PMR 只管理函数内部的临时订单和 pending markout；需要返回 Python 的结果继续使用普通 owning container，避免把 arena 生命周期泄漏到边界外。</p><p>本次现代化后的同机结果如下：</p><div class="table-container"><table><thead><tr><th>测量</th><th style="text-align:right">旧实现</th><th style="text-align:right">C++20 实现</th><th>解读</th></tr></thead><tbody><tr><td>500-row 短历史 signal 10s</td><td style="text-align:right">557.32 us</td><td style="text-align:right">553.17 us</td><td>快约 0.7%，短窗口本来就不是搬移瓶颈</td></tr><tr><td>quote + policy</td><td style="text-align:right">63.09 us</td><td style="text-align:right">63.18 us</td><td>统计上持平，现代化没有伤害 scalar path</td></tr><tr><td>60,480 history 满载 push（纯 C++ operation microbench）</td><td style="text-align:right">80.05 us</td><td style="text-align:right">0.0026 us</td><td>去掉 <code>erase(begin)</code> 后由 O(N) 变 O(1)</td></tr><tr><td>60,480 history full snapshot</td><td style="text-align:right">-</td><td style="text-align:right">111.23 us</td><td>常驻 ring + 定长输出下的完整计算</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：C++20 现代化主要减少数据搬移和生命周期错误，短路径不一定会显著变快。</li><li>不能说明什么：<code>0.0026 us</code> 不是 live 总延迟，也不是策略收益，只是一个容器操作微基准。</li><li>下一步验证：继续看真实窗口 golden parity、长 history p99，以及目标 Linux CPU 上的 PMU 或 wall-time soak。</li></ul></blockquote><p>第三行尤其不能直接解读成 live 总延迟快了三万倍：它只隔离测量“满载后保留尾部”的容器操作。真实路径还包含 Python event、pybind、模型和路由。它说明的是旧容器在长时间运行后存在确定的线性成本，而不是给系统吞吐量制造一个夸张数字。</p><p>验证也按风险分层完成：第一轮快速 quote/signal/replay 测试为 <code>18 passed, 4 skipped</code>；另行显式运行 May 正常、高波动、Feb 稀疏和 Jan 窗口的 golden parity 为 <code>4 passed</code>。此外三个 C++ 核心翻译单元用 <code>-std=c++20 -Wall -Wextra -Wpedantic</code> 编译为零 warning。由于短路径收益接近噪声，本次只更新 native 基础设施和研究路径，没有修改 live 配置。</p><h4 id="3-6-4-第二轮：消除-depth-分配，拆开热状态与诊断状态"><a href="#3-6-4-第二轮：消除-depth-分配，拆开热状态与诊断状态" class="headerlink" title="3.6.4 第二轮：消除 depth 分配，拆开热状态与诊断状态"></a>3.6.4 第二轮：消除 depth 分配，拆开热状态与诊断状态</h4><p>第一轮解决了 Python/C++ 边界和长 history 头删，但 profile 里还有三类成本：每次 requote 临时构造 L2 vectors、热订单携带完整 trace、长周期 vol-regime 每次重新扫历史。第二轮没有改策略公式，而是继续压缩数据运动。</p><h4 id="L2-使用-view，不再逐行复制"><a href="#L2-使用-view，不再逐行复制" class="headerlink" title="L2 使用 view，不再逐行复制"></a>L2 使用 view，不再逐行复制</h4><p>原始 L2 输入是四个 SoA 矩阵：bid price/qty、ask price/qty。把它们先拼成 <code>vector&lt;DepthLevel&gt;</code>，既分配又把 SoA 重新交错。现在 quote core 直接消费两个 span pair：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">DepthSideView</span> &#123;</span>    std::span&lt;<span class="hljs-keyword">const</span> DepthLevel&gt; levels; <span class="hljs-comment">// owned API 可用</span>    std::span&lt;<span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span>&gt; prices;     <span class="hljs-comment">// matrix row 可用</span>    std::span&lt;<span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span>&gt; quantities;    [[nodiscard]] <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">price</span><span class="hljs-params">(std::<span class="hljs-keyword">size_t</span> i)</span> <span class="hljs-keyword">const</span> <span class="hljs-keyword">noexcept</span></span>;    [[nodiscard]] <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">quantity</span><span class="hljs-params">(std::<span class="hljs-keyword">size_t</span> i)</span> <span class="hljs-keyword">const</span> <span class="hljs-keyword">noexcept</span></span>;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">DepthView</span> &#123;</span>    DepthSideView bids;    DepthSideView asks;&#125;;<span class="hljs-keyword">const</span> DepthView depth&#123;    DepthSideView&#123;&#123;&#125;, bid_px.<span class="hljs-built_in">row</span>(i), bid_qty.<span class="hljs-built_in">row</span>(i)&#125;,    DepthSideView&#123;&#123;&#125;, ask_px.<span class="hljs-built_in">row</span>(i), ask_qty.<span class="hljs-built_in">row</span>(i)&#125;,&#125;;<span class="hljs-keyword">const</span> <span class="hljs-keyword">auto</span> quote = <span class="hljs-built_in">compute_quote_core</span>(state, cfg, pred, depth);</code></pre></div><p>公开 pybind API 仍保留 owning <code>DepthSnapshot</code>，它只需提供 <code>.view()</code>。replay 和 batch 则直接 view NumPy buffer 的矩阵行，不再为每个 tick/row 分配两个 vector。</p><p>BBO/L2 的 current-time snapshot 也从每次 binary search 改成 loop-owned cursor：</p><div class="code-wrapper"><pre><code class="hljs cpp">bbo_idx = <span class="hljs-built_in">advance_index</span>(input.bbo_ts_ms, bbo_idx, ts);l2_idx = <span class="hljs-built_in">advance_index</span>(input.l2_ts_ms, l2_idx, ts);<span class="hljs-keyword">const</span> <span class="hljs-keyword">auto</span> book = <span class="hljs-built_in">book_snapshot_at</span>(input, ts, bbo_idx, l2_idx, ...);</code></pre></div><p>这里有一个不能“顺手优化”的边界：queue ahead 使用未来 <code>activate_ts</code>，而 latency jitter 会让相邻订单的 activation timestamp 不保证单调。它不能共享 current-time cursor，因此仍保留二分。错误共享 cursor 会把未来盘口泄漏给当前 quote，速度快了，回测却坏了。</p><h4 id="ReplayOrder-只保存会参与成交循环的字段"><a href="#ReplayOrder-只保存会参与成交循环的字段" class="headerlink" title="ReplayOrder 只保存会参与成交循环的字段"></a>ReplayOrder 只保存会参与成交循环的字段</h4><p>旧 <code>ReplayOrder</code> 同时持有完整 <code>SideQuoteContext</code> 和 <code>TraceOrderRow</code>。即使 trace 关闭，<code>transition_orders()</code>、<code>best_live_order()</code>、<code>process_side_fill()</code> 扫描的每个订单也背着一大块冷数据。现在热结构只保留成交状态：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ReplayOrder</span> &#123;</span>    Side side;    <span class="hljs-keyword">double</span> price, remaining, queue_left, queue_init;    std::<span class="hljs-keyword">int64_t</span> quote_ts, activate_ts, cancel_effective_ts, ttl_ms;    OrderState state;    <span class="hljs-keyword">bool</span> side_adverse, defense_guard, local_extreme_guard, quote_ev;    TraceOrderRow* trace = <span class="hljs-literal">nullptr</span>;&#125;;</code></pre></div><p>只有 <code>trace_quotes_max</code> 或 <code>trace_fills_max</code> 大于零时，才从 arena-backed <code>unsynchronized_pool_resource</code> 分配 cold row。hot order 用带 PMR deleter 的 RAII pointer；订单退出后 slot 回到 pool，不会像纯 monotonic allocation 一样随长窗口持续累积。输出 trace 仍复制到 owning result vector，pool pointer 不跨函数生命周期。</p><p>fill trace 的时间窗也改成 <code>lower_bound/upper_bound</code> 定位，不再从第零条成交开始扫；1s/5s/30s markout 同样用 <code>lower_bound</code> 找 horizon。trace 默认关闭，这项优化主要服务大规模 quote decomposition。</p><h4 id="summary-only-不是简单丢掉曲线"><a href="#summary-only-不是简单丢掉曲线" class="headerlink" title="summary-only 不是简单丢掉曲线"></a>summary-only 不是简单丢掉曲线</h4><p>PnL curves 原先还承担 Python 侧 Sharpe 和 max drawdown，所以不能直接禁用。C++ replay 现在每次 requote 在线维护 peak、drawdown、PnL delta、时间增量以及标准化 delta 的一、二阶矩：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> normalized = pnl_delta / std::<span class="hljs-built_in">sqrt</span>(dt_s);pnl_delta_sum += pnl_delta;pnl_dt_sum += dt_s;normalized_sum += normalized;normalized_sq_sum += normalized * normalized;peak_pnl = std::<span class="hljs-built_in">max</span>(peak_pnl, current_pnl);max_drawdown = std::<span class="hljs-built_in">max</span>(max_drawdown, peak_pnl - current_pnl);</code></pre></div><p>单次 API 的 <code>collect_curves=true</code> 保持兼容；C++ sweep、cap A/B、统一 <code>tick_ab.py</code> 和 quote-EV A/B 自动使用 summary-only。2M trades、200k requotes 的 synthetic replay 中，CPU 只从 42.18ms 降到 41.77ms，约 1%；真正收益是三条 200k vector 变成零行，也无需再转成 NumPy。</p><h4 id="长周期-moments-增量化，但保持数值稳定"><a href="#长周期-moments-增量化，但保持数值稳定" class="headerlink" title="长周期 moments 增量化，但保持数值稳定"></a>长周期 moments 增量化，但保持数值稳定</h4><p>长 history 的主要扫描来自 2,160/8,640 条 <code>return_abs</code> 均值和最多 60,480 条 <code>vol_regime_6h</code> z-score。它们现在由 fixed-count rolling moments 在 <code>push_history()</code> 时更新。</p><p>简单维护 <code>sum_sq - sum²/n</code> 在低方差序列上会严重消差；长历史 parity test 正好抓到了这个问题。因此实现使用支持删除最旧值的 Welford <code>mean/M2</code> 更新，而不是放宽测试：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">remove_value</span><span class="hljs-params">(<span class="hljs-keyword">double</span> x, std::<span class="hljs-keyword">size_t</span> n,</span></span><span class="hljs-params"><span class="hljs-function">                         <span class="hljs-keyword">double</span>&amp; mean, <span class="hljs-keyword">double</span>&amp; m2)</span> <span class="hljs-keyword">noexcept</span> </span>&#123;    <span class="hljs-keyword">const</span> <span class="hljs-keyword">double</span> next_mean = (n * mean - x) / (n - <span class="hljs-number">1</span>);    m2 = std::<span class="hljs-built_in">max</span>(<span class="hljs-number">0.0</span>, m2 - (x - mean) * (x - next_mean));    mean = next_mean;&#125;</code></pre></div><p><code>SignalFeatureEngine.compute_values()</code> 返回固定顺序的 NumPy array；<code>SIGNAL_FEATURE_NAMES</code> 只在模块初始化时创建一次。旧 dict API 仍兼容。满 60,480 history 时，fixed-array snapshot 从第一轮约 111.23us 降到 1.90us，兼容 dict 为 4.42us。短历史 live 整体仍受 Python feature merge/model 路径影响，没有据此切换 live 默认配置。</p><h4 id="只给逐行独立-batch-加线程"><a href="#只给逐行独立-batch-加线程" class="headerlink" title="只给逐行独立 batch 加线程"></a>只给逐行独立 batch 加线程</h4><p>单个 replay 的订单、库存、现金和 latency 状态强顺序依赖，仍保持单线程。depth quote batch 的每一行独立，才新增显式 <code>workers</code> 参数；默认 1，每 worker 至少 4,096 行：</p><div class="code-wrapper"><pre><code class="hljs cpp">std::vector&lt;std::jthread&gt; threads;<span class="hljs-keyword">for</span> (std::<span class="hljs-keyword">size_t</span> worker = <span class="hljs-number">0</span>; worker &lt; worker_count; ++worker) &#123;    threads.<span class="hljs-built_in">emplace_back</span>(run_rows, begin[worker], end[worker]);&#125;</code></pre></div><p>100k rows × 10 levels 的 Release benchmark：1 worker 为 35.48ms / 2.82M rows/s，4 workers 为 10.08ms / 9.92M rows/s，快 3.52x。参数已经贯通到 <code>cross_market_shock_audit.py --quote-context-workers N</code>，但不会自动开启：若外层已经按 month/window/arm 多进程并行，再把每个进程开满内部线程只会过度订阅。</p><p>第二轮验证结果为快速 suite <code>20 passed, 4 skipped</code>，四个真实窗口 golden parity <code>4 passed</code>，核心 C++20 翻译单元仍为零 warning。优化改变了 allocation、cache footprint 和输出策略，没有改变成交路径，也没有修改 live 配置。</p><h4 id="3-6-5-ARM-本机-benchmark-与-x86-live-的边界"><a href="#3-6-5-ARM-本机-benchmark-与-x86-live-的边界" class="headerlink" title="3.6.5 ARM 本机 benchmark 与 x86 live 的边界"></a>3.6.5 ARM 本机 benchmark 与 x86 live 的边界</h4><p>这里还要补一个容易被忽略的限制：我的训练和回测主要在 Apple Silicon/ARM64 本机上完成，而真正的 live 机器往往是 x86 Linux。两边的 CPU 前端、cache 层级、memory ordering、线程调度、LightGBM/OpenMP runtime 都可能不同。更细一点说，x86 的原子读改写通常对应 <code>LOCK</code> 前缀一类硬件原语和更强的内存模型；ARM 上常见实现会通过 load-exclusive / store-exclusive retry loop 或 LSE 指令完成，不同 CPU、编译参数和竞争程度下成本差异很大。</p><p>NarrowGate 当前 C++ hot path 并没有把 <code>std::atomic</code> 当作优化手段；代码里真正显式的 C++ 内部并发，是 <code>compute_quote_core_batch_depth(..., workers=N)</code> 的 <code>std::jthread</code> 分片。因此风险不是“某个 atomic trick 在 ARM 上慢”，而是更朴素：<strong>本机离线吞吐优化不等于 x86 live 尾延迟优化</strong>。</p><p>更专业一点说，我后来把优化项按 CPU/OS 层拆成下面这张表，而不是笼统地说“C++ 更快”：</p><div class="table-container"><table><thead><tr><th>层面</th><th>Apple Silicon / ARM 本机更容易看到的收益</th><th>x86 Linux live 需要重新判断的点</th><th>对 NarrowGate 的工程含义</th></tr></thead><tbody><tr><td>CPU 前端与 I-cache</td><td>本机编译器、P-core 前端、统一内存带来的微基准很稳定，适合看代码布局和 pybind 固定税</td><td>目标 live 机器通常是云上 Intel/AMD，32KiB L1I 很常见，SMT/虚拟化会改变 p99</td><td>只缩小整个 <code>.so</code> 的 <code>__text</code> 没意义；要看 live 事件实际经过的 hot instruction working set</td></tr><tr><td>D-cache / TLB / allocator</td><td>replay 的 <code>DepthView</code>、hot/cold order、summary-only curves 很容易在本机吞吐 benchmark 中体现</td><td>live 的状态更小，但 tail latency 更怕偶发分配、page fault、跨 NUMA/跨 socket 访问</td><td>replay 优化主要服务研究吞吐；live 侧要优先去掉每 tick 动态对象和字符串查找</td></tr><tr><td>memory ordering / atomics</td><td>ARM 上不同实现可能走 LL/SC retry 或 LSE，竞争时成本形态和 x86 不同</td><td>x86 有更强内存模型和 <code>LOCK</code> 前缀原语，但 false sharing 仍会毁掉 tail</td><td>当前 hot path 不用 <code>std::atomic</code>；未来 lock-free queue 必须分别在 ARM/x86 测</td></tr><tr><td>SIMD / 数学库</td><td>ARM64/NEON、Apple Accelerate/本机 wheel 影响训练和 NumPy/LightGBM 吞吐</td><td>x86 wheel 可能走 AVX2/AVX512/OpenBLAS/OpenMP，不同实例族差别很大</td><td><code>models/ml_model.py</code> 的 Apple 参数只服务本机训练；live venv 要固定 BLAS/OMP 线程</td></tr><tr><td>线程调度 / SMT / IRQ</td><td>本机桌面负载和核心调度相对可控</td><td>云上 2 vCPU 可能只是同一个物理核的两个 SMT 线程；ENA/NVMe IRQ 与 softirq 会抢 CPU</td><td><code>taskset</code> 不一定总是好事；先固定库线程，再观察 CPU migration 和 p99.9</td></tr><tr><td>Python/C++ ABI</td><td>pybind 往返在 ARM 上可能比 quote 数学本身更贵，导致 scalar C++ 负优化</td><td>x86 上 tuple ABI 可能足够便宜，但 dict/asdict 仍是固定税</td><td>live C++ 要迁“大包决策”，不要迁单个 scalar 函数；固定 tuple/array 比 dict 更重要</td></tr><tr><td>可观测性</td><td>本机可用 Instruments、<code>time</code>、Python profiler 和局部 microbench</td><td>云 KVM 可能不暴露 PMU，<code>perf stat</code> 看不到 cycles/L1I/iTLB</td><td>没有 PMU 时只能先用 wall-time p99/p99.9 soak；最终 promotion 要在真实 live 机型复测</td></tr></tbody></table></div><p>我把这些结果分成两类。</p><p>第一类是“本机有效但 live 不一定有效”：</p><ul><li><code>models/ml_model.py</code> 里的 Apple Silicon/ARM64 假设。LightGBM 的 native ARM64、NEON、统一内存和本机核心数，只说明本机训练吞吐；x86 研究机应重新设置 <code>MM_LGB_THREADS</code>、histogram pool 和 OpenMP 亲和性。</li><li>所有 multiprocessing sweep。<code>backtest_tick.py --sweep</code>、<code>tick_ab.py --workers</code>、quote EV A/B workers 缩短的是多月、多臂、多窗口研究 wall time；live 的决策路径仍然是单事件、强顺序、低尾延迟问题。</li><li><code>compute_quote_core_batch_depth(..., workers=N)</code> 的 worker 分片。100k rows × 10 levels 在本机 4 workers 快 3.52x，但如果外层已经按 month/window/arm 多进程并行，再在每个进程内部开满线程，只会过度订阅。x86 上 worker 数要重新扫。</li><li>tick replay 的 allocator/cache locality benchmark。<code>DepthView</code>、hot/cold order、summary-only curves 和 PMR trace pool 减少了 replay 的内存运动与 Python 转换，但它们首先服务离线研究吞吐，不自动证明 live quote/routing 更快。</li></ul><p>第二类是在 x86 live 目标机器上必须单独 benchmark 的开关：</p><div class="code-wrapper"><pre><code class="hljs bash">scripts/x86_live_env_audit.shscripts/x86_live_benchmark.shscripts/x86_live_soak.sh</code></pre></div><p>这些 benchmark 也不能只看 mean。live 关心 p99/p99.9、偶发抖动和是否触发跨语言 fallback。目标 x86 Linux 上应该同时看 <code>cycles</code>、<code>instructions</code>、<code>branch-misses</code>、<code>L1-icache-load-misses</code>、<code>iTLB-load-misses</code>、LLC miss、context switches。只有这些指标和策略 parity 一起过，native 开关才有资格从 shadow 变成配置候选。</p><p>后来我真的拿了一台 x86 EC2 机器做了这件事：Amazon Linux 2023、Intel Xeon Platinum 8259CL、2 vCPU（一个物理核的两个 SMT 线程）、L1d/L1i 都是 32KiB，clocksource 是 <code>tsc</code>。这台机器一开始甚至没有 <code>gcc/g++/cmake</code>，所以第一步不是调参数，而是把 live benchmark 环境和训练环境拆开。</p><p>这里有个很现实的坑：如果在 Linux live 机器上直接 <code>pip install -r requirements.txt</code>，<code>torch</code> 会默认拉一大串 CUDA wheels。它对这个 maker live path 没帮助，却会把 32G 根盘迅速塞满。我最后只装了 live benchmark 需要的轻量依赖：</p><div class="code-wrapper"><pre><code class="hljs bash">python3.11 -m venv .venv<span class="hljs-built_in">source</span> .venv/bin/activatepip install numpy pandas pyarrow lightgbm scikit-learn scipy \  pyyaml requests binance-futures-connector websocket-client \  pycryptodome zstandard pytestpip install -e cpp</code></pre></div><p>为了不再靠手敲命令，我加了两个脚本：</p><div class="code-wrapper"><pre><code class="hljs bash">scripts/x86_live_env_audit.shscripts/x86_live_benchmark.shscripts/x86_live_soak.sh</code></pre></div><p>第一个脚本记录 OS、CPU/cache、clocksource、sysctl、IRQ、Python/narrowgate_cpp import 和 NumPy backend。第二个脚本做短跑 smoke：Python baseline、C++ quote core、C++ signal features、候选组合和 compact live-routing ABI。第三个脚本只跑候选组合长样本 soak，用来观察 p99.9。</p><div class="code-wrapper"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> OMP_NUM_THREADS=1<span class="hljs-built_in">export</span> OPENBLAS_NUM_THREADS=1<span class="hljs-built_in">export</span> MKL_NUM_THREADS=1<span class="hljs-built_in">export</span> NUMEXPR_NUM_THREADS=1<span class="hljs-built_in">export</span> MALLOC_ARENA_MAX=1</code></pre></div><p>然后依次跑 Python baseline、C++ quote core、C++ signal features、候选组合和 compact live-routing ABI。</p><p>这次 x86 结果很有意思，因为它和 Apple Silicon 本机结论并不完全一致：</p><div class="table-container"><table><thead><tr><th>开关</th><th style="text-align:right">signal 10s mean/p99</th><th style="text-align:right"><code>_compute_quotes</code> mean/p99</th><th style="text-align:right"><code>quote+policy</code> mean/p99</th><th>结论</th></tr></thead><tbody><tr><td>Python baseline</td><td style="text-align:right">3431 / 4159 us</td><td style="text-align:right">152 / 201 us</td><td style="text-align:right">240 / 305 us</td><td>x86 baseline</td></tr><tr><td><code>NARROWGATE_CPP_QUOTE_CORE=1</code></td><td style="text-align:right">3328 / 4233 us</td><td style="text-align:right">121 / 158 us</td><td style="text-align:right">210 / 270 us</td><td>quote core 在 x86 上转正</td></tr><tr><td><code>NARROWGATE_CPP_SIGNAL_FEATURES=1</code></td><td style="text-align:right">1973 / 2347 us</td><td style="text-align:right">153 / 194 us</td><td style="text-align:right">237 / 302 us</td><td>signal features 在 x86 上明显有收益</td></tr><tr><td><code>QUOTE_CORE=1 + SIGNAL_FEATURES=1</code></td><td style="text-align:right">1949 / 2199 us</td><td style="text-align:right">120 / 160 us</td><td style="text-align:right">209 / 269 us</td><td>当前 x86 live 候选组合</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：ARM 本机结论不能直接外推到 x86 live；目标机型上 quote core 和 signal features 的方向都要重新测。</li><li>不能说明什么：短跑 smoke 不能代表长时间 p99.9，也不能代表网络、REST 和交易所响应。</li><li>下一步验证：固定 BLAS/OMP 线程后跑 soak，并确认没有 CPU migration、page fault 或 fallback 尖刺。</li></ul></blockquote><p>短跑只能说明方向，所以我又在同一台最终 live 机型上跑了更长的 soak：baseline 和候选各 <code>--n 100000 --signal-n 10000</code>，routing compact ABI <code>--n 200000</code>。这次输出 p99.9：</p><div class="table-container"><table><thead><tr><th>路径</th><th style="text-align:right">Python baseline mean/p99/p99.9</th><th style="text-align:right"><code>QUOTE_CORE + SIGNAL_FEATURES</code> mean/p99/p99.9</th><th>观察</th></tr></thead><tbody><tr><td>signal 10s features</td><td style="text-align:right">9610 / 17558 / 40658 us</td><td style="text-align:right">2167 / 4534 / 16115 us</td><td>C++ fixed-array 显著压低长历史尾延迟</td></tr><tr><td>live <code>_compute_quotes</code></td><td style="text-align:right">172 / 236 / 4380 us</td><td style="text-align:right">123 / 166 / 225 us</td><td>C++ quote core 降低均值，也避开本轮 quote-only 尖刺</td></tr><tr><td><code>quote + policy</code></td><td style="text-align:right">249 / 308 / 364 us</td><td style="text-align:right">219 / 281 / 358 us</td><td>组合路径稳定小幅改善，policy 仍是 Python 主体</td></tr><tr><td>compact routing ABI</td><td style="text-align:right">n/a</td><td style="text-align:right">1.17 / 1.24 / 2.03 us</td><td>pybind tuple 固定税很低</td></tr></tbody></table></div><blockquote><p><strong>这张表怎么读</strong></p><ul><li>说明什么：x86 上最明显的 live CPU 收益来自 fixed-array signal features；quote core 更像稳定降低小路径尾部。</li><li>不能说明什么：它不包含 Binance 网络延迟、REST new/cancel、交易所撮合和真实异常恢复。</li><li>下一步验证：在真实 live 进程旁路 shadow 打点，比较开启/关闭 native 开关时的 HEALTH、requote、ORDER_UPDATE 和 p99.9。</li></ul></blockquote><p>这说明一件事：<strong>架构边界不是文档措辞，而是真的会改变工程结论。</strong> 在本机 ARM 上，早期 <code>SIGNAL_FEATURES=1</code> 曾经是负优化；经过 fixed-array/ring-state 改造后，在 x86 上它反而变成最明显的 live CPU 收益。相反，<code>SIGNAL_STATE=1</code> 在这台 x86 上没有额外收益。pin 到单个 vCPU 对均值帮助也不大，甚至可能放大 VM 上的尾部抖动。</p><p><code>perf stat</code> 也给了一个现实提醒：这台 KVM 实例不暴露 <code>cycles</code>、<code>instructions</code>、<code>L1-icache-load-misses</code>、<code>iTLB-load-misses</code> 这些硬件事件，最后只能看到 context switches、cpu migrations 和 page faults。因此“x86 benchmark”也要继续细分：普通云主机能做 wall-time/tail-latency 复测，但真正要分析 L1I/iTLB/cache miss，可能需要裸金属、特定实例族或能暴露 PMU 的环境。</p><h3 id="3-7-live-路径的-C-优化：与-replay-完全不同的约束"><a href="#3-7-live-路径的-C-优化：与-replay-完全不同的约束" class="headerlink" title="3.7 live 路径的 C++ 优化：与 replay 完全不同的约束"></a>3.7 live 路径的 C++ 优化：与 replay 完全不同的约束</h3><p>前面几节讲的 quote core batch、tick replay 状态机和 streaming features，都是以”吞吐量”为优化目标：输入是连续数组或完整窗口，可以释放 GIL、多线程并行、用 SoA 布局喂给 SIMD。但 live 路径是另一种世界。</p><p>从 C++ 学习的角度看，live 优化最容易犯的错是把“语言更快”误解为“函数搬过去就会更快”。maker live path 里单个 quote 公式可能只要几十微秒，pybind 边界、Python 字典、dataclass 展开、日志对象构造和 feature merge 却可能比公式本身更贵。因此这里分析的重点不是 C++ 语法，而是 <strong>C++ 函数边界应该长什么样</strong>。</p><p>事件引擎每隔 <code>requote_interval</code> 秒被 Python main loop 调用一次 <code>tick()</code>，整个决策链跑在同一个线程上：</p><div class="code-wrapper"><pre><code class="hljs text">WebSocket callback → aggTrade/depth → signal.on_agg_trade()                                              ↓main loop: tick() ──► _requote()           │              ├── compute_signal()          # LightGBM predict           │              ├── compute_quote_core_live() # C++ compact path           │              └── _update_orders()           │                      ├── _build_side_policy()           │                      └── REST new/cancel           └── [sleep rq_interval]</code></pre></div><p>这里没有批量处理的机会：每次报价决策只有一组 state，一个 depth snapshot。释放 GIL 换并行也不适用——WebSocket 回调、quote 决策和 REST 调用都在同一线程中串行执行，GIL 切换反而是额外开销。</p><p>早期的错误迁移方式长这样：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-comment"># 每 tick 都创建大对象，再跨 pybind 边界，再返回大 dict。</span><span class="hljs-comment"># C++ 只算了 quote 公式，Python 仍然负责 policy/routing。</span>ctx = asdict(live_quote_state)               <span class="hljs-comment"># 14+ fields</span>cfg = asdict(live_quote_config)              <span class="hljs-comment"># 70+ fields</span>depth = [asdict(level) <span class="hljs-keyword">for</span> level <span class="hljs-keyword">in</span> l2_book] <span class="hljs-comment"># N levels</span>quote = narrowgate_cpp.compute_quote_core(ctx, cfg, depth)bid_policy = build_policy_from_dict(quote[<span class="hljs-string">&quot;bid_context&quot;</span>])ask_policy = build_policy_from_dict(quote[<span class="hljs-string">&quot;ask_context&quot;</span>])orders = python_route_orders(quote, bid_policy, ask_policy)</code></pre></div><p>这看起来“用了 C++”，但热路径仍然到处是 hash lookup、字符串 key、临时 dict/list、对象分配和 Python 属性访问。改造后的方向是把生命周期不同的数据拆开：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-comment"># config：几乎不变，缓存 native object</span>native_cfg = cached_cpp_config(cfg)<span class="hljs-comment"># state/pred：每 tick 改变，但字段位置固定</span>state_tuple = (    mid, inventory, sigma_sq, trade_intensity,    best_bid, best_ask, ber_active,    mo_ema_all, mo_ema_bid, mo_ema_ask, mo_ref,    position_open, hold_time_s, unrealized_pnl,)pred_tuple = (dir_10s, vol_10s, ret_10s, tox_bid, tox_ask)<span class="hljs-comment"># result：先返回 compact result，只有 quote EV/trace 才物化完整 context</span>quote = narrowgate_cpp.compute_quote_core_live(    state_tuple, native_cfg, pred_tuple, depth_bids, depth_asks)route = narrowgate_cpp.compute_live_routing_decision(    routing_input_tuple,    bid_policy_tuple,    ask_policy_tuple,)</code></pre></div><p>这里的学习点很清楚：C++ 不是为了替代 Python 的事件编排，而是为了让高频决策包使用固定布局、固定字段、固定分支，减少每次 tick 的动态对象税。</p><h4 id="3-7-1-热路径的三个生命周期层"><a href="#3-7-1-热路径的三个生命周期层" class="headerlink" title="3.7.1 热路径的三个生命周期层"></a>3.7.1 热路径的三个生命周期层</h4><p>live 路径的优化策略由三个不同的生命周期决定，不是统一的”全部 C++”：</p><p><strong>第一层：几乎不变的 config（按配置对象缓存）</strong></p><p><code>QuoteCoreConfig</code> 包含 70+ 个策略参数（gamma、kappa、adverse/defense 阈值、spread cap 系数等）。直接每次 requote 都从 Python cfg 解包再构造一次 native 对象，benchmark 测出来比 Python 还慢——单次约 300µs 的 object 构造完全抹平了 quote 数学的收益。</p><p>改造后用 <code>id(cfg)</code> + <code>weakref</code> 作缓存键（具体实现见第九节 9.1），每次报价的 C++ config 参数零分配，config reload 时才同步一次。</p><p><strong>第二层：每 tick 变化的 state（固定位置 tuple）</strong></p><p>策略状态（14 个字段：mid、inventory、sigma_sq、BBO、markout EMA 等）每次 requote 都不同，必须传入。用固定位置 tuple 代替 dict，C++ 端按下标读取，无 key hash、无对象构造（接口见第九节 9.1）。</p><p><strong>第三层：按需物化的诊断（功能驱动的懒加载）</strong></p><p>side policy 每 tick 只需 9 个字段；完整的 100+ 字段 context 只在 quote EV 或离线 trace 时才需要，由 <code>require_full_context</code> 控制（详见第九节 9.2）。</p><p>这三层合在一起，让 <code>compute_quote_core_live</code> 从最初的负优化（慢 59%）翻转为稳定正收益（快 16.4%）——不靠改进 quote math，只靠把「每 tick 固定税」改成「功能需要时才支付」。</p><h4 id="3-7-2-routing-bridge：当前还不是完整-native-决策包"><a href="#3-7-2-routing-bridge：当前还不是完整-native-决策包" class="headerlink" title="3.7.2 routing bridge：当前还不是完整 native 决策包"></a>3.7.2 routing bridge：当前还不是完整 native 决策包</h4><p><code>_build_side_policy()</code> 是 live 路径的另一个计算热点。它接收 quote_context、L2 metrics 和 pred，输出 <code>spread_mult</code>、<code>size_mult</code>、<code>allow_post</code>、<code>reason_mask</code>，大量工作是评估 10+ 个 policy guard 的条件。</p><p>这些条件判断本身不重，但每次需要访问 Python dict（<code>quote_ctx.get(&quot;side_adverse&quot;, False)</code> 等），在单 tick 热路径上积少成多。</p><p><code>NARROWGATE_CPP_LIVE_ROUTING</code> 仍只接管后半段的价格调整、spread cap、库存准入、size 和 cancel/replace 判断，但跨语言接口已经从动态 dict 收紧为固定布局：</p><div class="code-wrapper"><pre><code class="hljs python">bid_policy = self._build_side_policy(Side.BUY, mid, q, pred)ask_policy = self._build_side_policy(Side.SELL, mid, q, pred)routed = cpp_route.compute_live_routing_decision(    (        mid, q, base_bid_price, base_ask_price,        best_bid, best_ask, tick, lot, min_qty, min_notional,        order_size, max_inventory, eta, symmetric_size,        requote_threshold_bps, max_spread,        bid_alive, bid_active_price, bid_age_ms,        ask_alive, ask_active_price, ask_age_ms,    ),    (        bid_policy.allow_post,        bid_policy.allow_exposure_increase,        bid_policy.spread_mult,        bid_policy.size_mult,        bid_policy.order_ttl_ms,    ),    (        ask_policy.allow_post,        ask_policy.allow_exposure_increase,        ask_policy.spread_mult,        ask_policy.size_mult,        ask_policy.order_ttl_ms,    ),)</code></pre></div><p>native 侧对应的是普通结构体，而不是把业务逻辑继续堆在 pybind lambda 中：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">LiveRoutingPolicy</span> &#123;</span>    <span class="hljs-keyword">bool</span> allow_post;    <span class="hljs-keyword">bool</span> allow_exposure_increase;    <span class="hljs-keyword">double</span> spread_mult;    <span class="hljs-keyword">double</span> size_mult;    <span class="hljs-keyword">double</span> order_ttl_ms;&#125;;<span class="hljs-function">LiveRoutingResult <span class="hljs-title">compute_live_routing_decision</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> LiveRoutingInput&amp; input,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> LiveRoutingPolicy&amp; bid,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> LiveRoutingPolicy&amp; ask)</span></span>;</code></pre></div><p>它仍不是 quote → policy → routing 的完全 fused kernel，因为 <code>_build_side_policy()</code> 还在 Python；但 routing 边界自身已不再创建两个 20+ 字段 dict、逐个做字符串 key 查询、再创建结果 dict。旧/新二进制使用同一组输入做 100,000 次对照，checksum 完全一致：mean <code>6.024 → 0.472 us</code>，p99 <code>6.375 → 0.583 us</code>。这个约 92% 的下降只描述 routing binding，不代表完整 <code>_update_orders()</code> 或网络报单延迟也下降 92%。</p><p>最终更理想的 live C++ 决策包应该继续向下面这个形态收敛：一次调用接收 market state、model prediction、inventory state 和 live order state，返回两个 side 的完整动作，但仍不负责 REST IO。</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-keyword">class</span> <span class="hljs-title">OrderAction</span> :</span> std::<span class="hljs-keyword">uint8_t</span> &#123;    Keep,    Cancel,    Replace,    NewOrder,&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">LiveDecisionInput</span> &#123;</span>    <span class="hljs-keyword">double</span> mid;    <span class="hljs-keyword">double</span> best_bid;    <span class="hljs-keyword">double</span> best_ask;    <span class="hljs-keyword">double</span> tick_size;    <span class="hljs-keyword">double</span> lot_size;    <span class="hljs-keyword">double</span> inventory;    <span class="hljs-keyword">double</span> max_inventory;    <span class="hljs-keyword">double</span> sigma_sq;    <span class="hljs-keyword">double</span> ret_10s;    <span class="hljs-keyword">double</span> tox_bid;    <span class="hljs-keyword">double</span> tox_ask;    ActiveOrder bid_order;    ActiveOrder ask_order;    DepthView depth;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">SideDecision</span> &#123;</span>    OrderAction action;    <span class="hljs-keyword">double</span> price;    <span class="hljs-keyword">double</span> quantity;    <span class="hljs-keyword">double</span> ttl_ms;    std::<span class="hljs-keyword">uint32_t</span> reason_mask;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">LiveDecision</span> &#123;</span>    SideDecision bid;    SideDecision ask;    <span class="hljs-keyword">double</span> final_spread;    <span class="hljs-keyword">bool</span> inventory_blocked;&#125;;<span class="hljs-function">LiveDecision <span class="hljs-title">compute_live_decision</span><span class="hljs-params">(</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> LiveDecisionInput&amp; in,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> QuoteCoreConfig&amp; quote_cfg,</span></span><span class="hljs-params"><span class="hljs-function">    <span class="hljs-keyword">const</span> PolicyConfig&amp; policy_cfg)</span> </span>&#123;    <span class="hljs-keyword">auto</span> quote = <span class="hljs-built_in">compute_quote_core_compact</span>(in, quote_cfg);    <span class="hljs-keyword">auto</span> bid_policy = evaluate_side_policy&lt;Side::Buy&gt;(        quote.bid, in.inventory, in.tox_bid, policy_cfg);    <span class="hljs-keyword">auto</span> ask_policy = evaluate_side_policy&lt;Side::Sell&gt;(        quote.ask, in.inventory, in.tox_ask, policy_cfg);    <span class="hljs-keyword">return</span> <span class="hljs-built_in">route_quote_to_orders</span>(        quote, bid_policy, ask_policy, in.bid_order, in.ask_order);&#125;</code></pre></div><p>这段伪代码里有两个刻意保留的边界：</p><ul><li><code>ActiveOrder</code> 是当前本地状态，不在 C++ 里发 REST；C++ 只说“应该怎么做”，Python 负责实际 cancel/new 和异常处理。</li><li><code>reason_mask</code> 是整数位图，不是字符串列表；日志需要人类可读 reason 时再冷路径翻译。这样热路径不做字符串拼接，也不会因为审计文本拖慢每次报价。</li></ul><p>设计上有一点克制：进出 REST 的部分（cancel/new order、TTL check、GIL 相关操作）仍留在 Python，不放进 C++ call 内。这条边界的理由是：REST 调用本身的延迟在 10-50ms 量级，比 C++ 函数的收益大两个数量级；强行把 REST 放进 C++ 只会让错误更难 debug，而不会降低端到端延迟。</p><h4 id="3-7-3-最小且可预测的热指令工作集"><a href="#3-7-3-最小且可预测的热指令工作集" class="headerlink" title="3.7.3 最小且可预测的热指令工作集"></a>3.7.3 最小且可预测的热指令工作集</h4><p>HFT 代码短小，不是为了追求源码上的简洁，而是为了控制 CPU 前端真正看见的机器码。这里采用的标准是：<strong>让一次高频事件实际触达的指令工作集最小、稳定且分支可预测，而不是让整个扩展模块都塞进 L1I。</strong></p><p>这一区别很重要。优化后 <code>narrowgate_cpp</code> 的 <code>__text</code> 是 471,364 bytes，仍然大于本机性能核 192 KiB、能效核 128 KiB 的 L1I；但其中包括 pybind 注册、Python 类型转换、异常处理、批量研究接口和 trace binding，它们不会在一次 live requote 中全部执行。使用与发布扩展一致的 Release + pybind ThinLTO 诊断构建和 linker map 查看真正的函数尺寸，结果是：</p><div class="table-container"><table><thead><tr><th>native 函数</th><th style="text-align:right">text 大小</th><th>热路径判断</th></tr></thead><tbody><tr><td><code>compute_quote_core()</code></td><td style="text-align:right">7,528 B</td><td>live quote 主体紧凑，明显小于常见 32 KiB L1I</td></tr><tr><td>shared <code>compute_signal_feature_vector()</code></td><td style="text-align:right">9,184 B</td><td>stateful/legacy 共用的唯一大计算主体</td></tr><tr><td><code>simulate_tick_arrays()</code></td><td style="text-align:right">23,596 B</td><td>replay 顺序状态机接近 32 KiB 级别，仍需连同被调用 helper 用 PMU 验证</td></tr><tr><td>legacy <code>compute_signal_feature_overlay()</code> wrapper</td><td style="text-align:right">1,136 B</td><td>只构造 span view 并调用共享主体，不再复制大函数</td></tr><tr><td>depth batch pybind worker</td><td style="text-align:right">23,668 B</td><td>离线批量入口，不属于 live scalar 热路径</td></tr></tbody></table></div><p>诊断命令本身也应进入性能工程流程，而不是只看 benchmark 平均值：</p><div class="code-wrapper"><pre><code class="hljs bash">BIN=$(.venv/bin/python -c \  <span class="hljs-string">&#x27;import narrowgate_cpp; print(narrowgate_cpp.__file__)&#x27;</span>)xcrun llvm-size -A <span class="hljs-string">&quot;<span class="hljs-variable">$BIN</span>&quot;</span>xcrun llvm-objdump -d --demangle --no-show-raw-insn <span class="hljs-string">&quot;<span class="hljs-variable">$BIN</span>&quot;</span> \  &gt; /tmp/narrowgate.disasm<span class="hljs-comment"># 诊断构建额外生成 linker map：-Wl,-map,/tmp/narrowgate.map</span><span class="hljs-comment"># Linux 研究机再看 L1I/iTLB/branch miss 与 p99 是否同步恶化。</span>perf <span class="hljs-built_in">stat</span> -r 20 \  -e cycles,instructions,branches,branch-misses,\L1-icache-loads,L1-icache-load-misses,\iTLB-loads,iTLB-load-misses \  ./bench_hot_path</code></pre></div><p>源码审计也需要给模板设预算。当前模板维度主要是 <code>Side</code>、<code>QueueAheadMode</code>、固定 rolling window 和容器 view：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;Side S, QueueAheadMode Mode&gt;<span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">visible_queue_ahead</span><span class="hljs-params">(...)</span></span>;<span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">int</span> WindowSeconds&gt;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RollingStats</span>;</span></code></pre></div><p><code>Side x QueueAheadMode</code> 只有四种 queue 实例，<code>RollingStats&lt;60&gt;</code> 即使有多个对象也只有同一种类型；quote core 的大主体本身不是模板。这里没有 <code>EnableAdverse x EnableDefense x EnableQuoteEV x ...</code> 这样的布尔笛卡尔积，因此目前不存在明显的模板组合爆炸。side-specific helper 展开后，<code>compute_quote_core()</code> 总体仍只有约 7.4 KiB。</p><p>审计发现的两个缺口随后都做了收敛：</p><ol><li>routing 改为固定 22 字段 input tuple、两个 5 字段 policy tuple 和 11 字段 result tuple；binding 只负责位置解包，业务数学进入普通 <code>LiveRoutingInput/Policy/Result</code> C++ 函数。place/replace 时为了审计日志执行的 <code>asdict()</code> 仍保留，因为它不属于每次 routing 计算。</li><li><code>compute_signal_feature_vector&lt;Bars, History&gt;</code> 改成接收 <code>SegmentedSpanView&lt;Bar1s/FeatureHistoryRow&gt;</code> 的单一非模板主体。<code>CircularBuffer</code> 暴露 ring 的两段 span，legacy vector 暴露一段 span，无复制共享同一份机器码。相关大计算代码由 <code>8,868 + 10,120 B</code> 收到 <code>9,184 + 1,136 B</code>，减少 8,668 B；整个扩展 <code>__text</code> 从 478,392 降到 471,364 B。</li></ol><p>代码变小没有换来性能回退。满 60,480 history 的 <code>compute_values()</code> 对照中，mean <code>1.901 → 1.748 us</code>，p99 <code>2.500 → 1.917 us</code>。这说明 two-span 访问增加的一次边界判断，代价小于共享代码布局和编译器优化带来的收益；但这些仍是同机微基准，不能替代目标 Linux CPU 的 PMU 计数。</p><p>因此这里对模板元编程的约束是：只把真正高频、低基数的结构性分支固定到编译期；数值阈值和频繁实验的 policy 留在数据中；大段公共计算只保留一份非模板主体；trace、日志、异常和兼容 binding 保持冷路径。评估是否合格最终看的是固定/轮换模板实例实验中的 L1I MPKI、iTLB MPKI、branch miss 和 p99/p99.9，而不是 <code>.text</code> 总大小，也不是源码有多少行。</p><h4 id="3-7-4-live-路径不该做的事"><a href="#3-7-4-live-路径不该做的事" class="headerlink" title="3.7.4 live 路径不该做的事"></a>3.7.4 live 路径不该做的事</h4><p>这套设计也确定了 live 路径不迁移的边界：</p><ul><li><strong>WebSocket 层</strong>：网络延迟主导，C++ 写 WebSocket client 不缩短决策尾延迟，只增加维护面</li><li><strong>LightGBM 推理</strong>：模型本身已 native（LightGBM C API），Python 只做 feature → array 转换</li><li><strong>大块 Python 编排逻辑</strong>：config reload、日志、HEALTH 上报、REST 重连——这些不在 CPU 热点，放 Python 更容易验证和修改</li><li><strong>GIL 释放</strong>：live 主路径单线程，释放 GIL 无收益；WebSocket 回调在不同 thread 里，releasing GIL 反而引入竞争</li></ul><p>live 路径的结论和 offline 路径相同，但结论的来路完全不同：<strong>offline 关心吞吐，live 关心尾延迟；offline 可以 batch，live 只能 scalar；offline 释放 GIL 有意义，live 中通常没有</strong>。把 offline 的优化模式复制到 live，往往只是更快地得到一个无收益的结果。</p><blockquote><p><strong>本节工程结论</strong></p><ul><li>live hot path 不应该先迁 WebSocket/REST，而应该先收紧 WebSocket 上方的 rolling state、quote/policy/routing 边界。</li><li>单个公式迁到 C++ 很容易被 pybind、dict、dataclass 和日志对象物化反噬；稳定、高频、低基数的决策包才适合 native。</li><li>热路径目标是最小且可预测的指令工作集，而不是让整个 <code>.so</code> 塞进 L1I。</li><li>ARM 本机 microbench、offline sweep 加速、x86 live p99/p99.9 是三种不同证据，不能互相替代。</li></ul></blockquote><h2 id="4-五个读者一定会追问的实现细节"><a href="#4-五个读者一定会追问的实现细节" class="headerlink" title="4. 五个读者一定会追问的实现细节"></a>4. 五个读者一定会追问的实现细节</h2><p>前面把 NarrowGate 的算法假设、数据问题、C++ 边界和 live hot path 都展开了。但如果我是读者，尤其是做过低延迟回放或交易系统的人，读到这里还会继续追问：你说“双引擎 parity”，到底怎么对齐？你说“逐侧 pause”，到底看什么动态量？你说 compact context，具体从什么对象变成什么 ABI？你说 queue ahead，到底有没有承认自己看不见真实队列？你说 shadow mode，回测里的 fill probability 在实盘行情里有没有崩？</p><p>这一节专门回答这些“抓到深处”的问题。</p><h3 id="4-1-双引擎-parity：不是相信-C-，而是让-C-持续被-Python-审讯"><a href="#4-1-双引擎-parity：不是相信-C-，而是让-C-持续被-Python-审讯" class="headerlink" title="4.1 双引擎 parity：不是相信 C++，而是让 C++ 持续被 Python 审讯"></a>4.1 双引擎 parity：不是相信 C++，而是让 C++ 持续被 Python 审讯</h3><p>Python/C++ 双引擎最容易做成两套系统：Python 负责研究，C++ 负责速度，最后两个结果不一样，却不知道是策略变了、浮点误差、时间戳 as-of 边界，还是 C++ 少实现了一个 guard。</p><p>NarrowGate 里我采用的原则是：<strong>Python 仍然是研究口径的 reference implementation，C++ 必须在同一批 normalized arrays 上被持续审讯</strong>。</p><p>这里的 “parity” 不等于所有浮点数 bitwise 完全一致。tick replay 里有成交价、PnL、markout、inventory time、rounding、latency gate 等连续变量，要求 bitwise 一致反而会把工程带偏。真正重要的是三层断言：</p><ol><li><strong>路径级断言</strong>：fill 数、bid/ask split、最终库存、订单状态迁移、trace 行数必须一致或可解释；</li><li><strong>金额级断言</strong>：PnL、InvAdj、inventory time、spread summary 用很小 tolerance 对齐；</li><li><strong>边界级断言</strong>：NaN、缺盘口、长 gap、pending cancel fill、同 timestamp 多事件排序必须 fail fast 或有显式规则。</li></ol><p>整个流程可以概括成这样：</p><div class="code-wrapper"><pre><code class="hljs text">Parquet/CSV/raw logs        |        vPython loader: timezone, int64 timestamp, tick/lot rounding, bad segment mask        |        vNormalized NumPy arrays        |        +--------------------+        |                    |        v                    v Python replay          C++ replay reference              strict engine        |                    |        +---------+----------+                  v        summary + trace comparator</code></pre></div><p>伪代码大概是：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">assert_replay_parity</span>(<span class="hljs-params">window, params</span>):</span>    arrays = load_window_as_arrays(        window,        timestamp_dtype=<span class="hljs-string">&quot;int64_ms&quot;</span>,        price_dtype=<span class="hljs-string">&quot;float64&quot;</span>,        drop_bad_quality_segments=<span class="hljs-literal">True</span>,    )    py = simulate_tick_python(arrays, params)    cpp = simulate_tick_cpp(arrays, params, strict=<span class="hljs-literal">True</span>)    <span class="hljs-comment"># 路径级：这些不应该靠 tolerance 蒙混过关。</span>    <span class="hljs-keyword">for</span> key <span class="hljs-keyword">in</span> [        <span class="hljs-string">&quot;fills_total&quot;</span>,        <span class="hljs-string">&quot;fills_bid&quot;</span>,        <span class="hljs-string">&quot;fills_ask&quot;</span>,        <span class="hljs-string">&quot;final_inventory&quot;</span>,        <span class="hljs-string">&quot;n_requotes&quot;</span>,        <span class="hljs-string">&quot;trace_rows&quot;</span>,    ]:        <span class="hljs-keyword">assert</span> py[key] == cpp[key], key    <span class="hljs-comment"># 金额级：允许浮点末位误差，但不允许经济含义变化。</span>    <span class="hljs-keyword">for</span> key <span class="hljs-keyword">in</span> [        <span class="hljs-string">&quot;raw_pnl&quot;</span>,        <span class="hljs-string">&quot;inventory_adjusted_pnl&quot;</span>,        <span class="hljs-string">&quot;abs_inventory_time_s&quot;</span>,        <span class="hljs-string">&quot;avg_final_spread_bps&quot;</span>,    ]:        np.testing.assert_allclose(            py[key],            cpp[key],            rtol=<span class="hljs-number">1e-8</span>,            atol=<span class="hljs-number">1e-8</span>,            err_msg=key,        )</code></pre></div><p>这里踩过的坑很多，而且大多不是 C++ 语法问题：</p><ul><li><strong>时间戳必须是整数</strong>：不要在 hot loop 里用 float seconds。Binance、CryptoHFTData、内部日志可能混用毫秒、微秒和 ISO 字符串，先统一到 <code>int64</code>。</li><li><strong>as-of 边界必须一致</strong>：Python <code>searchsorted(..., side=&quot;right&quot;) - 1</code> 和 C++ <code>upper_bound - 1</code> 要写成同一条规则，否则刚好落在 book update timestamp 上的订单会错一格盘口。</li><li><strong>tick rounding 必须单点定义</strong>：<code>floor</code>、<code>ceil</code>、<code>round</code> 在买卖两侧含义不同，不能 Python 一套、C++ 一套。</li><li><strong>NaN 不能“顺手补 0”</strong>：spot/reference/orderbook 缺失时，应该进入 quality mask 或 segment reset，而不是让 C++ 默认值悄悄参与训练和回放。</li><li><strong>pending cancel fill 要显式建模</strong>：订单发出 cancel 后，在 cancel latency 到达前仍可能成交。Python 如果已经把它当撤单、C++ 还把它当 live order，fill path 会马上分叉。</li><li><strong>模块来源要 strict</strong>：两个项目都叫 <code>narrowgate_cpp</code> 时，同一个 venv 可能 import 到另一个 repo 的扩展。strict 模式下必须检查 <code>narrowgate_cpp.__file__</code>、字段 shape 和 ABI version。</li></ul><p>所以双引擎 parity 的本质不是“我把 Python 翻译成 C++ 了”，而是建立一套制度：<strong>每次 C++ 多迁一个状态机，都必须先在小真实窗口里通过路径级和金额级审讯，然后才允许进入 sweep/A/B</strong>。</p><h3 id="4-2-Side-Policy-的逐侧-pause：高波动先-widening，信息冲击才-pause"><a href="#4-2-Side-Policy-的逐侧-pause：高波动先-widening，信息冲击才-pause" class="headerlink" title="4.2 Side Policy 的逐侧 pause：高波动先 widening，信息冲击才 pause"></a>4.2 Side Policy 的逐侧 pause：高波动先 widening，信息冲击才 pause</h3><p>很多人一听到 “pause” 会理解成：市场波动大，所以不挂单。这个理解不够精确。</p><p>在 NarrowGate 里，高波动首先应该由 AS/GLFT 或 regime spread 处理，也就是把 spread 拉宽：</p><script type="math/tex; mode=display">\delta_{\text{side}} =\delta_{\text{base}}\cdot m_{\text{vol}}\cdot m_{\text{depth}}\cdot m_{\text{toxicity}}+ \Delta_{\text{inventory}}</script><p>如果只是 realized vol 高、盘口跳动快，但没有明显的方向性信息冲击，直接 pause 可能会把所有高 spread 补偿机会都关掉。因此 pause 更像“熔断某一侧暴露”的机制，主要看逐侧 adverse context，而不是看总波动。</p><p>核心判断可以写成人话：</p><ul><li><strong>widen</strong>：这一侧可能更危险，但还可以用更差的价格提供流动性；</li><li><strong>pause</strong>：这一侧成交后大概率是接信息风险，继续挂单没有意义；</li><li><strong>cooldown</strong>：市场刚打过这一侧，短时间内不急着恢复；</li><li><strong>decay</strong>：如果很久没有新证据，旧的 adverse 不能永久锁死策略。</li></ul><p>一个更接近当前策略口径的简化伪代码是：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">update_side_pause</span>(<span class="hljs-params">side, now, fill_markout_bps, state</span>):</span>    dt = now - state.last_update_ts    <span class="hljs-comment"># wall-clock decay：坏日、长 gap、静默行情后不能让旧 markout 永久锁死。</span>    state.markout_ema *= math.exp(-dt / state.tau_s)    <span class="hljs-keyword">if</span> fill_markout_bps <span class="hljs-keyword">is</span> <span class="hljs-keyword">not</span> <span class="hljs-literal">None</span>:        state.markout_ema = (            state.alpha * fill_markout_bps            + (<span class="hljs-number">1.0</span> - state.alpha) * state.markout_ema        )    <span class="hljs-keyword">if</span> state.markout_ema &lt; -state.pause_threshold_bps:        extra = state.base_pause_s * <span class="hljs-built_in">abs</span>(state.markout_ema) / state.pause_threshold_bps        state.pause_until = now + clamp(extra, state.min_pause_s, state.max_pause_s)    <span class="hljs-keyword">return</span> now &lt; state.pause_until</code></pre></div><p>在策略层面还要区分“增加库存风险的一侧”和“降低库存风险的一侧”：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">is_exposure_increasing</span>(<span class="hljs-params">side, inventory</span>):</span>    <span class="hljs-keyword">if</span> side == <span class="hljs-string">&quot;bid&quot;</span>:        <span class="hljs-keyword">return</span> inventory &gt;= <span class="hljs-number">0.0</span>      <span class="hljs-comment"># 继续买入会加大 long 暴露</span>    <span class="hljs-keyword">if</span> side == <span class="hljs-string">&quot;ask&quot;</span>:        <span class="hljs-keyword">return</span> inventory &lt;= <span class="hljs-number">0.0</span>      <span class="hljs-comment"># 继续卖出会加大 short 暴露</span></code></pre></div><p>因此 side policy 不是一个全局开关，而是一个逐侧、逐库存方向、逐市场状态的 gate：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">if</span> vol_high:    quote.spread *= vol_spread_multiplier<span class="hljs-keyword">if</span> side_toxic <span class="hljs-keyword">and</span> exposure_increasing:    quote.allow_post = <span class="hljs-literal">False</span>    quote.reason = <span class="hljs-string">&quot;ADVERSE_SIDE_PAUSE&quot;</span><span class="hljs-keyword">elif</span> side_toxic:    quote.spread *= adverse_widen_multiplier<span class="hljs-keyword">if</span> local_extreme_against_side <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> inventory_reducing:    quote.allow_post = <span class="hljs-literal">False</span>    quote.reason = <span class="hljs-string">&quot;DEFENSE_GUARD&quot;</span></code></pre></div><p>所以，高波动不必然 pause；pause 专门针对“这侧成交之后的 markout/毒性证据已经不能靠 spread 补偿”的状态。否则策略会把 volatility risk 和 information risk 混为一谈。</p><h3 id="4-3-Compact-Context：从嵌套对象到固定字段-ABI"><a href="#4-3-Compact-Context：从嵌套对象到固定字段-ABI" class="headerlink" title="4.3 Compact Context：从嵌套对象到固定字段 ABI"></a>4.3 Compact Context：从嵌套对象到固定字段 ABI</h3><p>live 路径上最反直觉的一点是：把单个报价公式搬到 C++ 可能更慢。原因不是 C++ 慢，而是 Python/C++ 边界太重。</p><p>早期路径接近这样：</p><div class="code-wrapper"><pre><code class="hljs python">ctx = &#123;    <span class="hljs-string">&quot;config&quot;</span>: asdict(config),    <span class="hljs-string">&quot;state&quot;</span>: asdict(engine_state),    <span class="hljs-string">&quot;inventory&quot;</span>: asdict(inventory_state),    <span class="hljs-string">&quot;prediction&quot;</span>: prediction_dict,    <span class="hljs-string">&quot;book&quot;</span>: &#123;        <span class="hljs-string">&quot;best_bid&quot;</span>: best_bid,        <span class="hljs-string">&quot;best_ask&quot;</span>: best_ask,        <span class="hljs-string">&quot;depth&quot;</span>: [&#123;<span class="hljs-string">&quot;price&quot;</span>: p, <span class="hljs-string">&quot;qty&quot;</span>: q&#125; <span class="hljs-keyword">for</span> p, q <span class="hljs-keyword">in</span> levels],    &#125;,&#125;result = narrowgate_cpp.compute_quote(ctx)</code></pre></div><p>这看起来很灵活，但在 live hot path 里会反复制造这些成本：</p><ul><li><code>asdict()</code> 深拷贝；</li><li>字符串 key 查找；</li><li>Python list/dict/object materialization；</li><li>pybind 再把动态对象解析成 C++ 类型；</li><li>C++ 算完以后再组一个 Python dict 返回。</li></ul><p>Compact Context 的方向不是“把所有东西都迁到 C++”，而是把边界变成固定字段 ABI：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">CompactMarketView</span> &#123;</span>    <span class="hljs-keyword">double</span> mid;    <span class="hljs-keyword">double</span> best_bid;    <span class="hljs-keyword">double</span> best_ask;    std::span&lt;<span class="hljs-keyword">const</span> DepthLevel&gt; bids;    std::span&lt;<span class="hljs-keyword">const</span> DepthLevel&gt; asks;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">CompactInventoryView</span> &#123;</span>    <span class="hljs-keyword">double</span> position;    <span class="hljs-keyword">double</span> abs_inventory_time_s;    <span class="hljs-keyword">double</span> signed_inventory_time_s;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">CompactPredictionView</span> &#123;</span>    <span class="hljs-keyword">double</span> ret_1s_bps;    <span class="hljs-keyword">double</span> ret_5s_bps;    <span class="hljs-keyword">double</span> toxicity;    <span class="hljs-keyword">double</span> vol_bps;    <span class="hljs-keyword">double</span> ref_move_bps;&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">CompactRoutingResult</span> &#123;</span>    <span class="hljs-keyword">double</span> bid_px;    <span class="hljs-keyword">double</span> ask_px;    <span class="hljs-keyword">double</span> bid_qty;    <span class="hljs-keyword">double</span> ask_qty;    <span class="hljs-keyword">bool</span> can_bid;    <span class="hljs-keyword">bool</span> can_ask;    <span class="hljs-keyword">uint32_t</span> bid_reason_mask;    <span class="hljs-keyword">uint32_t</span> ask_reason_mask;&#125;;</code></pre></div><p>Python 侧则只传固定顺序的 tuple/array 和已经缓存的 native config：</p><div class="code-wrapper"><pre><code class="hljs python">market = (    mid,    best_bid,    best_ask,    bid_prices_view,    bid_qtys_view,    ask_prices_view,    ask_qtys_view,)state = (    inventory,    abs_inventory_time_s,    signed_inventory_time_s,    bid_markout_ema,    ask_markout_ema,    bid_pause_until,    ask_pause_until,)pred = (    ret_1s_bps,    ret_5s_bps,    toxicity,    vol_bps,    ref_move_bps,)decision = cpp_live_routing(native_cfg, market, state, pred)</code></pre></div><p>架构上是：</p><div class="code-wrapper"><pre><code class="hljs text">WebSocket / REST / logging        |        | 仍然留在 Python        vPython normalized event state        |        | fixed tuple / array / span-like views        vC++ quote + side policy + routing bundle        |        | compact result: price, size, can_post, reason_mask        vPython order adapter        |        vexchange REST/WebSocket</code></pre></div><p>这样做的收益不是“C++ 数学快了多少”，而是减少边界上的动态对象。前面的 live benchmark 里也能看到这个现象：原始 routing 桥接大约是 <code>6us</code> 级别，compact ABI 后可以压到亚微秒级；但如果只把 scalar quote core 单独搬过去，pybind/dataclass 往返会把收益吃掉，甚至变慢。</p><p>这也是为什么 WebSocket 不急着迁 C++。行情接收、日志、健康检查、REST adapter 都不是最值得先动的地方。真正应该收紧的是 WebSocket 上方、下单 adapter 下方的这一小段：<strong>rolling state -&gt; quote -&gt; policy -&gt; route decision</strong>。</p><h3 id="4-4-Queue-Ahead：保守地承认自己看不见的队列"><a href="#4-4-Queue-Ahead：保守地承认自己看不见的队列" class="headerlink" title="4.4 Queue Ahead：保守地承认自己看不见的队列"></a>4.4 Queue Ahead：保守地承认自己看不见的队列</h3><p>bar 回测最容易自欺欺人的地方是：只要下一根 bar 的 low/high 碰到我的价格，就认为成交。maker 策略里这几乎一定会高估结果，因为真实世界里还有队列。</p><p>NarrowGate 的 tick replay 更保守：价格碰到只是必要条件，不是充分条件。订单进入盘口后，需要估计自己前面的 queue ahead，然后用后续 aggressive trade 去消耗这个 queue。</p><p>简化逻辑如下：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">activate_order</span>(<span class="hljs-params">side, quote_px, activation_ts, l2_book</span>):</span>    level_qty = l2_book.visible_qty_at(quote_px, activation_ts)    <span class="hljs-keyword">if</span> level_qty <span class="hljs-keyword">is</span> <span class="hljs-keyword">not</span> <span class="hljs-literal">None</span>:        <span class="hljs-comment"># 保守假设：自己排在当前可见 level 的后面。</span>        queue_left = level_qty    <span class="hljs-keyword">else</span>:        <span class="hljs-comment"># 没有可靠 L2 时，用距离 mid 的衰减模型兜底。</span>        distance = <span class="hljs-built_in">abs</span>(quote_px - mid_at(activation_ts))        queue_left = queue_base * math.exp(-queue_decay * distance)    <span class="hljs-keyword">return</span> LiveOrder(side=side, price=quote_px, queue_left=queue_left)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">process_trade</span>(<span class="hljs-params">order, trade</span>):</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> trade_crosses_order(trade, order):        <span class="hljs-keyword">return</span>    aggress_qty = trade.qty    <span class="hljs-comment"># 先消耗排在我前面的可见队列。</span>    consumed_ahead = <span class="hljs-built_in">min</span>(order.queue_left, aggress_qty)    order.queue_left -= consumed_ahead    aggress_qty -= consumed_ahead    <span class="hljs-comment"># 只有 queue ahead 被打穿后，剩余 aggressive qty 才可能打到我。</span>    <span class="hljs-keyword">if</span> order.queue_left &lt;= <span class="hljs-number">0.0</span> <span class="hljs-keyword">and</span> aggress_qty &gt; <span class="hljs-number">0.0</span>:        fill_qty = <span class="hljs-built_in">min</span>(order.remaining_qty, aggress_qty)        fill(order, fill_qty)</code></pre></div><p>这当然仍然不是“真实交易所撮合队列”。历史 Top-N 深度看不到三件事：</p><ul><li>这个 level 内部每个订单的真实排队顺序；</li><li>其他参与者在你前面的撤单；</li><li>你的订单到达交易所前，level 已经变化了多少。</li></ul><p>所以“不自欺欺人”的做法不是假装知道这些，而是把不可观测部分分成两类：</p><ol><li><strong>保守规则</strong>：默认排在可见 level 后面，不因为看见 level 变小就自动认为前面的人都撤了；</li><li><strong>校准参数</strong>：用 live/order log 反推 queue model、new/cancel latency、fill gate，让回放的 placed orders/day、fills/day、side split 和 live 同数量级。</li></ol><p>如果只用 Top-N L2，queue ahead 永远是估计值，而不是事实。因此 replay 结果要同时报告：</p><ul><li>placed orders/day；</li><li>fills/day；</li><li>fills / placed order；</li><li>cap/guard/pause rate；</li><li>active-trade-day fills；</li><li>quality-day fills；</li><li>full-calendar-day fills。</li></ul><p>这样才能区分“成交概率模型太保守”和“策略根本没有挂出足够多订单”。前者是 queue/fill calibration，后者往往是 guard/pause/requote gating 的问题。</p><h3 id="4-5-Shadow-Mode：在线校准比回测-PnL-更诚实"><a href="#4-5-Shadow-Mode：在线校准比回测-PnL-更诚实" class="headerlink" title="4.5 Shadow Mode：在线校准比回测 PnL 更诚实"></a>4.5 Shadow Mode：在线校准比回测 PnL 更诚实</h3><p>NarrowGate 不应该把 quote EV 直接从回测 A/B 推到 live。更合理的中间层是 shadow mode：实盘收行情，在线计算 quote、quote EV、side policy 和 would-place/would-fill，但不把它当成交易建议，也不把它当成真实成交承诺。</p><p>shadow mode 的核心问题不是“预测收益是多少”，而是：</p><script type="math/tex; mode=display">P_{\text{model}}(\text{fill} \mid \text{quote context})\quad \text{是否接近} \quadP_{\text{observed}}(\text{fill} \mid \text{live context})</script><p>以及：</p><script type="math/tex; mode=display">E[\text{markout}_{30s} \mid \text{predicted bucket}]\quad \text{是否和} \quad\text{realized markout bucket}\quad \text{同向、同量级}</script><p>需要看的不是一个 raw PnL 数字，而是一组校准表：</p><div class="table-container"><table><thead><tr><th>Shadow 检查</th><th>说明</th><th>如果失败</th></tr></thead><tbody><tr><td>fill probability calibration</td><td>预测的 <code>P(fill)</code> 分桶后，真实/可回放 fill rate 是否接近</td><td>quote EV 只能做排序，不能做阈值</td></tr><tr><td>realized-vs-pred markout</td><td>高 EV bucket 是否真的有更好 markout</td><td>EV label 或 feature 泄漏/错配</td></tr><tr><td>placed orders/day</td><td>shadow/replay 是否和 live 同数量级</td><td>guard/pause/requote gating 不一致</td></tr><tr><td>fills / placed order</td><td>每个挂单的成交概率是否接近 live</td><td>queue ahead 或 latency floor 不对</td></tr><tr><td>side split</td><td>bid/ask fill 是否失衡</td><td>side policy 或库存压力错误</td></tr><tr><td>inventory time</td><td>正 edge 是否靠堆库存换来</td><td>风险调整后不可用</td></tr></tbody></table></div><p>实盘里很常见的情况是：回测看起来排到了，live 却因为 latency floor 根本没进入同一个队列位置。这个问题不能用“C++ 更快”简单解决，因为它通常来自三层延迟：</p><ul><li>行情到达本机的网络/解码延迟；</li><li>策略从行情到下单的决策延迟；</li><li>订单从本机到交易所并 ack 的新单延迟。</li></ul><p>因此 shadow mode 需要把 measured new/cancel latency 注入 replay，再比较 live order log 与 replay would-fill：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">shadow_fill_check</span>(<span class="hljs-params">event, measured_latency</span>):</span>    quote = compute_quote(event)    activation_ts = event.ts + measured_latency.new_order_ms    replay_fill = replay_queue_fill(        side=quote.side,        price=quote.price,        activation_ts=activation_ts,        l2=event.future_l2,        trades=event.future_trades,    )    <span class="hljs-keyword">return</span> &#123;        <span class="hljs-string">&quot;pred_fill_prob&quot;</span>: quote.ev_fill_prob,        <span class="hljs-string">&quot;would_fill&quot;</span>: replay_fill <span class="hljs-keyword">is</span> <span class="hljs-keyword">not</span> <span class="hljs-literal">None</span>,        <span class="hljs-string">&quot;latency_ms&quot;</span>: measured_latency.new_order_ms,        <span class="hljs-string">&quot;queue_ahead&quot;</span>: replay_fill.queue_ahead <span class="hljs-keyword">if</span> replay_fill <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span>,    &#125;</code></pre></div><p>这也是为什么 quote EV promotion 要比普通模型上线更保守。当前这种逐侧 quote EV 如果 valid fills 仍然偏少，或者 calibration/Brier/bucket markout 不过 gate，即使某个月 ask-only A/B 看起来 raw 为正，也只能算 shadow/research 候选，不能直接当作 live 结论。</p><blockquote><p><strong>本节工程结论</strong></p><ul><li>双引擎 parity 靠 normalized arrays、strict mode、路径级断言和金额级断言，而不是靠“C++ 理论上等价”。</li><li>side pause 应该区分 volatility risk 和 information risk：高波动先 widening，逐侧 toxic/adverse 证据才 pause。</li><li>compact context 的重点是固定字段 ABI 和减少动态对象物化，不是把所有 Python 都迁掉。</li><li>queue ahead 必须承认不可观测撤单和真实排队顺序，用保守规则加 live 校准，而不是把价格触碰当成交。</li><li>shadow mode 的价值是校准 fill probability、markout bucket、latency floor 和 placed/fill 数量级，比单月回测 PnL 更诚实。</li></ul></blockquote><h2 id="5-从原理到实操：参数、队列、Shadow-与胶水层"><a href="#5-从原理到实操：参数、队列、Shadow-与胶水层" class="headerlink" title="5. 从原理到实操：参数、队列、Shadow 与胶水层"></a>5. 从原理到实操：参数、队列、Shadow 与胶水层</h2><p>前面讲了很多公式、架构和 benchmark，但普通金融工作者真正会卡在一个问题上：<strong>模型输出怎么变成我这一单要后撤几个 tick？</strong> 普通工程师也会卡在另一个问题上：<strong>Python 的研究矩阵到底怎么安全、低拷贝地跑进 C++ 状态机？</strong></p><p>这一节不再讲宏观动机，而是把几个落地口径写清楚。</p><h3 id="5-1-参数映射：gamma-kappa-sigma-不应该靠玄学拍脑袋"><a href="#5-1-参数映射：gamma-kappa-sigma-不应该靠玄学拍脑袋" class="headerlink" title="5.1 参数映射：gamma/kappa/sigma 不应该靠玄学拍脑袋"></a>5.1 参数映射：gamma/kappa/sigma 不应该靠玄学拍脑袋</h3><p>AS 公式里的几个参数在代码里有非常具体的含义：</p><div class="table-container"><table><thead><tr><th>参数 / 信号</th><th>代码里的来源</th><th>对报价的直接影响</th><th>调参时主要看什么</th></tr></thead><tbody><tr><td><code>gamma</code></td><td><code>strategy.gamma</code>，也会被库存比例和方向信号调整成 <code>g_eff</code></td><td>库存 reservation price 偏移、基础 spread 项</td><td><code>abs_inventory_time_s</code>、InvAdj、max inventory hit</td></tr><tr><td><code>sigma_sq</code></td><td>tick replay / live rolling variance；可与 LightGBM <code>vol_10s</code> 按 <code>vol_blend</code> 混合</td><td><code>gamma * sigma_sq</code>，以及 regime spread scale</td><td>spread&lt;100、cap hit、波动窗口下 markout</td></tr><tr><td><code>kappa</code></td><td><code>strategy.kappa</code> 或 <code>p3_kappa_eff</code>；可用 depth-kappa 根据近端深度调整</td><td><code>log(1 + gamma / kappa)</code> spread 项；<code>kappa</code> 越小 spread 越宽</td><td>fills/day、fills/order、queue calibration</td></tr><tr><td><code>dir_10s</code></td><td>LightGBM 方向概率</td><td>reservation price shift、asymmetry、gamma_dir_bonus</td><td>方向分桶 markout、库存是否被方向信号放大</td></tr><tr><td><code>ret_10s</code></td><td>LightGBM 未来收益预测</td><td><code>ret_skew * mid</code>，再被 <code>ret_shift_max_pct</code> 限幅</td><td>逐侧 1s/5s markout、cap compression</td></tr><tr><td><code>tox_bid/ask</code></td><td>LightGBM 逐侧 toxicity</td><td>side adverse widen / shrink / pause</td><td>toxic bucket realized markout、false pause rate</td></tr><tr><td>markout EMA</td><td>fill 后 maker-signed markout</td><td>markout spread scale、hybrid pause latch</td><td>pause 时长、恢复后 fill markout</td></tr></tbody></table></div><p>简化后的 quote mapping 可以这样理解：</p><div class="code-wrapper"><pre><code class="hljs python">sigma_sq = rolling_sigma_sq<span class="hljs-keyword">if</span> ml_enabled <span class="hljs-keyword">and</span> vol_blend &gt; <span class="hljs-number">0</span> <span class="hljs-keyword">and</span> pred_vol &gt; <span class="hljs-number">0</span>:    sigma_sq = (<span class="hljs-number">1</span> - vol_blend) * rolling_sigma_sq + vol_blend * pred_volkappa_base = p3_kappa_eff <span class="hljs-keyword">if</span> p3_kappa_eff &gt; <span class="hljs-number">0</span> <span class="hljs-keyword">else</span> cfg.kappakappa_used = depth_kappa(kappa_base, depth) <span class="hljs-keyword">if</span> use_depth_kappa <span class="hljs-keyword">else</span> kappa_baseg_eff = gamma<span class="hljs-keyword">if</span> <span class="hljs-built_in">abs</span>(inventory) &gt; <span class="hljs-number">0</span>:    g_eff *= <span class="hljs-number">1.0</span> + (<span class="hljs-built_in">abs</span>(inventory) / max_inventory) ** <span class="hljs-number">2</span>reservation = fair_price - inventory * g_eff * sigma_sqkappa_spread = <span class="hljs-built_in">max</span>(kappa_used * kappa_ratio, <span class="hljs-number">1e-12</span>)spread = gamma * sigma_sq + (<span class="hljs-number">2.0</span> / gamma) * log(<span class="hljs-number">1.0</span> + gamma / kappa_spread)spread *= regime_spread_scale<span class="hljs-keyword">if</span> ret_skew &gt; <span class="hljs-number">0</span>:    shift = clamp(        pred_ret * ret_skew * mid,        -ret_shift_max_pct * spread / <span class="hljs-number">2</span>,        +ret_shift_max_pct * spread / <span class="hljs-number">2</span>,    )    reservation += shiftbid = floor_tick(reservation - spread / <span class="hljs-number">2</span> * (<span class="hljs-number">1</span> - asym))ask = ceil_tick(reservation + spread / <span class="hljs-number">2</span> * (<span class="hljs-number">1</span> + asym))</code></pre></div><p>再往后才是逐侧 policy：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">if</span> tox_bid &gt;= adverse_toxicity_threshold:    bid_policy.spread_mult = <span class="hljs-built_in">max</span>(bid_policy.spread_mult, adverse_spread_mult)    bid_policy.size_mult = <span class="hljs-built_in">min</span>(bid_policy.size_mult, <span class="hljs-number">0.70</span>)    bid_policy.allow_exposure_increase = <span class="hljs-literal">False</span><span class="hljs-keyword">if</span> bid_side_adverse_pause:    bid_policy.allow_post = <span class="hljs-literal">False</span></code></pre></div><p>所以机器学习不是直接“预测价格然后下单”。它有三类入口：</p><ol><li><strong>改变物理 quote</strong>：<code>vol_10s</code>、<code>ret_10s</code>、<code>dir_10s</code> 影响 reservation、spread、asymmetry；</li><li><strong>改变 policy gate</strong>：<code>tox_bid/ask</code>、markout EMA、microprice shift 影响 widen/shrink/pause；</li><li><strong>改变风险读法</strong>：quote EV 与 shadow calibration 不直接替代 AS，而是判断某一侧 quote 是否值得继续提供流动性。</li></ol><p>实操调参时，不应该先问“哪个 <code>gamma</code> PnL 最高”，而应该按顺序看：</p><div class="code-wrapper"><pre><code class="hljs text">1. spread/cap sanity：spread&lt;100、cap hit、final spread distribution2. execution sanity：placed orders/day、fills/day、fills/order3. adverse sanity：1s/5s/30s maker-signed markout by side4. inventory sanity：abs inventory-time、InvAdj、pnl per inventory-hour5. calibration sanity：toxicity / quote EV bucket 是否和 realized markout 对齐</code></pre></div><p>如果一个参数组合 raw PnL 更高，但靠库存时间暴露翻倍换来，或者只在 cap_hit 很高的状态下好看，它就不是更稳的参数。</p><h3 id="5-2-Queue-Ahead：回测撮合状态机的初始化边界"><a href="#5-2-Queue-Ahead：回测撮合状态机的初始化边界" class="headerlink" title="5.2 Queue Ahead：回测撮合状态机的初始化边界"></a>5.2 Queue Ahead：回测撮合状态机的初始化边界</h3><p>普通 trader 最关心的不是“有没有 queue 模型”，而是：我的订单进 bid1 的那一刻，到底被假设排在哪里？</p><p>NarrowGate 的保守口径是：</p><ol><li>如果 activation timestamp 能找到历史 L2，且 quote price 对应某个可见 level，就假设自己排在该 level 可见数量之后；</li><li>如果没有可靠 L2，就用距离 mid 的指数衰减模型兜底；</li><li>后续主动成交先消耗 <code>queue_left</code>，打穿以后才轮到自己的订单；</li><li>不因为历史 level 数量变小就自动把“别人撤单”全部算作自己排位前进；</li><li>cancel latency 生效前，pending cancel order 仍然可能被 fill。</li></ol><p>伪代码如下：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">init_queue</span>(<span class="hljs-params">order, activation_ts, l2</span>):</span>    level_qty = l2.visible_qty_at(order.price, activation_ts)    <span class="hljs-keyword">if</span> level_qty <span class="hljs-keyword">is</span> <span class="hljs-keyword">not</span> <span class="hljs-literal">None</span>:        <span class="hljs-comment"># worst-case within visible top-N: 排在当前可见同价位后面</span>        <span class="hljs-keyword">return</span> level_qty    dist = <span class="hljs-built_in">abs</span>(order.price - mid_at(activation_ts))    <span class="hljs-keyword">return</span> queue_base * exp(-queue_decay * dist)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">on_aggressive_trade</span>(<span class="hljs-params">order, trade</span>):</span>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> crosses(order.price, trade.price, order.side):        <span class="hljs-keyword">return</span>    remaining = trade.qty    eaten_ahead = <span class="hljs-built_in">min</span>(order.queue_left, remaining)    order.queue_left -= eaten_ahead    remaining -= eaten_ahead    <span class="hljs-keyword">if</span> order.queue_left &lt;= <span class="hljs-number">0</span> <span class="hljs-keyword">and</span> remaining &gt; <span class="hljs-number">0</span>:        fill_qty = <span class="hljs-built_in">min</span>(order.remaining, remaining)        fill(order, fill_qty)</code></pre></div><p>这仍然不是交易所真实 FIFO 队列，因为 Top-N 历史盘口看不到隐藏单、真实 order id 排序、你前方订单撤单和交易所撮合内部细节。它的价值不是“完美复现撮合”，而是避免 bar touch 那种最危险的乐观假设。</p><p>如果要判断 queue 模型是否太保守或太乐观，不能只看 fills/day，要同时看：</p><div class="table-container"><table><thead><tr><th>指标</th><th>解释</th></tr></thead><tbody><tr><td>placed orders/day</td><td>策略是否真的挂出了足够多订单</td></tr><tr><td>fills/day</td><td>最终成交频率</td></tr><tr><td>fills / placed order</td><td>queue/fill 概率是否和 live 同数量级</td></tr><tr><td>bid/ask split</td><td>是否一侧被系统性高估</td></tr><tr><td>queue_left at fill</td><td>是否经常“刚好被打穿”</td></tr><tr><td>pending cancel fill count</td><td>cancel latency 下是否仍然能被打</td></tr></tbody></table></div><p>如果 fills/day 低一个数量级，但 fills/order 与 live 接近，问题往往不是 queue，而是策略在回测里被 guard/pause 挡掉了太多 quote。这个区别非常关键。</p><h3 id="5-3-Shadow-Mode-的硬-gate：方向对还不够，必须校准过关"><a href="#5-3-Shadow-Mode-的硬-gate：方向对还不够，必须校准过关" class="headerlink" title="5.3 Shadow Mode 的硬 gate：方向对还不够，必须校准过关"></a>5.3 Shadow Mode 的硬 gate：方向对还不够，必须校准过关</h3><p>Shadow mode 不是为了再看一个漂亮 PnL，而是为了回答：</p><script type="math/tex; mode=display">P_{\text{model}}(\text{fill}) \approx P_{\text{observed}}(\text{fill})</script><p>以及：</p><script type="math/tex; mode=display">E[\text{markout}\mid \text{predicted bucket}]\approxE[\text{realized markout}\mid \text{same bucket}]</script><p>实操上我会把 gate 分成四层：</p><div class="table-container"><table><thead><tr><th>Gate</th><th style="text-align:right">最低要求</th><th>含义</th></tr></thead><tbody><tr><td>样本量</td><td style="text-align:right">每 side validation valid fills <code>&gt;= 100</code>，更理想是数百</td><td>少于这个数，bucket calibration 很容易飘</td></tr><tr><td>fill calibration</td><td style="text-align:right">分桶 MAE <code>&lt;= 0.05</code></td><td>预测 20% fill 的桶，真实 fill rate 不能长期是 5% 或 50%</td></tr><tr><td>markout calibration</td><td style="text-align:right">bucket realized-vs-pred 同向，MAE 不恶化</td><td>高 EV bucket 至少不能有更差 markout</td></tr><tr><td>风险效率</td><td style="text-align:right">InvAdj 不差，abs inventory-time 不恶化，fills/day 不塌</td><td>不能用堆库存换 raw PnL</td></tr></tbody></table></div><p>当前 quote EV 的真实状态并没有过这些 gate。比如之前 strict-gated run 中，May valid fills 只有几十个，fill calibration MAE 也超阈值。因此即使 <code>ask_ev_soft_widen</code> 在某个 A/B 里 raw 看起来不错，也只能被标为 shadow/research 候选。</p><p>一个更像上线前检查表的伪代码是：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">promotion_gate</span>(<span class="hljs-params">summary</span>):</span>    <span class="hljs-keyword">return</span> (        summary.valid_filled_rows &gt;= <span class="hljs-number">100</span>        <span class="hljs-keyword">and</span> summary.fill_calibration_mae &lt;= <span class="hljs-number">0.05</span>        <span class="hljs-keyword">and</span> summary.markout_bucket_direction_ok        <span class="hljs-keyword">and</span> summary.inv_adj_delta &gt;= -small_tolerance        <span class="hljs-keyword">and</span> summary.abs_inventory_time_delta &lt;= <span class="hljs-number">0</span>        <span class="hljs-keyword">and</span> summary.monthly_sign_stability_ok    )</code></pre></div><p>这不是说阈值永远不变，而是说必须有硬门槛。没有门槛的 shadow mode，很容易退化成“我又看了一张更漂亮的图”。</p><h3 id="5-4-Python-C-胶水层：不是每-tick-传-JSON，而是一次传连续数组"><a href="#5-4-Python-C-胶水层：不是每-tick-传-JSON，而是一次传连续数组" class="headerlink" title="5.4 Python/C++ 胶水层：不是每 tick 传 JSON，而是一次传连续数组"></a>5.4 Python/C++ 胶水层：不是每 tick 传 JSON，而是一次传连续数组</h3><p>普通工程师最担心跨语言部分：Python 和 C++ 到底怎么一起工作？答案是：<strong>回测/batch 路径里，Python 负责 IO 和编排，C++ 负责一次调用内的状态机；不会每 tick 传 JSON。</strong></p><p>离线路径是：</p><div class="code-wrapper"><pre><code class="hljs text">Parquet / CSV / logs        |        vPython pandas/pyarrow loader        |        vNumPy contiguous arrays        |        vpybind11 binding        |        vC++ ArrayView / MatrixView / std::span        |        vsimulate full window / compute batch quote context        |        vsummary + optional trace arrays back to Python</code></pre></div><p>核心原则是：</p><ul><li>C++ 不在 hot loop 里读 parquet；</li><li>Python 不在每个 tick 调 C++；</li><li>pybind 只在窗口边界检查 dtype、shape、contiguous；</li><li><code>std::span</code> / view 只在函数调用期间借用 Python-owned memory；</li><li>C++ 不把这些指针存到函数返回之后；</li><li>trace、diagnostics、DataFrame 构造回到 Python 冷路径。</li></ul><p>伪代码可以理解为：</p><div class="code-wrapper"><pre><code class="hljs python">trades = load_trades_as_numpy(month)l2 = load_l2_as_numpy(month)params = build_cpp_params(config)result = narrowgate_cpp.simulate_tick_arrays(    trades.ts_ms,    trades.price,    trades.qty,    trades.is_buyer_maker,    params,    bbo_ts_ms=bbo.ts_ms,    l2_bid_px=l2.bid_px_matrix,    l2_bid_qty=l2.bid_qty_matrix,    l2_ask_px=l2.ask_px_matrix,    l2_ask_qty=l2.ask_qty_matrix,)summary = result.summarytrace = result.quote_trace <span class="hljs-keyword">if</span> trace_enabled <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span></code></pre></div><p>live 路径则不同。live 不能一次喂一整个月数组，所以只把很小的 compact context 过桥：</p><div class="code-wrapper"><pre><code class="hljs text">Python event state        |        vfixed tuple / small arrays / cached native config        |        vC++ quote + policy + routing decision        |        vcompact result: price, size, can_post, reason_mask        |        vPython REST adapter</code></pre></div><p>这也是为什么 scalar quote core 单独迁 C++ 反而可能慢：如果每次都构造 dict、dataclass、字符串 key，跨语言边界比数学本身更贵。真正要优化的是“边界形状”，不是盲目把更多 Python 改成 C++。</p><h3 id="5-5-Fixed-Array-Signal：哪些是-O-1-，哪些还不是"><a href="#5-5-Fixed-Array-Signal：哪些是-O-1-，哪些还不是" class="headerlink" title="5.5 Fixed-Array Signal：哪些是 O(1)，哪些还不是"></a>5.5 Fixed-Array Signal：哪些是 O(1)，哪些还不是</h3><p><code>fixed-array signal</code> 不是一个神秘概念，本质就是把“特征名字符串字典”换成“固定顺序数组”：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-keyword">class</span> <span class="hljs-title">SignalFeatureId</span> &#123;</span>    Return1,    Volatility60s,    TradeIntensity60s,    TakerQuoteImbalance10s,    <span class="hljs-comment">// ...</span>    Count,&#125;;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SignalFeatureVector</span> &#123;</span><span class="hljs-keyword">public</span>:    <span class="hljs-keyword">double</span>&amp; <span class="hljs-keyword">operator</span>[](SignalFeatureId id);    <span class="hljs-function"><span class="hljs-keyword">const</span> std::array&lt;<span class="hljs-keyword">double</span>, kSignalFeatureCount&gt;&amp; <span class="hljs-title">values</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;<span class="hljs-keyword">private</span>:    std::array&lt;<span class="hljs-keyword">double</span>, kSignalFeatureCount&gt; values_&#123;&#125;;&#125;;</code></pre></div><p>行情状态维护则分两层：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TradeBarAggregator</span> &#123;</span>    <span class="hljs-function">std::optional&lt;Bar1s&gt; <span class="hljs-title">update</span><span class="hljs-params">(ts_ms, price, qty, is_buyer_maker)</span></span>;    Bar1s current_;    <span class="hljs-keyword">int</span> last_trade_side_;    <span class="hljs-keyword">int</span> last_trade_run_len_;&#125;;<span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">int</span> WindowSeconds&gt;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RollingStats</span> &#123;</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push</span><span class="hljs-params">(ts_ms, value)</span> </span>&#123;        values.<span class="hljs-built_in">push_back</span>(&#123;ts_ms, value&#125;);        sum += value;        sum_sq += value * value;        <span class="hljs-built_in">expire</span>(ts_ms);    &#125;    <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">mean</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-function"><span class="hljs-keyword">double</span> <span class="hljs-title">stddev</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;&#125;;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SignalFeatureEngine</span> &#123;</span>    CircularBuffer&lt;Bar1s&gt; bars_;    CircularBuffer&lt;FeatureHistoryRow&gt; history_;    CountRollingMoments return_abs_2160_;    CountRollingMoments return_abs_8640_;    CountRollingMoments vol_regime_6h_60480_;&#125;;</code></pre></div><p>但这里也要诚实：当前并不是所有特征都已经做到完全 O(1) 增量。已经增量化的是一部分长窗口 rolling moments 和 trade/bar state；仍有一些 overlay 特征会扫描短窗口或使用 scratch buffer。原因是这些特征还处在研究变化阶段，过早把所有实验性 feature 都写成复杂 C++ 状态机，会让 parity 和调参变得更难。</p><p>当前工程取舍是：</p><ul><li>高频、稳定、低基数的窗口统计逐步进 C++；</li><li>特征顺序固定，用 array 输出，减少 dict/string 热路径；</li><li>实验性特征先保留 Python 或短窗口扫描；</li><li>只有当某个特征进入稳定模型并被 profile 证明是热点，再改成长期持有的 rolling state。</li></ul><p>这和前面 C++ 的原则一致：C++ 不是为了“看起来硬核”，而是为了把稳定边界收紧。</p><h3 id="5-6-Live-脏数据与异常：离线可以删，live-只能降级"><a href="#5-6-Live-脏数据与异常：离线可以删，live-只能降级" class="headerlink" title="5.6 Live 脏数据与异常：离线可以删，live 只能降级"></a>5.6 Live 脏数据与异常：离线可以删，live 只能降级</h3><p>离线数据坏了可以物理删除，live 不行。live 遇到乱序 tick、WebSocket 静默、盘口过期、bad trade、用户流漏成交时，策略必须降级，而不是继续假装状态健康。</p><p>当前 NarrowGate 的 live 防御层大概是：</p><div class="table-container"><table><thead><tr><th>异常</th><th>当前处理</th></tr></thead><tbody><tr><td>bad trade parse / 非法价格数量</td><td>计数并限频日志，不进入 bar state</td></tr><tr><td>aggTrade 静默</td><td>watchdog 切 raw <code>@trade</code> 或重连</td></tr><tr><td>execution/reference/spot stream 静默</td><td>market stream reconnect</td></tr><tr><td>execution depth 过期</td><td>cancel live quotes，停止 requote，原因 <code>stale_hard</code></td></tr><tr><td>bookTicker / post-only crossing</td><td>GTX reject 当作正常不可挂事件处理</td></tr><tr><td>user stream 漏 fill / REST sync 差异</td><td><code>SYNC_ADJUST</code> hard degrade，暂停增加暴露的一侧并重连 user stream</td></tr><tr><td>stale local order</td><td>周期性清理 <code>PENDING_NEW</code> 卡死订单</td></tr><tr><td>C++ 扩展异常</td><td>strict 模式直接 fail；非 strict live 可 fallback/disable native path</td></tr><tr><td>运行健康</td><td><code>HEALTH</code> 输出 position、PnL、库存时间、订单数、requote 数</td></tr></tbody></table></div><p>伪代码是：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">if</span> depth_age_s &gt; max_exec_book_age_s:    cancel_active_quotes()    skip_requote(reason=<span class="hljs-string">&quot;stale_hard&quot;</span>)<span class="hljs-keyword">if</span> stream_silent(symbol, timeout_s):    reconnect_market_streams()<span class="hljs-keyword">if</span> sync_adjust_count &gt;= threshold:    pause_exposure_increasing_quotes(duration_s)    reconnect_user_stream()<span class="hljs-keyword">if</span> bad_trade:    bad_trade_counter += <span class="hljs-number">1</span>    <span class="hljs-keyword">return</span></code></pre></div><p>这仍然不是完整高可用交易网关。它没有 kernel bypass、没有独立 order gateway 进程、没有完整 SPSC ring 隔离，也没有多机热备。当前目标是：在研究/影子/小规模 live-style 运行里，遇到脏数据时优先撤单、降级、报警、重连，而不是继续用坏状态报价。</p><blockquote><p><strong>本节实操结论</strong></p><ul><li>参数映射要从 <code>gamma/kappa/sigma -&gt; spread/reservation/tick</code> 讲清楚，再谈 ML 是否有用。</li><li>queue ahead 的可信度来自保守初始化、逐笔消耗和 live 校准，而不是声称知道真实 FIFO。</li><li>shadow mode 必须有样本量、fill calibration、markout calibration 和库存风险硬 gate。</li><li>Python/C++ 胶水层的关键是一次传连续数组或固定 tuple，不在每 tick 传动态对象。</li><li>fixed-array signal 当前是渐进式迁移：稳定热点进 C++，实验性特征保留更灵活的路径。</li><li>live 脏数据不能删除，只能通过 stale block、watchdog reconnect、SYNC_ADJUST degrade 和 telemetry 降级。</li></ul></blockquote><h2 id="6-已完成与未完成边界：不要把研究系统误读成-Alpha-终局"><a href="#6-已完成与未完成边界：不要把研究系统误读成-Alpha-终局" class="headerlink" title="6. 已完成与未完成边界：不要把研究系统误读成 Alpha 终局"></a>6. 已完成与未完成边界：不要把研究系统误读成 Alpha 终局</h2><p>如果站在金融 Alpha 的终局视角看，前面的内容还不能被理解为“这个 maker 系统已经解决了做市问题”。更准确的说法是：NarrowGate 已经完成了一个更严格的研究框架，让很多错误结论更难通过；但动态到达率、主动对冲、多 venue lead-lag 和 HFT 级线程模型这些真正决定上限的问题，仍然没有完全解决。</p><p>把完成度摊开，比把每个模块都写得很漂亮更重要。</p><div class="table-container"><table><thead><tr><th>追问</th><th>当前完成度</th><th>当前真实状态</th></tr></thead><tbody><tr><td>动态 <code>kappa</code> / <code>Lambda(delta)</code> 微观校准</td><td>部分完成</td><td>有 depth-kappa、P3 <code>kappa_eff</code> 和 general intensity hook，但 toxicity 主要进入 widen/pause policy，没有连续回写成 <code>kappa_eff(toxicity)</code></td></tr><tr><td>极端尾部下的 toxicity 处理</td><td>部分完成</td><td>已有逐侧 adverse guard、markout EMA、hybrid pause 和 wall-clock decay；还不是完整的 arrival-intensity deformation</td></tr><tr><td>库存主动瘦身 / hedging</td><td>部分完成</td><td>有库存时间积分、硬库存限制、可选 timeout/IOC/MARKET 兜底；没有跨市场 BTCUSDT perp 主动对冲器</td></tr><tr><td>多 venue 虚假流动性 / 延迟套利</td><td>部分完成</td><td>有 source audit、freshness gate、shock attribution；但本地缺少独立 venue 原始数据，不能验证真实 multi-venue lead-lag</td></tr><tr><td>Python/C++ replay parity</td><td>基本完成</td><td>synthetic parity + 真实窗口 golden parity 已覆盖 PnL、fills、库存、trace 行数等核心指标</td></tr><tr><td>C++ cache / allocator / hot path</td><td>部分完成</td><td>replay/batch/signal 已做 <code>DepthView</code>、fixed array、ring state、summary-only、hot/cold 拆分；live orderbook 还不是完整 native flat book</td></tr><tr><td>live 线程模型 / 网络 I/O 隔离</td><td>未完成</td><td>当前是 Python WebSocket callback threads + <code>SignalEngine</code> lock + 主循环 tick，不是 pinned reactor + SPSC ring buffer + strategy core</td></tr></tbody></table></div><h3 id="6-1-金融侧：AS-是坐标系，不是答案"><a href="#6-1-金融侧：AS-是坐标系，不是答案" class="headerlink" title="6.1 金融侧：AS 是坐标系，不是答案"></a>6.1 金融侧：AS 是坐标系，不是答案</h3><p>Avellaneda-Stoikov 给了一个非常有用的坐标系：</p><script type="math/tex; mode=display">r_t = S_t - q_t \gamma \sigma_t^2 (T-t)</script><script type="math/tex; mode=display">\delta^\ast \approx \frac{1}{\gamma}\log(1+\gamma/\kappa)</script><p>但真正困难的是右边这些变量如何变成实时市场状态，而不是公式本身。</p><p>当前 NarrowGate 对 <code>kappa</code> 的处理有三层：</p><ol><li>静态 <code>strategy.kappa</code> 作为 baseline；</li><li><code>p3_kappa_eff</code> / general intensity 用历史 fill probability 拟合更一般的 <code>Lambda(delta)</code>；</li><li>depth-kappa 用近端盘口深度把 <code>kappa</code> 做有限比例调整。</li></ol><p>这还不等于“动态到达率已经被完全校准”。真正金融上更尖锐的问题是：</p><script type="math/tex; mode=display">\kappa_{\text{eff}}= f(\text{depth}, \text{vol}, \text{toxicity}, \text{reference shock}, \text{latency floor})</script><p>当市场发生信息冲击时，远端 quote 的成交概率可能不是指数衰减，而是突然变成“深价位也会被扫”。这时 <code>kappa</code> 不是简单变大或变小，而是整条 <code>Lambda(delta)</code> 曲线形状改变。当前系统里 toxicity 更多是进入逐侧 policy：</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">if</span> toxicity &gt;= toxicity_threshold:    side_policy.spread_mult = <span class="hljs-built_in">max</span>(side_policy.spread_mult, widen_mult)    side_policy.size_mult = <span class="hljs-built_in">min</span>(side_policy.size_mult, shrink_mult)    side_policy.allow_exposure_increase = <span class="hljs-literal">False</span><span class="hljs-keyword">if</span> side_adverse_pause:    side_policy.allow_post = <span class="hljs-literal">False</span></code></pre></div><p>也就是说，当前实现更像：</p><script type="math/tex; mode=display">\text{toxicity} \rightarrow\{\text{widen}, \text{shrink}, \text{pause}\}</script><p>而不是：</p><script type="math/tex; mode=display">\text{toxicity} \rightarrow\kappa_{\text{eff}} \rightarrow \delta^\ast</script><p>这是一条明确的未完成边界。未来如果要真正“搞定 AS 坐标系”，需要让 toxicity、reference shock 和 live fill calibration 共同形变 <code>Lambda(delta)</code>，并要求变形后的曲线能通过 shadow fill probability calibration，而不只是让某个月 A/B 的 raw PnL 变好。</p><h3 id="6-2-库存终局：被动斜率不是万能退出路径"><a href="#6-2-库存终局：被动斜率不是万能退出路径" class="headerlink" title="6.2 库存终局：被动斜率不是万能退出路径"></a>6.2 库存终局：被动斜率不是万能退出路径</h3><p>库存控制现在已经比早期完整很多。系统会记录：</p><script type="math/tex; mode=display">\int |q_t|\,dt,\quad\int q_t\,dt,\quad\int q_t^2\,dt,\quad\int |q_t|S_t\,dt</script><p>这让 A/B 不只比较 raw PnL，还能比较“赚这点 PnL 占用了多少库存时间”。但这仍然不是库存终局。</p><p>做市系统真正被打穿时，reservation price 的斜率可能已经不够。举例说，当库存接近 <code>max_inventory</code>，且市场继续沿不利方向重定价时，继续靠“少挂增加库存的一侧、多挂减少库存的一侧”来恢复，可能会遇到两个问题：</p><ul><li>减仓一侧一直排不到队；</li><li>能排到队时，价格已经变差，库存损失扩大。</li></ul><p>当前 NarrowGate 有可选的 timeout close / IOC / emergency MARKET 兜底，但默认主线并不启用主动 taker 对冲。也就是说，它还没有实现这种路径：</p><div class="code-wrapper"><pre><code class="hljs text">BTCUSDC maker inventory        |        | risk budget breached        vchoose hedge venue / instrument        |        | estimate hedge cost + basis + latency        vBTCUSDT perp / spot taker hedge        |        vresidual basis + inventory budget update</code></pre></div><p>如果站在资金效率和风险终局看，下一阶段应该研究的是：</p><script type="math/tex; mode=display">\text{expected hedge cost}<\text{expected inventory tail loss}</script><p>只有当主动瘦身成本、basis risk、taker fee、延迟和残余库存都进入同一张账，库存控制才从“被动做市参数”变成“风险闭环”。当前项目还没有走到这里。</p><h3 id="6-3-多市场-reference：有信息不等于能赚钱"><a href="#6-3-多市场-reference：有信息不等于能赚钱" class="headerlink" title="6.3 多市场 reference：有信息不等于能赚钱"></a>6.3 多市场 reference：有信息不等于能赚钱</h3><p>BTCUSDT perp、BTCUSDT spot、BTCUSDC spot 确实有信息量。source audit 和 shock attribution 都显示：很多 adverse fills 能被 BTCUSDT reference move 解释，spot adverse return 也有增量信号。</p><p>但这不是最终答案。真正的问题是 reference 信号的时效性：</p><script type="math/tex; mode=display">\Delta t_{\text{lead-lag}}\quad \text{vs} \quad\Delta t_{\text{network}} + \Delta t_{\text{decision}} + \Delta t_{\text{exchange}}</script><p>如果 reference 的重定价领先本地执行市场 50ms，而你的端到端反应是 5ms，这个信号可能有执行价值；如果 reference 已经在 0.5ms 内传导完，本地 quote 看到它时，只是在看一个滞后指标。此时统一打开 <code>multi_market</code> 可能不是增强模型，而是把“已经被吸收的流动性冲击”和“真正的信息冲击”混在一起，导致该接的流动性不接、该躲的信息冲击又躲不掉。</p><p>当前 NarrowGate 对这个问题的回答是 partial：</p><ul><li>已经把 <code>multi_market.enabled</code> 从“收益开关”降级为“需要被审计的 source 条件变量”；</li><li>已经做了 local-only、perp、spot、组合 source profile 的隔离 A/B；</li><li>已经要求 freshness、basis residual、coverage 和 shock label；</li><li>但没有独立 venue 的原始毫秒/微秒级数据，因此不能验证真正的 cross-venue lead-lag 和 latency arbitrage。</li></ul><p>换句话说，现在能说的是：<strong>Binance 内部多源数据有解释力，但不能证明它形成了可执行的多 venue alpha</strong>。</p><h3 id="6-4-系统侧：C-replay-很强，live-架构还不是终局"><a href="#6-4-系统侧：C-replay-很强，live-架构还不是终局" class="headerlink" title="6.4 系统侧：C++ replay 很强，live 架构还不是终局"></a>6.4 系统侧：C++ replay 很强，live 架构还不是终局</h3><p>工程侧完成度更高，但也要分清 offline 和 live。</p><p>已经比较扎实的是：</p><ul><li>Python/C++ 双引擎 replay parity；</li><li>depth-aware quote-core batch；</li><li>C++ tick replay 状态机；</li><li><code>DepthView</code> / <code>std::span</code> 无分配访问；</li><li>fixed-array signal feature vector；</li><li>hot/cold order 拆分；</li><li>summary-only replay 输出；</li><li>x86 live 上的 p99/p99.9 soak benchmark。</li></ul><p>但 live 线程模型还不是 HFT 终局。当前架构更接近：</p><div class="code-wrapper"><pre><code class="hljs text">Binance WebSocket client callback threads        |        vPython WSHandler        |        vSignalEngine with Lock        |        vmain loop: engine.tick() every short sleep        |        vREST order adapter</code></pre></div><p>这和更硬核的低延迟架构不同：</p><div class="code-wrapper"><pre><code class="hljs text">NIC / kernel bypass or tuned socket        |        vpinned reactor thread        |        vSPSC ring buffer / sequence barrier        |        vpinned strategy thread: rolling state + quote + routing        |        vorder gateway thread</code></pre></div><p>前者适合研究、shadow、审计和可维护性；后者才是面向极端尾延迟的 live 系统。NarrowGate 目前还没有做 kernel bypass、CPU pinning、SPSC ring、NUMA partition、专用 order gateway，也没有把 WebSocket 解码和策略计算完全隔离。</p><p>这不影响当前 C++ 迁移的价值，因为当前 C++ 主要服务两个目标：</p><ol><li><strong>研究吞吐</strong>：让月份级 replay、source A/B、quote context 和 label 生成跑得动；</li><li><strong>边界收紧</strong>：让 live hot path 中稳定的小决策包更少依赖 Python 动态对象。</li></ol><p>但它确实说明：不能把当前项目包装成“完整 HFT live stack”。更诚实的定位是：<strong>一个带有 native replay/hot-path 原型的 maker 研究系统</strong>。</p><h3 id="6-5-下一阶段真正值得做的事"><a href="#6-5-下一阶段真正值得做的事" class="headerlink" title="6.5 下一阶段真正值得做的事"></a>6.5 下一阶段真正值得做的事</h3><p>如果继续往“金融大牛和系统大牛都会认可”的方向推进，下一阶段不是再加一个模型，而是补四个闭环：</p><ol><li><strong>动态 <code>Lambda(delta)</code> 闭环</strong>：用 live/shadow fill calibration，把 depth、toxicity、reference shock 映射到整条到达率曲线，而不是只做 widen/pause。</li><li><strong>主动瘦身闭环</strong>：设计 BTCUSDT perp/spot hedge simulator，把 hedge cost、basis risk、taker fee 和 inventory tail loss 放进同一张账。</li><li><strong>真实 multi-venue lead-lag 闭环</strong>：接入独立 venue 原始盘口/逐笔数据，验证 reference 信号是否早于本地可执行窗口。</li><li><strong>live threading 闭环</strong>：把 WebSocket decode、rolling state、quote decision 和 order gateway 分层，至少用 SPSC ring + pinned strategy thread 做一次 x86 p99/p99.9 对照。</li></ol><blockquote><p><strong>本节边界结论</strong></p><ul><li>NarrowGate 已经比较好地回答了“如何让 maker 研究少自欺欺人”。</li><li>它还没有完全回答“如何在尾部冲击中存活、如何主动对冲、如何穿透多 venue 延迟、如何构建 HFT 级 live stack”。</li><li>把这些未完成项写清楚，不会削弱项目；反而能防止读者把研究框架误读成已经闭环的交易系统。</li></ul></blockquote><h2 id="结论：算法假设、验证流程和-C-边界要同时成立"><a href="#结论：算法假设、验证流程和-C-边界要同时成立" class="headerlink" title="结论：算法假设、验证流程和 C++ 边界要同时成立"></a>结论：算法假设、验证流程和 C++ 边界要同时成立</h2><p>到现在，NarrowGate 给我的最大教训不是“C++ 很快”，而是研究系统要同时守住四条线：</p><ol><li><strong>数据连续性</strong>：坏日期、低 coverage 和跨 gap label 必须在统一质量层阻断；</li><li><strong>目标一致性</strong>：reference/spot 有预测信息，不代表一个总开关具有执行收益；</li><li><strong>风险完整性</strong>：raw PnL 之外，还要看 InvAdj、fill/markout、校准和库存时间；</li><li><strong>引擎一致性</strong>：Python、C++、回放与事件引擎必须共享同一套 policy 语义。</li></ol><p>目前 enhanced spot 已经证明“更多市场能帮助解释 adverse flow”，但没有证明 <code>multi_market.enabled=true</code> 是稳定收益开关。ask quote EV 是最值得继续观察的方向，但 validation fill 和 calibration 仍未过 gate。C++ replay 已经显著缩短快筛时间，但它的价值首先是让更多真实窗口、更多反例和更严格 parity 变得可负担。</p><p>这也回答了文章开头的问题：NarrowGate 当前得到的并不是一条“接入更多数据、换成 C++，收益自然变好”的直线。相反，它得到的是一套逐层收紧的否证流程：数据源先证明连续，feature 再证明非零且时间对齐，模型再证明校准，policy 再证明跨月收益和库存效率，最后 C++ 只负责让这些检验跑得更快、更一致。</p><p>换句话说，C++ 没有替项目找到策略答案。它做的是更有用的事：让错误的答案更快暴露，让好看的数字更难逃过验证。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>NarrowGate 从一个 AS 公式实验，走到了数据审计、跨市场冲击归因、逐侧 quote EV、库存时间积分和 C++ 状态机。这个过程不断把最初的问题变窄：不是“能不能预测 BTC”，而是“在这一侧、这个价格、这个盘口和这段库存暴露下，是否值得提供一次短暂的被动流动性”。</p><p>这可能也是“窄门”最贴切的地方。一个研究系统的成熟，不在于它接入了多少模型和数据源，而在于它愿意用多少层约束拒绝一个看起来很诱人的结论。</p><hr><p>本文及 NarrowGate 项目仅用于 C++ 低延时系统、市场微观结构、价格行为学、做市模型和机器学习回测方法的学习交流与技术研究，不构成财务、投资、法律或合规建议。中国大陆关于 crypto 交易及相关业务活动的监管环境具有不确定性；任何人将相关代码用于连接交易平台、交易、商业展业或投资决策，其合规风险、资金损失、技术故障及其他后果均由使用者自行承担，与作者无关。</p>]]></content>
    
    
    <categories>
      
      <category>C++</category>
      
    </categories>
    
    
    <tags>
      
      <tag>C++</tag>
      
      <tag>低延时</tag>
      
      <tag>Market Making</tag>
      
      <tag>市场微观结构</tag>
      
      <tag>数据质量</tag>
      
      <tag>机器学习</tag>
      
      <tag>pybind11</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 5: Lazy page allocation</title>
    <link href="/2023/02/21/MIT-6-S081-Lab-5-Lazy-Page-Allocation/"/>
    <url>/2023/02/21/MIT-6-S081-Lab-5-Lazy-Page-Allocation/</url>
    
    <content type="html"><![CDATA[<h4 id="Eliminate-allocation-from-sbrk"><a href="#Eliminate-allocation-from-sbrk" class="headerlink" title="Eliminate allocation from sbrk()"></a>Eliminate allocation from sbrk()</h4><p>我们先来看看这个图：</p><p><img src="https://img2020.cnblogs.com/blog/2002319/202101/2002319-20210104192210213-1712203988.png" alt=""></p><p>代码段不必多说，唯一要注意的点是：在原始 xv6 实现里，<code>sbrk()</code> 会触发实际物理页分配并建立映射；而本实验要改成“先只扩大地址空间，再按缺页异常补映射”的方式。</p><p>本实验你的任务是修改 <code>sys_sbrk()</code>使得其只增加/减少进程地址空间大小，而不真正地分配页面。<code>sbrk</code>是xv6提供的系统调用，它使得用户应用程序能扩大自己的<code>heap</code> ，相当于linux中的<code>brk</code>。</p><p>在xv6中，<code>sbrk</code>的实现默认是<code>eager allocation</code>，一旦调用了<code>sbrk</code>，内核就会立即分配物理内存给进程。我们可以利用<code>lazy allocation</code>来做，<code>sbrk</code>基本不做任何事，只是改变<code>p-&gt;sz</code>的大小。这里的 <code>n</code> 是按字节计算的增量，而不是“页数”；真正按页分配会延后到 page fault 发生时。</p><p>我们先来看看原版本：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64</span><span class="hljs-function"><span class="hljs-title">sys_sbrk</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">int</span> addr;  <span class="hljs-keyword">int</span> n;  <span class="hljs-keyword">if</span>(argint(<span class="hljs-number">0</span>, &amp;n) &lt; <span class="hljs-number">0</span>)<span class="hljs-comment">//检测传入的参数</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;  addr = myproc()-&gt;sz;<span class="hljs-comment">//uint64 sz,Size of process memory (bytes)</span>  <span class="hljs-keyword">if</span>(growproc(n) &lt; <span class="hljs-number">0</span>)<span class="hljs-comment">//分配n个bytes</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;  <span class="hljs-keyword">return</span> addr;&#125;</code></pre></div><p>我们看看growproc函数，就知道为什么要这么做了：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Grow or shrink user memory by n bytes.</span><span class="hljs-comment">// Return 0 on success, -1 on failure.</span><span class="hljs-function"><span class="hljs-keyword">int</span></span><span class="hljs-function"><span class="hljs-title">growproc</span><span class="hljs-params">(<span class="hljs-keyword">int</span> n)</span></span><span class="hljs-function"></span>&#123;  uint sz;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">p</span> =</span> myproc();  sz = p-&gt;sz;  <span class="hljs-keyword">if</span>(n &gt; <span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">if</span>((sz = uvmalloc(p-&gt;pagetable, sz, sz + n)) == <span class="hljs-number">0</span>) &#123;      <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;    &#125;  &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(n &lt; <span class="hljs-number">0</span>)&#123;    sz = uvmdealloc(p-&gt;pagetable, sz, sz + n);  &#125;  p-&gt;sz = sz;  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div><p>大家看，在上述函数中，使用uvmalloc函数是会为这个进程的虚拟地址分配实际的物理内存的。</p><p>在sys_sbrk()函数中，用来分配页面的是growproc函数，我们直接修改这部分：</p><div class="code-wrapper"><pre><code class="hljs c">uint new_addr=addr+n;<span class="hljs-comment">//直接把地址+n，分配虚拟地址</span>myproc()-&gt;sz=newaddr;</code></pre></div><p>然后对new_addr控制在合理的范围之内范围：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>(newaddr&gt;=MAXVA||newaddr&lt;=<span class="hljs-number">0</span>)&#123;<span class="hljs-keyword">return</span> addr;&#125;</code></pre></div><h4 id="Lazy-allocation"><a href="#Lazy-allocation" class="headerlink" title="Lazy allocation"></a>Lazy allocation</h4><p>本实验中，你的目标是修改trap.c和其他地方的代码，使得在你的代码能够在缺页时分配一块新的内存并建立映射。</p><p>page fault可以让地址映射关系变得动态起来，这也是我们实现lazy allocation的关键。当发生page fault的时候，xv6内核会打印出错的虚拟地址，并且会将这个地址保存在STVAL寄存器中；当用户程序触发 page fault，控制流会切换到内核，再由内核根据 <code>stval</code> 中的故障地址判断如何处理。</p><p>对于不同场景，page fault 的 <code>scause</code> 值不同：<code>13</code> 常见于 load page fault，<code>15</code> 常见于 store/AMO page fault，<code>12</code> 常见于 instruction page fault。</p><h5 id="缺页处理"><a href="#缺页处理" class="headerlink" title="缺页处理"></a>缺页处理</h5><p>我们可以先检查是否为缺页错误。当我们看到了一个page fault，相应的虚拟地址小于当前<em>p-&gt;sz</em>，同时大于stack，那么我们就知道这是一个来自于heap的地址，但是内核还没有分配任何物理内存。所以对于这个page fault的响应也理所当然的直接明了：在page fault handler中，通过kalloc函数分配一个内存page；初始化这个page内容为0；将这个内存page映射到user page table中；最后重新执行指令。比方说，如果是load指令，或者store指令要访问属于当前进程但是还未被分配的内存，在我们映射完新申请的物理内存page之后，重新执行指令应该就能通过了。</p><p>在usertrap函数中，我们可以为缺页异常添加检测：如果缺页异常确实来自 lazy allocation 的地址区间，就分配物理内存并建立页表映射，代码如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>((r_scause() == <span class="hljs-number">13</span> || r_scause() == <span class="hljs-number">15</span>) &amp;&amp; trap_by_lazyallocation)&#123; <span class="hljs-comment">// 缺页异常，并且发生异常的地址进行过懒分配</span>      <span class="hljs-comment">// 分配物理内存，并在页表创建映射</span>   &#125; <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">// 如果不是缺页异常，或者是在非懒加载地址上发生缺页异常，则抛出错误并杀死进程</span>     <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;usertrap(): unexpected scause %p pid=%d\n&quot;</span>, r_scause(), p-&gt;pid);     <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;            sepc=%p stval=%p\n&quot;</span>, r_sepc(), r_stval());     p-&gt;killed = <span class="hljs-number">1</span>;   &#125;</code></pre></div><p>那么我们如何检测是否是lazy allocation导致的错误呢？我们需要定义一个函数<code>trap_by_lazyallocation</code>，其实现如下：</p><div class="code-wrapper"><pre><code class="hljs c">uint64 va = r_stval();<span class="hljs-comment">// 获取触发缺页异常的虚拟地址</span><span class="hljs-keyword">if</span>(va&lt;p-&gt;sz)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;&#125;<span class="hljs-keyword">else</span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div><p>也就是说，当我们的这个要访问的页面小于进程的整个逻辑内存的页面的数量时，说明是缺页的。但是测试之后发现不对，我们再次修改：</p><div class="code-wrapper"><pre><code class="hljs c">((pte = walk(p-&gt;pagetable, va, <span class="hljs-number">0</span>))==<span class="hljs-number">0</span>) || ((*pte &amp; PTE_V)==<span class="hljs-number">0</span>)</code></pre></div><p>同时还要确认这个 va 当前没有有效映射（PTE_V 未置位），这样才能把它归类为“应由懒分配补齐”的缺页。那具体如何分配呢？</p><p>我们可以通过<code>uvmalloc</code>函数来做到分配物理页面，先来看看<code>uvmalloc</code>函数：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64</span><span class="hljs-function"><span class="hljs-title">uvmalloc</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> pagetable, uint64 oldsz, uint64 newsz)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">char</span> *mem;  uint64 a;  <span class="hljs-keyword">if</span>(newsz &lt; oldsz)    <span class="hljs-keyword">return</span> oldsz;  oldsz = PGROUNDUP(oldsz);  <span class="hljs-keyword">for</span>(a = oldsz; a &lt; newsz; a += PGSIZE)&#123;    mem = kalloc();    <span class="hljs-keyword">if</span>(mem == <span class="hljs-number">0</span>)&#123;      uvmdealloc(pagetable, a, oldsz);      <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;    <span class="hljs-built_in">memset</span>(mem, <span class="hljs-number">0</span>, PGSIZE);    <span class="hljs-keyword">if</span>(mappages(pagetable, a, PGSIZE, (uint64)mem, PTE_W|PTE_X|PTE_R|PTE_U) != <span class="hljs-number">0</span>)&#123;      kfree(mem);      uvmdealloc(pagetable, a, oldsz);      <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;  &#125;  <span class="hljs-keyword">return</span> newsz;&#125;</code></pre></div><p>我们仿照上述的<code>uvmalloc</code>函数来编写下面的<code>uvm_lazy</code>函数：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-built_in">memset</span>(mem, <span class="hljs-number">0</span>, PGSIZE);mappages(p-&gt;pagetable, PGROUNDDOWN(va), PGSIZE, (uint64)mem, PTE_W|PTE_X|PTE_R|PTE_U)</code></pre></div><p>这样就完成了 lazy 分配路径的核心映射逻辑。</p><hr><p>补充一个 <code>segmentation fault</code> 的知识：<code>SIGSEGV</code> 本质上是“进程访问了当前页表权限不允许的虚拟地址”，并不等价于“越过了某个段的边界”。</p><p>在 xv6 中，如果用户态访问未映射页、访问超出 <code>p-&gt;sz</code> 的地址，或违反页权限（例如向只读页写入），内核同样会在 trap 中识别异常并杀死进程。Linux 里看到的 “Segmentation fault” 也是这类地址访问异常在用户态的表现，底层主要依赖页表和权限位检查。</p><p>常见触发场景包括：空指针解引用、野指针、越界访问、向只读映射写入，或执行不可执行页面等。</p><p>指针越界访问是触发 <code>SIGSEGV</code> 的常见原因之一。</p><hr>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 1: Xv6 and Unix utilities</title>
    <link href="/2022/12/12/MIT-6-S081-Lab-1-Xv6-and-Unix-Utilities/"/>
    <url>/2022/12/12/MIT-6-S081-Lab-1-Xv6-and-Unix-Utilities/</url>
    
    <content type="html"><![CDATA[<p>在这个实验中要实现几个用户级别的应用程序，其对应的系统调用在<code>kernel</code>中都已经被实现好了。</p><h4 id="sleep"><a href="#sleep" class="headerlink" title="sleep"></a><em>sleep</em></h4><p>本实验要为 xv6 实现 UNIX 程序 sleep； 您的睡眠应暂停用户指定的滴答数。 滴答是 xv6 内核定义的时间概念，即来自定时器芯片的两次中断之间的时间。</p><p>我们检查参数，如果出现不是数字的参数就<code>exit(-1)</code>，否则进行sleep。代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/types.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/stat.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;user/user.h&quot;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc,<span class="hljs-keyword">char</span> *argv[])</span></span>&#123;  <span class="hljs-keyword">while</span>(argv[<span class="hljs-number">1</span>][i]!=<span class="hljs-string">&#x27;&#x27;)&#123;</span><span class="hljs-string">    if(argv[1][i]&gt;&#x27;</span><span class="hljs-number">9&#x27;</span>||argv[<span class="hljs-number">1</span>][i]&lt;<span class="hljs-string">&#x27;0&#x27;</span>)&#123;      write(<span class="hljs-number">1</span>, <span class="hljs-string">&quot;error\n&quot;</span>, <span class="hljs-number">6</span>);      <span class="hljs-built_in">exit</span>(<span class="hljs-number">-1</span>);    &#125;    i++;   &#125;  <span class="hljs-keyword">int</span> times=atoi(argv[<span class="hljs-number">1</span>]);  sleep(times);  <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div><p>值得注意的是，程序中我们使用了一些系统调用函数，如sleep函数，write函数。我们可以在user/user.h中一窥这些函数的原型：</p><p>我们来分析一下<code>write</code>函数，我们可以看到<code>write</code>函数的声明为<code>int write(int,const void*,int);</code>其中，参数中第一个<code>int</code>为文件描述符<code>fd</code>，参数中第二个<code>const void*</code>为内存地址，第三个<code>int</code>为写入的字节数量，意思就是说：将参数<code>buf</code>所指的内存写入<code>count</code>个字节到参数<code>fd</code>所指的文件，其中，fd为文件描述符。大家可以看到上述代码中的<code>write</code>函数的调用：<code>write(1,&quot;error\n&quot;,6)</code>，其中<code>fd=1</code>代表标准输出<code>stdout</code>，也就是会打印到显示器。</p><p>我们来看看<code>write</code>在内核中的实现<code>sys_write</code>:</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64</span><span class="hljs-function"><span class="hljs-title">sys_write</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> *<span class="hljs-title">f</span>;</span><span class="hljs-comment">//存储文件描述符</span>  <span class="hljs-keyword">int</span> n;<span class="hljs-comment">//存储写入的字节数量</span>  uint64 p;<span class="hljs-comment">//存储内存地址</span>  <span class="hljs-keyword">if</span>(argfd(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, &amp;f) &lt; <span class="hljs-number">0</span> || argint(<span class="hljs-number">2</span>, &amp;n) &lt; <span class="hljs-number">0</span> || argaddr(<span class="hljs-number">1</span>, &amp;p) &lt; <span class="hljs-number">0</span>)<span class="hljs-comment">//分别写入</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;  <span class="hljs-keyword">return</span> filewrite(f, p, n);&#125;</code></pre></div><p>在进入内核的时候，系统调用函数会将参数保存在寄存器中，然后调用<code>argfd</code>等函数将参数取出，保存在<code>f，p，n</code>中，调用<code>filewrite</code>函数。我们注意到<code>struct file</code>结构，其结构如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> &#123;</span> FD_NONE, FD_PIPE, FD_INODE, FD_DEVICE &#125; type;  <span class="hljs-keyword">int</span> ref; <span class="hljs-comment">// reference count</span>  <span class="hljs-keyword">char</span> readable;  <span class="hljs-keyword">char</span> writable;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">pipe</span> *<span class="hljs-title">pipe</span>;</span> <span class="hljs-comment">// FD_PIPE</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">ip</span>;</span>  <span class="hljs-comment">// FD_INODE and FD_DEVICE</span>  uint off;          <span class="hljs-comment">// FD_INODE</span>  <span class="hljs-keyword">short</span> major;       <span class="hljs-comment">// FD_DEVICE</span>&#125;</code></pre></div><p><code>filewrite</code>是xv6操作系统中用于向文件写入数据的系统级函数。它在内核层面处理所有文件写入操作。函数参数 <code>filewrite(struct file **f*, uint64 *addr*, int *n*)</code></p><ul><li><p><code>f</code>: 文件结构体指针，包含文件的所有元数据</p></li><li><p><code>addr</code>: 用户空间的虚拟地址，指向要写入的数据</p></li><li><p><code>n</code>: 要写入的字节数</p></li></ul><p>根据文件类型执行不同的写入操作：</p><ul><li>管道文件 (FD_PIPE)：</li></ul><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>(f-&gt;type == FD_PIPE)&#123;ret = pipewrite(f-&gt;pipe, addr, n);&#125;</code></pre></div><p>调用<code>pipewrite</code>函数处理管道写入。</p><ul><li>设备文件 (FD_DEVICE)</li></ul><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(f-&gt;type == FD_DEVICE)&#123;<span class="hljs-keyword">if</span>(f-&gt;major &lt; <span class="hljs-number">0</span> || f-&gt;major &gt;= NDEV || !devsw[f-&gt;major].write)<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;ret = devsw[f-&gt;major].write(<span class="hljs-number">1</span>, addr, n);&#125;</code></pre></div><p>检查设备号是否有效，然后调用相应设备的写函数。</p><ul><li><p>普通文件 (FD_INODE)：这部分最复杂，需要特殊处理。</p><p>分块写入数据：</p></li></ul><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>;<span class="hljs-keyword">while</span>(i &lt; n)&#123;  <span class="hljs-keyword">int</span> n1 = n - i;  <span class="hljs-keyword">if</span>(n1 &gt; max)    n1 = max;  <span class="hljs-comment">// ...写入逻辑...</span>  i += r;&#125;</code></pre></div><p>每次写入块时都使用事务保证原子性：</p><div class="code-wrapper"><pre><code class="hljs c">begin_op();  <span class="hljs-comment">// 开始事务</span>ilock(f-&gt;ip);  <span class="hljs-comment">// 锁定inode</span><span class="hljs-keyword">if</span> ((r = writei(f-&gt;ip, <span class="hljs-number">1</span>, addr + i, f-&gt;off, n1)) &gt; <span class="hljs-number">0</span>)  f-&gt;off += r;  <span class="hljs-comment">// 更新文件偏移量</span>iunlock(f-&gt;ip);  <span class="hljs-comment">// 解锁inode</span>end_op();  <span class="hljs-comment">// 结束事务</span></code></pre></div><p>说到这里了，我们提一下<code>buffer IO</code>，我感觉<code>xv6</code>是没有实现<code>buffer IO</code>的，源码里面没有找到相关的说明和代码，但是在<code>linux</code>里面是实现了的。<code>buffer IO</code>是为了提高读写效率和保护磁盘，比如我们通过<code>read</code>函数将<code>fd</code>对应的文件拷贝count个字节到<code>buf</code>对应的内存，这个时候如果是<code>buffer io</code>机制，那么我们就会先将count个字节拷贝到page cache中，然后再拷贝到<code>buf</code>对应的用户空间中。<code>write</code>操作类似。</p><p>上次有位面试官问了我一个问题：什么时候将脏页刷回磁盘？</p><p>我查了一下，有的说是进程退出的时候刷回去，有的说是定时刷回去。在<code>CMU15445</code>细说。</p><hr><p>我们将在后面的实验中继续学习<code>syscall</code>。</p><h4 id="pingpong"><a href="#pingpong" class="headerlink" title="pingpong"></a><em>pingpong</em></h4><p>编写一个程序，使用 UNIX 系统调用在两个进程之间通过一对管道“乒乓”一个字节，每个管道一个。 父母应该向孩子发送一个字节； 子进程应该打印“<pid>: received ping”，其中 <pid> 是它的进程 ID，将管道上的字节写入父进程，然后退出； 父母应该从孩子那里读取字节，打印“<pid>: received pong”，然后退出。</p><p>一些提示：</p><ul><li>使用管道创建管道。</li><li>使用 fork 创建一个孩子。</li><li>使用 read 从管道读取，并使用 write 写入管道。</li><li>使用 getpid 查找调用进程的进程 ID。</li><li>将程序添加到 Makefile 中的 UPROGS。</li><li>xv6 上的用户程序有一组有限的库函数可供它们使用。 可以在 user/user.h 中看到列表； 源（系统调用除外）位于 user/ulib.c、user/printf.c 和 user/umalloc.c。</li></ul><p>代码如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/types.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/stat.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;user/user.h&quot;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;  <span class="hljs-keyword">int</span> p_filedes[<span class="hljs-number">2</span>],s_filedes[<span class="hljs-number">2</span>];  pipe(p_filedes);  pipe(s_filedes);  <span class="hljs-keyword">char</span> buf[<span class="hljs-number">4</span>];  <span class="hljs-keyword">if</span>(fork()==<span class="hljs-number">0</span>)&#123;    read(p_filedes[<span class="hljs-number">0</span>],buf,<span class="hljs-number">4</span>);    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d: received %s\n&quot;</span>,getpid(),buf);    write(s_filedes[<span class="hljs-number">1</span>],<span class="hljs-string">&quot;pong&quot;</span>,<span class="hljs-number">4</span>);  &#125;<span class="hljs-keyword">else</span>&#123;    write(p_filedes[<span class="hljs-number">1</span>],<span class="hljs-string">&quot;ping&quot;</span>,<span class="hljs-number">4</span>);    read(s_filedes[<span class="hljs-number">0</span>],buf,<span class="hljs-number">4</span>);    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d: received %s\n&quot;</span>,getpid(),buf);  &#125;  <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div><p>在上述代码中，我们使用了两个管道，p_filedes和s_filedes，来传递父进程和子进程之间的信息。</p><p>借此机会，我们来分析一下<code>read</code>函数，read函数原型为<code>read(int,void *,int)</code>，其意义为将文件描述符<code>fd</code>所指向的文件读取<code>count</code>个数据到<code>buf</code>中。如<code>read(0,buf,10)</code>就是将标准输入读取10个字节到buf中。其底层实现为<code>sys_read</code>，</p><p>这让我想到了HUST大三学生在2021年做的lab1，通过程序演示多进程并发执行和进程软中断、管道通信。实验具体描述如下：</p><ul><li><p>父进程先建立一个管道,然后创建两个进程:子进程1和子进程2;</p></li><li><p>父进程每隔1秒向管道发送消息(消息数量有上限) :</p><p>I send you x times. (x的初值为1,每次发送后对x做加1操作)</p></li><li><p>子进程1、2从管道接收消息,并显示在屏幕上。</p></li><li>父进程能捕获软中断信号SIGINT(按键盘的Ctrl+C键),捕获到该信号后,父进程分别向两个子进程发出软中断信号SIGUSR1。</li><li>子进程能捕获父进程发出的SIGUSR1信号,捕获到该信号后,分别输出下列信息后终止:<br>Child Process l is Killed by Parent!<br>Child Process 2 is Killed by Parent!</li><li>父进程等待两个子进程终止后,释放管道并输出如下信息后终止:<br>Parent Process is Killed!</li></ul><p>这个实验可以通过如下框架进行设计：</p><div class="code-wrapper"><pre><code class="hljs javascript"><span class="hljs-function"><span class="hljs-title">main</span>(<span class="hljs-params"> </span>)</span>&#123;    创建无名管道;    设置信号SIGINT处理;    创建子进程<span class="hljs-number">1</span>、<span class="hljs-number">2</span>;    定时发送数据;    等待子进程<span class="hljs-number">1</span>、<span class="hljs-number">2</span>退出;    关闭管道;    打印信息、退出;&#125;父进程SIGINT信号处理&#123;    发SIGUSR1给子进程<span class="hljs-number">1</span>;    发SIGUSR2给子进程<span class="hljs-number">2</span>;    等待子进程<span class="hljs-number">1</span>、<span class="hljs-number">2</span>退出;    关闭管道;    打印信息、退出;&#125;子进程<span class="hljs-number">1</span>/<span class="hljs-number">2</span>&#123;    设置信号SIGINT处理;    设置SIGUSR1或<span class="hljs-number">2</span>处理;    <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>) &#123;        从管道接收数据;        显示数据;        计数器++;    &#125;    关闭管道;    打印信息、退出;&#125;SIGUSR1/<span class="hljs-number">2</span>信号处理&#123;    关闭管道;    打印信息;    退出;&#125;</code></pre></div><p>具体代码如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;unistd.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span><span class="hljs-meta-string">&lt;string.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span><span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span><span class="hljs-meta-string">&lt;time.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;sys/wait.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;sys/types.h&gt;</span></span><span class="hljs-keyword">int</span> pid_1,pid_2;<span class="hljs-keyword">int</span> filedes_1[<span class="hljs-number">2</span>],filedes_2[<span class="hljs-number">2</span>];<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-keyword">int</span> sig)</span></span><span class="hljs-function"></span>&#123;kill(pid_1,SIGUSR1);kill(pid_2,SIGUSR1);    waitpid(pid_1,<span class="hljs-literal">NULL</span>,<span class="hljs-number">0</span>);    waitpid(pid_2,<span class="hljs-literal">NULL</span>,<span class="hljs-number">0</span>);    close(filedes_1[<span class="hljs-number">0</span>]);    close(filedes_1[<span class="hljs-number">1</span>]);    close(filedes_2[<span class="hljs-number">0</span>]);    close(filedes_2[<span class="hljs-number">1</span>]);    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;Parent Process is Killed!\n&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">fun1</span><span class="hljs-params">(<span class="hljs-keyword">int</span> sig)</span></span><span class="hljs-function"></span>&#123;<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;\nChild1 process1  is  killed by parent!\n&quot;</span>);    close(filedes_1[<span class="hljs-number">0</span>]);    close(filedes_1[<span class="hljs-number">1</span>]);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">fun2</span><span class="hljs-params">(<span class="hljs-keyword">int</span> sig)</span></span><span class="hljs-function"></span>&#123;<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;\nChild2 process2  is  killed by parent!\n&quot;</span>);    close(filedes_2[<span class="hljs-number">0</span>]);    close(filedes_2[<span class="hljs-number">1</span>]);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;    pipe(filedes_1);    pipe(filedes_2);    <span class="hljs-keyword">char</span> s[<span class="hljs-number">80</span>];    <span class="hljs-keyword">int</span> x=<span class="hljs-number">0</span>;    pid_1=fork();    <span class="hljs-keyword">if</span>(pid_1&gt;<span class="hljs-number">0</span>)&#123;        pid_2=fork();        <span class="hljs-keyword">if</span>(pid_2&gt;<span class="hljs-number">0</span>)&#123;            signal(SIGINT,fun);            <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>)&#123;                x++;                <span class="hljs-keyword">char</span> buf[<span class="hljs-number">80</span>];                <span class="hljs-built_in">sprintf</span>(buf, <span class="hljs-string">&quot;I send you %d times&quot;</span>, x);                write(filedes_1[<span class="hljs-number">1</span>],buf,<span class="hljs-keyword">sizeof</span>(buf));                write(filedes_2[<span class="hljs-number">1</span>],buf,<span class="hljs-keyword">sizeof</span>(buf));                sleep(<span class="hljs-number">1</span>);            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        &#125;        <span class="hljs-keyword">else</span>&#123;            signal(SIGINT,SIG_IGN);            signal(SIGUSR1,fun2);            <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>)&#123;                read(filedes_2[<span class="hljs-number">0</span>],s,<span class="hljs-keyword">sizeof</span>(s));                <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%s c2\n&quot;</span>,s);                sleep(<span class="hljs-number">1</span>);            &#125;        &#125;    &#125;    <span class="hljs-keyword">else</span>&#123;        signal(SIGINT,SIG_IGN);        signal(SIGUSR1,fun1);        <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>)&#123;            read(filedes_1[<span class="hljs-number">0</span>],s,<span class="hljs-keyword">sizeof</span>(s));            <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%s c1\n&quot;</span>,s);            sleep(<span class="hljs-number">1</span>);        &#125;    &#125;&#125;</code></pre></div><h4 id="xargs"><a href="#xargs" class="headerlink" title="xargs"></a><em>xargs</em></h4><p>xarg是给命令传递参数的一个过滤器，也是组合多个命令的一个工具。xarg可以将管道或标准输入数据转换成命令行参数，也能够从文件的输出中读取数据。</p><p>在实验中我们要完成的xargs程序与linux命令类似。</p><p>代码如下：</p><div class="code-wrapper"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/types.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/param.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;user/user.h&quot;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> *argv[])</span> </span>&#123;    <span class="hljs-keyword">char</span> line[<span class="hljs-number">256</span>], *p[MAXARG], ch;    <span class="hljs-keyword">int</span> lines = <span class="hljs-number">0</span>, linen, ps = <span class="hljs-number">0</span>, pn, i, j;    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; argc - <span class="hljs-number">1</span>; i++) &#123;        p[ps++] = line + lines;        <span class="hljs-keyword">for</span> (j = <span class="hljs-number">0</span>; j &lt; <span class="hljs-built_in">strlen</span>(argv[i + <span class="hljs-number">1</span>]); j++)            line[lines++] = argv[i + <span class="hljs-number">1</span>][j];        line[lines++] = <span class="hljs-string">&#x27;\0&#x27;</span>;    &#125;    linen = lines; pn = ps; p[pn++] = line + linen;    <span class="hljs-keyword">while</span> (read(<span class="hljs-number">0</span>, &amp;ch, <span class="hljs-number">1</span>) &gt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">if</span> (ch == <span class="hljs-string">&#x27;\n&#x27;</span>) &#123;            line[linen++] = <span class="hljs-string">&#x27;\0&#x27;</span>; p[pn++] = <span class="hljs-number">0</span>;            <span class="hljs-keyword">if</span> (fork() == <span class="hljs-number">0</span>) exec(argv[<span class="hljs-number">1</span>], p);            <span class="hljs-keyword">else</span> &#123;                wait(<span class="hljs-number">0</span>); linen = lines; pn = ps; p[pn++] = line + linen;            &#125;        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ch == <span class="hljs-string">&#x27; &#x27;</span>) &#123;            line[linen++] = <span class="hljs-string">&#x27;\0&#x27;</span>; p[pn++] = line + linen;        &#125; <span class="hljs-keyword">else</span> line[linen++] = ch;    &#125;    <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>记一次manjaro OS恢复</title>
    <link href="/2022/05/29/%E8%AE%B0%E4%B8%80%E6%AC%A1manjaro-OS%E6%81%A2%E5%A4%8D/"/>
    <url>/2022/05/29/%E8%AE%B0%E4%B8%80%E6%AC%A1manjaro-OS%E6%81%A2%E5%A4%8D/</url>
    
    <content type="html"><![CDATA[<p>我的PC前段时间短路了，更换主板之后重新启动电脑没有manjaro OS和Windows的启动项选择界面了，而是直接进入Winows，查看BOIS之后发现只有一个Winows启动项，遂开始恢复。</p><p>首先我发现只有一个Winows启动项，于是直接在Winows下管理员cmd中输入如下命令增加启动项：</p><div class="code-wrapper"><pre><code class="hljs bash"><span class="hljs-variable">$bcdedit</span> /<span class="hljs-built_in">set</span> <span class="hljs-string">&quot;&#123;bootmgr&#125;&quot;</span> path \EFI\Manjaro\grubx64.efi</code></pre></div><p>重启之后我发现已经有了选择界面了，这就说明我的grub并没有受到影响，于是直接启动Manjaro，得到如下结果：</p><div class="code-wrapper"><pre><code class="hljs bash">mount:/new_root:cant find UUID-xxxxxxxxxxxxxxx.You are now being dropped into an emergency shell.sh:cant access tty:job control turn off[rootfs]<span class="hljs-comment">#</span></code></pre></div><p>我的第一反应是UUID有问题，于是下载了最新版的manjaro kde iso制作live cd，用live cd启动系统，开启KDE分区管理器，查看磁盘情况，会有两种情况：</p><p>1.live cd的os不能识别硬盘，也就是说当我点击安装系统的时候反馈<code>there are no partitions to install on manjaro</code>，这是硬盘的驱动设置有问题，于是我重启电脑，打开bios，storage-&gt;rst mode，将rst mode设置为AHCI（不要理会它的警告，尽管这会导致你无法进入Windows系统），然后直接进入grub的manjaro，这个时候发现可以直接使用了。</p><p>2.live cd的os能识别硬盘，这个时候的话就说明你的UUID确实有问题了，应该想办法恢复UUID。打开终端，开始恢复。</p><div class="code-wrapper"><pre><code class="hljs bash"><span class="hljs-comment">#blkid</span>//输入这个命令之后会出现磁盘所有分区的信息，找到系统的root区的UUID记住<span class="hljs-comment">#sudo mount /dev/nvme0n0p6 /mnt</span>//这里我的root区是nvme0n0p6，将/mnt挂载到这里<span class="hljs-comment">#sudo nona /mnt/etc/fstab</span>//开始输入正确的UUID<span class="hljs-comment">#genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</span></code></pre></div><p>完成之后重启进入原manjaro系统，发现可以运行了。</p><hr><p>补充，如何优雅的在Windows中关闭Intel rst，使得我们在AHCI下可以运行Windows：</p><div class="code-wrapper"><pre><code class="hljs awk"><span class="hljs-number">1</span>.以管理员身份打开cmd，不是那个普通的cmd哈。<span class="hljs-number">2</span>.输入bcdedit /set safeboot minimal回车<span class="hljs-number">3</span>.Restart the computer and enter BIOS Setup<span class="hljs-number">4</span>.Change the SATA Operation mode to AHCI from either IDE or RAID<span class="hljs-number">5</span>.Save changes and <span class="hljs-keyword">exit</span> Setup and Windows will automatically boot to Safe Mode.<span class="hljs-number">6</span>.以管理员身份打开cmd，不是那个普通的cmd哈。<span class="hljs-number">7</span>.bcdedit /deletevalue safeboot回车<span class="hljs-number">8</span>.Reboot once more and Windows will automatically start with AHCI drivers enabled.<span class="hljs-number">9</span>.恢复正常了，正常使用Windows。</code></pre></div><p>最后我们来测试一下CPU性能，看本次短路有没有冲击到CPU：</p><p>参考文章：</p><ol><li><a href="https://blog.csdn.net/qq_43391414/article/details/120606567">https://blog.csdn.net/qq_43391414/article/details/120606567</a></li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>CMU 15-445 Project 3: Query execution</title>
    <link href="/2022/05/16/CMU-15-445-Project-3-Query-Execution/"/>
    <url>/2022/05/16/CMU-15-445-Project-3-Query-Execution/</url>
    
    <content type="html"><![CDATA[<h4 id="TASK-1-SYSTEM-CATALOG"><a href="#TASK-1-SYSTEM-CATALOG" class="headerlink" title="TASK #1 - SYSTEM CATALOG"></a>TASK #1 - SYSTEM CATALOG</h4><p>在这个任务中，我们会修改<code>rc/include/catalog/catalog.h</code>以允许 DBMS 将新表添加到数据库并使用名称或内部对象标识符 ( <code>table_oid_t</code>) 检索它们。</p><p>我们在最新的BusTub中可以看到<code>catalog</code>的实现，但是这个版本只能实现通过6个测试，</p><p>先来看看<code>TableInfo</code>的结构体：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">TableInfo</span> &#123;</span>  <span class="hljs-comment">/**</span><span class="hljs-comment">   * Construct a new TableInfo instance.</span><span class="hljs-comment">   * @param schema The table schema</span><span class="hljs-comment">   * @param name The table name</span><span class="hljs-comment">   * @param table An owning pointer to the table heap</span><span class="hljs-comment">   * @param oid The unique OID for the table</span><span class="hljs-comment">   */</span>  <span class="hljs-built_in">TableInfo</span>(Schema schema, std::string name, std::unique_ptr&lt;TableHeap&gt; &amp;&amp;table, <span class="hljs-keyword">table_oid_t</span> oid)      : schema_&#123;std::<span class="hljs-built_in">move</span>(schema)&#125;, name_&#123;std::<span class="hljs-built_in">move</span>(name)&#125;, table_&#123;std::<span class="hljs-built_in">move</span>(table)&#125;, oid_&#123;oid&#125; &#123;&#125;  <span class="hljs-comment">/** The table schema */</span>  Schema schema_;  <span class="hljs-comment">/** The table name */</span>  <span class="hljs-keyword">const</span> std::string name_;  <span class="hljs-comment">/** An owning pointer to the table heap */</span>  std::unique_ptr&lt;TableHeap&gt; table_;  <span class="hljs-comment">/** The table OID */</span>  <span class="hljs-keyword">const</span> <span class="hljs-keyword">table_oid_t</span> oid_;&#125;;</code></pre></div><p>我们可以看看<code>catalog</code>的成员变量：</p><div class="code-wrapper"><pre><code class="hljs c++">  std::unordered_map&lt;<span class="hljs-keyword">table_oid_t</span>, std::unique_ptr&lt;TableInfo&gt;&gt; tables_;<span class="hljs-comment">//无序map,也就是表信息集合</span>  std::unordered_map&lt;std::string, <span class="hljs-keyword">table_oid_t</span>&gt; table_names_;<span class="hljs-comment">//表名集合</span><span class="hljs-comment">//这里我们可以看出寻表是表名-&gt;oid-&gt;表信息</span>std::atomic&lt;<span class="hljs-keyword">table_oid_t</span>&gt; next_table_oid_&#123;<span class="hljs-number">0</span>&#125;;<span class="hljs-comment">//用atomic类来做多线程编程</span><span class="hljs-comment">//index与上述结构相似</span>std::unordered_map&lt;<span class="hljs-keyword">index_oid_t</span>, std::unique_ptr&lt;IndexInfo&gt;&gt; indexes_;std::unordered_map&lt;std::string, std::unordered_map&lt;std::string, <span class="hljs-keyword">index_oid_t</span>&gt;&gt; index_names_;<span class="hljs-comment">//在index_names_中，是以&lt;string,&lt;string,index_oid_t&gt;&gt;作为结构，第一个string是index的name，第二个string是</span>std::atomic&lt;<span class="hljs-keyword">index_oid_t</span>&gt; next_index_oid_&#123;<span class="hljs-number">0</span>&#125;;</code></pre></div><div class="code-wrapper"><pre><code class="hljs php"><span class="hljs-comment">/**</span><span class="hljs-comment"> * Create a new table and return its metadata.</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> txn The transaction in which the table is being created</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> table_name The name of the new table</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> schema The schema of the new table</span><span class="hljs-comment"> * <span class="hljs-doctag">@return</span> A (non-owning) pointer to the metadata for the table</span><span class="hljs-comment"> */</span></code></pre></div><p>从上面的注释可以看出，<code>CreateTable</code> 的核心并不复杂：先为表分配新的 <code>table_oid_t</code>，再用表名、schema 和 <code>TableHeap</code> 构造 <code>TableInfo</code>，最后同时维护 <code>tables_</code> 和 <code>table_names_</code> 两个索引结构。需要注意的是，<code>table_names_</code> 负责从表名查到 OID，<code>tables_</code> 负责从 OID 查到真正的元数据，两者必须保持一致。</p><h4 id="TASK-2-EXECUTORS"><a href="#TASK-2-EXECUTORS" class="headerlink" title="TASK #2 - EXECUTORS"></a>TASK #2 - EXECUTORS</h4><p>Query Execution 这一部分的重点是把逻辑计划节点落实成可以迭代执行的 executor。BusTub 的 executor 通常都遵循 <code>Init()</code> + <code>Next(Tuple *tuple, RID *rid)</code> 的模式：<code>Init()</code> 负责初始化子 executor 或内部状态，<code>Next()</code> 每次产出一条结果，直到返回 <code>false</code>。</p><p>比较容易出错的点有三个：</p><ol><li><code>SeqScanExecutor</code> 不只是遍历表，还要应用 predicate，并且返回的 tuple schema 要和计划节点保持一致。</li><li><code>InsertExecutor</code>、<code>UpdateExecutor</code>、<code>DeleteExecutor</code> 不只是改表数据，还要维护相关索引，否则后续查询会读到不一致的结果。</li><li>聚合和 join executor 需要先想清楚“拉模型”的执行顺序。比如 hash join 通常先把一侧构建成哈希表，再由另一侧驱动匹配；aggregation 通常先消费完子 executor，再按 group key 输出结果。</li></ol><h4 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h4><p>Project 3 的难点不在单个函数，而在执行器之间的契约：每个 executor 都通过 <code>Next()</code> 向上层交付 tuple，上层不应该知道下层是顺序扫描、索引扫描、连接还是聚合。只要保持这个接口稳定，复杂查询就可以由多个简单 executor 组合出来。这也是数据库执行层最重要的抽象。</p>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CMU 15-445</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go select 与 I/O 多路复用</title>
    <link href="/2022/04/03/Go-select-and-IO-multiplexing/"/>
    <url>/2022/04/03/Go-select-and-IO-multiplexing/</url>
    
    <content type="html"><![CDATA[<h3 id="select"><a href="#select" class="headerlink" title="select"></a>select</h3><p>Go 的 <code>select</code> 关键字用到了“多路等待”的思想，但它本身不是操作系统层面的 I/O 多路复用，因为 <code>select</code> 面向的是 channel，而不是文件描述符。先看下面的例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<span class="hljs-keyword">import</span> (<span class="hljs-string">&quot;fmt&quot;</span><span class="hljs-string">&quot;sync&quot;</span>)<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;c1 := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">chan</span> <span class="hljs-keyword">int</span>, <span class="hljs-number">1</span>)<span class="hljs-keyword">var</span> i1, i2 <span class="hljs-keyword">int</span><span class="hljs-keyword">var</span> count sync.WaitGroupcount.Add(<span class="hljs-number">1</span>)<span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<span class="hljs-keyword">defer</span> count.Done()<span class="hljs-keyword">select</span> &#123;<span class="hljs-keyword">case</span> i1 = &lt;-c1:fmt.Println(<span class="hljs-string">&quot;i1 received from c1&quot;</span>, i1)<span class="hljs-keyword">case</span> i2 = &lt;-c1:fmt.Println(<span class="hljs-string">&quot;i2 received from c1&quot;</span>, i2)&#125;&#125;()c1 &lt;- <span class="hljs-number">1</span>count.Wait()<span class="hljs-built_in">close</span>(c1)&#125;</code></pre></div><p>当多个 <code>case</code> 同时就绪时，Go 会伪随机选择其中一个执行，所以这个例子有时会运行 <code>i1 = &lt;-c1</code>，有时会运行 <code>i2 = &lt;-c1</code>。这个随机性可以避免固定顺序带来的饥饿问题，但它并不保证公平调度，只能说明程序不能依赖某个 <code>case</code> 总是先被执行。</p><h4 id="和-I-O-多路复用的关系"><a href="#和-I-O-多路复用的关系" class="headerlink" title="和 I/O 多路复用的关系"></a>和 I/O 多路复用的关系</h4><p>操作系统里的 I/O 多路复用通常指 <code>select</code>、<code>poll</code>、<code>epoll</code>、<code>kqueue</code> 这一类机制：一个线程同时监听多个 fd，当某个 fd 可读或可写时再处理它。Go 网络库内部确实会使用运行时的 netpoller，在 Linux 上通常对应 <code>epoll</code>，在 macOS 上通常对应 <code>kqueue</code>。不过这些细节被 runtime 封装起来了，业务代码一般只需要阻塞读写连接，runtime 会在 I/O 未就绪时挂起 goroutine，并在 fd 就绪后唤醒它。</p><p>所以可以这样区分：<code>select</code> 是 Go 语言层面对多个 channel 操作的等待；I/O 多路复用是操作系统层面对多个 fd 的等待。两者思想相似，抽象层次不同。写 Go 服务时，通常把网络 I/O 交给标准库和 runtime，把业务并发协调交给 goroutine、channel 和 <code>select</code>。</p><h3 id="select-的执行语义"><a href="#select-的执行语义" class="headerlink" title="select 的执行语义"></a>select 的执行语义</h3><p><code>select</code> 并不是简单地从上到下检查 <code>case</code>。它大致遵循这些语义：</p><ol><li>先对所有 channel 表达式和发送值表达式求值。</li><li>找出当前已经可以执行的 <code>case</code>。</li><li>如果有多个 ready case，伪随机选择一个。</li><li>如果没有 ready case 且存在 <code>default</code>，执行 <code>default</code>。</li><li>如果没有 ready case 且没有 <code>default</code>，当前 goroutine 阻塞，等待某个 channel 就绪。</li></ol><p>这解释了一个常见误区：<code>default</code> 会让 <code>select</code> 变成非阻塞轮询。如果在循环里写一个带 <code>default</code> 的 <code>select</code>，又没有任何 sleep、timer 或阻塞点，就很容易把 CPU 打满。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">for</span> &#123;<span class="hljs-keyword">select</span> &#123;<span class="hljs-keyword">case</span> msg := &lt;-ch:handle(msg)<span class="hljs-keyword">default</span>:<span class="hljs-comment">// 空 default 会导致忙等</span>&#125;&#125;</code></pre></div><p>更好的方式通常是让 goroutine 阻塞在 channel、timer 或 context 上：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">for</span> &#123;<span class="hljs-keyword">select</span> &#123;<span class="hljs-keyword">case</span> msg := &lt;-ch:handle(msg)<span class="hljs-keyword">case</span> &lt;-ctx.Done():<span class="hljs-keyword">return</span>&#125;&#125;</code></pre></div><h3 id="runtime-netpoller"><a href="#runtime-netpoller" class="headerlink" title="runtime netpoller"></a>runtime netpoller</h3><p>Go 的网络 I/O 表面上看起来是一个 goroutine 阻塞在 <code>Read</code> 或 <code>Write</code> 上，但 runtime 不会为每个连接固定占用一个操作系统线程。当 fd 没有就绪时，runtime 会把 goroutine 挂起，把 fd 注册到 netpoller；当内核通知 fd 可读或可写时，runtime 再把对应 goroutine 放回可运行队列。</p><p>在不同平台上，netpoller 背后的机制不同：Linux 常用 <code>epoll</code>，macOS/BSD 常用 <code>kqueue</code>，Windows 使用 IOCP。业务代码通常不需要直接操作这些系统调用，这是 Go 网络库能够支撑大量连接的重要原因。</p><p>可以把这里的分工理解为：</p><ol><li>内核负责告诉 runtime 哪些 fd 就绪。</li><li>runtime 负责把等待这些 fd 的 goroutine 唤醒。</li><li>调度器负责把可运行 goroutine 安排到 P/M 上执行。</li></ol><h3 id="select、channel-和网络-I-O-如何配合"><a href="#select、channel-和网络-I-O-如何配合" class="headerlink" title="select、channel 和网络 I/O 如何配合"></a>select、channel 和网络 I/O 如何配合</h3><p>网络连接的 <code>Read</code>/<code>Write</code> 并不是 channel 操作，所以不能直接把 fd 写进 <code>select</code>。常见做法是由一个 goroutine 负责读连接，再把解析后的消息送入 channel；业务层通过 <code>select</code> 同时等待消息、超时和取消信号。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">select</span> &#123;<span class="hljs-keyword">case</span> msg := &lt;-messages:handle(msg)<span class="hljs-keyword">case</span> &lt;-time.After(time.Second):<span class="hljs-keyword">return</span> ErrTimeout<span class="hljs-keyword">case</span> &lt;-ctx.Done():<span class="hljs-keyword">return</span> ctx.Err()&#125;</code></pre></div><p>需要注意 <code>time.After</code> 在循环中会反复创建 timer。高频循环中更推荐复用 <code>time.Timer</code>，避免制造额外分配和 GC 压力。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>Go 的 <code>select</code> 和操作系统 I/O 多路复用解决的是不同层次的问题。<code>select</code> 用来组合多个 channel 事件，让业务并发控制更清晰；netpoller 用来把大量网络 fd 的等待交给内核，并把就绪事件重新接回 Go 调度器。理解这条链路之后，就能解释为什么 Go 可以用同步写法处理高并发网络 I/O，也能知道什么时候应该警惕忙等、timer 分配和 goroutine 泄漏。</p>]]></content>
    
    
    <categories>
      
      <category>Go语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Go</tag>
      
      <tag>I/O multiplexing</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go 标准库：container、reflect、unsafe 与 atomic</title>
    <link href="/2022/03/20/Go-Container-Reflect-Unsafe-and-Atomic/"/>
    <url>/2022/03/20/Go-Container-Reflect-Unsafe-and-Atomic/</url>
    
    <content type="html"><![CDATA[<h3 id="Go-map"><a href="#Go-map" class="headerlink" title="Go map"></a>Go map</h3><h4 id="底层实现"><a href="#底层实现" class="headerlink" title="底层实现"></a>底层实现</h4><p>Go语言的map底层实现基于哈希表。Hash会把任意长度输入映射到有限值域，因此一定会存在冲突。Go map 不是“传统链地址法”的单链表实现，而是“bucket + overflow bucket”结构：每个 bucket 固定容纳若干键值对，冲突严重时通过 overflow bucket 串接扩展。值得注意的是：==Java1.8中在冲突链较长时会做树化优化，而 Go map 走的是 bucket 扩容与渐进迁移路线。==</p><p>使用哈希表的目的就是要快速查找到目标key，然而，随着向map中添加的key越来越多，key发生碰撞的概率也越来越大，bucket中的8个cell会被逐渐塞满，插入、查找和删除key的效率也会越来越低，最理想的情况是bucket只装入一个key，这样，就能达到$O(1)$的效率，但这样空间消耗太大，代价太高。</p><p>底层源码如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-comment">// A header for a Go map.</span><span class="hljs-keyword">type</span> hmap <span class="hljs-keyword">struct</span> &#123;count     <span class="hljs-keyword">int</span> <span class="hljs-comment">// # live cells == size of map.  Must be first (used by len() builtin)</span>flags     <span class="hljs-keyword">uint8</span>B         <span class="hljs-keyword">uint8</span>  <span class="hljs-comment">// log_2 of # of buckets (can hold up to loadFactor * 2^B items)</span>noverflow <span class="hljs-keyword">uint16</span> hash0     <span class="hljs-keyword">uint32</span> <span class="hljs-comment">// hash seed</span>buckets    unsafe.Pointer <span class="hljs-comment">// array of 2^B Buckets. may be nil if count==0.</span>oldbuckets unsafe.Pointer <span class="hljs-comment">// previous bucket array of half the size, non-nil only when growing</span>nevacuate  <span class="hljs-keyword">uintptr</span>        <span class="hljs-comment">// progress counter for evacuation (buckets less than this have been evacuated)</span>extra *mapextra <span class="hljs-comment">// optional fields</span>&#125;</code></pre></div><p>其中，<code>buckets unsafe.Pointer</code>指向具体的buckets数组，这个数组是<code>bmap</code>结构，如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-comment">// A bucket for a Go map.</span><span class="hljs-keyword">type</span> bmap <span class="hljs-keyword">struct</span> &#123;tophash [bucketCnt]<span class="hljs-keyword">uint8</span>&#125;</code></pre></div><p>在实际编译期间，Go语言的<code>bmap</code>会经过反射生成真正的<code>bmap</code>类型：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> bmap <span class="hljs-keyword">struct</span> &#123;  topbits  [<span class="hljs-number">8</span>]<span class="hljs-keyword">uint8</span>  keys     [<span class="hljs-number">8</span>]keytype  values   [<span class="hljs-number">8</span>]valuetype  pad      <span class="hljs-keyword">uintptr</span>  overflow <span class="hljs-keyword">uintptr</span>&#125;</code></pre></div><p>map的访问即通过给定的key在map中寻找其对应value，过程如下：</p><ol><li>原始的key通过hash函数映射成64位二进制；</li><li>末尾x位代表<code>bmap</code>的位置，从<code>[]bmap</code>中找到对应的<code>bmap</code>，值得注意的是，bmap的数量一般是2的幂，如果末尾x位代表<code>bmap</code>的位置就代表有$2^x$个<code>bmap</code>；</li><li>首8位对应key的tophash，在bmap中进行检索，首先会比较<code>bmap</code>顶层的<code>tophash</code>与原始key的<code>tophash</code>是否相同，若不相同则直接跳过比较下一个；若相同则进一步比较key是否相同。</li></ol><h3 id="container"><a href="#container" class="headerlink" title="container"></a><code>container</code></h3><p>众所周知，Go对于一些基本的数据结构封装得不是很好，在<code>container</code>中也只有三种常见的数据结构——链表、环形链表和堆。</p><h4 id="container-list"><a href="#container-list" class="headerlink" title="container/list"></a><code>container/list</code></h4><p>我们先来看看<code>list</code>的写入与打印：</p><div class="code-wrapper"><pre><code class="hljs go">l := list.New()l.PushBack(<span class="hljs-number">1</span>) <span class="hljs-comment">//尾插</span>l.PushBack(<span class="hljs-number">2</span>)fmt.Println(l)</code></pre></div><p>我们看看打印结果：</p><div class="code-wrapper"><pre><code class="hljs go">&amp;&#123;&#123;<span class="hljs-number">0xc00007c180</span> <span class="hljs-number">0xc00007c1b0</span> &lt;<span class="hljs-literal">nil</span>&gt; &lt;<span class="hljs-literal">nil</span>&gt;&#125; <span class="hljs-number">2</span>&#125;</code></pre></div><p>为什么打印结果是这样的，如果我们弄懂<code>list</code>的底层结构，就能知道答案了。<code>list</code>的底层结构如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Element <span class="hljs-keyword">struct</span> &#123;<span class="hljs-comment">// Next and previous pointers in the doubly-linked list of elements.</span><span class="hljs-comment">// To simplify the implementation, internally a list l is implemented</span><span class="hljs-comment">// as a ring, such that &amp;l.root is both the next element of the last</span><span class="hljs-comment">// list element (l.Back()) and the previous element of the first list</span><span class="hljs-comment">// element (l.Front()).</span>next, prev *Element<span class="hljs-comment">// The list to which this element belongs.</span>list *List<span class="hljs-comment">// The value stored with this element.</span>Value <span class="hljs-keyword">interface</span>&#123;&#125;&#125;<span class="hljs-comment">// List represents a doubly linked list.</span><span class="hljs-comment">// The zero value for List is an empty list ready to use.</span><span class="hljs-keyword">type</span> List <span class="hljs-keyword">struct</span> &#123;root Element <span class="hljs-comment">// sentinel list element, only &amp;root, root.prev, and root.next are used</span><span class="hljs-built_in">len</span>  <span class="hljs-keyword">int</span>     <span class="hljs-comment">// current list length excluding (this) sentinel element</span>&#125;</code></pre></div><p>结合上面的打印，我们可以发现，<code>&#123;0xc00007c180 0xc00007c1b0 &lt;nil&gt; &lt;nil&gt;&#125;</code> 是<code>root Element</code>，<code>2</code>是<code>len int</code>。我们接着打印：</p><div class="code-wrapper"><pre><code class="hljs go">l := list.New()<span class="hljs-comment">//初始化</span>l.PushBack(<span class="hljs-number">1</span>) <span class="hljs-comment">//尾插</span>l.PushBack(<span class="hljs-number">2</span>)fmt.Println(l.Front())fmt.Println(l.Back())fmt.Println(l)</code></pre></div><p>其打印结果如下：</p><div class="code-wrapper"><pre><code class="hljs go">&amp;&#123;<span class="hljs-number">0xc00007c1b0</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">1</span>&#125;&amp;&#123;<span class="hljs-number">0xc00007c150</span> <span class="hljs-number">0xc00007c180</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">2</span>&#125;&amp;&#123;&#123;<span class="hljs-number">0xc00007c180</span> <span class="hljs-number">0xc00007c1b0</span> &lt;<span class="hljs-literal">nil</span>&gt; &lt;<span class="hljs-literal">nil</span>&gt;&#125; <span class="hljs-number">2</span>&#125;</code></pre></div><p>从这个结果我们不难看出，<code>list</code>其实是一个环形结构。其中，<code>root</code>的next指向链表的<code>front</code>，<code>root</code>的pre指向链表的<code>back</code>，而<code>root</code>自身是不存储数据的。如果我们再加上一个数据，就会变成这样了：</p><div class="code-wrapper"><pre><code class="hljs go">l := list.New()<span class="hljs-comment">//初始化</span>l.PushBack(<span class="hljs-number">1</span>) <span class="hljs-comment">//尾插</span>l.PushBack(<span class="hljs-number">2</span>)l.PushBack(<span class="hljs-number">3</span>)fmt.Println(l.Front())fmt.Println(l.Back())fmt.Println(l)</code></pre></div><p>其打印结果如下：</p><div class="code-wrapper"><pre><code class="hljs go">&amp;&#123;<span class="hljs-number">0xc00007c1b0</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">1</span>&#125;&amp;&#123;<span class="hljs-number">0xc00007c150</span> <span class="hljs-number">0xc00007c1b0</span> <span class="hljs-number">0xc00007c150</span> <span class="hljs-number">3</span>&#125;&amp;&#123;&#123;<span class="hljs-number">0xc00007c180</span> <span class="hljs-number">0xc00007c1e0</span> &lt;<span class="hljs-literal">nil</span>&gt; &lt;<span class="hljs-literal">nil</span>&gt;&#125; <span class="hljs-number">3</span>&#125;</code></pre></div><h4 id="container-heap"><a href="#container-heap" class="headerlink" title="container/heap"></a><code>container/heap</code></h4><p>我们来看看如何初始化一个heap，如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Student <span class="hljs-keyword">struct</span> &#123;name  <span class="hljs-keyword">string</span>score <span class="hljs-keyword">int</span>&#125;h := &amp;StudentHeap&#123;&#123;name: <span class="hljs-string">&quot;xiaoming&quot;</span>, score: <span class="hljs-number">82</span>&#125;,&#123;name: <span class="hljs-string">&quot;xiaozhang&quot;</span>, score: <span class="hljs-number">88</span>&#125;,&#123;name: <span class="hljs-string">&quot;laowang&quot;</span>, score: <span class="hljs-number">85</span>&#125;&#125;heap.Init(h)</code></pre></div><p>先来读读<code>Init()</code>函数，如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">Init</span><span class="hljs-params">(h Interface)</span></span> &#123;<span class="hljs-comment">// heapify</span>n := h.Len()<span class="hljs-keyword">for</span> i := n/<span class="hljs-number">2</span> - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span>; i-- &#123;down(h, i, n)&#125;&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">down</span><span class="hljs-params">(h Interface, i0, n <span class="hljs-keyword">int</span>)</span> <span class="hljs-title">bool</span></span> &#123;i := i0<span class="hljs-keyword">for</span> &#123;j1 := <span class="hljs-number">2</span>*i + <span class="hljs-number">1</span><span class="hljs-keyword">if</span> j1 &gt;= n || j1 &lt; <span class="hljs-number">0</span> &#123; <span class="hljs-comment">// j1 &lt; 0 after int overflow</span><span class="hljs-keyword">break</span>&#125;j := j1 <span class="hljs-comment">// left child</span><span class="hljs-keyword">if</span> j2 := j1 + <span class="hljs-number">1</span>; j2 &lt; n &amp;&amp; h.Less(j2, j1) &#123;j = j2 <span class="hljs-comment">// = 2*i + 2  // right child</span>&#125;<span class="hljs-keyword">if</span> !h.Less(j, i) &#123;<span class="hljs-keyword">break</span>&#125;h.Swap(i, j)i = j&#125;<span class="hljs-keyword">return</span> i &gt; i0&#125;</code></pre></div><p>可以看到，这两个函数跟我们的堆排序很像，还是讲一下，</p><h4 id="container-ring"><a href="#container-ring" class="headerlink" title="container/ring"></a><code>container/ring</code></h4><blockquote><p>环的结构有点特殊，环的尾部就是头部，所以每个元素实际上就可以代表自身的这个环。 它不需要像 list 一样保持 list 和 element 两个结构，只需要保持一个结构就行。</p><p>——<a href="https://books.studygolang.com/The-Golang-Standard-Library-by-Example/chapter03/03.3.html">1. 3.3 container — 容器数据类型：heap、list 和 ring</a></p></blockquote><p>我觉得这句话总结得很好，</p><h3 id="reflect"><a href="#reflect" class="headerlink" title="reflect"></a><code>reflect</code></h3><h4 id="reflect-DeepEqual"><a href="#reflect-DeepEqual" class="headerlink" title="reflect.DeepEqual"></a><code>reflect.DeepEqual</code></h4><p><code>DeepEqual</code>函数用来判断两个值是否深度一致。这和<code>==</code>有什么不同吗？我们来看看下面例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> S <span class="hljs-keyword">struct</span>&#123;name <span class="hljs-keyword">string</span>    age <span class="hljs-keyword">int</span>&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;     a := S&#123;        name:    <span class="hljs-string">&quot;aa&quot;</span>,        age:     <span class="hljs-number">1</span>,    &#125;    b := S&#123;        name:    <span class="hljs-string">&quot;aa&quot;</span>,        age:     <span class="hljs-number">1</span>,    &#125;   fmt.Println(a == b)&#125;</code></pre></div><p>其中<code>a==b</code>为<code>true</code>。如果我们将结构体中加入一个指针，就会返回false，这是因为指针指向的地址不一样，如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> S <span class="hljs-keyword">struct</span> &#123;    Name    <span class="hljs-keyword">string</span>    Age     <span class="hljs-keyword">int</span>    Address *<span class="hljs-keyword">int</span>&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    a := S&#123;        Name:    <span class="hljs-string">&quot;aa&quot;</span>,        Age:     <span class="hljs-number">1</span>,        Address: <span class="hljs-built_in">new</span>(<span class="hljs-keyword">int</span>),    &#125;    b := S&#123;        Name:    <span class="hljs-string">&quot;aa&quot;</span>,        Age:     <span class="hljs-number">1</span>,        Address: <span class="hljs-built_in">new</span>(<span class="hljs-keyword">int</span>),    &#125;   fmt.Println(a == b)&#125;</code></pre></div><p>这时会返回<code>false</code>。</p><p>值得注意的是：<code>slice</code>、<code>map</code>、<code>function</code>是不能参与比较的，如果在结构体中出现以上类型，会<code>panic</code>。在这里我们就有一个疑问？到底是什么原因导致<code>slice</code>、<code>map</code>、<code>function</code>不能进行比较，如果是因为引用类型的原因，<code>channel</code>却可以进行比较。我们先来看看比较<code>slice</code>的时候返回的错误，如下所示：</p><div class="code-wrapper"><pre><code class="hljs go">invalid operation: a == b (<span class="hljs-keyword">struct</span> containing []<span class="hljs-keyword">int</span> cannot be compared)</code></pre></div><p>有的博主说<code>slice</code>不可比较有两个原因：</p><ul><li>引用类型，比较地址没有意义。</li><li>切片有len，cap，比较的维度不好衡量，因此go设计的时候就不允许切片可比较。</li></ul><p>但是我们来看看这个情况：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> S <span class="hljs-keyword">struct</span> &#123;Name    <span class="hljs-keyword">string</span>Age     <span class="hljs-keyword">int</span>Ch<span class="hljs-keyword">chan</span> []<span class="hljs-keyword">int</span>&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;a := S&#123;Name:    <span class="hljs-string">&quot;aa&quot;</span>,Age:     <span class="hljs-number">1</span>,Ch:      <span class="hljs-built_in">make</span>(<span class="hljs-keyword">chan</span> []<span class="hljs-keyword">int</span>),&#125;b := S&#123;Name:    <span class="hljs-string">&quot;aa&quot;</span>,Age:     <span class="hljs-number">1</span>,Ch:      <span class="hljs-built_in">make</span>(<span class="hljs-keyword">chan</span> []<span class="hljs-keyword">int</span>),&#125;fmt.Println(a == b)&#125;</code></pre></div><p>但是在<code>[]int</code>的<code>channel</code>是可以比较的，这就会比较迷惑。</p><p>无独有偶，两个<code>interface&#123;&#125;</code>是能比较的，其规则如下：</p><ul><li>两个类型相同且值相同的接口比较结果为<code>true</code>；</li><li>两个类型相同且值不同的接口比较结果为<code>false</code>；</li><li>两个类型不同的接口比较结果为<code>false</code>；</li><li>如果空接口保存了上面的<code>slice</code>、<code>map</code>和<code>function</code>的值，则不能比较。</li></ul><p>总结：类型不同的空接口间的比较结果不相同，不能比较空接口中的动态值。</p><p>回到正题，我们来看看<code>reflect.DeepEqual</code>，<code>reflect.DeepEqual</code>在可以时（主要是基本类型）会使用<code>==</code>；但还会比较<code>array</code>、<code>slice</code>的<strong>成员</strong>，<code>map</code>的<strong>键值对</strong>，<strong>结构体字段</strong>进行深入比对。如下所示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;m1 := <span class="hljs-keyword">map</span>[<span class="hljs-keyword">int</span>]<span class="hljs-keyword">interface</span>&#123;&#125;&#123;<span class="hljs-number">1</span>: []<span class="hljs-keyword">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;, <span class="hljs-number">2</span>: <span class="hljs-number">3</span>, <span class="hljs-number">3</span>: <span class="hljs-string">&quot;a&quot;</span>&#125;m2 := <span class="hljs-keyword">map</span>[<span class="hljs-keyword">int</span>]<span class="hljs-keyword">interface</span>&#123;&#125;&#123;<span class="hljs-number">1</span>: []<span class="hljs-keyword">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;, <span class="hljs-number">2</span>: <span class="hljs-number">3</span>, <span class="hljs-number">3</span>: <span class="hljs-string">&quot;a&quot;</span>&#125;<span class="hljs-keyword">if</span> reflect.DeepEqual(m1, m2) &#123;fmt.Println(<span class="hljs-string">&quot;相等&quot;</span>)&#125;&#125;<span class="hljs-comment">//输出”相等”</span></code></pre></div><p>当然，我们还是要从它的源码进行分析:</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">DeepEqual</span><span class="hljs-params">(x, y <span class="hljs-keyword">interface</span>&#123;&#125;)</span> <span class="hljs-title">bool</span></span> &#123;<span class="hljs-keyword">if</span> x == <span class="hljs-literal">nil</span> || y == <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> x == y&#125;v1 := ValueOf(x)v2 := ValueOf(y)<span class="hljs-keyword">if</span> v1.Type() != v2.Type() &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;<span class="hljs-keyword">return</span> deepValueEqual(v1, v2, <span class="hljs-built_in">make</span>(<span class="hljs-keyword">map</span>[visit]<span class="hljs-keyword">bool</span>))&#125;</code></pre></div><p>我们可以看到，如果<code>x==nil</code>或者<code>y==nil</code>，就比较二者的指针，否则比较二者的类型，如果类型相同，那么我们就用<code>deepValueEqual</code>函数进行比较。这个函数的核心逻辑可以概括为：先处理类型和值的边界条件，再按 <code>Kind</code> 递归比较。源码中还有 <code>visited</code> 结构，用来避免带环的数据结构导致无限递归。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">deepValueEqual</span><span class="hljs-params">(v1, v2 Value, visited <span class="hljs-keyword">map</span>[visit]<span class="hljs-keyword">bool</span>)</span> <span class="hljs-title">bool</span></span> &#123;<span class="hljs-keyword">if</span> !v1.IsValid() || !v2.IsValid() &#123;<span class="hljs-keyword">return</span> v1.IsValid() == v2.IsValid()&#125;<span class="hljs-keyword">if</span> v1.Type() != v2.Type() &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;<span class="hljs-keyword">switch</span> v1.Kind() &#123;<span class="hljs-keyword">case</span> Array:<span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; v1.Len(); i++ &#123;<span class="hljs-keyword">if</span> !deepValueEqual(v1.Index(i), v2.Index(i), visited) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;&#125;<span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><span class="hljs-keyword">case</span> Slice:<span class="hljs-keyword">if</span> v1.IsNil() != v2.IsNil() || v1.Len() != v2.Len() &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;<span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; v1.Len(); i++ &#123;<span class="hljs-keyword">if</span> !deepValueEqual(v1.Index(i), v2.Index(i), visited) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;&#125;<span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><span class="hljs-keyword">case</span> Map:<span class="hljs-keyword">if</span> v1.IsNil() != v2.IsNil() || v1.Len() != v2.Len() &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;<span class="hljs-keyword">for</span> _, key := <span class="hljs-keyword">range</span> v1.MapKeys() &#123;<span class="hljs-keyword">if</span> !deepValueEqual(v1.MapIndex(key), v2.MapIndex(key), visited) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;&#125;<span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><span class="hljs-keyword">case</span> Struct:<span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; v1.NumField(); i++ &#123;<span class="hljs-keyword">if</span> !deepValueEqual(v1.Field(i), v2.Field(i), visited) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;&#125;<span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><span class="hljs-keyword">default</span>:<span class="hljs-keyword">return</span> valueInterface(v1, <span class="hljs-literal">false</span>) == valueInterface(v2, <span class="hljs-literal">false</span>)&#125;&#125;</code></pre></div><p>所以 <code>DeepEqual</code> 并不是“万能等号”。它适合测试和调试，不适合作为高频业务路径上的比较工具：它会走反射，比较规则也未必符合业务语义。例如空 slice 和 nil slice 在 <code>DeepEqual</code> 下并不相等。</p><h3 id="unsafe"><a href="#unsafe" class="headerlink" title="unsafe"></a><code>unsafe</code></h3><blockquote><p><strong>Unsafe code</strong>是一种绕过go类型安全和内存安全检查的Go代码。大多数情况，unsafe code是和指针相关的。但是要记住使用unsafe code有可能会损害你的程序，所以，如果你不完全确定是否需要用到unsafe code就不要使用它。</p><p><a href="https://studygolang.com/articles/22265">关于 Go 中的 unsafe 包</a></p></blockquote><p>Go 里面没有 <code>warning</code> 这个说法。相比于 C/C++，Go 中一些危险的操作由 <code>unsafe</code> 包提供。</p><h4 id="unsafe-Pointer"><a href="#unsafe-Pointer" class="headerlink" title="unsafe.Pointer"></a><code>unsafe.Pointer</code></h4><p>我以前有一个疑问：既然指针代表的是地址，那为什么还要分类呢？原来，针区分类型是为了在通过指针访问它所指向的存储空间的时候，能够正确访问，如果通过一个char类型的指针操作一个int的变量，如果值的二进制数据超过1字节，那么就造成数据错误。</p><p>我们先看如下例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<span class="hljs-keyword">var</span> value <span class="hljs-keyword">int64</span> = <span class="hljs-number">5</span><span class="hljs-keyword">var</span> p1 = &amp;value<span class="hljs-keyword">var</span> p2 = (*<span class="hljs-keyword">int32</span>)(unsafe.Pointer(p1))fmt.Println(p1)fmt.Println(p2)&#125;</code></pre></div><p>输出结果如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-number">0xc00001e0d0</span><span class="hljs-number">0xc00001e0d0</span></code></pre></div><p>可以发现这两个指针所指向的地址是一样的，这个方法能让你创造一个<code>int32</code>的<code>p2</code>指针去指向一个<code>int64</code>的<code>value</code>变量，而这个变量是使用<code>p1</code>指针去访问的。我们通过以下例子来说明：</p><div class="code-wrapper"><pre><code class="hljs go">x := <span class="hljs-keyword">int32</span>(<span class="hljs-number">5</span>)<span class="hljs-keyword">var</span> p=(*<span class="hljs-keyword">byte</span>)(unsafe.Pointer(&amp;x))s:=*pfmt.Println(s)<span class="hljs-comment">//输出5</span></code></pre></div><p>但是从<code>byte</code>转换成<code>int32</code>的时候会出现问题：</p><div class="code-wrapper"><pre><code class="hljs go">b:=<span class="hljs-string">&#x27;5&#x27;</span><span class="hljs-keyword">var</span> p=(*<span class="hljs-keyword">int32</span>)(unsafe.Pointer(&amp;b))i:=*pfmt.Println(i)<span class="hljs-comment">//输出53</span></code></pre></div><p>这是因为存放的<code>b</code>中存放的实际是ASCII码，故而转换成int时输出53。那么为什么转换成<code>byte</code>时就会输出5呢，我们用反射来看s的实际类型，显示是<code>uint8</code>，也就是说我们只是在这只是修改了指针类型 或者说只是修改了编译器解释他的类型。</p><h4 id="uintptr和unsafe-Pointer的区别"><a href="#uintptr和unsafe-Pointer的区别" class="headerlink" title="uintptr和unsafe.Pointer的区别"></a><code>uintptr</code>和<code>unsafe.Pointer</code>的区别</h4><p>uintptr本质是一个无符号的整型，它的长度可以用来保存一个指针地址。unsafe包提供的Pointer表示可以指向任意类型的指针。</p><p>uintptr往往用来进行指针计算，因为它是整型，所以很容易计算出下一个指针所指向的位置。</p><ul><li>unsafe.Pointer只是单纯的通用指针类型，用于转换不同类型指针，它不可以参与指针运算；</li><li>而uintptr是用于指针运算的，GC 不把 uintptr 当指针，也就是说 uintptr 无法持有对象， uintptr 类型的目标会被回收；</li><li>unsafe.Pointer 可以和 普通指针 进行相互转换；</li><li>unsafe.Pointer 可以和 uintptr 进行相互转换。</li></ul><h3 id="sync"><a href="#sync" class="headerlink" title="sync"></a><code>sync</code></h3><p>sync包中提供了常见的并发编程同步原语，所谓原语，一般是指由若干条指令组成的程序段，用来实现某个特定的功能，在执行过程中不可被中断，否则就会出现操作错误，造成系统混乱。</p><h4 id="sync-Mutex"><a href="#sync-Mutex" class="headerlink" title="sync.Mutex"></a><code>sync.Mutex</code></h4><p>sync.Mutex是sync包中使用最广泛的原语，它允许在共享资源上互斥访问。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> mutex <span class="hljs-keyword">struct</span>&#123;state<span class="hljs-keyword">int32</span>sema<span class="hljs-keyword">uint32</span>&#125;</code></pre></div><p>上述是<code>mutex</code>的结构，<code>state</code>表示当前互斥锁的状态，<code>sema</code>是用于控制锁状态的信号量。在<code>state</code>中有一个标志位叫<code>mutexStarving</code>，表示当前的互斥锁是否进入了饥饿状态，在正常状态下，锁的等待者会按照先进先出的顺序获取锁；在饥饿模式下，互斥锁会直接交给等待队列最前面的 Goroutine。新的 Goroutine 在该状态下不能获取锁、也不会进入自旋状态，它们只会在队列的末尾等待。如果一个 Goroutine 获得了互斥锁并且它在队列的末尾或者它等待的时间少于 1ms，那么当前的互斥锁就会切换回正常模式。</p><h5 id="sync-Mutex-Lock"><a href="#sync-Mutex-Lock" class="headerlink" title="sync.Mutex.Lock"></a><code>sync.Mutex.Lock</code></h5><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(m *Mutex)</span> <span class="hljs-title">Lock</span><span class="hljs-params">()</span></span> &#123;<span class="hljs-keyword">if</span> atomic.CompareAndSwapInt32(&amp;m.state, <span class="hljs-number">0</span>, mutexLocked) &#123;<span class="hljs-keyword">return</span>&#125;m.lockSlow()&#125;</code></pre></div><p>在这里用到原子操作CAS，常常被用于自旋机制，具体讲解在下面。当锁的状态是0时，就会将<code>mutexLocked</code>位置设置为1，如果互斥锁的状态不是0就会调用<code>lockSlow</code>尝试通过自旋等待锁的释放。</p><h5 id="sync-Mutex-Unlock"><a href="#sync-Mutex-Unlock" class="headerlink" title="sync.Mutex.Unlock"></a><code>sync.Mutex.Unlock</code></h5><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(m *Mutex)</span> <span class="hljs-title">Unlock</span><span class="hljs-params">()</span></span> &#123;<span class="hljs-built_in">new</span> := atomic.AddInt32(&amp;m.state, -mutexLocked)<span class="hljs-keyword">if</span> <span class="hljs-built_in">new</span> != <span class="hljs-number">0</span> &#123;m.unlockSlow(<span class="hljs-built_in">new</span>)&#125;&#125;</code></pre></div><p>在<code>Unlock</code>中，用到了原子操作Add，这里这个原子操作的意思是：如果只是<code>mutexLocked</code>位为1，那么我们将state设置为0，return；否则，调用<code>UnlockSlow</code>进行缓慢解锁。也就是说如果有其他goroutine等待着，那么我们就会进入缓慢解锁的状态——移交锁的所有权给下一个goroutine。</p><h4 id="sync-RWMutex"><a href="#sync-RWMutex" class="headerlink" title="sync.RWMutex"></a><code>sync.RWMutex</code></h4><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> RWMutex <span class="hljs-keyword">struct</span> &#123;w           Mutex  <span class="hljs-comment">// held if there are pending writers</span>writerSem   <span class="hljs-keyword">uint32</span> <span class="hljs-comment">// semaphore for writers to wait for completing readers</span>readerSem   <span class="hljs-keyword">uint32</span> <span class="hljs-comment">// semaphore for readers to wait for completing writers</span>readerCount <span class="hljs-keyword">int32</span>  <span class="hljs-comment">// number of pending readers</span>readerWait  <span class="hljs-keyword">int32</span>  <span class="hljs-comment">// number of departing readers</span>&#125;</code></pre></div><p>我们可以看到，<code>RWMutex</code>继承了<code>Mutex</code>，并且增加了几个成员。我们先来说说这几个成员：</p><ul><li><code>writerSem</code>：写者等待读者完成的信号量；</li><li><code>readerSem</code>：读者等待写者完成的信号量；</li><li><code>readerCount</code>：当前有几个读者共享锁；</li><li><code>readerWait</code>：离开的读者数。</li></ul><h5 id="Rlock-amp-RUnlock"><a href="#Rlock-amp-RUnlock" class="headerlink" title="Rlock&amp;RUnlock"></a><code>Rlock</code>&amp;<code>RUnlock</code></h5> <div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">if</span> atomic.AddInt32(&amp;rw.readerCount, <span class="hljs-number">1</span>) &lt; <span class="hljs-number">0</span> &#123;<span class="hljs-comment">// A writer is pending, wait for it.</span>runtime_SemacquireMutex(&amp;rw.readerSem, <span class="hljs-literal">false</span>, <span class="hljs-number">0</span>)&#125;</code></pre></div><p>如上述所示，上读锁的就是给<code>readerCount</code>+1，如果有写者占用，就</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">if</span> r := atomic.AddInt32(&amp;rw.readerCount, <span class="hljs-number">-1</span>); r &lt; <span class="hljs-number">0</span> &#123;<span class="hljs-comment">// Outlined slow-path to allow the fast-path to be inlined</span>rw.rUnlockSlow(r)&#125;</code></pre></div><p>如上述所示，解读锁就是给<code>readerCount</code>-1，如果有</p><h5 id="Lock-amp-Unlock"><a href="#Lock-amp-Unlock" class="headerlink" title="Lock&amp;Unlock"></a><code>Lock</code>&amp;<code>Unlock</code></h5><div class="code-wrapper"><pre><code class="hljs go">r := atomic.AddInt32(&amp;rw.readerCount, rwmutexMaxReaders)<span class="hljs-keyword">if</span> r &gt;= rwmutexMaxReaders &#123;race.Enable()throw(<span class="hljs-string">&quot;sync: Unlock of unlocked RWMutex&quot;</span>)&#125;<span class="hljs-comment">// Unblock blocked readers, if any.</span><span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-keyword">int</span>(r); i++ &#123;runtime_Semrelease(&amp;rw.readerSem, <span class="hljs-literal">false</span>, <span class="hljs-number">0</span>)&#125;</code></pre></div><h4 id="sync-atomic"><a href="#sync-atomic" class="headerlink" title="sync/atomic"></a><code>sync/atomic</code></h4><blockquote><p>CPU执行一条汇编语句就是一个原子操作吗？</p></blockquote><p>原子操作是指不会被多线程调度机制打断的操作，这种操作一旦开始，就一直运行到结束，中间不会有任何context switch。原子操作是不可分割的，在执行完毕之前不会被任何其它任务或事件中断。在单处理器系统（<strong>UniProcessor</strong>）中，能够在单条指令中完成的操作都可以认为是” 原子操作”，因为中断只能发生于指令之间。</p><p>说到这里，我们来看看维基百科上对原子操作的判定：</p><blockquote><p>如果这个操作所处的层(layer)的更高层不能发现其内部实现与结构，那么这个操作是一个原子(atomic)操作。</p></blockquote><p>再看看其定义：</p><blockquote><p>原子操作可以是一个步骤，也可以是多个操作步骤，但是其顺序不可以被打乱，也不可以被切割而只执行其中的一部分。将整个操作视作一个整体是原子性的核心特征。</p></blockquote><p>也就是说，绝大多数情况下，一个机器指令就是一个原子操作。在Intel的参考手册，CPU基于以下三种机制保证了多核中加锁的原子操作：</p><blockquote><p>（1）一些基本的内存读写操作是本身已经被硬件提供了原子性保证（例如读写单个字节的操作）；<br>（2）一些需要保证原子性但是没有被第（1）条机制提供支持的操作（例如read-modify-write）可以通过使用”LOCK#”来锁定总线，从而保证操作的原子性<br>（3）因为很多内存数据是已经存放在L1/L2 cache中了，对这些数据的原子操作只需要与本地的cache打交道，而不需要与总线打交道，所以CPU就提供了cache coherency机制来保证其它的那些也cache了这些数据的processor能读到最新的值。</p></blockquote><p>上面（1）就是由硬件保持的原子操作，具体来说有以下操作：</p><blockquote><p>• Reading or writing a byte（一个字节的读写）<br>• Reading or writing a word aligned on a 16-bit boundary（对齐到16位边界的字的读写）<br>• Reading or writing a doubleword aligned on a 32-bit boundary（对齐到32位边界的双字的读写）</p><p>• Reading or writing a quadword aligned on a 64-bit boundary（对齐到64位边界的四字的读写）<br>• 16-bit accesses to uncached memory locations that fit within a 32-bit data bus（未缓存且在32位数据总线范围之内的内存地址的访问）</p><p>• Unaligned 16-, 32-, and 64-bit accesses to cached memory that fit within a cache line（对单个cache line中缓存地址的未对齐的16/32/64位访问)</p></blockquote><p><a href="https://mp.weixin.qq.com/s/zeggpIOKJMRBmMOCRCsfhA">参考文章</a></p><p>下面我们再看看（2）总线锁。</p><h5 id="总线锁"><a href="#总线锁" class="headerlink" title="总线锁"></a>总线锁</h5><p>处理器会自动保证基本的内存操作的原子性。处理器保证从系统内存当中读取或者写入一个字节是原子的，意思是当一个处理器读取一个字节时，其他处理器不能访问这个字节的内存地址。</p><p>上述操作就是通过总线锁实现的，<strong>在x86 平台上，CPU提供了在指令执行期间对</strong>总线加锁的手段，这样能实现并行时的原子操作。</p><h5 id="Go支持的原子操作概述"><a href="#Go支持的原子操作概述" class="headerlink" title="Go支持的原子操作概述"></a>Go支持的原子操作概述</h5><p>Win32 API中常用的原子操作主要有三类，一种是加一减一操作，二是比较交换操作，三赋值操作。对于一个整数类型<code>T</code>，<code>sync/atomic</code>标准库包提供了下列原子操作函数。 其中<code>T</code>可以是内置<code>int32</code>、<code>int64</code>、<code>uint32</code>、<code>uint64</code>和<code>uintptr</code>类型。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">AddT</span><span class="hljs-params">(addr *T, delta T)</span><span class="hljs-params">(<span class="hljs-built_in">new</span> T)</span></span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">LoadT</span><span class="hljs-params">(addr *T)</span> <span class="hljs-params">(val T)</span></span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">StoreT</span><span class="hljs-params">(addr *T, val T)</span></span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">SwapT</span><span class="hljs-params">(addr *T, <span class="hljs-built_in">new</span> T)</span> <span class="hljs-params">(old T)</span></span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">CompareAndSwapT</span><span class="hljs-params">(addr *T, old, <span class="hljs-built_in">new</span> T)</span> <span class="hljs-params">(swapped <span class="hljs-keyword">bool</span>)</span></span></code></pre></div><p>我们看看<code>CompareAndSwap</code>的源码：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-comment">// sync/atomic 中的声明形式大致如下：</span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">CompareAndSwapInt32</span><span class="hljs-params">(addr *<span class="hljs-keyword">int32</span>, old, <span class="hljs-built_in">new</span> <span class="hljs-keyword">int32</span>)</span> <span class="hljs-params">(swapped <span class="hljs-keyword">bool</span>)</span></span><span class="hljs-comment">// amd64 上最终会落到类似这样的原子指令序列：</span><span class="hljs-comment">// MOVQ addr, BX</span><span class="hljs-comment">// MOVL old, AX</span><span class="hljs-comment">// MOVL new, CX</span><span class="hljs-comment">// LOCK</span><span class="hljs-comment">// CMPXCHGL CX, 0(BX)</span><span class="hljs-comment">// SETEQ swapped</span></code></pre></div><p>CAS 的语义是：只有当 <code>*addr == old</code> 时，才把 <code>*addr</code> 更新为 <code>new</code>，并返回 <code>true</code>；否则不修改内存并返回 <code>false</code>。这个检查和更新必须作为一个不可分割的整体完成，所以底层需要 CPU 原子指令支持。</p><p>在 x86 上，<code>LOCK CMPXCHG</code> 会让这次读-改-写对其他核心可见为一个原子操作。Go 的 <code>sync/atomic</code> 还规定了内存顺序语义：原子操作不仅要保证单个变量更新不可分割，还要避免编译器和 CPU 把相关内存读写重排到不符合并发语义的位置。</p><p>CAS 常见于无锁结构和锁的 fast path，但它不是“免费加速”。高竞争下 CAS 会不断失败，goroutine 可能反复自旋，最终还不如互斥锁稳定。写并发代码时，应该先用 <code>sync.Mutex</code>、channel 等更清晰的同步方式；只有在热点路径已经明确、竞争模型也清楚时，再考虑直接使用 <code>sync/atomic</code>。</p><h3 id="encoding-json"><a href="#encoding-json" class="headerlink" title="encoding/json"></a><code>encoding/json</code></h3><h4 id="Marshal"><a href="#Marshal" class="headerlink" title="Marshal"></a><code>Marshal</code></h4><p><code>json.Marshal</code> 会把 Go 值编码成 JSON 字节序列。它的核心流程是：先根据动态类型找到对应的 encoder，再递归编码字段、数组、切片、map 等结构。</p><p>常见规则如下：</p><ol><li>结构体字段默认使用字段名作为 JSON key。</li><li>可以通过 struct tag 控制字段名、忽略字段或省略零值。</li><li>未导出的字段不会被编码。</li><li><code>map</code> 的 key 如果不是 string、整数或实现了 <code>encoding.TextMarshaler</code>，通常不能直接编码成 JSON object。</li><li>循环引用无法编码，会返回错误。</li></ol><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> User <span class="hljs-keyword">struct</span> &#123;ID    <span class="hljs-keyword">int</span>    <span class="hljs-string">`json:&quot;id&quot;`</span>Name  <span class="hljs-keyword">string</span> <span class="hljs-string">`json:&quot;name&quot;`</span>Email <span class="hljs-keyword">string</span> <span class="hljs-string">`json:&quot;email,omitempty&quot;`</span>&#125;b, err := json.Marshal(User&#123;ID: <span class="hljs-number">1</span>, Name: <span class="hljs-string">&quot;nanbei&quot;</span>&#125;)<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> err&#125;fmt.Println(<span class="hljs-keyword">string</span>(b)) <span class="hljs-comment">// &#123;&quot;id&quot;:1,&quot;name&quot;:&quot;nanbei&quot;&#125;</span></code></pre></div><p><code>omitempty</code> 的判断基于零值：空字符串、0、false、nil 指针、nil slice、nil map 都会被省略。需要区分“字段不存在”和“字段存在但为空”时，应该使用指针或自定义类型表达。</p><h4 id="Unmarshal"><a href="#Unmarshal" class="headerlink" title="Unmarshal"></a><code>Unmarshal</code></h4><p><code>json.Unmarshal</code> 会把 JSON 字节序列解码到传入的指针里。这里必须传指针，否则函数无法修改调用方的变量。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">var</span> user User<span class="hljs-keyword">if</span> err := json.Unmarshal(data, &amp;user); err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> err&#125;</code></pre></div><p>反序列化时有几个容易踩坑的点：</p><ol><li>JSON number 默认解到 <code>interface&#123;&#125;</code> 时会变成 <code>float64</code>，大整数可能丢精度。需要保留精度时可以使用 <code>Decoder.UseNumber()</code>。</li><li>JSON 中不存在的字段不会覆盖结构体里已有字段，因此复用结构体变量时要先清空。</li><li>多余字段默认会被忽略；如果希望严格校验，可以使用 <code>Decoder.DisallowUnknownFields()</code>。</li><li><code>null</code> 解到指针、slice、map 时会得到 nil；解到普通数字或字符串字段时不会把它变成特殊值。</li></ol><p>如果需要自定义编码/解码，可以实现 <code>MarshalJSON</code> 和 <code>UnmarshalJSON</code>：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(u User)</span> <span class="hljs-title">MarshalJSON</span><span class="hljs-params">()</span> <span class="hljs-params">([]<span class="hljs-keyword">byte</span>, error)</span></span> &#123;<span class="hljs-keyword">type</span> Alias User<span class="hljs-keyword">return</span> json.Marshal(<span class="hljs-keyword">struct</span> &#123;AliasKind <span class="hljs-keyword">string</span> <span class="hljs-string">`json:&quot;kind&quot;`</span>&#125;&#123;Alias: Alias(u), Kind: <span class="hljs-string">&quot;user&quot;</span>&#125;)&#125;</code></pre></div><p><code>encoding/json</code> 最大的优点是标准、稳定、反射能力强；缺点是反射带来的性能开销较明显。高性能场景可以考虑代码生成或专用 JSON 库，但业务系统的默认选择仍然应该先从标准库开始。</p>]]></content>
    
    
    <categories>
      
      <category>Go语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Go</tag>
      
      <tag>Go standard library</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go runtime: panic 与 defer</title>
    <link href="/2022/03/12/Go-runtime-panic-and-defer/"/>
    <url>/2022/03/12/Go-runtime-panic-and-defer/</url>
    
    <content type="html"><![CDATA[<h3 id="Go-panic"><a href="#Go-panic" class="headerlink" title="Go panic"></a>Go panic</h3><p>大多数编程语言都提供了异常处理机制，恰恰相反，Go延续了C语言的风格，并未提供异常处理机制。但在Go中，提供了panic异常，从某种意义上说，它也非常接近其他语言的异常处理。</p><p>Go语言在编译期间就能捕获大量异常，但是有些异常只能发生在运行期间，比如运行期间的除以0的异常，数组越界错误等。</p><p>除了程序本身的问题导致panic异常外，也可以直接在程序里调用panic函数引发panic异常。panic函数原型如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">panic</span><span class="hljs-params">(v <span class="hljs-keyword">interface</span>&#123;&#125;)</span></span></code></pre></div><p>当我们直接调用panic函数时就会引发程序panic异常。如下面例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">f</span><span class="hljs-params">(x <span class="hljs-keyword">int</span>)</span></span> &#123;    fmt.Printf(<span class="hljs-string">&quot;f(%d)\n&quot;</span>, x+<span class="hljs-number">0</span>/x) <span class="hljs-comment">// 当参数为 0 时，引发 panic 异常</span>    <span class="hljs-keyword">defer</span> fmt.Printf(<span class="hljs-string">&quot;%d\n&quot;</span>, x)    f(x - <span class="hljs-number">1</span>)&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    f(<span class="hljs-number">3</span>)    fmt.Println(<span class="hljs-string">&quot;end&quot;</span>)&#125;</code></pre></div><p>当程序发生 panic 时，当前函数会中断正常执行，并沿着调用栈向外传播。在传播过程中，每一层已经注册的 <code>defer</code> 都会按后进先出的顺序执行。以上面的例子来说，<code>f(0)</code> 触发除零错误后，panic 会从 <code>f(0)</code> 逐层传到 <code>f(1)</code>、<code>f(2)</code>、<code>f(3)</code>，直到所有已注册的 deferred 函数都执行完。</p><h5 id="Go-defer"><a href="#Go-defer" class="headerlink" title="Go defer"></a>Go defer</h5><p>Go 语言的 <code>defer</code> 语句会把后面跟随的调用延迟到当前函数返回前执行。多个 <code>defer</code> 的执行顺序是后进先出，也就是说先注册的最后执行，最后注册的最先执行。延迟语句常常用于释放资源、关闭文件、解锁互斥锁，或者在 panic 发生时做收尾工作。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">readFile</span><span class="hljs-params">(name <span class="hljs-keyword">string</span>)</span> <span class="hljs-title">error</span></span> &#123;    f, err := os.Open(name)    <span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;        <span class="hljs-keyword">return</span> err    &#125;    <span class="hljs-keyword">defer</span> f.Close()    <span class="hljs-comment">// 这里处理文件内容。无论中途正常返回还是发生 panic，Close 都有机会被执行。</span>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>&#125;</code></pre></div><p>需要注意的是，<code>defer</code> 后面的函数参数会在注册 defer 时求值，而不是在真正执行 defer 时才求值。理解这一点可以避免很多闭包和循环变量带来的误判。</p><p><code>panic</code> 和 <code>defer</code> 经常一起出现，但它们不是异常处理的替代品。Go 更推荐用 <code>error</code> 表达可预期的失败，用 <code>panic</code> 表达程序无法继续运行的严重错误。只有在边界层，例如 HTTP 中间件或 goroutine 入口处，才适合用 <code>recover</code> 把 panic 收敛下来，记录日志并保护进程不被单个请求拖垮。</p><h3 id="defer-的几个细节"><a href="#defer-的几个细节" class="headerlink" title="defer 的几个细节"></a>defer 的几个细节</h3><p><code>defer</code> 最容易误解的地方是参数求值时机。下面这段代码最终打印的是 <code>0</code>，因为 <code>defer fmt.Println(i)</code> 注册时，参数 <code>i</code> 已经被求值：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    i := <span class="hljs-number">0</span>    <span class="hljs-keyword">defer</span> fmt.Println(i)    i = <span class="hljs-number">10</span>&#125;</code></pre></div><p>如果 defer 后面是闭包，闭包内部读取的是变量本身，结果就不同：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    i := <span class="hljs-number">0</span>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123; fmt.Println(i) &#125;()    i = <span class="hljs-number">10</span>&#125;</code></pre></div><p>这段代码会打印 <code>10</code>。循环中使用 defer 或闭包时尤其要注意这个差异。</p><h3 id="recover-的边界"><a href="#recover-的边界" class="headerlink" title="recover 的边界"></a>recover 的边界</h3><p><code>recover</code> 只有在 deferred 函数中直接调用才会生效。它不能跨 goroutine 捕获 panic，也不能在普通函数调用链里随便调用。</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">safeRun</span><span class="hljs-params">(fn <span class="hljs-keyword">func</span>()</span>)</span> &#123;    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;            log.Printf(<span class="hljs-string">&quot;panic recovered: %v&quot;</span>, r)        &#125;    &#125;()    fn()&#125;</code></pre></div><p>每个 goroutine 都有自己的栈。某个 goroutine panic 时，只会展开自己的栈；如果没有在这个 goroutine 的 defer 链上 recover，程序最终会崩溃。因此后台任务的入口处通常应该有统一的 recover 边界。</p><h3 id="runtime-视角"><a href="#runtime-视角" class="headerlink" title="runtime 视角"></a>runtime 视角</h3><p>从 runtime 角度看，<code>defer</code> 和 <code>panic</code> 都是围绕 goroutine 栈组织的。函数注册 defer 时，runtime 会记录一个延迟调用；函数返回或 panic 展开栈时，再按后进先出的顺序执行这些调用。panic 传播时会不断执行当前栈帧上的 defer，如果某个 defer 成功 recover，panic 传播停止，函数从 defer 之后的路径返回。</p><p>早期 Go 版本中 defer 成本较高，所以很多性能敏感代码会避免在热点循环中使用 defer。现在编译器已经能对一部分 defer 做 open-coded 优化，普通资源释放场景不必过度担心。但在极端热点路径上，仍然应该用 benchmark 说话，而不是凭感觉判断 defer 是否昂贵。</p><h3 id="使用建议"><a href="#使用建议" class="headerlink" title="使用建议"></a>使用建议</h3><ol><li>可预期错误返回 <code>error</code>，不可恢复的程序状态才使用 <code>panic</code>。</li><li>在 goroutine 入口、HTTP/RPC 中间件等边界层统一 recover。</li><li>recover 后至少记录堆栈，否则问题会被静默吞掉。</li><li>不要用 panic/recover 模拟业务分支，它会让控制流变得隐蔽。</li><li>热点循环中大量 defer 需要 benchmark 验证成本。</li></ol>]]></content>
    
    
    <categories>
      
      <category>Go语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Go runtime</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 6: Copy-on-write fork</title>
    <link href="/2022/02/24/MIT-6-S081-Lab-6-Copy-on-Write-Fork/"/>
    <url>/2022/02/24/MIT-6-S081-Lab-6-Copy-on-Write-Fork/</url>
    
    <content type="html"><![CDATA[<blockquote><p>早期的fork()实现较为“简单粗暴”，会将父进程的物理内存完整拷贝一份，并映射到子进程的内存空间中，这种方式在很多情况下是不必要的，一部分虚拟内存是只读的，对他们进行拷贝是一种浪费，其次，由于进程有时会在调用fork函数之后立即调用exec以载入新的可执行文件，重置地址空间，之前的内存拷贝就完全失去了意义。因此可以用写时拷贝技术来对fork的实现进行优化。对于本来就是只读的虚拟页，父进程和子进程可以共享这些页，减少了拷贝的开销。但对于容易发生变化的虚拟页，如果出现了写操作，就会触发写时拷贝。</p><p>——陈海波《现代操作系统：原理与实现》</p></blockquote><p>传统的<code>fork</code>函数会复制数据段、堆、栈，而代码段则不会复制，而是采用映射的方式映射到父进程的代码段，这也很好解释，因为我们不需要去更改代码段的内容，也就没必要复制。</p><p>当两个进程拥有很多相同的内存数据，如果把这些数据相同的内存页在物理内存中仅存储一份，然后以只读的方式映射给两个应用程序，那么就能显著地节约物理内存资源。如下图所示：</p><p><code>PTE</code>除了记录物理页号，还记录了别的信息，我们回忆实验3里面讲的，<code>PTE</code>中有一些标志位，用来表示虚拟页的权限的权限位。写时拷贝正是利用表示“是否可写”的权限位来实现的。</p><p><code>COW fork()</code>只为孩子创建一个页表，用户内存的 PTE 指向父级的物理页面。<code>COW fork()</code>将 parent 和 child 中的所有用户 PTE 标记为不可写。当任一进程尝试写入这些 COW 页之一时，CPU 将强制发生页错误。内核页面错误处理程序检测到这种情况，为出错进程分配物理内存页面，将原始页面复制到新页面中，并修改出错进程中的相关 PTE 以引用新页面，这次使用PTE 标记为可写。当页面错误处理程序返回时，用户进程将能够写入它的页面副本。</p><p>值得注意的是：<font color=#FF0000>当多个用户进程的虚拟内存都指向相同的物理内存page，父进程退出的时候我们要更加小心，因为我们要判断是否能立即释放相应的物理page，因为子进程可能同样会用到这个物理内存所存储的数据。</font>这个时候我们要引入引用计数器<code>count</code>，当物理内存的引用数为0的时候，我们就能释放这个物理页表了。</p><hr><p>值得注意的是，在linux中有一个<code>vfork()</code>，可以用来解决创建子进程后又马上调用<code>exec()</code>函数的情况，与<code>fork</code>函数一样，<code>vfork</code>函数也是用来创建子进程的。且该函数创建出的子进程与父进程共用一个地址空间。通过<code>vfork</code>创建的子进程会执行完后，才到父进程执行。</p><hr><p>我们需要修改<code>uvmcopy()</code>，在复制副进程的内存到子进程的时候，不立即进行复制数据，而是建立指向原物理页的映射，并将父子两端的页表项都设置为不可写。</p><p>我们首先看到<code>sys_fork</code>函数：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64</span><span class="hljs-function"><span class="hljs-title">sys_fork</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">return</span> fork();&#125;</code></pre></div><p>这个<code>fork</code>函数如下所示，我将自己的理解写在了注释中：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span></span><span class="hljs-function"><span class="hljs-title">fork</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">int</span> i, pid;  <span class="hljs-comment">//新建proc</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">np</span>;</span>  <span class="hljs-comment">//准备好当前的proc,用来复制</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">p</span> =</span> myproc();  <span class="hljs-comment">// Allocate process.</span>  <span class="hljs-keyword">if</span>((np = allocproc()) == <span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;  &#125;  <span class="hljs-comment">// Copy user memory from parent to child.</span>  <span class="hljs-comment">//分配内存</span>  <span class="hljs-keyword">if</span>(uvmcopy(p-&gt;pagetable, np-&gt;pagetable, p-&gt;sz) &lt; <span class="hljs-number">0</span>)&#123;    freeproc(np);    release(&amp;np-&gt;lock);    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;  &#125;  np-&gt;sz = p-&gt;sz;  <span class="hljs-comment">// copy saved user registers.</span>  *(np-&gt;trapframe) = *(p-&gt;trapframe);  <span class="hljs-comment">// Cause fork to return 0 in the child.</span>  np-&gt;trapframe-&gt;a0 = <span class="hljs-number">0</span>;  <span class="hljs-comment">// increment reference counts on open file descriptors.</span>  <span class="hljs-keyword">for</span>(i = <span class="hljs-number">0</span>; i &lt; NOFILE; i++)    <span class="hljs-keyword">if</span>(p-&gt;ofile[i])      np-&gt;ofile[i] = filedup(p-&gt;ofile[i]);  np-&gt;cwd = idup(p-&gt;cwd);    safestrcpy(np-&gt;name, p-&gt;name, <span class="hljs-keyword">sizeof</span>(p-&gt;name));    pid = np-&gt;pid;  release(&amp;np-&gt;lock);  <span class="hljs-comment">//获取锁</span>  acquire(&amp;wait_lock);    np-&gt;parent = p;  release(&amp;wait_lock);  acquire(&amp;np-&gt;lock);  np-&gt;state = RUNNABLE;    release(&amp;np-&gt;lock);  <span class="hljs-comment">//返回进程id</span>  <span class="hljs-keyword">return</span> pid;&#125;</code></pre></div><p>我们先来说说进程的<code>state</code>字段：</p><p>A question about the <code>fork</code> function</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">procstate</span> &#123;</span> UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE &#125;;<span class="hljs-comment">//未运行、已运行（终止）、阻塞、就绪、运行、僵死</span></code></pre></div><blockquote><p>英文术语<em>zombie process</em>源自丧尸——不死之人，隐喻子进程已死但仍然没有被回收。与正常进程不同，<code>kill</code>命令对僵尸进程无效。</p></blockquote><p>在类Unix系统中，僵死进程是指完成执行，但在操作系统进程表中仍然存在其进程控制块，处于”终止状态“的进程。</p><p>下面是一个进程僵死的例子，解释在注释中：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;sys/wait.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;unistd.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;<span class="hljs-keyword">pid_t</span> pids[<span class="hljs-number">10</span>];<span class="hljs-keyword">int</span> i;<span class="hljs-keyword">for</span> (i = <span class="hljs-number">9</span>; i &gt;= <span class="hljs-number">0</span>; --i) &#123;pids[i] = fork();<span class="hljs-keyword">if</span> (pids[i] == <span class="hljs-number">0</span>) &#123;sleep(i+<span class="hljs-number">1</span>);_exit(<span class="hljs-number">0</span>);<span class="hljs-comment">//在这之后已经完成执行了</span>&#125;&#125;<span class="hljs-keyword">for</span> (i = <span class="hljs-number">9</span>; i &gt;= <span class="hljs-number">0</span>; --i)        <span class="hljs-comment">//我们在这里有个wait，所以操作系统会保留其PCB，这就是僵尸装在</span>waitpid(pids[i], <span class="hljs-literal">NULL</span>, <span class="hljs-number">0</span>);<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div><p>在<code>kernel/proc.c</code>的<code>fork</code>函数中，我们会将子进程设置为<code>RUNNABLE</code>状态，也就是拿到当前的就绪队列中，等待调度了，值得注意的是，在对<code>np</code>的状态进行状态赋值的时候我们会上锁，为什么呢？</p><p>我们来看看<code>kernel/proc.c</code>中的<code>proc</code>函数，调用了<code>uvmcopy</code>函数，将父进程的页表以及内存拷贝到子进程，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Copy user memory from parent to child.</span><span class="hljs-keyword">if</span>(uvmcopy(p-&gt;pagetable, np-&gt;pagetable, p-&gt;sz) &lt; <span class="hljs-number">0</span>)&#123;<span class="hljs-comment">//将副进程的页表拷贝</span>  freeproc(np);<span class="hljs-comment">//释放一个proc结构和挂在它上面的数据</span>  release(&amp;np-&gt;lock);  <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;&#125;np-&gt;sz = p-&gt;sz;</code></pre></div><p><code>uvmcopy</code>函数如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span></span><span class="hljs-function"><span class="hljs-title">uvmcopy</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> old, <span class="hljs-keyword">pagetable_t</span> <span class="hljs-keyword">new</span>, uint64 sz)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">pte_t</span> *pte;  uint64 pa, i;  uint flags;  <span class="hljs-keyword">char</span> *mem;  <span class="hljs-keyword">for</span>(i = <span class="hljs-number">0</span>; i &lt; sz; i += PGSIZE)&#123;    <span class="hljs-keyword">if</span>((pte = walk(old, i, <span class="hljs-number">0</span>)) == <span class="hljs-number">0</span>)      <span class="hljs-keyword">continue</span>; <span class="hljs-comment">// 如果一个页不存在，则认为是懒加载的页，忽略即可</span>    <span class="hljs-keyword">if</span>((*pte &amp; PTE_V) == <span class="hljs-number">0</span>)      <span class="hljs-keyword">continue</span>; <span class="hljs-comment">// 如果一个页不存在，则认为是懒加载的页，忽略即可</span>    pa = PTE2PA(*pte);    flags = PTE_FLAGS(*pte);    <span class="hljs-comment">//if((mem = kalloc()) == 0)</span>      <span class="hljs-comment">//goto err;</span>    <span class="hljs-comment">//memmove(mem, (char*)pa, PGSIZE);</span>    <span class="hljs-keyword">if</span>(mappages(<span class="hljs-keyword">new</span>, i, PGSIZE, (uint64)mem, flags) != <span class="hljs-number">0</span>)&#123;      <span class="hljs-comment">//kfree(mem);</span>      <span class="hljs-keyword">goto</span> err;    &#125;  &#125;  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; err:  uvmunmap(<span class="hljs-keyword">new</span>, <span class="hljs-number">0</span>, i / PGSIZE, <span class="hljs-number">1</span>);  <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;&#125;</code></pre></div><p>当我们在面试的时候很多面试官也会问<code>malloc</code>的底层实现，那么我们就根据xv版本的代码来具体看看：</p>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CMU 15-445 Project 2: B+Tree index</title>
    <link href="/2022/02/15/CMU-15-445-Project-2-BPlusTree/"/>
    <url>/2022/02/15/CMU-15-445-Project-2-BPlusTree/</url>
    
    <content type="html"><![CDATA[<p>这个实验2真的是折磨死我了。</p><h4 id="Tree-Index"><a href="#Tree-Index" class="headerlink" title="Tree Index"></a>Tree Index</h4><p>索引<strong>是一个排序的列表</strong>，在这个列表中存储着索引的值和包含这个值的数据所在行的物理地址，在数据十分庞大的时候，索引可以大大加快查询的速度，这是因为使用索引后可以不用扫描全表来定位某行的数据，而是先通过索引表找到该行数据对应的物理地址然后访问相应的数据。</p><p>hash table的时间复杂度是$O(1)$。在B+ Tree中，因为它是平衡的，所以它的时间复杂度始终是$O(log n)$，也就是说，对于一个叶子结点上的任何key来说，不管它距离根节点有多远，时间复杂度始终是$O(logn)$，不管我们删除多少次，插入多少次，修改周围的东西，它始终是$O(logn)$。对于一个多路查找树（M-way search tree）而言，在树中的每个节点处，它可以通过$&gt;=M$条不同的路线到达其他节点，它是完美平衡的，当我在对树进行修改时，该数据结构会始终保持平衡性，也就是说任何叶子节点到根节点始终是$O(logn)$，我们还要维护的一件事是，保证每个节点至少都是半满的状态（Andy教授课程讲义），MySQL的InnoDB引擎就是默认B+树索引，自适应hash 索引，但是哈希索引失去了有序性：无法用于排序和分组，只支持精确查找、无法用于部分查找和范围查找。</p><p>值得注意的是，hash索引一般用于精确查找，B+树索引更适合范围查找。下面就是建立索引的SQL语句：</p><div class="code-wrapper"><pre><code class="hljs sql"><span class="hljs-comment">-- 在normal_table表的id列建立名为normal_index的普通索引</span><span class="hljs-keyword">create</span> index normal_index <span class="hljs-keyword">on</span> normal_table(id);</code></pre></div><hr><h5 id="B树"><a href="#B树" class="headerlink" title="B树"></a>B树</h5><p>B树是一种多路平衡查找树，其定义如下：</p><ul><li>每个节点最多有m-1个关键字</li><li>根节点最少可以只有1个关键字</li><li>非根节点至少有m/2个关键字</li><li>每个节点中的关键字都按照从小到大的顺序排列，每个关键字的左子树中的所有关键字都小于它，而右子树中的所有关键字都大于它</li><li>所有叶子节点都位于同一层，或者说根节点到每个叶子节点的长度都相同</li><li>每个节点都存有索引和数据，也就是对应的key和value</li></ul><p>所以，根节点的关键字数量范围：$1&lt;=k&lt;=m-1$，非根节点的关键字数量范围：$m/2&lt;=k&lt;=m-1$，其中m为阶数，表示了一个节点最多有多少个孩子节点。比如有一个5阶的B树，根节点数量范围：$1&lt;=k&lt;=4$，非根节点数量范围：$2&lt;=k&lt;=4$。</p><h6 id="B树插入操作"><a href="#B树插入操作" class="headerlink" title="B树插入操作"></a>B树插入操作</h6><p>插入的时候，我们需要记住一个规则：判断当前节点key的个数是否小于等于m-1，如果满足，直接插入即可，如果不满足，用节点的中间key将这个节点分为左右两部分，中间的节点放到父节点中即可。如下例子（例子来源于<a href="https://segmentfault.com/a/1190000020416577">博客</a>)：</p><p>在5阶B树中，节点最多有4个key，最少有2个key。</p><ul><li>插入18，70，50，40</li></ul><ul><li>插入22</li></ul><p>插入22时，发现这个节点的关键字已经大于4了，所以需要进行分裂，分裂的规则在上面已经讲了。分裂之后如下：</p><ul><li>接着插入23，25，39</li></ul><ul><li>分裂之后如下：</li></ul><h6 id="B树删除操作"><a href="#B树删除操作" class="headerlink" title="B树删除操作"></a>B树删除操作</h6><p>B树的删除操作相对于一个插入操作是相对复杂一些的，如下面例子：</p><ul><li>现在有一个初始状态是下面这样的B树，然后进行删除操作：</li></ul><ul><li>删除15，这种情况是删除叶子节点的元素，如果删除之后，节点数还是大于m/2，这种情况只要直接删除即可。</li></ul><ul><li>接着，我们把22删除，这种情况的规则，22是非叶子节点，对于非叶子节点的删除，我们需要用后继key（元素）覆盖要删除的key，然后在后续key所在的子支中删除该后继key。对于删除22，需要将后继元素24移动到被删除的22所在的节点。对于删除22,需要将后继元素24移到被删除的22所在的节点。</li></ul><ul><li>此时我们发现26所在的节点只有一个元素，小于2（m/2)个，这个节点不符合要求，这时候的规则就是向兄弟节点借元素，如下图所示：</li></ul><ul><li>移动之后，跟兄弟节点合并</li></ul><h5 id="B-树"><a href="#B-树" class="headerlink" title="B+树"></a>B+树</h5><p>B+树其实和B树是非常相似的，我们可以看看相同点。</p><ul><li>根节点至少一个元素</li><li>非跟节点元素范围：$m/2&lt;=k&lt;=m-1$</li></ul><h6 id="不同点"><a href="#不同点" class="headerlink" title="不同点"></a>不同点</h6><ul><li>B+树有两种类型的节点：内部节点和叶子节点，内部节点就是非叶子节点，内部节点不存储数据，只存储索引，数据都存储在叶子节点。</li><li>内部节点的key都按照从小到大的顺序排列，对于内部节点中的一个key，左树中的所有key都小于它，右子树中的key都大于等于它。叶子节点中的记录也按照key的大小排列。</li><li>每个叶子节点都存有相邻叶子节点的指针，叶子节点本身依关键字的大小自小而大顺序链接。</li><li>父节点存有右孩子第一个元素的索引。</li></ul><p>如下图所示：</p><h6 id="插入操作"><a href="#插入操作" class="headerlink" title="插入操作"></a>插入操作</h6><p>对于插入操作很简单，只需要记住一个技巧即可：当节点元素数量大于m-1的时候，按中间元素分裂成左右两部分，中间元素分裂到父节点当作索引存储，但是，本身中间元素还是分裂右边这一部分。</p><p>下面以一颗5阶B+树的插入过程为例，5阶B+树的节点最少2个元素，最多4个元素。</p><ul><li>插入5，10，15，20</li></ul><ul><li>插入25，此时元素数量大于4个了，分裂</li></ul><ul><li>接着插入26，30，继续分裂</li></ul><h6 id="删除操作"><a href="#删除操作" class="headerlink" title="删除操作"></a>删除操作</h6><p>对于删除操作是比B树简单一些的，因为叶子节点有指针的存在，向兄弟节点借元素时，不需要通过父节点了，而是可以通过兄弟节点移动即可（前提是兄弟节点的元素大于m/2），然后更新父节点的索引；如果兄弟节点的元素不大于m/2（兄弟节点也没有多余的元素），则将当前节点和兄弟节点合并，并且删除父节点中的key，下面我们看看具体的实现：</p><ul><li>初始状态</li></ul><ul><li>删除10，删除后，不满足要求，发现左边兄弟节点有多余的元素，所以去借元素，最后，修改父节点索引。</li></ul><ul><li>删除元素5，发现不满足要求，并且发现左右兄弟节点都没有多余的元素，所以，可以选择和兄弟节点合并，最后修改父节点索引。</li></ul><ul><li>发现父节点索引也不满足条件，所以，需要做跟上面一步一样的操作</li></ul><p>B+树相对于B树有一些优势，如下：</p><ul><li>单一节点存储的元素更多，使得查询的IO次数更多，所以也就使得它更适合作为数据库MySQL的底层数据结构。</li><li>所有的查询都要查找到叶子节点，查询性能是稳定的，而B树每个节点都可以查找到数据，所以不稳定。</li><li>所有的叶子节点形成了一个有序链表，更便于查找。</li><li>B+树的空间利用率更高，可减少I/O次数，磁盘读写代价更低。</li><li>增删文件时，效率更高，因为B+树的叶子节点包含所有关键字，并以有序的链表结构存储，这样可以很好的增删效率。</li></ul><hr><p>在这里插入红黑树的知识点：</p><p>要理解红黑树，我觉得还是要和二叉搜索树、AVL树结合起来理解。</p><p>我们都知道，二叉搜索树的性质是顺序存储，缺点是不平衡，在极端情况下会变成链表；AVL树的性质是平衡，如果我们将二者的性质结合起来，就成了红黑树的雏形，红黑树按照顺序存储，还可以保持一定的平衡（放弃了AVL树绝对平衡的性质，而是根据自身红黑的性质保持相对平衡，插入与删除时会比AVL树更方便），这样就稳定了查询时间。</p><p>红黑树是一种含有红黑结点并能自平衡的二叉查找树。它必须满足下面性质：</p><p>性质1：每个节点要么是黑色，要么是红色。</p><p>性质2：根节点是黑色。</p><p>性质3：每个叶子节点（NIL）是黑色。 </p><p>性质4：每个红色结点的两个子结点一定都是黑色。</p><p>性质5：任意一结点到每个叶子结点的路径都包含数量相同的黑结点。（保证这棵树尽量是平衡的。）</p><hr><h5 id="TASK-1-B-TREE-PAGES"><a href="#TASK-1-B-TREE-PAGES" class="headerlink" title="TASK #1 - B+TREE PAGES"></a>TASK #1 - B+TREE PAGES</h5><p>我们首先要了解B+树中的key-value映射，key是我们要找的关键字，value指向下一个节点。内部节点的key都按照从小到大的顺序排列，对于内部节点中的一个key，左树中的所有key都小于它，右子树中的key都大于等于它。叶子节点中的记录也按照key的大小排列。父节点存有右孩子第一个元素的索引。</p><h6 id="B-TREE-PARENT-PAGE"><a href="#B-TREE-PARENT-PAGE" class="headerlink" title="B+TREE PARENT PAGE"></a>B+TREE PARENT PAGE</h6><p>这是内部页和叶页都继承的父类，它只包含两个子类共享的信息。<strong>即所有的节点都复用一个header数据域类，其定义在b_plus_tree_page.h中</strong>，我们先来看看如下定义：</p><div class="table-container"><table><thead><tr><th>Variable Name</th><th>Size</th><th>Description</th></tr></thead><tbody><tr><td>page<em>type</em></td><td>4</td><td>Page Type (internal or leaf)//页面类型，内部页或者叶子页</td></tr><tr><td>lsn_</td><td>4</td><td>Log sequence number (Used in Project 4)//日志序列号？</td></tr><tr><td>size_</td><td>4</td><td>Number of Key &amp; Value pairs in page//页面键值对的数量</td></tr><tr><td>max<em>size</em></td><td>4</td><td>Max number of Key &amp; Value pairs in page//键值对最大数</td></tr><tr><td>parent<em>page_id</em></td><td>4</td><td>Parent Page Id//父页面ID</td></tr><tr><td>page<em>id</em></td><td>4</td><td>Self Page Id//页面ID</td></tr></tbody></table></div><p>在这里我们要实现一些简单的set和get函数，因为非常简单，所以就不列举出来了，但是有一个函数不太简单，那就是<code>GetMinSize</code>函数，正如我们在前面所说的，根节点可以拥有少于半数的元素，而且根节点也有可能是叶节点。我们知道，内部节点的array中的第一个键值对中的键是不可用的，正如下图所示：</p><p>我们发现键总是比值少一个。所以说，如果是根节点且根节点是内部节点，那么可以拥有最少2个数组项，其中第0项是无效key的那个；如果是根节点且根节点是叶子节点，那么最少有1个数组项。</p><p>在这种情况下，要想实现就很难了，所以我一开始直接简单粗暴的用内部节点来编写这个函数，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">BPlusTreePage::GetMinSize</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;<span class="hljs-keyword">return</span> max_size_/<span class="hljs-number">2</span>;&#125;</code></pre></div><p>不出意外是会在后续的测试中出错，所以我们改一下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">BPlusTreePage::GetMinSize</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">if</span>(<span class="hljs-built_in">IsRootPage</span>())&#123;    <span class="hljs-keyword">return</span> <span class="hljs-built_in">IsLeafPage</span>()?<span class="hljs-number">1</span>:<span class="hljs-number">2</span>;  &#125;  <span class="hljs-keyword">if</span>(<span class="hljs-built_in">IsLeafPage</span>())&#123;    <span class="hljs-keyword">return</span> <span class="hljs-built_in">ceil</span>(max_size_);  &#125;  <span class="hljs-keyword">return</span> <span class="hljs-built_in">ceil</span>(max_size_<span class="hljs-number">-1</span>)/<span class="hljs-number">2</span>+<span class="hljs-number">1</span>;&#125;</code></pre></div><h6 id="B-TREE-INTERNAL-PAGE"><a href="#B-TREE-INTERNAL-PAGE" class="headerlink" title="B+TREE INTERNAL PAGE"></a>B+TREE INTERNAL PAGE</h6><p>我们可以先了解一下内部节点的声明，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">/**</span><span class="hljs-comment"> * Store n indexed keys and n+1 child pointers (page_id) within internal page.</span><span class="hljs-comment"> * Pointer PAGE_ID(i) points to a subtree in which all keys K satisfy:</span><span class="hljs-comment"> * K(i) &lt;= K &lt; K(i+1).</span><span class="hljs-comment"> * <span class="hljs-doctag">NOTE:</span> since the number of keys does not equal to number of child pointers,</span><span class="hljs-comment"> * the first key always remains invalid. That is to say, any search/lookup</span><span class="hljs-comment"> * should ignore the first key.</span><span class="hljs-comment"> *</span><span class="hljs-comment"> * Internal page format (keys are stored in increasing order):</span><span class="hljs-comment"> *  --------------------------------------------------------------------------</span><span class="hljs-comment"> * | HEADER | KEY(1)+PAGE_ID(1) | KEY(2)+PAGE_ID(2) | ... | KEY(n)+PAGE_ID(n) |</span><span class="hljs-comment"> *  --------------------------------------------------------------------------</span><span class="hljs-comment"> */</span></code></pre></div><p>在这个声明当中，我们可以得知，内部节点会有n个key和n+1个page_id，所以，为了使得key和value对应，第一个key是无效的，任何的search/lookup必须忽略第一个key。其实啊，这第一个key我们可以理解为header，父节点的指针就是指向这里。</p><ul><li><p>获取/设置key的函数，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">KeyType <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::KeyAt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-comment">// replace with your own code</span>  KeyType key=array_[index].first;  <span class="hljs-keyword">return</span> key;&#125;<span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::SetKeyAt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index, <span class="hljs-keyword">const</span> KeyType &amp;key)</span> </span>&#123;  array_[index].first=key;&#125;</code></pre></div></li><li><p>接着是通过value获取index和通过index获取value，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::ValueIndex</span><span class="hljs-params">(<span class="hljs-keyword">const</span> ValueType &amp;value)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">int</span> index=<span class="hljs-number">0</span>;  <span class="hljs-keyword">while</span>(array_[index].second!=value)&#123;    index++;  &#125;  <span class="hljs-keyword">return</span> index;&#125;<span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">ValueType <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::ValueAt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> <span class="hljs-keyword">const</span> </span>&#123;  ValueType value=array_[index].second;  <span class="hljs-keyword">return</span> value;&#125;</code></pre></div></li><li><p>接着是通过key从当前节点查找下一节点，如下所示（具体实现看代码注释）：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">ValueType <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::Lookup</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-comment">//采用二分查找</span>  <span class="hljs-keyword">int</span> left=<span class="hljs-number">1</span>;  <span class="hljs-keyword">int</span> right=<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>;  <span class="hljs-keyword">int</span> mid;  <span class="hljs-keyword">while</span>(left&lt;=right)&#123;    mid=left+(right-left)/<span class="hljs-number">2</span>;    <span class="hljs-keyword">int</span> res=<span class="hljs-built_in">comparator</span>(array_[mid].first,key);    <span class="hljs-keyword">if</span>(res==<span class="hljs-number">0</span>)&#123;<span class="hljs-comment">//如果找到直接返回</span>      <span class="hljs-keyword">return</span> array_[mid].second;    &#125;    <span class="hljs-keyword">if</span>(res&lt;<span class="hljs-number">0</span>)&#123;      left=mid+<span class="hljs-number">1</span>;    &#125;    <span class="hljs-keyword">if</span>(res&gt;<span class="hljs-number">0</span>)&#123;      right=mid<span class="hljs-number">-1</span>;    &#125;  &#125;  <span class="hljs-keyword">int</span> target=left;  <span class="hljs-comment">//如果key比array中的所有key都大，就返回最后一个key的value</span>  <span class="hljs-keyword">if</span>(target&gt;=<span class="hljs-built_in">GetSize</span>())&#123;    <span class="hljs-keyword">return</span> array_[<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>].second;  &#125;  <span class="hljs-comment">//否则返回target-1的value</span>  <span class="hljs-keyword">return</span> array_[target<span class="hljs-number">-1</span>].second;&#125;</code></pre></div></li><li><p>我们先来实现<code>CopyNFrom</code>函数，这个函数的意思就是最简单的插入操作，至于其他更复杂的操作则在其他函数实现。那么这就很简单了，我们可以直接将key-value插入进去。在这里我们用到了copy函数，我们来说一说:</p><div class="code-wrapper"><pre><code class="hljs c++">std::<span class="hljs-built_in">copy</span>(iterator source_first, iterator source_end, iterator target_start);</code></pre></div><p>在这里，我们设置<code>source_first=items</code>，设置<code>source_end=items+size</code>，设置目标迭代器为array_，这样我们就将items到items+size的元素复制到了array。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::CopyNFrom</span><span class="hljs-params">(MappingType *items, <span class="hljs-keyword">int</span> size, BufferPoolManager *buffer_pool_manager)</span> </span>&#123;  std::<span class="hljs-built_in">copy</span>(items,items+size,array_);  <span class="hljs-built_in">IncreaseSize</span>(size);&#125;</code></pre></div></li><li><p>然后是<code>PopulateNewRoot</code>函数，这里模拟的是新建一个root页面，在上面我们已经将过了，直接贴代码：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::PopulateNewRoot</span><span class="hljs-params">(<span class="hljs-keyword">const</span> ValueType &amp;old_value, <span class="hljs-keyword">const</span> KeyType &amp;new_key,<span class="hljs-keyword">const</span> ValueType &amp;new_value)</span> </span>&#123;  <span class="hljs-comment">//oldvalue为第一个值</span>  array_[<span class="hljs-number">0</span>].second=old_value;  <span class="hljs-comment">//后面一对键值对</span>  <span class="hljs-built_in">SetKeyAt</span>(<span class="hljs-number">1</span>,new_key);  array_[<span class="hljs-number">1</span>].second=new_value;  <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">2</span>);&#125;</code></pre></div></li><li><p><code>InsertNodeAfter</code>函数，在old_value后面加入新的key-value对，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::InsertNodeAfter</span><span class="hljs-params">(<span class="hljs-keyword">const</span> ValueType &amp;old_value, <span class="hljs-keyword">const</span> KeyType &amp;new_key,<span class="hljs-keyword">const</span> ValueType &amp;new_value)</span> </span>&#123;  <span class="hljs-comment">//在旧的value之后插入新的key-value</span>  <span class="hljs-keyword">int</span> index= <span class="hljs-built_in">ValueIndex</span>(old_value);  <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>;i&gt;index;i--)&#123;    array_[i+<span class="hljs-number">1</span>]=array_[i];  &#125;  array_[index+<span class="hljs-number">1</span>]=&#123;new_key,new_value&#125;;  <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">1</span>);  <span class="hljs-keyword">return</span> <span class="hljs-built_in">GetSize</span>();&#125;</code></pre></div></li><li><p>然后我们来实现分裂操作，也就是节点中元素数量大于Max_Size时，我们需要做的操作，如下图所示。其中，<code>recipient</code>是分裂出来的page，，分裂的时候移动一半键值对去<code>recipient</code>page，这个时候当前page的键值对数量是等于M的。</p></li></ul><p>  代码如下：</p>  <div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::MoveHalfTo</span><span class="hljs-params">(BPlusTreeInternalPage *recipient,BufferPoolManager *buffer_pool_manager)</span> </span>&#123;  <span class="hljs-comment">// 拷贝</span>  <span class="hljs-keyword">int</span> last_index = <span class="hljs-built_in">GetSize</span>() - <span class="hljs-number">1</span>;  <span class="hljs-keyword">int</span> start = last_index / <span class="hljs-number">2</span> + <span class="hljs-number">1</span>;  <span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>;  <span class="hljs-keyword">int</span> j = start;  <span class="hljs-keyword">while</span> (j &lt;= last_index) &#123;    recipient-&gt;array_[i].first = array_[j].first;    recipient-&gt;array_[i].second = array_[j].second;    i++;    j++;  &#125;  <span class="hljs-comment">// 维护size</span>  <span class="hljs-built_in">SetSize</span>(start);  recipient-&gt;<span class="hljs-built_in">SetSize</span>(last_index - start + <span class="hljs-number">1</span>);  <span class="hljs-comment">// 维护孩子节点的parent_page_id，根据valueat函数得到page_id，子节点的page_id，</span>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; recipient-&gt;<span class="hljs-built_in">GetSize</span>(); i++) &#123;    <span class="hljs-keyword">auto</span> page_id = recipient-&gt;<span class="hljs-built_in">ValueAt</span>(i);    <span class="hljs-keyword">auto</span> page = buffer_pool_manager-&gt;<span class="hljs-built_in">FetchPage</span>(page_id);    BPlusTreePage *bp = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(page-&gt;<span class="hljs-built_in">GetData</span>());    bp-&gt;<span class="hljs-built_in">SetParentPageId</span>(recipient-&gt;<span class="hljs-built_in">GetPageId</span>());    buffer_pool_manager-&gt;<span class="hljs-built_in">UnpinPage</span>(page_id, <span class="hljs-literal">true</span>);  &#125;&#125;</code></pre></div><ul><li><p>对于删除操作，我们首先要实现<code>Remove</code>函数，利用<code>copy</code>函数实现，这里就不贴代码了。</p></li><li><p>接下来我们重点要实现的就是这几个Move函数，对于这几个函数，我们会用到第一个实验中的buffer_pool_manager，我们挑一个函数进行讲解。</p><p>对于<code>MoveAllTo</code>函数，我们要将节点中的所有键值对删除到“收件人”节点。此外我们还要将孩子节点的父节点转化为recipient，</p></li></ul><p>​        除此之外，我们还要将该节点的父节点的子节点换成recipient。代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_INTERNAL_PAGE_TYPE::MoveAllTo</span><span class="hljs-params">(BPlusTreeInternalPage *recipient, <span class="hljs-keyword">const</span> KeyType &amp;middle_key,</span></span><span class="hljs-params"><span class="hljs-function">                                               BufferPoolManager *buffer_pool_manager)</span> </span>&#123;  <span class="hljs-keyword">auto</span> parent_page = <span class="hljs-keyword">reinterpret_cast</span>&lt;B_PLUS_TREE_INTERNAL_PAGE_TYPE *&gt;  (buffer_pool_manager-&gt;<span class="hljs-built_in">FetchPage</span>(<span class="hljs-built_in">GetParentPageId</span>())-&gt;<span class="hljs-built_in">GetData</span>());  <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i&lt; <span class="hljs-built_in">GetSize</span>(); i++)&#123;    <span class="hljs-comment">//获取孩子节点页面</span>    <span class="hljs-keyword">auto</span> page = buffer_pool_manager-&gt;<span class="hljs-built_in">FetchPage</span>(array_[i].second);    <span class="hljs-keyword">auto</span> child_page = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(page-&gt;<span class="hljs-built_in">GetData</span>());    <span class="hljs-comment">//将孩子节点页面的父节点转换为recipient</span>    child_page-&gt;<span class="hljs-built_in">SetParentPageId</span>(recipient-&gt;<span class="hljs-built_in">GetPageId</span>());    buffer_pool_manager-&gt;<span class="hljs-built_in">UnpinPage</span>(child_page-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">true</span>);  &#125;  <span class="hljs-keyword">int</span> prior_size = recipient-&gt;<span class="hljs-built_in">GetSize</span>();  recipient-&gt;<span class="hljs-built_in">SetKeyAt</span>(prior_size, middle_key);  recipient-&gt;<span class="hljs-built_in">SetValueAt</span>(prior_size, <span class="hljs-built_in">ValueAt</span>(<span class="hljs-number">0</span>));  recipient-&gt;<span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">1</span>); <span class="hljs-comment">//注意别忘了更新</span>  recipient-&gt;<span class="hljs-built_in">CopyNFrom</span>(array_+<span class="hljs-number">1</span>, <span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>, buffer_pool_manager);  <span class="hljs-built_in">SetSize</span>(<span class="hljs-number">0</span>);  buffer_pool_manager-&gt;<span class="hljs-built_in">UnpinPage</span>(parent_page-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">true</span>);&#125;</code></pre></div><h6 id="B-TREE-LEAF-PAGE"><a href="#B-TREE-LEAF-PAGE" class="headerlink" title="B+TREE LEAF PAGE"></a>B+TREE LEAF PAGE</h6><p>在这里leafpage的value是RID，什么是RID呢？RID=page_id + slot number。</p><ul><li><p>一些简单的set/get函数，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">page_id_t</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::GetNextPageId</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> next_page_id_; &#125;<span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::SetNextPageId</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> next_page_id)</span> </span>&#123;  next_page_id_=next_page_id;&#125;</code></pre></div></li><li><p>KeyIndex函数要求我们找到第一个大于key的元素的索引，我们还是可以用二分法进行查找，代码如下所示，这里就不写注释了：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::KeyIndex</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">int</span> left=<span class="hljs-number">0</span>;  <span class="hljs-keyword">int</span> right=<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>;  <span class="hljs-keyword">while</span>(left&lt;=right)&#123;    <span class="hljs-keyword">int</span> mid=left+(right-left)/<span class="hljs-number">2</span>;    <span class="hljs-keyword">int</span> res=<span class="hljs-built_in">comparator</span>(key,array_[mid].first);    <span class="hljs-keyword">if</span>(res==<span class="hljs-number">0</span>)&#123;      <span class="hljs-keyword">return</span> mid;    &#125;    <span class="hljs-keyword">if</span>(res&gt;<span class="hljs-number">0</span>)&#123;      left=mid+<span class="hljs-number">1</span>;    &#125;<span class="hljs-keyword">else</span>&#123;      right=mid<span class="hljs-number">-1</span>;    &#125;  &#125;  <span class="hljs-keyword">return</span> right+<span class="hljs-number">1</span>;&#125;</code></pre></div></li><li><p>返回index对应的key，以及返回index对应的key-value对，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">KeyType <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::KeyAt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-comment">// replace with your own code</span>  KeyType key = array[index].first;  <span class="hljs-keyword">return</span> key;&#125;<span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">const</span> MappingType &amp;<span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::GetItem</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;  <span class="hljs-keyword">return</span> array[index];&#125;</code></pre></div></li><li><p>leafpage的插入操作，我们用上文中的<code>KeyIndex</code>函数找到相应的index，然后插入进去，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::Insert</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">const</span> ValueType &amp;value, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> </span>&#123;  <span class="hljs-keyword">int</span> index=<span class="hljs-built_in">KeyIndex</span>(key,comparator);  <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>;i&gt;=index;i--)&#123;    array_[i+<span class="hljs-number">1</span>]=array_[i];  &#125;  array_[index].first=key;  array_[index].second=value;  <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">1</span>);  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div></li><li><p><code>MoveFirstToEndOf</code>函数：将此页面中的第一个键值对删除到recipient页面。</p><p>在这个函数中要调用<code>CopyLastFrom</code>函数，我们将第一个键值读取出来用<code>CopyLastFrom</code>函数写入recipient中，然后用copy函数将当前page array向右边移动一格。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::MoveFirstToEndOf</span><span class="hljs-params">(BPlusTreeLeafPage *recipient)</span> </span>&#123;  MappingType item=array_[<span class="hljs-number">0</span>];  recipient-&gt;<span class="hljs-built_in">CopyLastFrom</span>(item);  std::<span class="hljs-built_in">copy</span>(array_+<span class="hljs-number">1</span>, array_+<span class="hljs-built_in">GetSize</span>(), array_);<span class="hljs-comment">//当前page元素均向右移动一格</span>  <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">-1</span>);&#125;</code></pre></div><p>相同的道理，<code>MoveLastToFrontOf</code>如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::MoveLastToFrontOf</span><span class="hljs-params">(BPlusTreeLeafPage *recipient)</span> </span>&#123;      <span class="hljs-comment">/*</span><span class="hljs-comment">      * 更新recipient page的信息</span><span class="hljs-comment">      */</span>  MappingType item=array_[<span class="hljs-built_in">GetSize</span>()<span class="hljs-number">-1</span>];  recipient-&gt;<span class="hljs-built_in">CopyFirstFrom</span>(item);  <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">-1</span>);&#125;</code></pre></div></li></ul><h5 id="debug日志（这里就是我debug的心得了，大家不必看，我只是写着凑字数"><a href="#debug日志（这里就是我debug的心得了，大家不必看，我只是写着凑字数" class="headerlink" title="debug日志（这里就是我debug的心得了，大家不必看，我只是写着凑字数"></a>debug日志（这里就是我debug的心得了，大家不必看，我只是写着凑字数</h5><p>注意：在此时我还没有完成构建二叉树的任务，这一部分的测试是根据而他人的代码调试的。</p><p>大家看这种情况，说明我的max_size并没有设置好，我们来debug：</p><p>这里错误就很明显了我们找到对应的位置，原来insert函数写错了,应该返回size但是我误写成返回0了，当我更改后变成这个这个样子：</p><p>那么在这里为什么会有两个5呢，被insert了两次？其实这里是test文件中的测试方式，测试插入相同的元素会不会有变化。那么如果我们不准插入相同的key，能不能解决这个问题呢？（这其实是后面生成树的时候的函数中的错误，但是我们还是在leafpage的insert中进行一个判断，更改代码后如下所示：</p><p>这个时候我们发现已经基本正确了，但是最后一个叶子节点的指向有问题，所以我们还要debug。</p><p>当我看了网上人家的代码后，我发现错误是<code>MoveHalfTo</code>函数，在这个函数中要求我们更新<code>recipient</code>页面的nextpage，当我按照提示设置<code>nextpaage</code>之后却发现出现了上述情况，这到底是哪里错了我暂且蒙古。删除相关语句后如下所示。</p><p>当我自己完成了整个<code>b_plus_tree</code>的函数之后，我再次进行测试，发现出现如下错误：</p><p>这个错误也是相当离谱的，说明我们设置的min_size没有成功而且只发生在leaf_page，但是我们之前的测试是正确的，也就是说错误应该是在<code>b_plus_tree</code>的函数里面，如果我们这个错误不解决，那么后面的<code>coalesce</code>函数肯定会出错。但先不管了，我们先完成后面的函数。</p><h5 id="TASK-2-A-B-TREE-DATA-STRUCTURE-INSERTION-amp-POINT-SEARCH"><a href="#TASK-2-A-B-TREE-DATA-STRUCTURE-INSERTION-amp-POINT-SEARCH" class="headerlink" title="TASK #2.A - B+TREE DATA STRUCTURE (INSERTION &amp; POINT SEARCH)"></a>TASK #2.A - B+TREE DATA STRUCTURE (INSERTION &amp; POINT SEARCH)</h5><p>在这个任务中我们要实现<code>b_plus_tree.cpp</code>中的函数，才能完成一个完整的b+树结构。（到这里就开始折磨了</p><p>在这里补充一下RID的结构，</p><p>我们还是先来分析一下给定的<code>b_plus_tree</code>类中的数据结构，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">// member variable</span>std::string index_name_;<span class="hljs-comment">//</span><span class="hljs-keyword">page_id_t</span> root_page_id_;<span class="hljs-comment">//root_page_id,用来引导整个b+树</span>BufferPoolManager *buffer_pool_manager_;<span class="hljs-comment">//</span>KeyComparator comparator_;<span class="hljs-keyword">int</span> leaf_max_size_;<span class="hljs-comment">//b+树的叶子节点的最大容量</span><span class="hljs-keyword">int</span> internal_max_size_;<span class="hljs-comment">//中间节点的最大容量</span></code></pre></div><ul><li><p>首先是<code>IsEmpty</code>函数，对于该函数，我们只要判断root_page_id是否有效就可以判断b+树是否为空，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">BPLUSTREE_TYPE::IsEmpty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">return</span> root_page_id_==INVALID_PAGE_ID;&#125;</code></pre></div></li><li><p><code>IsEmpty</code>函数往往与<code>StartNewTree</code>函数一起调用，对于<code>StartNewTree</code>函数来说，我们首先将root_page_id对应的页面写到buffer pool，然后然后对其进行初始化，将key-value插入进去，最后unpin。关键代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++">Page *new_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">NewPage</span>(&amp;root_page_id_)<span class="hljs-comment">//to buffer pool</span>LeafPage *leaf_page = <span class="hljs-keyword">reinterpret_cast</span>&lt;LeafPage *&gt;(new_page);leaf_page-&gt;<span class="hljs-built_in">Init</span>(root_page_id_,INVALID_PAGE_ID,leaf_max_size_);<span class="hljs-comment">//初始化</span>leaf_page-&gt;<span class="hljs-built_in">Insert</span>(key, value, comparator_);<span class="hljs-comment">//insert</span>buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(root_page_id_, <span class="hljs-literal">true</span>);  <span class="hljs-comment">// unpin</span></code></pre></div></li><li><p><code>InsertIntoLeaf</code>函数，将常量键值对插入叶页，用户需要先找到正确的叶子页面作为插入目标，然后再看通过叶子页面查看插入键是否存在。 如果存在，返回，否则插入条目。 如有必要，请记住处理拆分。因为我们只支持唯一键，如果用户尝试插入重复键返回 false，否则返回 true。注意，我们这里判断是否分裂用的是<code>if (leaf_page-&gt;GetSize() == maxSize(leaf_page) + 1)</code>。做到这一步，我们发现其实还必须得搞懂<code>FindLeafPage</code>这个函数，那么这个函数咋搞呢？先看其本身的代码：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">//这个时候我是有点疑惑的，如果说这个函数要的是返回参数键所在的页面，但是如果键没有在页面之中应该返回什么呢？如果说键没有在页面之中，那就说明可以插入，这个时候应该怎么插入呢？其实这里的意思只是说返回离参数键最近的页面，所以我们调用的lookup函数。</span><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">Page *<span class="hljs-title">BPLUSTREE_TYPE::FindLeafPage</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">bool</span> leftMost)</span> </span>&#123;  <span class="hljs-keyword">throw</span> <span class="hljs-built_in">Exception</span>(ExceptionType::NOT_IMPLEMENTED, <span class="hljs-string">&quot;Implement this for test&quot;</span>);&#125;</code></pre></div><p>修改之后：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">Page *<span class="hljs-title">BPLUSTREE_TYPE::FindLeafPage</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">bool</span> leftMost)</span> </span>&#123;  Page *page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(root_page_id_);  BPlusTreePage *node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(page-&gt;<span class="hljs-built_in">GetData</span>());  <span class="hljs-comment">//如果说root节点就是叶子节点，那么我们直接返回这个节点？</span>  <span class="hljs-keyword">if</span>(node-&gt;<span class="hljs-built_in">IsLeafPage</span>())&#123;    <span class="hljs-keyword">return</span> page;  &#125;  <span class="hljs-comment">//如果说不是的话</span>  <span class="hljs-keyword">while</span> (!node-&gt;<span class="hljs-built_in">IsLeafPage</span>()) &#123;<span class="hljs-comment">//循环查找</span>    InternalPage *i_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;InternalPage *&gt;(node);<span class="hljs-comment">//强转为inner节点</span>    <span class="hljs-keyword">page_id_t</span> child_node_page_id;    <span class="hljs-keyword">if</span>(leftMost)&#123;<span class="hljs-comment">//这是最理想的情况</span>      child_node_page_id = i_node-&gt;<span class="hljs-built_in">ValueAt</span>(<span class="hljs-number">0</span>);<span class="hljs-comment">//此时已经获取了最左边的page_id了</span>    &#125;<span class="hljs-keyword">else</span>&#123;      child_node_page_id = i_node-&gt;<span class="hljs-built_in">Lookup</span>(key,comparator_);          &#125;    <span class="hljs-keyword">auto</span> child_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(child_node_page_id);<span class="hljs-comment">//根据id获取页面</span>    <span class="hljs-keyword">auto</span> child_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(child_page-&gt;<span class="hljs-built_in">GetData</span>());    node=child_node;    page=child_page;  &#125;  <span class="hljs-keyword">return</span> page;&#125;</code></pre></div><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">//如果需要新建树</span>  <span class="hljs-keyword">if</span> (<span class="hljs-built_in">IsEmpty</span>()) &#123;    <span class="hljs-built_in">StartNewTree</span>(key, value);    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  &#125;  <span class="hljs-comment">// 1. if insert key is exist</span>  <span class="hljs-keyword">if</span> (leaf_page-&gt;<span class="hljs-built_in">Lookup</span>(key, <span class="hljs-literal">nullptr</span>, comparator_)) &#123;    buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(leaf_page-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">false</span>); <span class="hljs-comment">// unpined page</span>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-comment">// 1. if insert key is exist</span>  <span class="hljs-keyword">if</span> (leaf_page-&gt;<span class="hljs-built_in">Lookup</span>(key, <span class="hljs-literal">nullptr</span>, comparator_)) &#123;    buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(leaf_page-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">false</span>); <span class="hljs-comment">// unpined page</span>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;   <span class="hljs-comment">// 2. insert entry</span>  leaf_page-&gt;<span class="hljs-built_in">Insert</span>(key, value, comparator_);     <span class="hljs-keyword">if</span> (leaf_page-&gt;<span class="hljs-built_in">GetSize</span>() == <span class="hljs-built_in">maxSize</span>(leaf_page) + <span class="hljs-number">1</span>) &#123;    LeafPage *new_leaf = <span class="hljs-built_in">Split</span>(leaf_page);    <span class="hljs-built_in">InsertIntoParent</span>(leaf_page, new_leaf-&gt;<span class="hljs-built_in">KeyAt</span>(<span class="hljs-number">0</span>), new_leaf, transaction);  &#125;</code></pre></div></li><li><p>我们的<code>Split</code>函数和<code>InsertIntoParent</code>函数是分开的，我们用下面两幅图来解释这个过程：</p></li></ul><p>  上面这两幅图解释了<code>split</code>和<code>insertintoparent</code>这两个过程。</p><p>  在这里，如果说old_node是根节点，那么整棵树直接升高一层，我们创建一个新节点R作为根节点，其关键字为key，左右孩子分别为old_node和new_node，如上图所示。</p>  <div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">if</span> (old_node-&gt;<span class="hljs-built_in">IsRootPage</span>()) &#123;  <span class="hljs-comment">// old node为根结点</span>   <span class="hljs-keyword">page_id_t</span> new_page_id = INVALID_PAGE_ID;   <span class="hljs-comment">//拿一个新的页面到buffer pool，最为新的根节点写入</span>   Page *new_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">NewPage</span>(&amp;new_page_id);   root_page_id_ = new_page_id;<span class="hljs-comment">// 注意初始化parent page id和max_size</span>   InternalPage *new_root_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;InternalPage *&gt;(new_page-&gt;<span class="hljs-built_in">GetData</span>());   new_root_node-&gt;<span class="hljs-built_in">Init</span>(new_page_id, INVALID_PAGE_ID, internal_max_size_);     <span class="hljs-comment">// 修改新的根结点的孩子指针，即array[0].second指向old_node，array[1].second指向new_node；对于array[1].first则赋值为key</span>   new_root_node-&gt;<span class="hljs-built_in">PopulateNewRoot</span>(old_node-&gt;<span class="hljs-built_in">GetPageId</span>(), key, new_node-&gt;<span class="hljs-built_in">GetPageId</span>());   <span class="hljs-comment">// 修改old_node和new_node的父指针</span>   old_node-&gt;<span class="hljs-built_in">SetParentPageId</span>(new_page_id);   new_node-&gt;<span class="hljs-built_in">SetParentPageId</span>(new_page_id);<span class="hljs-comment">// dirty置为true</span>   buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(new_page-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">true</span>);       <span class="hljs-built_in">UpdateRootPageId</span>(<span class="hljs-number">0</span>);    <span class="hljs-keyword">return</span>;  &#125;</code></pre></div><p>  如果说old_node不是根结点，我们找到old_node的父节点进行操作，先直接插入<code>(key,new_node-&gt;page_id)</code>到父节点，如果插入后父节点满了，则需要对父结点再进行拆分(Split)，并继续递归。</p><ul><li><p><code>Split</code>函数中，用户拆分输入页面并返回新创建的页面。使用模板 N 表示内部页面或叶子页面。用户需要首先向缓冲池管理器请求新页面（注意：抛出如果返回值为 nullptr，则会出现“内存不足”异常），然后从输入页面移动一半键值对到新创建页面。</p><ul><li>这里说的抛出很好理解：</li></ul><div class="code-wrapper"><pre><code class="hljs c++">Page *new_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">NewPage</span>(&amp;root_page_id_);  <span class="hljs-keyword">if</span> (new_page == <span class="hljs-literal">nullptr</span>) &#123;    <span class="hljs-keyword">throw</span> <span class="hljs-string">&quot;out of memory&quot;</span>;  &#125;</code></pre></div><ul><li><p>对于叶子节点来说，我们除了要<code>movehalfto</code>绑定相应的父节点，还要对叶子节点链表进行更新，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++">new_leaf_node-&gt;<span class="hljs-built_in">SetNextPageId</span>(leaf_node-&gt;<span class="hljs-built_in">GetNextPageId</span>());leaf_node-&gt;<span class="hljs-built_in">SetNextPageId</span>(new_leaf_node-&gt;<span class="hljs-built_in">GetPageId</span>());</code></pre></div></li><li><p>对于内部节点，我们则只进行<code>movehalfto</code>和绑定相应的父节点：</p><div class="code-wrapper"><pre><code class="hljs c++">new_internal_node-&gt;<span class="hljs-built_in">Init</span>(new_page_id, internal_node-&gt;<span class="hljs-built_in">GetParentPageId</span>(),internal_max_size_);   internal_node-&gt;<span class="hljs-built_in">MoveHalfTo</span>(new_internal_node,buffer_pool_manager_);</code></pre></div></li></ul></li><li><p>对于<code>InsertIntoLeaf</code>函数来说</p><p>那么怎么结合<code>InsertIntoLeaf</code>函数和<code>split</code>函数呢？这两个函数的调用主要是发生在<code>insert</code>函数里面，如果插入之后使得page分裂，那么就肯定要调用<code>split</code>函数和<code>insertintoleaf</code>函数。可以参考上面的<code>insert</code>函数。</p></li><li><p>实现GET，分为3步，第一步找到那个叶子的page，在叶子page里找这个key，找到这个key之后放入result中，用完最后unpin 这个page。</p></li><li><p>对于Insert函数，如果树里面还没有数据，调用StartNewTree函数新建，否则调用InsertIntoLeaf函数插入。</p></li><li><p>对于FindLeafPage函数，他的参数为key和leftmost，我们要找到包含key的节点，如果leftmost为true，那么就不管key，而去找到最左边的叶子节点，否则，找到包含key的叶子节点。</p><p>这里我们就可以分成true和false两种情况，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">page_id_t</span> next_page_id = leftMost ? internal_node-&gt;<span class="hljs-built_in">ValueAt</span>(<span class="hljs-number">0</span>) : internal_node-&gt;<span class="hljs-built_in">Lookup</span>(key, comparator_);</code></pre></div></li></ul><h5 id="TASK-2-B-B-TREE-DATA-STRUCTURE-DELETION"><a href="#TASK-2-B-B-TREE-DATA-STRUCTURE-DELETION" class="headerlink" title="TASK #2.B - B+TREE DATA STRUCTURE (DELETION)"></a>TASK #2.B - B+TREE DATA STRUCTURE (DELETION)</h5><ul><li>首先我们要在<code>b_plus_tree_leaf</code>函数中完成<code>RemoveAndDeleteRecord</code>函数，根据提示可以很快完成该函数，如下所示：</li></ul><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::RemoveAndDeleteRecord</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> </span>&#123;  <span class="hljs-keyword">int</span> index= <span class="hljs-built_in">KeyIndex</span>(key,comparator);  <span class="hljs-keyword">if</span>(index &lt; <span class="hljs-built_in">GetSize</span>() &amp;&amp; (<span class="hljs-built_in">comparator</span>(key, array_[index].first) == <span class="hljs-number">0</span>) ) &#123;    std::<span class="hljs-built_in">copy</span>(array_ + index + <span class="hljs-number">1</span>, array_ + <span class="hljs-built_in">GetSize</span>(), array_ + index);    <span class="hljs-built_in">IncreaseSize</span>(<span class="hljs-number">-1</span>);  &#125;  <span class="hljs-keyword">return</span> <span class="hljs-built_in">GetSize</span>();&#125;</code></pre></div><p>如果删除导致某些页面低于占用率阈值，您的 B+Tree 索引应该正确执行合并或重新分配。</p><ul><li><p><code>Coalesce</code>函数，将所有键值对从一个页面移动到其兄弟页面，并通知缓冲池管理器删除此页。 父页面必须调整为</p><ul><li>考虑删除信息。 记得处理 coalesce 或</li><li>必要时递归重新分配。</li><li>使用模板 N 表示内部页面或叶子页面。</li><li>@param neighbor_node 输入“节点”的兄弟页面</li><li>@param 来自方法 coalesceOrRedistribute() 的节点输入</li><li>@param parent 输入“节点”的父页面</li><li>@return true 表示要删除父节点，false 表示不删除</li><li>发生</li></ul></li><li><p>对于<code>AdjustRoot</code>函数，最重要的是判断其是否应该删除掉根节点，这里有两种情况我们是要删除掉根节点的，也就是根节点是leaf_节点且其size为0,或者其不是leaf_page其size为1，至于为什么这样请看我前面的。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">if</span> (old_root_node-&gt;<span class="hljs-built_in">IsLeafPage</span>() &amp;&amp; old_root_node-&gt;<span class="hljs-built_in">GetSize</span>() == <span class="hljs-number">0</span>) &#123;    root_page_id_ = INVALID_PAGE_ID;    <span class="hljs-built_in">UpdateRootPageId</span>(<span class="hljs-literal">false</span>);    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  &#125;  <span class="hljs-keyword">if</span> (!old_root_node-&gt;<span class="hljs-built_in">IsLeafPage</span>() &amp;&amp; old_root_node-&gt;<span class="hljs-built_in">GetSize</span>() == <span class="hljs-number">1</span>) &#123;    <span class="hljs-keyword">auto</span> internal_page = <span class="hljs-keyword">reinterpret_cast</span>&lt;InternalPage *&gt;(old_root_node);    <span class="hljs-keyword">auto</span> new_root_id = internal_page-&gt;<span class="hljs-built_in">RemoveAndReturnOnlyChild</span>();    <span class="hljs-keyword">auto</span> new_root_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(new_root_id);    <span class="hljs-keyword">auto</span> new_root_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(new_root_page);    new_root_node-&gt;<span class="hljs-built_in">SetParentPageId</span>(new_root_id);    root_page_id_ = new_root_id;    buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(new_root_node-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">true</span>);    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  &#125;  <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;</code></pre></div></li></ul><p>​            结果如上图所示，我们的输入文件中只有一个元素1，当我们删除掉这个元素之后，我们再生成dot文件，就会出现生成文件为空的提示。</p><h5 id="debug日志"><a href="#debug日志" class="headerlink" title="debug日志"></a>debug日志</h5><p>当我们进行<code>b_plus_tree_leaf_page_test</code>测试时，出现了如下错误：</p><div class="code-wrapper"><pre><code class="hljs c++">#<span class="hljs-number">0</span> <span class="hljs-number">0x7f4598902c1d</span> in bustub::BPlusTreeLeafPage&lt;bustub::GenericKey&lt;<span class="hljs-number">8ul</span>&gt;, bustub::RID, bustub::GenericComparator&lt;<span class="hljs-number">8ul</span>&gt; &gt;::<span class="hljs-built_in">Lookup</span>(bustub::GenericKey&lt;<span class="hljs-number">8ul</span>&gt; <span class="hljs-keyword">const</span>&amp;, bustub::RID*, bustub::GenericComparator&lt;<span class="hljs-number">8ul</span>&gt; <span class="hljs-keyword">const</span>&amp;) <span class="hljs-keyword">const</span> /home/databrains/CLionProjects/bustub-master/src/storage/page/b_plus_tree_leaf_page.cpp:<span class="hljs-number">158</span></code></pre></div><p>这个错误的出现是由于数组越界，而<code>keyindex</code>函数和<code>array_</code>的不当使用确实会引起数组越界，于是我们对其进行一些修改：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::Lookup</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, ValueType *value, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">int</span> pos = <span class="hljs-built_in">KeyIndex</span>(key, comparator);  <span class="hljs-keyword">if</span>(pos&gt;=<span class="hljs-built_in">GetSize</span>()||<span class="hljs-built_in">comparator</span>(key, array_[pos].first) == <span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  *value=array_[pos].second;  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div><p>发现这样是治标不治本，其原因在于我们在检查是否含有key的时候入的value实参往往是<code>nullptr</code>，所以我们给它增加一个判断：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">B_PLUS_TREE_LEAF_PAGE_TYPE::Lookup</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, ValueType *value, <span class="hljs-keyword">const</span> KeyComparator &amp;comparator)</span> <span class="hljs-keyword">const</span> </span>&#123;  <span class="hljs-keyword">int</span> pos = <span class="hljs-built_in">KeyIndex</span>(key, comparator);  <span class="hljs-keyword">if</span>(pos&gt;=<span class="hljs-built_in">GetSize</span>()||<span class="hljs-built_in">comparator</span>(key, array_[pos].first) != <span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-keyword">if</span>(value== <span class="hljs-literal">nullptr</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  &#125;  (*value)=array_[pos].second;  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div><p>我们进行以下测试，其中测试文件为1,2,3,4,5,6：</p><div class="code-wrapper"><pre><code class="hljs node-repl">#./test/b_plus_tree_print_test<span class="hljs-meta">&gt;</span> <span class="javascript"><span class="hljs-number">3</span> <span class="hljs-number">3</span></span><span class="hljs-meta">&gt;</span> <span class="javascript">f input.txt</span><span class="hljs-meta">&gt;</span> <span class="javascript">d <span class="hljs-number">4</span></span><span class="hljs-meta">&gt;</span> <span class="javascript">g tree.dot</span><span class="hljs-meta">&gt;</span> <span class="javascript">q</span></code></pre></div><p>得到如下结果：</p><p>我们发现有两个错误，第一个错误是叶子节点的<code>min_size</code>，第二个错误是没有完全删除掉page，我们先来解决第二个错误：</p><p>在这里<code>minsize</code>依然是1，说明是<code>InsertIntoParent</code>或<code>Split</code>函数的错误，当我们的input文件只有一个数据1时，其输出如下：</p><p>这也佐证了我们的说法。</p><p>当输入为1，2，3，4，5，6的时候，DEBUG如下：</p><p>在<code>InsertIntoParent</code>函数中就已经变成这样了，经过多次排查，问题出在<code>Split</code>函数当中，妈的终于找到错误了，原来是<code>GetMinSize</code>写错了。。。</p><p>我们现在看到第一个错误，在这里我认为应该删除没有数据的leaf和其父节点中相应的键值对，但其实不删除也没错。健壮的函数会保证其增删查改的正确性，只不过开销会更大。</p><h5 id="TASK-3-INDEX-ITERATOR"><a href="#TASK-3-INDEX-ITERATOR" class="headerlink" title="TASK #3 - INDEX ITERATOR"></a>TASK #3 - INDEX ITERATOR</h5><p>我们都知道，在B+树中，叶子节点link起来，可以实现快速range scan。在这个任务中，我们要构建一个通用索引迭代器来有效地检索所有叶子页面。其中，Begin函数返回一个状态为初始化的迭代器对象，End返回一个指向结束的迭代器对象。</p><p>我们先来看看iterator中的几个成员：</p><div class="code-wrapper"><pre><code class="hljs c++">BufferPoolManager *buffer_pool_manager_;<span class="hljs-comment">//用来操作当前被写入buffer pool的节点</span>Page *page_;<span class="hljs-keyword">int</span> index_;LeafPage *leaf_;</code></pre></div><ul><li><p>对于Begin函数，我们先直接用FindLeafPage找到最左边的节点，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++">Page *leaf_page = <span class="hljs-built_in">FindLeafPage</span>(<span class="hljs-built_in">KeyType</span>(), <span class="hljs-literal">true</span>);<span class="hljs-keyword">return</span> <span class="hljs-built_in">INDEXITERATOR_TYPE</span>(buffer_pool_manager_, leaf_page, <span class="hljs-number">0</span>);</code></pre></div></li><li><p>对于End函数，入参为void，构造一个代表结束的索引迭代器，叶节点中的键/值对，return : 索引迭代器。我们先找到最左边的叶子节点，然后从最左边移动到最右边。</p><div class="code-wrapper"><pre><code class="hljs c++">Page *leaf_page = <span class="hljs-built_in">FindLeafPage</span>(<span class="hljs-built_in">KeyType</span>(), <span class="hljs-literal">true</span>);LeafPage *leaf_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;LeafPage *&gt;(leaf_page-&gt;<span class="hljs-built_in">GetData</span>());<span class="hljs-keyword">while</span> (leaf_node-&gt;<span class="hljs-built_in">GetNextPageId</span>() != INVALID_PAGE_ID) &#123;    <span class="hljs-keyword">page_id_t</span> next_page_id = leaf_node-&gt;<span class="hljs-built_in">GetNextPageId</span>();    buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(leaf_node-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">false</span>);  <span class="hljs-comment">// page unpinned</span>    Page *next_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(next_page_id);  <span class="hljs-comment">// next_page pinned</span>    leaf_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;LeafPage *&gt;(next_page-&gt;<span class="hljs-built_in">GetData</span>());  &#125;<span class="hljs-keyword">return</span> <span class="hljs-built_in">INDEXITERATOR_TYPE</span>(buffer_pool_manager_, leaf_page, leaf_node-&gt;<span class="hljs-built_in">GetSize</span>());</code></pre></div></li><li><p>重载++，在这个部分的时候，我们可以看看其他模板类中的迭代器++是怎么重载的，在这里我们只要搞清楚leaf range的逻辑就可以了，index代表当前叶子节点中的键值对的序号，如果跳到下一个节点，那么index将从零重新开始运算，最后返回的是当前对象。</p><div class="code-wrapper"><pre><code class="hljs c++">index_++;  <span class="hljs-keyword">if</span> (index_ == leaf_-&gt;<span class="hljs-built_in">GetSize</span>() &amp;&amp; leaf_-&gt;<span class="hljs-built_in">GetNextPageId</span>() != INVALID_PAGE_ID) &#123;<span class="hljs-comment">//如果index等于节点的长度，且节点不是最后一个节点，那么我们就要跳到下一个节点</span>    Page *next_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(leaf_-&gt;<span class="hljs-built_in">GetNextPageId</span>());  <span class="hljs-comment">// pin next leaf page</span>    buffer_pool_manager_-&gt;<span class="hljs-built_in">UnpinPage</span>(page_-&gt;<span class="hljs-built_in">GetPageId</span>(), <span class="hljs-literal">false</span>);  <span class="hljs-comment">//unpin当前page</span>        page_ = next_page;    leaf_ = <span class="hljs-keyword">reinterpret_cast</span>&lt;LeafPage *&gt;(page_-&gt;<span class="hljs-built_in">GetData</span>());  <span class="hljs-comment">// 更新到下一个节点</span>    index_ = <span class="hljs-number">0</span>;                                              <span class="hljs-comment">// 重置index</span>  &#125;  <span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;</code></pre></div></li><li><p>重载*，这里的返回的是当前的键值对。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">return</span> leaf_-&gt;<span class="hljs-built_in">GetItem</span>(index_);<span class="hljs-comment">//这个没啥好说的，返回相应的key-value就行</span></code></pre></div></li><li><p>重载==，怎么判断两个迭代器对象是否相等，我们只需要判断二者的位置是否相等就可以了，也就是先比较二者是否处于同一个叶子节点，再比较二者的index值是否相等。</p><div class="code-wrapper"><pre><code class="hljs c++">INDEX_TEMPLATE_ARGUMENTS<span class="hljs-keyword">bool</span> INDEXITERATOR_TYPE::<span class="hljs-keyword">operator</span>==(<span class="hljs-keyword">const</span> IndexIterator &amp;itr) <span class="hljs-keyword">const</span> &#123;  <span class="hljs-keyword">return</span> leaf_-&gt;<span class="hljs-built_in">GetPageId</span>() == itr.leaf_-&gt;<span class="hljs-built_in">GetPageId</span>() &amp;&amp; index_ == itr.index_;  <span class="hljs-comment">// leaf page和index均相同</span>&#125;</code></pre></div></li><li><p>重载!=，这里我们直接用之前重载的==就可以了。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">return</span> <span class="hljs-keyword">static_cast</span>&lt;<span class="hljs-keyword">bool</span>&gt;(*<span class="hljs-keyword">this</span> != itr);</code></pre></div></li></ul><h5 id="TASK-4-CONCURRENT-INDEX"><a href="#TASK-4-CONCURRENT-INDEX" class="headerlink" title="TASK #4 - CONCURRENT INDEX"></a>TASK #4 - CONCURRENT INDEX</h5><p>在这部分，您需要更新您原来的单线程 B+Tree 索引，使其能够支持并发操作。我们将使用课堂和教科书中描述的闩锁技术。</p><p>在<code>rwlatch.h</code>中，实现了<code>ReaderWriterLatch</code>类，我们看看其成员：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">mutex_t</span> mutex_;<span class="hljs-keyword">cond_t</span> writer_;<span class="hljs-keyword">cond_t</span> reader_;<span class="hljs-keyword">uint32_t</span> reader_count_&#123;<span class="hljs-number">0</span>&#125;;<span class="hljs-keyword">bool</span> writer_entered_&#123;<span class="hljs-literal">false</span>&#125;;</code></pre></div><p>其中，条件变量<code>writer_</code>和<code>reader_</code>，设置了<code>reader_count</code>，代表读者的引用计数。</p><p>我们来看看其底层的函数实现：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">RLock</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;<span class="hljs-keyword">mutex_t</span>&gt; <span class="hljs-title">latch</span><span class="hljs-params">(mutex_)</span></span>;  <span class="hljs-keyword">while</span> (writer_entered_ || reader_count_ == MAX_READERS) &#123;      <span class="hljs-comment">//如果上了写锁或者读者达到上限，就等待，这个时候应该是自旋</span>    reader_.<span class="hljs-built_in">wait</span>(latch);  &#125;  reader_count_++;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">WLock</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;<span class="hljs-keyword">mutex_t</span>&gt; <span class="hljs-title">latch</span><span class="hljs-params">(mutex_)</span></span>;  <span class="hljs-keyword">while</span> (writer_entered_) &#123;      <span class="hljs-comment">//如果上了写锁就不能再上了</span>    reader_.<span class="hljs-built_in">wait</span>(latch);  &#125;  writer_entered_ = <span class="hljs-literal">true</span>;  <span class="hljs-keyword">while</span> (reader_count_ &gt; <span class="hljs-number">0</span>) &#123;      <span class="hljs-comment">//如果现在还有读者也不能上</span>    writer_.<span class="hljs-built_in">wait</span>(latch);  &#125;&#125;</code></pre></div><p>这个实现挺有意思的。</p><p>当我们没有实现并发的时候，在做<code>b_plus_tree_concurrent_test</code>的时候，出错的概率不是一定的，而是根据随机的并发而不确定的，这导致我一会pass 5 test，一会pass 3 test。</p><p>所以说当务之急是完成并发。这里我参考了<a href="https://www.cnblogs.com/JayL-zxl/p/14333395.html">周小伦</a>大神的实现。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">Page *<span class="hljs-title">BPLUSTREE_TYPE::FindLeafPageRW</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">bool</span> left_most, <span class="hljs-keyword">enum</span> OpType op, Transaction *transaction)</span> </span>&#123;  Page *page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(root_page_id_);  <span class="hljs-comment">// now root page is pin</span>  BPlusTreePage *node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(page-&gt;<span class="hljs-built_in">GetData</span>());  <span class="hljs-keyword">while</span> (!node-&gt;<span class="hljs-built_in">IsLeafPage</span>()) &#123;    <span class="hljs-keyword">if</span> (op == OpType::Read) &#123;      page-&gt;<span class="hljs-built_in">RLatch</span>();      <span class="hljs-built_in">UnlatchAndUnpin</span>(op,transaction);    &#125; <span class="hljs-keyword">else</span> &#123;      <span class="hljs-comment">// else is write op</span>      page-&gt;<span class="hljs-built_in">WLatch</span>();      <span class="hljs-keyword">if</span> (<span class="hljs-built_in">IsSafe</span>(node, op)) &#123;        <span class="hljs-built_in">UnlatchAndUnpin</span>(op, transaction);      &#125;    &#125;    transaction-&gt;<span class="hljs-built_in">AddIntoPageSet</span>(page);    InternalPage *internal_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;InternalPage *&gt;(node);    <span class="hljs-keyword">page_id_t</span> next_page_id = left_most ? internal_node-&gt;<span class="hljs-built_in">ValueAt</span>(<span class="hljs-number">0</span>) : internal_node-&gt;<span class="hljs-built_in">Lookup</span>(key, comparator_);    Page *next_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(next_page_id);  <span class="hljs-comment">// next_level_page pinned</span>    BPlusTreePage *next_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(next_page);    page = next_page;    node = next_node;  &#125;  <span class="hljs-keyword">return</span> page;&#125;</code></pre></div><p>这是周小伦大神的实现。</p><h6 id="如何实现并发"><a href="#如何实现并发" class="headerlink" title="如何实现并发"></a>如何实现并发</h6><p>我们来看看Andy在lab中怎么说的（机器翻译）：</p><ul><li>搜索：从根页面开始，抓住子页面上的读取（R）闩锁，然后在您登陆子页面后立即释放父页面上的闩锁。</li><li>插入：从根页面开始，抓住子页面的写（W）锁存器。一旦孩子被锁定，检查它是否安全，在这种情况下，不是满的。如果孩子是安全的，则释放祖先的所有锁。</li><li>删除：从根页面开始，抓住子页面的写（W）锁存器。一旦孩子被锁定，检查它是否安全，在这种情况下，至少半满。 （注意：对于根页面，我们需要用不同的标准检查）如果孩子是安全的，释放所有祖先的锁。</li></ul><hr><p>我们在B+树的并发控制中要保证以下结果的正确性：</p><ul><li><strong>逻辑正确性</strong>：能否看到应该看到的结果？</li><li><strong>物理正确性</strong>：内部的数据结构表示是否被保护？（比如，在对B+树索引遍历时，当一个线程需要跳转到下一节点时，其它线程有可能会改变B+树的结构，使得指针指向为无效地址）</li></ul><h6 id="LATCH-CRABBING-COUPLING"><a href="#LATCH-CRABBING-COUPLING" class="headerlink" title="LATCH CRABBING / COUPLING"></a>LATCH CRABBING / COUPLING</h6><p>这是一个允许多线程同时访问/修改B+树的协议。其基本思想为：</p><ul><li>获得父节点的latch</li><li>获得子节点的latch</li><li>当安全的情况下才释放父节点的latch</li></ul><p>在读写锁中，当有一个 线程获得写锁定，其它无论是读锁定还是写锁定都将阻塞直到写解锁；当有一个线程获得读锁定，其它读锁定仍然可以继续；当有一个或任意多个读锁定，写锁定将等待所有读锁定解锁之后才能够进行写锁定。</p><ol><li>同时只能有一个线程能够获得写锁定。</li><li>同时可以有任意多个线程获得读锁定。</li><li>同时只能存在写锁定或读锁定（读和写互斥）。</li><li>加入<code>MaxReader</code>，来防止写进程饥饿，也就是读进程太多以至于等待时间过长而对写进程造成明显影响。</li></ol><hr><p>我们来看读写锁在B+树中的具体表现：</p><p>查找38，给根节点上读锁：</p><p><img src="https://img-blog.csdnimg.cn/20201012221558584.jpg?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>当我们来到下一个B节点，给B上了读锁，确保已经安全，释放A的读锁。</p><p><img src="https://img-blog.csdnimg.cn/20201012221656748.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p><p><img src="https://img-blog.csdnimg.cn/20201012221749942.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>以此类推，找到38。</p><p><img src="https://img-blog.csdnimg.cn/20201012221919398.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p><img src="https://img-blog.csdnimg.cn/20201012221956330.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>当我们要删除时，因为我们有可能修改了叶子节点之后会修改内部节点甚至是根节点，因此我们给每一个访问的节点都上写锁。</p><p><img src="https://img-blog.csdnimg.cn/20201012222108813.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>子节点并没超过半满，所以是不安全的，不能释放父节点的锁。</p><p><img src="https://img-blog.csdnimg.cn/20201012222151930.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>我们发现此时节点已经超过半满，可以释放祖先节点。</p><p><img src="https://img-blog.csdnimg.cn/20201012222350984.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>以此类推，直到删除数据并释放所有写锁。</p><p><img src="https://img-blog.csdnimg.cn/20201012222504107.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p><img src="https://img-blog.csdnimg.cn/20201012222542426.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0luZ3N1aWZvbg==,size_16,color_FFFFFF,t_70#pic_center" alt=""></p><p>插入操作同理，我们如果发现节点没有半满，就可以释放祖先节点的写锁。</p><p>值得注意的是，<strong>释放锁时，我们应当从上往下释放，这样可以让等待的线程尽早执行</strong>。</p><p>我们来开始上锁：</p><p>这样做可以实现并发，但是实现不了高并发，要实现高并发还是得优化算法：</p><hr><p>在上面介绍的操作过程中我们发现：</p><p>我们只有在知道了子孙节点是满足特定条件的情况下才能释放祖先节点的写锁，这样可能会造成不必要的阻塞。</p><p>我们可以一开始就使用读锁，如果找到一个满足状态的节点就说明该节点的所有祖先节点都不用使用写锁，否则就要使用写锁。</p><p>可以使用一个队列，将每次访问的节点入队，给每个节点设置一个标志位，即它的儿子节点是否满足条件。访问完叶子节点之后，我们根据标志位将节点出队。</p><p>我们看到上面那副图，我们依次访问ABDH节点，A的儿子节点B不满足条件，将A的标志位设置为0，B的儿子节点满足条件，将B的标志位设置为0，D的儿子节点满足条件，将D的标志位设置为1，叶子节点H的标志位始终为0。这样我们就得到了序列0110（从叶子节点到根节点），我们再搜索一次，找到离叶子节点最近的那个1，设置一个计数器safe_count，这个1和之前的节点都是安全的，然后我们根据计数器再次从根节点访问，就像上面那副图，其实ABD都不用上写锁，我们直接就把队列中110对应的节点DBA都上读锁且访问下一个节点之前马上释放，这样我们就只给最后一个叶子节点上了写锁。</p><hr><p>改进：</p><p>我们可以设置一个缓冲队列，将节点的指针放进去，比如A通过A-&gt;array[1].second访问B节点，我们就把这个指针放进去，然后，一旦遇到节点安全，我怕们就把访问这个节点的指针之前入队的指针出队，只留下这个访问这个节点的指针，我们搜索完一遍之后直接通过这个指针搜索这个节点，从这个节点开始上写锁，这样也达到了相同的目的，且开销更小。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">INDEX_TEMPLATE_ARGUMENTS</span><span class="hljs-function">Page *<span class="hljs-title">BPLUSTREE_TYPE::FindLeafPageRW</span><span class="hljs-params">(<span class="hljs-keyword">const</span> KeyType &amp;key, <span class="hljs-keyword">bool</span> left_most, <span class="hljs-keyword">enum</span> OpType op, Transaction *transaction,Page *safe_page)</span> </span>&#123;  Page *page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(root_page_id_);    <span class="hljs-comment">//获取root_page</span>  BPlusTreePage *node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(page-&gt;<span class="hljs-built_in">GetData</span>());     <span class="hljs-keyword">while</span> (!node-&gt;<span class="hljs-built_in">IsLeafPage</span>()) &#123;          <span class="hljs-keyword">if</span> (op == OpType::Read) &#123;<span class="hljs-comment">//读操作不变</span>      page-&gt;<span class="hljs-built_in">RLatch</span>();      <span class="hljs-built_in">UnlatchAndUnpin</span>(op,transaction);    &#125; <span class="hljs-keyword">else</span> &#123;      <span class="hljs-comment">// 读锁</span>      page-&gt;<span class="hljs-built_in">RLatch</span>();      <span class="hljs-keyword">if</span> (<span class="hljs-built_in">IsSafe</span>(node, op)) &#123;        safe_page=page;<span class="hljs-comment">//设置safe_page为当前page</span>      &#125;    &#125;    transaction-&gt;<span class="hljs-built_in">AddIntoPageSet</span>(page);    InternalPage *internal_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;InternalPage *&gt;(node);    <span class="hljs-keyword">page_id_t</span> next_page_id = left_most ? internal_node-&gt;<span class="hljs-built_in">ValueAt</span>(<span class="hljs-number">0</span>) : internal_node-&gt;<span class="hljs-built_in">Lookup</span>(key, comparator_);    Page *next_page = buffer_pool_manager_-&gt;<span class="hljs-built_in">FetchPage</span>(next_page_id);  <span class="hljs-comment">// next_level_page pinned</span>    BPlusTreePage *next_node = <span class="hljs-keyword">reinterpret_cast</span>&lt;BPlusTreePage *&gt;(next_page);    page = next_page;    node = next_node;  &#125;    <span class="hljs-keyword">return</span> page;&#125;</code></pre></div><hr><p>这样看似操作更多，但是我们上的是读锁，只是为了防止被其他进程写入，比第一种方案一直上写锁要更好我觉得，特别是在数据量很大和读进程更多的时候。</p><p>如果我们存储的数据量足够大，我觉得我这个相较于Andy讲的基础算法算是实现了高并发的。</p>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CMU 15-445</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CMU 15-445 Project 0 &amp; 1</title>
    <link href="/2022/02/03/CMU-15-445-Project-0-and-1/"/>
    <url>/2022/02/03/CMU-15-445-Project-0-and-1/</url>
    
    <content type="html"><![CDATA[<h4 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h4><p>正如CMU 15-445的主讲教授所说，这门课并不会教你如何使用数据库去构建应用程序或网站，也不是教你如何去部署和管理数据库的课，这门课的核心课程是有关数据库管理系统的设计与实现。</p><h3 id="PROJECT-0-C-PRIMER"><a href="#PROJECT-0-C-PRIMER" class="headerlink" title="PROJECT #0 - C++ PRIMER"></a>PROJECT #0 - C++ PRIMER</h3><p>这个热身实验只是让我们把项目文件下载到本地，配置好相关的git，检验一下自己的C++能力，因此对其简要说明。</p><p>在这个项目中，您将实现三个类模板：<code>Matrix</code>、<code>RowMatrix</code>和<code>RowMatrixOperations</code>。这些矩阵是简单的二维矩阵，必须支持加法、矩阵乘法和简化的<a href="https://en.wikipedia.org/wiki/General_matrix_multiply">通用矩阵乘法</a>(GEMM) 运算（我们只要修改一个文件：p0_starter.h）。</p><p>这个就不多说了。</p><h3 id="PROJECT-1-BUFFER-POOL"><a href="#PROJECT-1-BUFFER-POOL" class="headerlink" title="PROJECT #1 - BUFFER POOL"></a>PROJECT #1 - BUFFER POOL</h3><hr><p>以前我从来没思考过buffer和cache的区别，为什么叫buffer pool而不叫cache pool，在做这个实验时，突然想到这个问题了，现记录如下（与实验无太大关系）：</p><ol><li>buffer与cache操作的对象不一样<ul><li>buffer（缓冲）是为了提高内存和硬盘之间的数据交互的速度而设计的。</li><li>cache（缓存）是为了提高CPU和内存之间的数据交互速度设计的，也就是平常见到的一级缓存、二级缓存等。</li><li>CPU在执行程序所用的指令和读取数据都是针对内存的，也就是从内存中取得的。由于内存读写速度慢，为了提高cpu和内存之间数据交换的速度，在cpu和内存之间增加了cache，它的速度比内存快。</li><li>缓冲是根据磁盘的读写设计的，把分散的写操作集中进行，减少磁盘碎片和磁盘的反复寻道，从而提高系统性能。</li></ul></li><li>或者可以这样说，buffer是系统两端处理速度平衡时使用的，而cache是系统两端不匹配时的一种折衷策略。我们在组原中学习过TLB（translation lookaside buffer），这玩意名字虽然叫buffer，但其实是一种cache。</li></ol><p>所以说，这里之所以叫buffer，那是因为它直接和磁盘进行数据交互。</p><p>在Free命令中显示的buffer和cache，它们都是占用内存：</p><ul><li><p>buffer : 作为buffer cache的内存，是块设备的读写缓冲区，更靠近存储设备，或者直接就是disk的缓冲区。</p></li><li><p>cache: 是内存的缓冲区 。</p></li></ul><p>补充，linux中buffer cache和page cache的概念和二者的区别：</p><p>Page cache在linux读写文件时，它用于缓存文件的逻辑内容，从而加快对磁盘上映像和数据的访问。具体说是加速对文件内容的访问，buffer cache缓存文件的具体内容——物理磁盘上的磁盘块，这是加速对磁盘的访问。</p><p>简单说来，page cache用来缓存文件数据，buffer cache用来缓存磁盘数据。在有文件系统的情况下，对文件操作，那么数据会缓存到page cache，如果直接采用dd等工具对磁盘进行读写，那么数据会缓存到buffer cache。</p><p>由于我对文件系统所知甚少，后面再继续补充。</p><hr><p>在传统的数据库管理系统中，数据的存储介质是磁盘，文件是以page的形式存储在磁盘上的。对于结构化数据来说，一条记录会被保存在磁盘上的某个数据块中。在对某条记录进行处理时，可以通过代表该记录地址的page_id从磁盘上获取该记录，随后系统会把存储有该条记录的数据块从磁盘读到缓冲区，缓冲区分为多个frame，每个frame可以保存一个磁盘块，再从缓冲区将该条记录读到线程或事务的工作区进行处理，处理结束后将更新的记录写回缓冲区中的数据块，再由数据库管理系统将修改过的数据块写回到磁盘上。</p><p>在buffer pool中，有一个又一个的chunk块，我们称之为frame，我们可以将page放置在其中，一个frame对应一个page，这就是很简单的一对一拷贝，page可以以它们想要的任何顺序放置在frame中，如下图所示：</p><p>在此之上，我们需要一个额外的indirection层，如果我想要某个特定的page，通过这个indirection层我就知道它在哪个frame中——也就是pagetable，pagetable就是一个hash表，它用来跟踪内存中有哪些page，如果我们想找一个特定的page，通过page表和page id，我们就可以知道这个page在哪个frame中。数据库系统必须维护一些额外的元数据，以此来跟踪当前buffer池中发生了什么。下面是对这些元数据的详解：</p><p>dirty_page——这个数据告诉我们当我们从磁盘中读取到这个page后，这个page是否被修改过。</p><p>pin_count——引用计数，它用来跟踪想要使用该page的当前线程数量或者是正在查询该page的数量，这意味着我们并不想该page写出到磁盘上，因为我可能回去对它进行更新。从该page被放入内存到我对它进行更新这段时间内，我不想让该page被移除或者是交换回磁盘，这也将阻止我们去移除那些还未被安全写回磁盘的page。这也是我们后面的Pin函数想要表达的意思。</p><p>基本上来说，就是管理我们自己的内存，但我们也要去跟踪事务或查询是如何修改page的，在我们想做任何事之前，我们必须保护pagetable免受其他人污染或者是被其他人覆盖里面的东西。因此我们会用到Latches，下面是Locks和Latches的区别。</p><hr><blockquote><p>在数据库的世界中，lock是某种更高级的逻辑原语，它会去保护数据库中的逻辑内容，例如，tuple、表、以及数据库，事物会在运行的时候去持有这个lock，这就意味着可以有多个查询，数据库系统可以为我们提供这些东西，它能将这些暴露给我们这些开发人员。</p><p>Latch是一种底层保护原语，我们使用它来保护数据库系统内部的关键部分，比如，保护数据结构和保护内存区域，我们在执行操作的期间内，会持有这些Latch,用来保护某些东西，比如说，如果我去更新我的pagetable，我会在我要去修改的地方加上一个latch，修改完后，我会将它释放。</p></blockquote><p>后面教授就讲得很绕了，甚至spin mutex都讲出来了，我先去查查资料，后面填坑。</p><hr><p>另一个我们需要去区分的就是page目录和pagetable，二者之间的区别我在MIT6.S081 labs的博客里面也说过，我们还是来听听Andy教授怎么说：</p><blockquote><p>page目录的作用是用来找到page在我们数据库文件中的位置，比如我们想要找到page_no_1，它会告诉我们要找的那个page在这些数据库文件中的什么地方，page中的offset值有哪些，所以我们将page目录写在磁盘上，如果系统崩溃了，恢复后我们想知道该在哪里可以找到我么拥有的page，就可以访问page目录找到。</p><p>pagetable是内存中的映射，它将page_id映射到它们在buffer池中frame的位置。</p></blockquote><hr><p>当我们想将一个page放到内存中，但是内存中没有位置了应该怎么办呢？在替换策略中，我们最关心的就是正确性，如果说某个数据我们并没有真正地使用完，那么我们不想将它写出或移除，我们要确保我们所移除的page是在未来不太会被用到的那些page。</p><p>总而言之，为什么要使用buffer pool manager呢？</p><ol><li>如果没有缓冲池，那么数据都位于磁盘上，每次访问数据都要将其从磁盘读取到内存，下一次访问相同的数据时，还需要从磁盘读取，非常耗时。</li><li>现实中内存是有限的，如果我们在内存中缓存了page 1，2，3，已经存满内存了现在想访问page4，就要用到buffer pool manager，通过LRU机制进行替换。</li></ol><h5 id="CLOCK算法（了解）"><a href="#CLOCK算法（了解）" class="headerlink" title="CLOCK算法（了解）"></a>CLOCK算法（了解）</h5><hr><p>clock算法会给每个page设置一个标志位，唯一需要跟踪的就是每个page的标志位（referrence bit），它会告诉你自上次检查过该page之后，这个page是否被访问了。我们用环形队列来组织page，就像是一个clock，用一个能够旋转的指针去检查标志位是被设置为1还是0，如果是0，我们就可以知道从上次检查过之后该page就没有被访问过，因此我们就可以将该page从环形buffer中移除。</p><hr><p>我们将为BusTub DBMS构建一个面向磁盘的存储管理器。这样的存储管理器假定数据库的主要存储位置在磁盘上，project_1的任务是在存储管理器中实现缓冲池，缓冲池负责将物理页面从主存移动到磁盘，值的注意的是，DBMS需要支持大于系统可用内存量的数据库；多个线程将同时访问内部数据结构，因此我们需要确保他们的关键部分受到锁存器的保护。</p><p>我们可以维护一个单独的数据结构，比如队列，它根据page的时间戳进行排序，在某个时刻，有人对一个page进行读和写，我们只需要把该page从队列中拿出来，然后处理完放到队列的末尾，因为队列是先进先出的。</p><p>一句话，实验一的目标便在于主动管理磁盘中的页在内存中的缓存，从而最小化磁盘访问次数、最大化相关数据连续。</p><p>我们需要在存储管理器中实现以下组件：</p><ul><li>LRU更换策略</li><li>缓冲池管理器实例</li><li>并行缓冲池管理器</li></ul><h4 id="LRU更换策略"><a href="#LRU更换策略" class="headerlink" title="LRU更换策略"></a>LRU更换策略</h4><p>LRU（Least recently used，最近最少使用）算法根据数据的历史访问记录来进行淘汰数据，其核心思想是“如果数据最近被访问过，那么将来被访问的几率也更高”。也就是说我们这里定义一个<code>lru_list</code>，我们把pin_count为0的frame_id存放在lru_list，然后每次按照顺序出队（victim），如果在队列中的时候，这个frame_id被访问了，那么我们就从队列中删除它，直到它的pin_count再次为0，再次将它入队，循环往复。这样就达到了LRU的机制。</p><p>我们先来分析一下<code>include/buffer/replacer.h</code>中定义的父类Replacer。Replacer 是一个跟踪页面使用情况的抽象类。我们要实现以下方法：</p><ol><li><p>Victim(frame_id_t*)：Replacer跟踪与所有元素相比最近访问次数最少的对象并删除，将其删除页号存储在输出参数中，并返回 True。如果 Replacer 为空则返回 False。</p></li><li><p>Pin(frame_id_t)：在将page固定到BufferPoolManager中的frame后，应调用此方法。它应该从LRUReplacer中删除包含固定page的frame。</p></li><li><p>Unpin(frame_id_t)：当页面的引用计数变为 0 时，应该调用此方法。这个方法应该将未包含固定 page 的 frame 添加到 LRUReplacer，表示可能会被替换掉。（注意，需要判断是否超出了内存大小，如果超过了，则删除较新的页面，然后再添加。）</p></li><li><p>Size()：此方法返回当前在LRUReplacer中的页面数。</p></li></ol><p>在LRUReplacer中我们要实现一个队列<code>std::list&lt;frame_id_t&gt; lru_list_;</code>，这个队列用来维护page写入frame。我们可以将LRUReplacer当作是一个替换器，在初始状态下，LRUReplacer应当被初始化为不包含任何frames。当page加入到buffer pool的某个frame后，调用Pin删除在LRU队列中的frame<em>id，表示该frame已经不应该成为victim；Unpin则将未固定page的frame加入到LRU队列，表示该frame使用情况比较差（页面的引用计数变为0）；victim方法则是将LRU队列中最后一名frame_id弹出（在另外的对应的函数中会删除对应页面）。LRUReplacer的最大页数与缓冲池的大小相同（这样做是为了管理缓冲池中的所有frames），因此我们在LRUReplacer的构造函数中将capacity</em>的大小设置为参数num_pages，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++">LRUReplacer::<span class="hljs-built_in">LRUReplacer</span>(<span class="hljs-keyword">size_t</span> num_pages) &#123;  capacity_ = num_pages;  <span class="hljs-comment">// Initialization the capacity</span>&#125;</code></pre></div><p>当我们将page加入到BufferPoolManager中的frame后，需要从LRUReplacer中删除相应的frame。Pin函数如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">LRUReplacer::Pin</span><span class="hljs-params">(<span class="hljs-keyword">frame_id_t</span> frame_id)</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">if</span> (map_.<span class="hljs-built_in">count</span>(frame_id) == <span class="hljs-number">0</span>) &#123;    <span class="hljs-keyword">return</span>;  &#125;<span class="hljs-comment">//</span>  <span class="hljs-keyword">auto</span> iter = map_[frame_id];  lru_list_.<span class="hljs-built_in">erase</span>(iter);  map_.<span class="hljs-built_in">erase</span>(frame_id);&#125;</code></pre></div><p><strong>值得注意的是，我们在对LRUReplacer进行操作时应该上锁，以避免内存中特定的数据结构不会因为并发访问而导致错误。</strong>其实我们在每个函数前面用unique_lock是最稳妥的，既不会死锁，也保证安全。</p><p>当页面的引用计数为0时，应该调用Unpin，这个方法应该将未固定 page 的 frame 添加到 LRUReplacer，表示有被victim的风险。代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">LRUReplacer::Unpin</span><span class="hljs-params">(<span class="hljs-keyword">frame_id_t</span> frame_id)</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">if</span> (map_.<span class="hljs-built_in">count</span>(frame_id) != <span class="hljs-number">0</span>) &#123;<span class="hljs-comment">//如果map里面已经没有这一帧/页了，就直接返回</span>    <span class="hljs-keyword">return</span>;  &#125;  <span class="hljs-keyword">if</span> (lru_list_.<span class="hljs-built_in">size</span>() == capacity_) &#123;    <span class="hljs-keyword">return</span>;  &#125;  lru_list_.<span class="hljs-built_in">push_front</span>(frame_id);  map_.<span class="hljs-built_in">emplace</span>(frame_id, lru_list_.<span class="hljs-built_in">begin</span>());&#125;</code></pre></div><p>Replacer跟踪与所有元素相比最近访问次数最少的对象并删除，将其删除页号存储在输出参数中，并返回 True。如果 Replacer 为空则返回 False。代码如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">LRUReplacer::Victim</span><span class="hljs-params">(<span class="hljs-keyword">frame_id_t</span> *frame_id)</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">if</span> (map_.<span class="hljs-built_in">empty</span>()) &#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;<span class="hljs-comment">//将需要删除的frame_id写入参数</span>  *frame_id = lru_list_.<span class="hljs-built_in">back</span>();  map_.<span class="hljs-built_in">erase</span>(*frame_id);  lru_list_.<span class="hljs-built_in">pop_back</span>();  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div><h4 id="缓冲池管理器实例"><a href="#缓冲池管理器实例" class="headerlink" title="缓冲池管理器实例"></a>缓冲池管理器实例</h4><p>接下来，需要在系统中实现缓冲池管理器实例，该BufferPoolManagerInstance负责从DiskManager中读取数据库页并将它们存储在内存中。</p><p>我们来看看该类的数据结构：</p><ul><li><p><code>pages_</code>，保存在buffer pool中的page数组；</p></li><li><p><code>disk_manager</code>，磁盘管理器；</p></li><li><p><code>log_manager</code></p></li><li><p><code>page_table_</code>，页表，保存了page_id到frame_id的映射；</p></li><li><p><code>replacer</code>，查找未固定的页面进行替换；</p></li><li><p><code>free_list_</code>，没有被page填充的frame list。</p></li></ul><p>刚看到这个任务的时候无从下手，还是一个函数一个函数分析。</p><ul><li><p><code>FlushPgImp</code>函数，我们要将在内存中的page内容刷新到磁盘。</p><p>题外话：由刷盘想到的——面试官问我，刷盘时机怎么选择，就比如微博的热榜tag数据，本来这个tag不是热数据，但是这个tag数据可能激增，激增之后刷盘策略要怎么改变来适应，热度又会慢慢降低，这个时候又怎么改变刷盘策略，主要是刷盘时机。</p></li></ul><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">BufferPoolManagerInstance::FlushPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// Make sure you call DiskManager::WritePage</span>  <span class="hljs-comment">//在这里也是一样，每个函数前面加上unique_lock</span>  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-comment">//如果page id不合法，或者page_id不存在于页表中，直接返回</span>  <span class="hljs-keyword">if</span>(page_id==INVALID_PAGE_ID||page_table_.<span class="hljs-built_in">count</span>(page_id)==<span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-comment">//找到之后获取frame_id</span>  <span class="hljs-keyword">auto</span> iter=page_table_.<span class="hljs-built_in">find</span>(page_id);  <span class="hljs-keyword">frame_id_t</span> frame_id=iter-&gt;second;  <span class="hljs-comment">//获取相应的page</span>  Page *page=&amp;pages_[frame_id];  <span class="hljs-comment">//将page写入磁盘</span>  disk_manager_-&gt;<span class="hljs-built_in">WritePage</span>(page-&gt;page_id_, page-&gt;data_);<span class="hljs-comment">//这里的page-&gt;page_id_就是参数page_id</span>  <span class="hljs-comment">//写回磁盘之后将page设置为不是脏页</span>  page-&gt;is_dirty_ = <span class="hljs-literal">false</span>;<span class="hljs-comment">//这里的is_dirty就是说是否在数据更改之后被刷回到盘里了，如果数据没有更改就是false,数据更改了还没有被刷回到盘里就是true</span>  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div><ul><li><code>FlushAllPgsImp</code>函数，刷新所有页面到磁盘。</li></ul><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">BufferPoolManagerInstance::FlushAllPgsImp</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">size_t</span> i=<span class="hljs-number">0</span>;i&lt;pool_size_;++i)&#123;      Page *page=&amp;pages_[i];      <span class="hljs-built_in">FlushPgImp</span>(page-&gt;page_id_);    &#125;&#125;</code></pre></div><ul><li><code>NewPgImp</code>函数：在缓冲池创建一个新页面。</li></ul><p>  如果</p><ol><li>确保你调用<code>allocatepage</code>函数；</li><li>如果所有在buffer pool的page都pinned，那么返回nullptr；</li><li><p>从空闲列表或替换器中选择一个受害者页面 P。 始终首先从空闲列表中选择。</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">page_id_t</span> <span class="hljs-title">BufferPoolManagerInstance::AllocatePage</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">const</span> <span class="hljs-keyword">page_id_t</span> next_page_id = next_page_id_;  <span class="hljs-comment">//一旦拿出一个next_page_id，就加上num_instances，到下一个next_page_id</span>  next_page_id_ += num_instances_;  <span class="hljs-built_in">ValidatePageId</span>(next_page_id);  <span class="hljs-keyword">return</span> next_page_id;&#125;</code></pre></div><p>那么我们看具体的代码实现：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">Page *<span class="hljs-title">BufferPoolManagerInstance::NewPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> *page_id)</span> </span>&#123;  <span class="hljs-comment">// 0.   Make sure you call AllocatePage!</span>  <span class="hljs-comment">// 1.   If all the pages in the buffer pool are pinned, return nullptr.</span>  <span class="hljs-comment">// 2.   Pick a victim page P from either the free list or the replacer. Always pick from the free list first.</span>  <span class="hljs-comment">// 3.   Update P&#x27;s metadata, zero out memory and add P to the page table.</span>  <span class="hljs-comment">// 4.   Set the page ID output parameter. Return a pointer to P.</span>  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">bool</span> all_pinned = <span class="hljs-literal">true</span>;  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">size_t</span> i = <span class="hljs-number">0</span>; i &lt; pool_size_; ++i) &#123;    <span class="hljs-keyword">if</span> (pages_[i].pin_count_ &lt;= <span class="hljs-number">0</span>) &#123;      all_pinned = <span class="hljs-literal">false</span>;      <span class="hljs-keyword">break</span>;    &#125;  &#125;  <span class="hljs-keyword">if</span> (all_pinned) &#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">nullptr</span>;  &#125;  <span class="hljs-keyword">frame_id_t</span> frame_id=<span class="hljs-number">-1</span>;  <span class="hljs-keyword">if</span>(!free_list_.<span class="hljs-built_in">empty</span>())&#123;    <span class="hljs-keyword">if</span>(!replacer_-&gt;<span class="hljs-built_in">Victim</span>(&amp;frame_id))&#123;      <span class="hljs-keyword">return</span> <span class="hljs-literal">nullptr</span>;    &#125;  &#125;<span class="hljs-keyword">else</span>&#123;    frame_id=free_list_.<span class="hljs-built_in">front</span>();    free_list_.<span class="hljs-built_in">pop_front</span>();  &#125;  *page_id=<span class="hljs-built_in">AllocatePage</span>();<span class="hljs-comment">//</span>  Page *page = &amp;pages_[frame_id];<span class="hljs-comment">//我们找到pages序列中对应的要被替换的一帧</span>  <span class="hljs-comment">//disk page-&gt;pool page</span>  <span class="hljs-keyword">if</span> (page-&gt;<span class="hljs-built_in">IsDirty</span>()) &#123;<span class="hljs-comment">//如果这一页数据是dirty,先把这一页数据写回去</span>    disk_manager_-&gt;<span class="hljs-built_in">WritePage</span>(page-&gt;page_id_, page-&gt;data_);    page-&gt;is_dirty_ = <span class="hljs-literal">false</span>;  &#125;  page_table_.<span class="hljs-built_in">erase</span>(page-&gt;page_id_);  <span class="hljs-keyword">if</span> (*page_id != INVALID_PAGE_ID) &#123;    page_table_.<span class="hljs-built_in">emplace</span>(*page_id, frame_id);  &#125;  page-&gt;<span class="hljs-built_in">ResetMemory</span>();  page-&gt;page_id_ = *page_id;  replacer_-&gt;<span class="hljs-built_in">Pin</span>(frame_id);  page-&gt;pin_count_=<span class="hljs-number">1</span>;  <span class="hljs-keyword">return</span> page;&#125;</code></pre></div></li></ol><ul><li><p><code>FetchPgImp</code>函数，从缓冲池中获取请求的页面，我们可以从官方注释中具体了解这个方法要实现的任务：</p><ol><li>在页表中寻找请求页面P；<ul><li>如果页面P存在，调用Pin方法后返回；</li><li>如果页面P不存在，则从空闲列表或替换其中查找替换页R；</li></ul></li><li>将脏页写回磁盘；</li><li>从页表中删除R，插入P；</li><li>更新P的元数据，从磁盘读入页面内容，返回一个指向P的指针。</li></ol></li></ul><p>​        代码如下，具体解析在注释中：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">Page *<span class="hljs-title">BufferPoolManagerInstance::FetchPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// 1.     Search the page table for the requested page (P).</span>  <span class="hljs-comment">// 1.1    If P exists, pin it and return it immediately.</span>  <span class="hljs-comment">// 1.2    If P does not exist, find a replacement page (R) from either the free list or the replacer.</span>  <span class="hljs-comment">//        Note that pages are always found from the free list first.</span>  <span class="hljs-comment">// 2.     If R is dirty, write it back to the disk.</span>  <span class="hljs-comment">// 3.     Delete R from the page table and insert P.</span>  <span class="hljs-comment">// 4.     Update P&#x27;s metadata, read in the page content from disk, and then return a pointer to P.</span>  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">if</span>(page_id==INVALID_PAGE_ID)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">nullptr</span>;  &#125;  <span class="hljs-keyword">auto</span> iter = page_table_.<span class="hljs-built_in">find</span>(page_id);  <span class="hljs-keyword">if</span>(iter==page_table_.<span class="hljs-built_in">end</span>())&#123;    <span class="hljs-keyword">frame_id_t</span> frame_id=<span class="hljs-number">-1</span>;    <span class="hljs-keyword">if</span>(free_list_.<span class="hljs-built_in">empty</span>()&amp;&amp;!replacer_-&gt;<span class="hljs-built_in">Victim</span>(&amp;frame_id))&#123;      <span class="hljs-keyword">return</span> <span class="hljs-literal">nullptr</span>;    &#125;    <span class="hljs-keyword">if</span>(!free_list_.<span class="hljs-built_in">empty</span>())&#123;      frame_id=free_list_.<span class="hljs-built_in">front</span>();      free_list_.<span class="hljs-built_in">pop_front</span>();    &#125;<span class="hljs-keyword">else</span>&#123;      <span class="hljs-keyword">auto</span> old_page_id=page_table_[frame_id];      <span class="hljs-keyword">auto</span> page=&amp;pages_[frame_id];      <span class="hljs-keyword">if</span>(page-&gt;<span class="hljs-built_in">IsDirty</span>())&#123;        disk_manager_-&gt;<span class="hljs-built_in">WritePage</span>(page-&gt;page_id_,page-&gt;data_);        page-&gt;is_dirty_=<span class="hljs-literal">false</span>;      &#125;      page_table_.<span class="hljs-built_in">erase</span>(old_page_id);    &#125;    page_table_.<span class="hljs-built_in">emplace</span>(page_id, frame_id);    <span class="hljs-keyword">auto</span> tar = &amp;pages_[frame_id];    tar-&gt;page_id_=page_id;    tar-&gt;<span class="hljs-built_in">ResetMemory</span>();    disk_manager_-&gt;<span class="hljs-built_in">ReadPage</span>(page_id, tar-&gt;data_);    replacer_-&gt;<span class="hljs-built_in">Pin</span>(frame_id);    tar-&gt;pin_count_ = <span class="hljs-number">1</span>;    <span class="hljs-keyword">return</span> tar;  &#125;<span class="hljs-comment">//如果pool中有该page</span>  <span class="hljs-keyword">frame_id_t</span> frame_id=iter-&gt;second;  <span class="hljs-keyword">auto</span> tar=&amp;pages_[frame_id];<span class="hljs-comment">//获取相应的page_id</span>  replacer_-&gt;<span class="hljs-built_in">Pin</span>(frame_id);<span class="hljs-comment">//不管replacer里面有没有我们都调用一次</span>  tar-&gt;pin_count_++;  <span class="hljs-keyword">return</span> tar;&#125;</code></pre></div><ul><li><code>UnpinPgImp函数</code>：这个函数就是完成对该页面操作之后，进行Unpin操作。<ol><li>如果一开始<code>pin_couter==0</code>，直接返回。</li><li>如果这个页面的<code>pin_couter&gt;0</code>，我们直接返回。</li><li>如果这个页面的<code>pin_couter==0</code>，我们需要给它加入到LRU_Replacer中。<strong>因为没有人引用它，所以它可以成为被替换的候选人</strong>。</li></ol></li></ul><p>​        其代码如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">BufferPoolManagerInstance::UnpinPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id, <span class="hljs-keyword">bool</span> is_dirty)</span> </span>&#123;  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-comment">//如果page id不合法，或者page_id不存在于页表中，直接返回</span>  <span class="hljs-keyword">if</span>(page_id==INVALID_PAGE_ID||page_table_.<span class="hljs-built_in">count</span>(page_id)==<span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-comment">//找到之后获取frame_id</span>  <span class="hljs-keyword">auto</span> iter=page_table_.<span class="hljs-built_in">find</span>(page_id);  <span class="hljs-keyword">frame_id_t</span> frame_id = iter-&gt;second;  Page *page = &amp;pages_[frame_id];  <span class="hljs-comment">//如果直接是等于0的，说明不需要unpin操作，直接返回</span>  <span class="hljs-keyword">if</span> (page-&gt;pin_count_ == <span class="hljs-number">0</span>) &#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-comment">//如果之前被调用过，执行下列操作</span>  page-&gt;pin_count_--;  <span class="hljs-keyword">if</span> (page-&gt;pin_count_ == <span class="hljs-number">0</span>) &#123;    replacer_-&gt;<span class="hljs-built_in">Unpin</span>(frame_id);  &#125;  <span class="hljs-keyword">if</span> (is_dirty) &#123;    page-&gt;is_dirty_ = <span class="hljs-literal">true</span>;  &#125;  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div><ul><li><p><code>DeletePgImp</code>函数：从缓冲池删除一个页面，如果页面存在但无法删除，则返回 false，如果页面不存在或删除成功，则返回 true。</p><ol><li>Make sure you call DeallocatePage!</li><li>Search the page table for the requested page (P).</li><li>If P des not exist, return true.</li><li>If P exists, but has a non-zero pin-count, return fale. Someone is using the page.</li><li>Otherwise, P can be deleted. Remove P from the page table, reset its metadata and return it to the free list.</li></ol><p>代码如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">BufferPoolManagerInstance::DeletePgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// 0.   Make sure you call DeallocatePage!</span>  <span class="hljs-comment">// 1.   Search the page table for the requested page (P).</span>  <span class="hljs-comment">// 1.   If P does not exist, return true.</span>  <span class="hljs-comment">// 2.   If P exists, but has a non-zero pin-count, return false. Someone is using the page.</span>  <span class="hljs-comment">// 3.   Otherwise, P can be deleted. Remove P from the page table, reset its metadata and return it to the free list.</span>  <span class="hljs-function">std::unique_lock&lt;std::mutex&gt; <span class="hljs-title">lck</span><span class="hljs-params">(latch_)</span></span>;  <span class="hljs-keyword">auto</span> iter = page_table_.<span class="hljs-built_in">find</span>(page_id);  <span class="hljs-keyword">if</span>(iter==page_table_.<span class="hljs-built_in">end</span>())&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  &#125;  <span class="hljs-keyword">if</span>(pages_[iter-&gt;second].pin_count_&gt;<span class="hljs-number">0</span>)&#123;    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;  &#125;  <span class="hljs-built_in">DeallocatePage</span>(page_id);  <span class="hljs-keyword">frame_id_t</span> frame_id=page_table_[page_id];  <span class="hljs-keyword">auto</span> page=&amp;pages_[frame_id];  <span class="hljs-keyword">if</span>(page-&gt;<span class="hljs-built_in">IsDirty</span>())&#123;    disk_manager_-&gt;<span class="hljs-built_in">WritePage</span>(page-&gt;page_id_,page-&gt;data_);    page-&gt;is_dirty_=<span class="hljs-literal">false</span>;  &#125;  page-&gt;<span class="hljs-built_in">ResetMemory</span>();  page-&gt;page_id_=INVALID_PAGE_ID;  free_list_.<span class="hljs-built_in">push_back</span>(frame_id);  page_table_.<span class="hljs-built_in">erase</span>(page_id);  <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;&#125;</code></pre></div></li></ul><h4 id="并行缓冲池管理器"><a href="#并行缓冲池管理器" class="headerlink" title="并行缓冲池管理器"></a>并行缓冲池管理器</h4><p>正如上个任务中看到的，每一个缓冲池都有一个锁来保证线程安全。这可能导致性能问题，因为每个线程在与缓冲池交互时都会争夺该锁。一个可行的解决方案是同时维护多个缓冲池，每个缓冲池都有独立的锁。</p><p>ParallelBufferPoolManager是一个包含多个bufferpoolmanagerinstance的类。对于每个操作，其都会选择一个实例并调用该实例。</p><p>我们使用给定的page_id来确实使用哪个特定的实例。如果我们有多个实例，则需要某种方法将page_id映射到[0, num_instances)中。对于本项目，我们将使用模运算，即page_id mod num_instances来将给定的page_id映射到正确的范围。</p><p>当ParallelBufferPoolManager初始化的时候，他的起始索引应该是0。此后每创建一个新的页面，都应从起始索引开始尝试每个实例直到某个成功。然后将起始指数加1。</p><p>确保当你创建单独的BufferPoolManagerInstances时，你调用的构造函数应使用uint32_t num_instances和uint32_t instance_index，来保证页面id被正确地创建。</p><p>上面是机器翻译的，我来总结一下：也就是说，现在我们可以在系统中搞多个缓冲池，每个缓冲池都有自己的锁，这样来实现并行。那么如何确定我们要查找的page位于哪个缓冲池呢，我们可以根据<strong>某种映射方式</strong>，也就是说，我们可以用page_id%num_instances来确定page_id是位于哪一个缓冲池实例。</p><p>我们还是先来看看<code>ParallelBufferPoolManager</code>类中的数据结构，如下所示：</p><ul><li><code>size_t pool_size_</code>：缓冲池的页数</li><li><code>uint32_t num_instances_ = 1</code>：实例的数量</li><li><code>uint32_t instance_index_ = 0</code>：实例的索引</li></ul><p>有了上面的讲解，那么下面两个函数的实现就很容易了：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ParallelBufferPoolManager::GetPoolSize</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-comment">// Get size of all BufferPoolManagerInstances</span>  <span class="hljs-keyword">return</span> pool_size_ * num_instances_;&#125;<span class="hljs-function">BufferPoolManager *<span class="hljs-title">ParallelBufferPoolManager::GetBufferPoolManager</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// Get BufferPoolManager responsible for handling given page id. You can use this method in your other methods.</span>  <span class="hljs-keyword">return</span> parallel_bmi_[page_id% num_instances_];&#125;</code></pre></div><p>对于缓冲区实例的操作也很简单了，我们只需要在获得page_id参数之后用<code>GetBufferPoolManager</code>，来知道是在哪一个实例当中，就可以了，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function">Page *<span class="hljs-title">ParallelBufferPoolManager::FetchPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// Fetch page for page_id from responsible BufferPoolManagerInstance</span>  BufferPoolManager *manager = <span class="hljs-built_in">GetBufferPoolManager</span>(page_id);  <span class="hljs-keyword">return</span> manager-&gt;<span class="hljs-built_in">FetchPage</span>(page_id);&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ParallelBufferPoolManager::UnpinPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id, <span class="hljs-keyword">bool</span> is_dirty)</span> </span>&#123;  <span class="hljs-comment">// Unpin page_id from responsible BufferPoolManagerInstance</span>  BufferPoolManager *manager = <span class="hljs-built_in">GetBufferPoolManager</span>(page_id);  <span class="hljs-keyword">return</span> manager-&gt;<span class="hljs-built_in">UnpinPage</span>(page_id, is_dirty);&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ParallelBufferPoolManager::FlushPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// Flush page_id from responsible BufferPoolManagerInstance</span>  BufferPoolManager *manager = <span class="hljs-built_in">GetBufferPoolManager</span>(page_id);  <span class="hljs-keyword">return</span> manager-&gt;<span class="hljs-built_in">FlushPage</span>(page_id);&#125;<span class="hljs-function">Page *<span class="hljs-title">ParallelBufferPoolManager::NewPgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> *page_id)</span> </span>&#123;  <span class="hljs-comment">// create new page. We will request page allocation in a round robin manner from the underlying</span>  <span class="hljs-comment">// BufferPoolManagerInstances</span>  <span class="hljs-comment">// 1.   From a starting index of the BPMIs, call NewPageImpl until either 1) success and return 2) looped around to</span>  <span class="hljs-comment">// starting index and return nullptr</span>  <span class="hljs-comment">// 2.   Bump the starting index (mod number of instances) to start search at a different BPMI each time this function</span>  <span class="hljs-comment">// is called</span>  Page *page = <span class="hljs-literal">nullptr</span>;  <span class="hljs-keyword">size_t</span> index = start_id_;  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">size_t</span> i = <span class="hljs-number">0</span>; i &lt; num_instances_; ++i) &#123;    page = parallel_bmi_[index]-&gt;<span class="hljs-built_in">NewPage</span>(page_id);    <span class="hljs-keyword">if</span> (page != <span class="hljs-literal">nullptr</span>) &#123;      start_id_ = (index + <span class="hljs-number">1</span>) % num_instances_;      <span class="hljs-keyword">break</span>;    &#125;    index = (index + <span class="hljs-number">1</span>) % num_instances_;  &#125;  <span class="hljs-keyword">return</span> page;&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ParallelBufferPoolManager::DeletePgImp</span><span class="hljs-params">(<span class="hljs-keyword">page_id_t</span> page_id)</span> </span>&#123;  <span class="hljs-comment">// Delete page_id from responsible BufferPoolManagerInstance</span>  BufferPoolManager *manager = <span class="hljs-built_in">GetBufferPoolManager</span>(page_id);  <span class="hljs-keyword">return</span> manager-&gt;<span class="hljs-built_in">DeletePage</span>(page_id);&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ParallelBufferPoolManager::FlushAllPgsImp</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-comment">// flush all pages from all BufferPoolManagerInstances</span>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">size_t</span> i = <span class="hljs-number">0</span>; i &lt; num_instances_; i++) &#123;    parallel_bmi_[i]-&gt;<span class="hljs-built_in">FlushAllPages</span>();  &#125;&#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CMU 15-445</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 4: Traps</title>
    <link href="/2022/02/01/MIT-6-S081-Lab-4-Traps/"/>
    <url>/2022/02/01/MIT-6-S081-Lab-4-Traps/</url>
    
    <content type="html"><![CDATA[<h4 id="先来说说trap"><a href="#先来说说trap" class="headerlink" title="先来说说trap"></a>先来说说trap</h4><p>我们知道，用户态切换到内核态的有两种情况：一种是系统调用（syscall），另一种是异常（trap）或中断（interruption），在前面的实验中我们学习了系统调用。我们先来回忆一下在lab1中做过的<code>sleep</code>任务。在进入内核后，会调用<code>sys_sleep</code>函数。关于用户态转到内核态我们在lab2中已经讲过了。</p><h5 id="trap机制"><a href="#trap机制" class="headerlink" title="trap机制"></a>trap机制</h5><p>在x86中我们常常说中断，但是在xv6中我们都是使用trap来代表他们，因为trap是传统的Unix术语。trap是由在CPU上运行的当前进程导致的；而中断是由设备导致的，可能与当前进程毫无关系。但是他们都<strong>依赖相同的硬件机制在用户模式和内核模式之间进行切换</strong>。</p><p>在从用户态转到内核态的时候，都相当于执行了一个中断响应的过程，因为系统调用实际上最终是中断机制实现的。其具体过程如下：</p><ol><li>首先，保存32个用户寄存器，因为我们需要恢复用户应用程序的执行。但是这些寄存器又要被内核代码所使用，所以在trap之前，我们必须在某处保存这32个用户寄存器。（当然，stack pointer也在这32个寄存器里面的）</li><li>程序计数器也要在某个地方保存，因为我们需要在用户程序运行中断的位置继续执行用户程序。</li><li>将CPU的字段改为内核态（硬件标志位）。</li><li>SATP寄存器也要改变，此时SATP寄存器正指向user page table，我们要将SATP指向kernel   page table。</li><li>我们要将堆栈寄存器指向位于内核的一个地址，因为我们需要一个堆栈来调用内核的C函数。</li><li>一旦我们设置好了，我们需要跳入内核的C代码。</li></ol><hr><p>我们在实验2中知道，用户态在进入内核态的时候调用了一个entry，如下所示：</p><div class="code-wrapper"><pre><code class="hljs awk">fork: li a7, SYS_fork ecall ret.global <span class="hljs-keyword">exit</span></code></pre></div><p>ecall是什么呢？ecall就是将CPU中的mode标志位设置为supervisor，并且设置程序计数器的值为STVEC寄存器的值。内核会将trampoline page的地址存在STVEC寄存器中，所以ecall的下一条指令的位置是STVEC指向的地址，也就是trampoline page的起始地址。</p><h5 id="kernel-trampoline-S"><a href="#kernel-trampoline-S" class="headerlink" title="kernel/trampoline.S"></a><code>kernel/trampoline.S</code></h5><p>trampoline.S从user space过渡到kernel space，kernel space返回到user space。STVEC寄存器中存储的是 trampoline page 的起始位置。<strong>进入内核前，首先需要在该位置处执行一些初始化的操作。例如，切换页表、切换栈指针等操作</strong>。cpu 从 trampoline page 处开始进行取指执行。接下来需要<strong>保存所有寄存器的值</strong>，寄存器的值是如何保存的呢？当寄存器里面的还都是用户程序的数据的时候，我们需要非常小心，我们在这个时间点不能使用任何寄存器，否则我们是没法恢复寄存器数据的。如果内核在这个时间点使用了任何一个寄存器，内核会覆盖寄存器内的用户数据，如果我们要尝试恢复用户数据，我们就不能恢复寄存器中的正确数据，用户程序的执行也会相应的出错。比如说，trampoline里面有个csrrw指令，这是trampoline page的第一条指令，这条指令的意思是交换a0和sscratch中的内容，在这之后内核就可以任意使用a0寄存器了。</p><p>以便在系统调用后恢复调用前的状态。然后我们要根据中断的原因执行相应的处理：产生中断的原因有很多，比如<strong>系统调用、运算时除以0、使用了一个未被映射的虚拟地址、或者是设备中断等等</strong>。这里是因为系统调用，所以以系统调用的方式进行处理。然后才是内核态执行系统调用函数，将ecall指令时保存在a7的参数取出来，完成相应的系统调用函数，系统调用完成之后将寄存器的值取出来，从该地址存储的指令处开始执行，恢复到用户态。</p><h4 id="RISC-V-assembly"><a href="#RISC-V-assembly" class="headerlink" title="RISC-V assembly"></a>RISC-V assembly</h4><p>源文件<code>call.c</code>代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/param.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/types.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/stat.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;user/user.h&quot;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">g</span><span class="hljs-params">(<span class="hljs-keyword">int</span> x)</span> </span>&#123;  <span class="hljs-keyword">return</span> x+<span class="hljs-number">3</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">f</span><span class="hljs-params">(<span class="hljs-keyword">int</span> x)</span> </span>&#123;  <span class="hljs-keyword">return</span> g(x);&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>&#123;  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d %d\n&quot;</span>, f(<span class="hljs-number">8</span>)+<span class="hljs-number">1</span>, <span class="hljs-number">13</span>);  <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div><p>我们先编译一下源文件得到<code>call.asm</code>，部分代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs asm">void main(void) &#123;  1c:1141                addisp,sp,-16  1e:e406                sdra,8(sp)  20:e022                sds0,0(sp)  22:0800                addis0,sp,16  printf(&quot;%d %d\n&quot;, f(8)+1, 13);  24:4635                lia2,13</code></pre></div><ol><li><p>Q:Which registers contain arguments to functions? For example, which register holds 13 in main’s call to <code>printf</code>?</p><p>A:a0-a7.A2 holds 13 in main’s call to printf.</p></li><li><p>Q:Where is the call to function <code>f</code> in the assembly code for main? Where is the call to <code>g</code>? (Hint: the compiler may inline functions.)</p><p>A:nowhere, compiler optimization by inline function.</p><p>其实是没有这样的代码。 g(x) 被内联到 f(x) 中，然后 f(x) 又被进一步内联到 main() 中。所以看到的不是函数跳转，而是优化后的内联函数。</p></li><li><p>Q:At what address is the function <code>printf</code> located?</p></li></ol><h4 id="Backtrace"><a href="#Backtrace" class="headerlink" title="Backtrace"></a>Backtrace</h4><p>使用这些帧指针向上遍历堆栈并在每个堆栈帧中打印保存的返回地址。</p><p>我们知道x86使用函数参数压栈的方式来保护函数参数，而xv6使用寄存器的方式保存参数。无论是x86还是xv6,函数调用时，都需要将返回地址和父函数的栈帧起始地址压入栈中。即被调用函数的栈帧中保存着这两个值，在xv6中，fp为当前函数的栈顶指针，sp为栈底指针。fp-8存放返回地址，fp-16存放原栈帧。</p><p>我们看如下这幅图：</p><p><img src="https://pic4.zhimg.com/80/v2-d7a2b1baeaf71d7e0c2cf306d80c49d7_720w.jpg" alt=""></p><p>fp指向当前栈帧的开始地址，sp指向结束地址，我们可以看到，栈是从高地址到低地址，fp是高地址，sp是低地址，fp-8是<code>return address</code>，fp-16是<code>to prev.frame</code>，分别代表返回地址和原栈帧。</p><p>我们根据提示，将backtrace的原型添加到<code>kernel/defs.h</code>中，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// printf.c</span><span class="hljs-function"><span class="hljs-keyword">void</span>            <span class="hljs-title">printf</span><span class="hljs-params">(<span class="hljs-keyword">char</span>*, ...)</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span>            <span class="hljs-title">panic</span><span class="hljs-params">(<span class="hljs-keyword">char</span>*)</span> __<span class="hljs-title">attribute__</span><span class="hljs-params">((noreturn))</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span>            <span class="hljs-title">printfinit</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span>            <span class="hljs-title">backtrace</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;</code></pre></div><p>GCC 编译器将当前执行函数的帧顶指针存储在寄存器<code>s0</code>中。因此我们通过<code>r_fp</code>函数可以知道当前函数的栈顶指针fp：</p><div class="code-wrapper"><pre><code class="hljs c">kernel/riscv.h:<span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">inline</span> uint64</span><span class="hljs-function"><span class="hljs-title">r_fp</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;  uint64 x;  <span class="hljs-function"><span class="hljs-keyword">asm</span> <span class="hljs-title">volatile</span><span class="hljs-params">(<span class="hljs-string">&quot;mv %0, s0&quot;</span> : <span class="hljs-string">&quot;=r&quot;</span> (x) )</span></span>;  <span class="hljs-keyword">return</span> x;&#125;</code></pre></div><p><code>backtrace</code>函数如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">backtrace</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>&#123;  uint64 fp = <span class="hljs-built_in">r_fp</span>(), top = <span class="hljs-built_in">PGROUNDUP</span>(fp);  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;backtrace:\n&quot;</span>);  <span class="hljs-keyword">for</span>(; fp &lt; top; fp = *((uint64*)(fp<span class="hljs-number">-16</span>))) &#123;<span class="hljs-comment">//我们知道，每个fp-16就是原栈帧，我们通过*(fp-16)得到原栈帧，保存在fp中，然后打印*(fp-8)</span>    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%p\n&quot;</span>, *((uint64*)(fp<span class="hljs-number">-8</span>)));  &#125;&#125;</code></pre></div><h4 id="Alarm"><a href="#Alarm" class="headerlink" title="Alarm"></a>Alarm</h4><p>在本练习中，您将向xv6添加一项功能，该功能会在使用CPU时间的情况下定期向进程发出警报。这对于想要限制消耗多少CPU时间的计算密集型进程，或者对于想要进行计算但还希望采取一些定期操作的进程很有用。</p><p>您应该添加一个新的<code>sigalarm(interval,handler)</code>系统调用。 如果应用程序调用<code>sigalarm(n,fn)</code>，则在程序每消耗n个“ tick” CPU时间之后，内核应导致调用应用程序函数fn。 当fn返回时，应用程序应从中断处恢复。</p><h6 id="实现过程"><a href="#实现过程" class="headerlink" title="实现过程"></a>实现过程</h6><p>我们首先看到测试函数，代码解析在注释中：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">test0</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">int</span> i;  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;test0 start\n&quot;</span>);<span class="hljs-comment">//开始测试</span>  count = <span class="hljs-number">0</span>;<span class="hljs-comment">//计数器为0</span>  sigalarm(<span class="hljs-number">2</span>, periodic);<span class="hljs-comment">//在消耗2个cpu时间之后，调用periodic函数</span>  <span class="hljs-keyword">for</span>(i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">1000</span>*<span class="hljs-number">500000</span>; i++)&#123;    <span class="hljs-keyword">if</span>((i % <span class="hljs-number">1000000</span>) == <span class="hljs-number">0</span>)      write(<span class="hljs-number">2</span>, <span class="hljs-string">&quot;.&quot;</span>, <span class="hljs-number">1</span>);    <span class="hljs-keyword">if</span>(count &gt; <span class="hljs-number">0</span>)      <span class="hljs-keyword">break</span>;  &#125;  sigalarm(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<span class="hljs-comment">//停止生成定期警报调用</span>  <span class="hljs-keyword">if</span>(count &gt; <span class="hljs-number">0</span>)&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;test0 passed\n&quot;</span>);  &#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;\ntest0 failed: the kernel never called the alarm handler\n&quot;</span>);  &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">periodic</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;  count = count + <span class="hljs-number">1</span>;  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;alarm!\n&quot;</span>);<span class="hljs-comment">//打印&quot;alarm&quot;</span>  sigreturn();<span class="hljs-comment">//调用sigreturn函数</span>&#125;</code></pre></div><ul><li>在<code>user/user.h</code>，我们需要做如下声明：</li></ul><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">sigalarm</span><span class="hljs-params">(<span class="hljs-keyword">int</span> ticks, <span class="hljs-keyword">void</span> (*handler)())</span></span>;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">sigreturn</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;</code></pre></div><ul><li>在 user/usys.pl 添加 entry：</li></ul><div class="code-wrapper"><pre><code class="hljs c">entry(<span class="hljs-string">&quot;sigalarm&quot;</span>);entry(<span class="hljs-string">&quot;sigreturn&quot;</span>);</code></pre></div><ul><li>在 kernel/syscall.h 中添加函数调用码：</li></ul><div class="code-wrapper"><pre><code class="hljs c">frame pointer。<span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_sigalarm  22</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_sigreturn 23</span></code></pre></div><ul><li>在 kernel/syscall.c 添加函数调用代码：</li></ul><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_sigalarm</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_sigreturn</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;[SYS_sigalarm]  sys_sigalarm,[SYS_sigreturn] sys_sigreturn,</code></pre></div><p>我们需要在proc结构体中添加字段，用于记录进程的滴答数——<code>uint64 ticks</code>；还需要一个<code>uint64 interval</code>，ticks达到interval便中断；当然，还需要一个函数指针<code>alarm_pointer</code>，用来中断时的调用。</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>(which_dev == <span class="hljs-number">2</span>) &#123;<span class="hljs-comment">//which_dev为2说明是因为定时引起的</span>    <span class="hljs-keyword">if</span>(p-&gt;interval) &#123;      <span class="hljs-keyword">if</span>(p-&gt;ticks == p-&gt;interval) &#123;<span class="hljs-comment">//当alarm_interval==0 &amp;&amp; alarm_handler==0时，内核应该停止产生周期性的报警调用</span>        p-&gt;ticks = <span class="hljs-number">0</span>;          p-&gt;trapframe-&gt;epc = p-&gt;handler;      &#125;      p-&gt;ticks++;    &#125;    yield();  &#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 3: Page tables</title>
    <link href="/2022/01/19/MIT-6-S081-Lab-3-Page-Tables/"/>
    <url>/2022/01/19/MIT-6-S081-Lab-3-Page-Tables/</url>
    
    <content type="html"><![CDATA[<blockquote><p>当我第一次学习它，还是一个学生的时候，我想它是很直接的，能有多难，就是一个表要将虚拟地址映射到物理地址。或许有点复杂，但不是那么复杂。只有当用它编程时，我才知道虚拟内存是巧妙的，迷人的，非常强大。</p></blockquote><p>在本次实验中，您将探索页表并修改它们以简化将数据从用户空间复制到内核空间的函数。在讲解实验之前，我们先要了解页表结构。</p><p>页表（pagetable）是做地址翻译与权限控制的：把 Virtual Address (VA) 映射到 Physical Address (PA)，并检查访问权限位。更准确地说，页表不是“用虚拟地址直接做索引的一维数组”，而是按页粒度组织、由多级索引逐层查找的映射结构。页表本身放在内存中，硬件通过页表基址寄存器定位其起点。</p><p>如何解决页表占用内存过大呢？</p><ul><li>不要为每个地址创建一个表单条目，而是为每个page创建一条表单条目</li><li><strong>多级页表</strong></li></ul><p>我们首先来了解一下pagetable是如何实现的：</p><h4 id="页表"><a href="#页表" class="headerlink" title="页表"></a>页表</h4><h5 id="页表的实现"><a href="#页表的实现" class="headerlink" title="页表的实现"></a>页表的实现</h5><p><img src="https://pic2.zhimg.com/v2-2635bdb5168de78ce3ad23f9857fd7e9_b.jpg" alt=""></p><p>假设一个page的大小为4kb,对于虚拟地址，将它划分为两部分，index和offset，index用来查找page，offset对应的是一个page中的哪个字节。当MMU在做地址翻译的时候，通过读取虚拟内存地址中的index可以知道物理内存中的page号，这个page号对应了物理内存中的4096个字节。之后虚拟内存地址中的offset指向了page中的4096个字节中的某一个，假设offset是12，那么page中的第12个字节被使用了。将offset加上page的起始地址，就可以得到物理内存地址。</p><p>以上图为例，每个页表仍然需要2^27个条目，每个条目占8个字节，每个页表占内存1GB，而且每个进程都会有一个页表，这仍然是不能接受的。到目前为止，最大的问题是即使应用程序所引用的只是虚拟地址空间中很小的一部分，也总是需要一个完整的页表驻留在内存中。（上述例子来源于<a href="https://wjrsbu.smartapps.cn/zhihu/article?id=369660512&amp;isShared=1&amp;_swebfr=1&amp;_swebFromHost=baiduboxapp">博客</a>)</p><h5 id="多级页表"><a href="#多级页表" class="headerlink" title="多级页表"></a>多级页表</h5><p><img src="https://pic1.zhimg.com/v2-3eaf43267f882245cc176d23274c1978_b.jpg" alt=""></p><p>以xv6使用的RISC-V框架为例，使用三级页表。我们之前所说的VA中的27bit的index，实际上是由3个9bit的数字组成（L2，L1，L0）。对于L2，其被用来索引最高级的page directory，directory中的一个条目被称为PTE(Page Table Entry)是64bit，而一个page directory是4KB，这样的话，一个PT中就有512个PTE，也就是说我们的L2的9bit可以对应一个page directory的PTE，而PTE的组成部分是PPN+Flags，这个PPN指向中间级的page directory。同理，我们使用L1找到中间级page directory的PPN，在最低级的pagedirectory，我们可以得到对应于虚拟内存地址的物理内存地址。</p><p>多级页表相较于一级页表，能大大减少pagetable的大小，优化空间。</p><h5 id="x86页表的实现"><a href="#x86页表的实现" class="headerlink" title="x86页表的实现"></a>x86页表的实现</h5><p>x86页表在物理内存中像一棵两层的树，也就是两级页表。树的根是一个4096字节的页目录，其中包含1024个PTE的条目（每个条目占4字节），每个条目是指向一个页表页，每个页表页包含1024个32位PTE的数组（PTE就是虚拟地址到物理地址映射的条目），如下图所示：</p><p>如上图所示，经典 32 位两级分页里常见划分是 10 位 Dir、10 位 table、12 位 offset。先用 Dir 定位页目录项，再用 table 定位页表项，最后把页框号与 12 位 offset 组合得到物理地址。</p><h4 id="xv6对页表的使用"><a href="#xv6对页表的使用" class="headerlink" title="xv6对页表的使用"></a>xv6对页表的使用</h4><p><img src="https://img-blog.csdnimg.cn/img_convert/493f49ab5868db801f35592c0434adda.png" alt=""></p><p>这个是xv6中PTE的结构，其中，53-10位是PPN，余下几位是flag位，弄懂标志位的含义对本实验至关重要，第0位V表示该PTE是否可用，第1位R表示该PTE是否可读；第2位W表示该PTE是否可写；第3位X表示是否可执行的。</p><p>虚拟内存有两个主要的优点：</p><ul><li>隔离性：虚拟内存使操作系统可以为每个应用程序提供独立地址空间。应用程序不能直接访问其他进程或内核的内存，除非内核显式建立了可访问映射。</li><li>提供了一层抽象：处理器和所有的指令都可以使用虚拟地址，而内核会定义从虚拟地址到物理地址的映射关系。</li></ul><h4 id="Print-a-page-table"><a href="#Print-a-page-table" class="headerlink" title="Print a page table"></a>Print a page table</h4><p>该任务是编写一个打印页表内容的函数，定义一个名为<code>vmprint()</code>的函数。</p><p><code>在 exec.c 中的return argc</code>之前插入<code>if(p-&gt;pid==1) vmprint(p-&gt;pagetable)</code>，以打印第一个进程的页表。也就是说</p><p>这个pagetable_t参数指的就是进程的页表号，或者说对应的页表中的首地址（暂时来看这是最好的理解）。</p><p>在riscv.h中定义了如下所示的宏，也就是说pagetable_t参数是一个uint64类型的指针，一个页表有512个PTE。</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> uint64 *<span class="hljs-keyword">pagetable_t</span>; <span class="hljs-comment">// 512 PTEs</span></code></pre></div><p>1.我们在exec.c的<code>return argc</code>前加入下列代码：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>(p-&gt;pid==<span class="hljs-number">1</span>)&#123;vmprint(p-&gt;pagetable);&#125;</code></pre></div><p>2.我们在defs.h中定义vmprint函数的原型，以便在exec.c中调用它：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span>            <span class="hljs-title">vmprint</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span>)</span></span></code></pre></div><p>3.编写vmprint函数如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span> </span><span class="hljs-function"><span class="hljs-title">printwalk</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> pagetable, uint level)</span> </span>&#123;  <span class="hljs-keyword">char</span>* prefix;  <span class="hljs-comment">//不同级别的页表打印不同的..</span>  <span class="hljs-keyword">if</span> (level == <span class="hljs-number">2</span>) prefix = <span class="hljs-string">&quot;..&quot;</span>;  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (level == <span class="hljs-number">1</span>) prefix = <span class="hljs-string">&quot;.. ..&quot;</span>;  <span class="hljs-keyword">else</span> prefix = <span class="hljs-string">&quot;.. .. ..&quot;</span>;  <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">512</span>; i++)&#123; <span class="hljs-comment">// 每级页表有512项</span>    <span class="hljs-keyword">pte_t</span> pte = pagetable[i];    <span class="hljs-keyword">if</span>(pte &amp; PTE_V)&#123; <span class="hljs-comment">// 该页表项有效</span>      uint64 pa = PTE2PA(pte); <span class="hljs-comment">// 将pte中转化为pa</span>      <span class="hljs-comment">//打印PTE对应的PA</span>      <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%s%d: pte %p pa %p\n&quot;</span>, prefix, i, pte, pa);      <span class="hljs-keyword">if</span>((pte &amp; (PTE_R|PTE_W|PTE_X)) == <span class="hljs-number">0</span>)&#123; <span class="hljs-comment">// 如果有下一级页表</span>         printwalk((<span class="hljs-keyword">pagetable_t</span>)pa, level - <span class="hljs-number">1</span>);      &#125;    &#125;  &#125;&#125; <span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">vmprint</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> pagetable)</span> </span>&#123;  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;page table %p\n&quot;</span>, pagetable);  printwalk(pagetable, <span class="hljs-number">2</span>);&#125;</code></pre></div><p>如果PTE_V==1，则说明该页表项有效。如果pte中的PTE_R、PTE_W和PTE_X位都是0，则说明有下一级页表，可以通过递归进入下一级页表。</p><h4 id="A-kernel-page-table-per-process"><a href="#A-kernel-page-table-per-process" class="headerlink" title="A kernel page table per process"></a>A kernel page table per process</h4><p>xv6为每个进程的用户地址空间提供一个单独的页表，然而从用户态进入内核态中，多个进程使用的是一个内核页表。这个任务的目标是让每一个进程进入内核态后，都能有自己的独立内核页表。</p><p>首先，我们要在进程的结构体proc中添加一个<code>pagetable_t kernel_pagetable</code>，表示每个进程在内核态中有自己独立的内核页表，结构体如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// kernel/proc.h</span><span class="hljs-comment">// Per-process state</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spinlock</span> <span class="hljs-title">lock</span>;</span>  <span class="hljs-comment">// p-&gt;lock must be held when using these:</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">procstate</span> <span class="hljs-title">state</span>;</span>        <span class="hljs-comment">// Process state</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">parent</span>;</span>         <span class="hljs-comment">// Parent process</span>  <span class="hljs-keyword">void</span> *chan;                  <span class="hljs-comment">// If non-zero, sleeping on chan</span>  <span class="hljs-keyword">int</span> killed;                  <span class="hljs-comment">// If non-zero, have been killed</span>  <span class="hljs-keyword">int</span> xstate;                  <span class="hljs-comment">// Exit status to be returned to parent&#x27;s wait</span>  <span class="hljs-keyword">int</span> pid;                     <span class="hljs-comment">// Process ID</span>  <span class="hljs-comment">// these are private to the process, so p-&gt;lock need not be held.</span>  uint64 kstack;               <span class="hljs-comment">// Virtual address of kernel stack</span>  uint64 sz;                   <span class="hljs-comment">// Size of process memory (bytes)</span>  <span class="hljs-keyword">pagetable_t</span> pagetable;       <span class="hljs-comment">// User page table</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">trapframe</span> *<span class="hljs-title">trapframe</span>;</span> <span class="hljs-comment">// data page for trampoline.S</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">context</span> <span class="hljs-title">context</span>;</span>      <span class="hljs-comment">// swtch() here to run process</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> *<span class="hljs-title">ofile</span>[<span class="hljs-title">NOFILE</span>];</span>  <span class="hljs-comment">// Open files</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">cwd</span>;</span>           <span class="hljs-comment">// Current directory</span>  <span class="hljs-keyword">char</span> name[<span class="hljs-number">16</span>];               <span class="hljs-comment">// Process name (debugging)</span>  <span class="hljs-keyword">pagetable_t</span> kernel_pb;     <span class="hljs-comment">// Kernel page table （在 proc 中添加该 field）</span>&#125;;</code></pre></div><p>至于proc结构体在下一个实验前细说。</p><p>在<code>kernel/vm.c</code>中，我们发现了如下变量：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">/*</span><span class="hljs-comment"> * the kernel&#x27;s page table.</span><span class="hljs-comment"> */</span><span class="hljs-keyword">pagetable_t</span> kernel_pagetable;</code></pre></div><p>这就是我们内核中的唯一的<code>kernel_pagetable</code>，在原版本中，vm.c中的<code>void kvminit(void)</code>会调用kvmake函数来修改kernel_pagetable，那么现在我们需要修改kvminit函数，来让我们的每个进程在内核中都有自己的内核页表。我们先来看看原版本中的实现：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Initialize the one kernel_pagetable</span><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">kvminit</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  kernel_pagetable = kvmmake();&#125;<span class="hljs-function"><span class="hljs-keyword">pagetable_t</span></span><span class="hljs-function"><span class="hljs-title">kvmmake</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">pagetable_t</span> kpgtbl;  kpgtbl = (<span class="hljs-keyword">pagetable_t</span>) kalloc();  <span class="hljs-built_in">memset</span>(kpgtbl, <span class="hljs-number">0</span>, PGSIZE);  <span class="hljs-comment">// uart registers</span>  kvmmap(kpgtbl, UART0, UART0, PGSIZE, PTE_R | PTE_W);  <span class="hljs-comment">// virtio mmio disk interface</span>  kvmmap(kpgtbl, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W);  <span class="hljs-comment">// PLIC</span>  kvmmap(kpgtbl, PLIC, PLIC, <span class="hljs-number">0x400000</span>, PTE_R | PTE_W);  <span class="hljs-comment">// map kernel text executable and read-only.</span>  kvmmap(kpgtbl, KERNBASE, KERNBASE, (uint64)etext-KERNBASE, PTE_R | PTE_X);  <span class="hljs-comment">// map kernel data and the physical RAM we&#x27;ll make use of.</span>  kvmmap(kpgtbl, (uint64)etext, (uint64)etext, PHYSTOP-(uint64)etext, PTE_R | PTE_W);  <span class="hljs-comment">// map the trampoline for trap entry/exit to</span>  <span class="hljs-comment">// the highest virtual address in the kernel.</span>  kvmmap(kpgtbl, TRAMPOLINE, (uint64)trampoline, PGSIZE, PTE_R | PTE_X);  <span class="hljs-comment">// map kernel stacks</span>  proc_mapstacks(kpgtbl);    <span class="hljs-keyword">return</span> kpgtbl;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">kvmmap</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> kpgtbl, uint64 va, uint64 pa, uint64 sz, <span class="hljs-keyword">int</span> perm)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">if</span>(mappages(kpgtbl, va, sz, pa, perm) != <span class="hljs-number">0</span>)    panic(<span class="hljs-string">&quot;kvmmap&quot;</span>);&#125;</code></pre></div><p>我们可以模仿内核页表初始化的方式初始化用户进程的kernel pagetable。</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-comment">// 模仿vm.c中kvminit的方式构建每个进程自己</span><span class="hljs-comment">// 的内核映射表 <span class="hljs-doctag">TODO:</span>删除</span><span class="hljs-function"><span class="hljs-keyword">pagetable_t</span></span><span class="hljs-function"><span class="hljs-title">proc_kpt_init</span><span class="hljs-params">()</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">pagetable_t</span> kpt;  kpt = <span class="hljs-built_in">uvmcreate</span>();  <span class="hljs-keyword">if</span> (kpt == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;  <span class="hljs-built_in">uvmmap</span>(kpt, UART0, UART0, PGSIZE, PTE_R | PTE_W);  <span class="hljs-built_in">uvmmap</span>(kpt, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W);  <span class="hljs-built_in">uvmmap</span>(kpt, CLINT, CLINT, <span class="hljs-number">0x10000</span>, PTE_R | PTE_W);  <span class="hljs-built_in">uvmmap</span>(kpt, PLIC, PLIC, <span class="hljs-number">0x400000</span>, PTE_R | PTE_W);  <span class="hljs-built_in">uvmmap</span>(kpt, KERNBASE, KERNBASE, (uint64)etext-KERNBASE, PTE_R | PTE_X);  <span class="hljs-built_in">uvmmap</span>(kpt, (uint64)etext, (uint64)etext, PHYSTOP-(uint64)etext, PTE_R | PTE_W);  <span class="hljs-built_in">uvmmap</span>(kpt, TRAMPOLINE, (uint64)trampoline, PGSIZE, PTE_R | PTE_X);  <span class="hljs-keyword">return</span> kpt;&#125;<span class="hljs-comment">// 添加映射到用户进程的kernel pagetable</span><span class="hljs-function"><span class="hljs-keyword">void</span> </span><span class="hljs-function"><span class="hljs-title">uvmmap</span><span class="hljs-params">(<span class="hljs-keyword">pagetable_t</span> pagetable, uint64 va, uint64 pa, uint64 sz, <span class="hljs-keyword">int</span> perm)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">if</span>(<span class="hljs-built_in">mappages</span>(pagetable, va, sz, pa, perm) != <span class="hljs-number">0</span>)    <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;uvmmap&quot;</span>);&#125;</code></pre></div><h4 id="Simplify-copyin-copyinstr"><a href="#Simplify-copyin-copyinstr" class="headerlink" title="Simplify copyin/copyinstr"></a>Simplify <code>copyin/copyinstr</code></h4>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Stanford CS144 Lab 4: TCP connection</title>
    <link href="/2022/01/13/Stanford-CS144-Lab-4-TCP-Connection/"/>
    <url>/2022/01/13/Stanford-CS144-Lab-4-TCP-Connection/</url>
    
    <content type="html"><![CDATA[<p>在这一关要求实现一个TCPConnection类，基于TCP的有限状态机，将 sender 和 receiver 封装起来。也就是说，作为一个TCP客户端，我们有自己的sender和receiver，现在要用这个客户端与其他主机进行联系，所以我们要实现TCP的各种FSM。</p><p>在tests文件夹中，我们可以看到很多关于fsm的测试文件，这些就是基于不同的有限状态机的测试，因此，我们掌握了TCP的有限状态机，才能将这个实验完成。这里的 fsm 测试名不是 RFC 里标准状态名，而是 CS144 按测试场景拆出来的名字。可以先把它们理解成下面这些场景：</p><div class="table-container"><table><thead><tr><th>状态机</th><th>含义</th></tr></thead><tbody><tr><td>fsm_connect</td><td>主动打开连接，发送 SYN，并在收到 SYN+ACK 后补 ACK，完成三次握手。</td></tr><tr><td>fsm_listen</td><td>被动打开连接，先收到对端 SYN，再发送 SYN+ACK，模拟服务端接受连接。</td></tr><tr><td>fsm_ack_rst</td><td>收到带 RST 的报文后立即异常关闭连接，并把 sender/receiver 的 stream 标记为 error。</td></tr><tr><td>fsm_ack_active</td><td>本端主动关闭时发送 FIN，并正确处理对端对 FIN 的 ACK。</td></tr><tr><td>fsm_ack_passive</td><td>对端先发送 FIN，本端进入被动关闭路径，需要 ACK 对端 FIN，并在本端输入结束后再发送自己的 FIN。</td></tr><tr><td>fsm_active_close</td><td>完整主动关闭流程：本端 <code>end_input()</code> 后发送 FIN，等待对端 ACK 和 FIN，最后进入 TIME_WAIT。</td></tr><tr><td>fsm_passive_close</td><td>完整被动关闭流程：先收到对端 FIN，之后本端写完数据并发送 FIN，最终双方关闭。</td></tr><tr><td>fsm_retx</td><td>重传定时器相关场景：超时后重发未确认 segment，超过最大重传次数后发送 RST 并关闭。</td></tr><tr><td>fsm_win</td><td>接收窗口和 ACK 字段相关场景：每次发出 segment 前都要带上正确 ackno 和 window size。</td></tr></tbody></table></div><p>把这些测试场景串起来看，<code>TCPConnection</code> 要做的事其实很清楚：<code>TCPSender</code> 负责“我还能发什么”，<code>TCPReceiver</code> 负责“我收到了什么以及该 ACK 到哪里”，而 <code>TCPConnection</code> 负责把两边粘起来，处理 RST、ACK、窗口、定时器和连接生命周期。</p><p>我们先来看看到底要实现哪些类</p><p>在上述代码中，可以看到有一个之前没有接触过的类——<code>TCPConfig</code>，所以我们首先来了解一下<code>TCPConfig</code>。我们到<code>TCPConfig</code>类中可以看到，它是对一些相关参数设置默认大小，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TCPConfig</span> &#123;</span>  <span class="hljs-keyword">public</span>:    <span class="hljs-keyword">static</span> <span class="hljs-keyword">constexpr</span> <span class="hljs-keyword">size_t</span> DEFAULT_CAPACITY = <span class="hljs-number">64000</span>;  <span class="hljs-comment">//!&lt; Default capacity</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">constexpr</span> <span class="hljs-keyword">size_t</span> MAX_PAYLOAD_SIZE = <span class="hljs-number">1000</span>;   <span class="hljs-comment">//!&lt; Conservative max payload size for real Internet</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">constexpr</span> <span class="hljs-keyword">uint16_t</span> TIMEOUT_DFLT = <span class="hljs-number">1000</span>;     <span class="hljs-comment">//!&lt; Default re-transmit timeout is 1 second</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">constexpr</span> <span class="hljs-keyword">unsigned</span> MAX_RETX_ATTEMPTS = <span class="hljs-number">8</span>;   <span class="hljs-comment">//!&lt; Maximum re-transmit attempts before giving up</span>    <span class="hljs-keyword">uint16_t</span> rt_timeout = TIMEOUT_DFLT;       <span class="hljs-comment">//!&lt; Initial value of the retransmission timeout, in milliseconds</span>    <span class="hljs-keyword">size_t</span> recv_capacity = DEFAULT_CAPACITY;  <span class="hljs-comment">//!&lt; Receive capacity, in bytes</span>    <span class="hljs-keyword">size_t</span> send_capacity = DEFAULT_CAPACITY;  <span class="hljs-comment">//!&lt; Sender capacity, in bytes</span>    std::optional&lt;WrappingInt32&gt; fixed_isn&#123;&#125;;&#125;;</code></pre></div><p>在<code>_receiver</code>等变量中通过<code>TCPConfig</code>默认大小。刚看到<code>TCPConnection</code>类时可能无从下手，我们先运行一下check程序，从第一个 failed 入手（没错，本懒狗是边做lab边写博客，面向tests编程）。</p><p>我们可以看到第一个 failed 出现在第35个测试，也就是fsm_passive_close，这个测试检测的是有限状态机中的初始状态（？）我们在项目中找到指定位置，开始debug，发现是<code>void TCPConnection::connect()</code>方法的错误，当我们要建立连接的时候，我们想到的还是三次握手，如下图：</p><p>（还是以客户端为例子）首先，我们要进行第一次握手，也就是客户端发送SYN报文，在TCPSender类中，我们实现了fill_window方法，里面包含了三次握手中第一次握手的过程。在TCPConnection中，有相关的建立连接的方法，即<code>void TCPConnection::connect()</code>方法，于是我们将sender中的握手的过程加入函数当中，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPConnection::connect</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">// send SYN</span>    _sender.<span class="hljs-built_in">fill_window</span>();    <span class="hljs-built_in">real_send</span>();&#125;</code></pre></div><p>这就是第一次握手时的场景，调用了fill_window()，在没有预设参数的情况下，fill_window会默认是第一次握手，从而设置对应的syn和seqno值。这样就完成了建立连接的第一步。为了使得发送报文可以模块化操作，实现real_send函数，real_send函数负责将_sender.segment_out队列中的报文拿出来，放入到 _segment_out队列中，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">TCPConnection::real_send</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">bool</span> isSend = <span class="hljs-literal">false</span>;    <span class="hljs-keyword">while</span> (!_sender.<span class="hljs-built_in">segments_out</span>().<span class="hljs-built_in">empty</span>()) &#123;        isSend = <span class="hljs-literal">true</span>;        TCPSegment segment = _sender.<span class="hljs-built_in">segments_out</span>().<span class="hljs-built_in">front</span>();        _sender.<span class="hljs-built_in">segments_out</span>().<span class="hljs-built_in">pop</span>();        <span class="hljs-built_in">set_ack_and_windowsize</span>(segment);        _segments_out.<span class="hljs-built_in">push</span>(segment);    &#125;    <span class="hljs-keyword">return</span> isSend;&#125;</code></pre></div><p>当收到服务器的第二次握手时，就会调用TCPConnection中的<code>void TCPConnection::segment_received(const TCPSegment &amp;seg)</code>函数，这个函数的参数是收到的segment，当收到这个segment的时候，客户端就要进行第三次握手，当然，我们的第三次握手就可以看成正常的发送报文了。那么我们来看看收到报文之后应该如何处理。首先，我们将segment给到我们的 _receiver中：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">// give the segment to receiver</span>_receiver.<span class="hljs-built_in">segment_received</span>(seg);</code></pre></div><p><img src="https://imgconvert.csdnimg.cn/aHR0cDovL2ltZy5ibG9nLmNzZG4ubmV0LzIwMTcwNjA2MDg0ODUxMjcy?x-oss-process=image/format,png" alt=""></p><p>在TCP协议中，rst用来异常的关闭连接。在TCP的设计中它是不可或缺的，发送rst段关闭连接时，不必等缓冲区的数据都发送出去，直接丢弃缓冲区中的数据，而接收端收到rst后，也不必发送ack来确认。</p><p>什么时候发送RST包：</p><ol><li>建立连接的SYN到达某端口，但是该端口上没有正在监听的服务。</li><li>TCP收到了一个根本不存在的连接上的分节。</li><li>请求超时。 使用setsockopt的SO_RCVTIMEO选项设置recv的超时时间。接收数据超时时，会发送RST包。</li></ol><p>当收到的segment的rst段为1，那么我们直接断开连接（非正常关闭），而不必发送ack来确认，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">if</span> (seg.<span class="hljs-built_in">header</span>().rst) &#123;    _sender.<span class="hljs-built_in">stream_in</span>().<span class="hljs-built_in">set_error</span>();    _receiver.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">set_error</span>();    _active = <span class="hljs-literal">false</span>;    <span class="hljs-keyword">return</span>;&#125;</code></pre></div><p>在<code>write</code>函数中，我们需要将数据写入到stream中，然后返回写入stream的data的长度，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">TCPConnection::write</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;data)</span> </span>&#123;    <span class="hljs-comment">//如果data的为空则直接返回</span>    <span class="hljs-keyword">if</span> (data.<span class="hljs-built_in">empty</span>()) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    <span class="hljs-comment">//否则将数据写入_sender的stream_in，等待fill_window将其封装为报文</span>    <span class="hljs-keyword">size_t</span> actually_write = _sender.<span class="hljs-built_in">stream_in</span>().<span class="hljs-built_in">write</span>(data);    <span class="hljs-comment">//封装data为segment</span>    _sender.<span class="hljs-built_in">fill_window</span>();    <span class="hljs-built_in">real_send</span>();    <span class="hljs-comment">//返回实际长度</span>    <span class="hljs-keyword">return</span> actually_write;&#125;</code></pre></div><p>在关闭TCP连接也就是“四次握手”的时候，我们会有一个<code>TIME_WAIT</code>的状态，在这个状态下客户端会等待2MSL才断开，这其实有两个原因：</p><ol><li>保证全双工的连接能够可靠关闭。</li><li>保证这次连接的数据段彻底从网络中消失。</li></ol><p>在这里我们要创建一个real_send()函数，我们都知道在sender中其实只是把数据传到了队列segment_out里面来作为发送数据的操作，在这个任务中我们将完成剩余的操作，也就是将数据发送出去，所以我们需要完成real_send()函数。</p><p>在segment_received()函数中，我们要模拟的是TCP收到数据的情况，其框架如下：</p><div class="code-wrapper"><pre><code class="hljs c++">_receiver.<span class="hljs-built_in">segment_received</span>(seg);<span class="hljs-comment">//将收到的seg交给_receiver的相关函数进行处理</span><span class="hljs-keyword">if</span> (seg.<span class="hljs-built_in">header</span>().ack) &#123;<span class="hljs-comment">//收到ack报文的相关操作，也就是将其ackno和win交给_sender的相关函数，让其准备下一次发送</span>    <span class="hljs-comment">//</span>&#125;</code></pre></div><p>在tick函数中，</p><p>在lab4中，我们可以检测出前面实验的错误：</p><ol><li><p>我删除了很多不必要的代码</p></li><li></li></ol><hr><h5 id="TCP可靠传输"><a href="#TCP可靠传输" class="headerlink" title="TCP可靠传输"></a>TCP可靠传输</h5><p>做到这里，我们的TCP也就算实现了基本功能，我们也就知道了TCP协议如何保证可靠传输：</p><ol><li>流量控制：TCP连接双方都有固定大小的缓冲空间，TCP接收端只允许发送端发送接收端缓冲区能接纳的数据。当接收方来不及处理发送方的数据的时候，就提示发送方降低发送的速率。</li><li>拥塞控制：当网络拥塞时，减少数据的发送（这个在lab中没有什么体现）。</li><li>超时重传：当TCP发出一个段后，就会启动一个定时器，如果不能及时收到确认，就重发这个报文段。</li><li>校验和：（这个好像在后面的实验中有体现）。</li></ol><p>再补充一个知识点——<code>socket</code></p><h5 id="TCP长连接"><a href="#TCP长连接" class="headerlink" title="TCP长连接"></a>TCP长连接</h5><p>在HTTP1.0中，默认使用的是短连接。也就是说，浏览器和服务器每进行一次HTTP操作，就建立一次连接，但任务结束后就中断连接。但从HTTP1.1开始，默认使用长连接，用以保持连接特性。使用长连接的HTTP协议，会在响应头加入如下字段:</p><div class="code-wrapper"><pre><code class="hljs armasm"><span class="hljs-symbol">Connection:</span><span class="hljs-meta">keep</span>-alive</code></pre></div><p>HTTP协议的长连接和短连接，实质上是TCP协议的长连接和短连接。</p><h6 id="TCP保活机制"><a href="#TCP保活机制" class="headerlink" title="TCP保活机制"></a>TCP保活机制</h6><p>保活功能主要为服务器应用提供，服务器应用希望知道客户主机是否崩溃，从而可以代表客户使用资源。如果客户已经消失，使得服务器上保留一个半开放的连接，而服务器又在等待来自客户端的数据，则服务器将应远等待客户端的数据，保活功能就是试图在服务器端检测到这种半开放的连接。对于设置了keepalive来说，当<strong>tcp</strong>检测到对端<strong>socket</strong>不再可用时<strong>(</strong>不能发出探测包<strong>,</strong>或探测包没有收到<strong>ACK</strong>的响应包<strong>),select</strong>会返回<strong>socket</strong>可读<strong>,</strong>并且在<strong>recv</strong>时返回<strong>-1,</strong>同时置上<strong>errno</strong>为ETIMEDOUT。此时TCP的状态是断开的。</p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Stanford CS144 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081: xv6 实验参考书解析</title>
    <link href="/2022/01/10/MIT-6-S081-xv6-Book-Notes/"/>
    <url>/2022/01/10/MIT-6-S081-xv6-Book-Notes/</url>
    
    <content type="html"><![CDATA[<h3 id="第0章"><a href="#第0章" class="headerlink" title="第0章"></a>第0章</h3><h4 id="操作系统接口"><a href="#操作系统接口" class="headerlink" title="操作系统接口"></a>操作系统接口</h4><p>本书通过xv6操作系统来阐述操作系统的概念，它提供Unix操作系统中的基本接口，同时模仿Unix的内部设计。</p><blockquote><p>xv6提供了传统的内核概念，<strong>即一个向其他运行的程序提供服务的特殊程序</strong>，每一个运行中的进程都有指令、数据、栈的内存空间。进程通过系统调用使用内核服务。系统调用会进入内核，让内核执行服务然后返回。所以进程总是在用户空间和内核空间之间交替运行。</p></blockquote><p>我们看到上面加粗的那句话：</p><blockquote><p>xv6提供了传统的内核概念，即一个向其他运行的程序提供服务的特殊程序。</p></blockquote><p>如果说一个进程分了用户空间和内核空间，那么用户态-&gt;内核态相当于是cpu特权级别的切换和栈指针的切换，但进程还是那个进程。但上面那句话表达的意思是一旦用户进入内核态就又是另一个进程，这一点我感觉有点歧义。后面查阅了其他资料后才有了解释：</p><hr><p>在 Linux 内核里，进程和线程都用 <code>task_struct</code> 描述。线程通常可理解为“共享地址空间等资源的一组 task”，而不是单独再定义一套完全不同的内核实体；它们的执行上下文、栈等仍是各自独立的。</p><p>回到 xv6，默认实现里核心抽象是 <code>proc</code>，每个 <code>proc</code> 对应一条执行流；它并没有像 Linux 那样提供完整的“同进程多线程”用户模型。</p><p>因此，上面那句“内核是向其他程序提供服务的特殊程序”更适合理解为“内核/用户态的职责划分”，而不是在这里直接推出 xv6 的线程模型。</p><p>（后面我们可以看到，这个解释其实依然有问题，但他的参考书是这样写的，我们只能暂时这样理解）</p><hr><p>一个xv6进程由两部分组成，一部分是用户内存空间（指令、数据、栈），另一部分是仅对内核可见的进程状态。我们直接看看<code>proc</code>结构：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Per-process state</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spinlock</span> <span class="hljs-title">lock</span>;</span>  <span class="hljs-comment">// p-&gt;lock must be held when using these:</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">procstate</span> <span class="hljs-title">state</span>;</span>        <span class="hljs-comment">// Process state</span>  <span class="hljs-keyword">void</span> *chan;                  <span class="hljs-comment">// If non-zero, sleeping on chan</span>  <span class="hljs-keyword">int</span> killed;                  <span class="hljs-comment">// If non-zero, have been killed</span>  <span class="hljs-keyword">int</span> xstate;                  <span class="hljs-comment">// Exit status to be returned to parent&#x27;s wait</span>  <span class="hljs-keyword">int</span> pid;                     <span class="hljs-comment">// Process ID</span>  <span class="hljs-comment">// wait_lock must be held when using this:</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">parent</span>;</span>         <span class="hljs-comment">// Parent process</span>  <span class="hljs-comment">// these are private to the process, so p-&gt;lock need not be held.</span>  uint64 kstack;               <span class="hljs-comment">// Virtual address of kernel stack</span>  uint64 sz;                   <span class="hljs-comment">// Size of process memory (bytes)</span>  <span class="hljs-keyword">pagetable_t</span> pagetable;       <span class="hljs-comment">// User page table</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">trapframe</span> *<span class="hljs-title">trapframe</span>;</span> <span class="hljs-comment">// data page for trampoline.S</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">context</span> <span class="hljs-title">context</span>;</span>      <span class="hljs-comment">// swtch() here to run process</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> *<span class="hljs-title">ofile</span>[<span class="hljs-title">NOFILE</span>];</span>  <span class="hljs-comment">// Open files</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">cwd</span>;</span>           <span class="hljs-comment">// Current directory</span>  <span class="hljs-keyword">char</span> name[<span class="hljs-number">16</span>];               <span class="hljs-comment">// Process name (debugging)</span>  <span class="hljs-keyword">pagetable_t</span> kernel_pt;&#125;;</code></pre></div><p>如上所示，<code>uint64 kstack;</code>代表内核栈地址，而<code>enum procstate state;</code>则是进程状态，<code>pagetable_t pagetable;</code>则是用户空间的页表。</p><p>我们来看看具体的资源分布情况：</p><ul><li>指令实现了程序的运算。</li><li>数据是用于运算中的变量。</li><li>栈管理了程序的过程调用。</li></ul><p>进程通过系统调用使用内核服务。系统调用会进入内核，让内核执行服务然后返回，所以进程总是在用户空间和内核空间中交替运行的。</p><div class="table-container"><table><thead><tr><th>系统调用</th><th>描述</th></tr></thead><tbody><tr><td>fork()</td><td>创建进程</td></tr><tr><td>exit()</td><td>结束当前进程</td></tr><tr><td>wait()</td><td>等待子进程结束</td></tr><tr><td>kill(pid)</td><td>结束pid所指进程</td></tr><tr><td>getpid()</td><td>获取当前进程pid</td></tr><tr><td>sleep(n)</td><td>睡眠n秒</td></tr><tr><td>exec(filename,*argv)</td><td>加载并执行一个文件</td></tr><tr><td>sbrk(n)</td><td>为进程内存空间增加n字节</td></tr><tr><td>open(filename,flags)</td><td>打开文件，flag指定读写模式</td></tr><tr><td>read(fd,buf,n)</td><td>从文件中读取n个字节到buf</td></tr><tr><td>write(fd,buf,n)</td><td>从buf中写n个字节到文件</td></tr><tr><td>close(fd)</td><td>关闭打开的fd</td></tr><tr><td>dup(fd)</td><td>复制fd</td></tr><tr><td>pipe(p)</td><td>创建管道，并把读和写的fd返回到p</td></tr><tr><td>chdir(dirname)</td><td>改变当前目录</td></tr><tr><td>mkdir(dirname)</td><td>创建新的目录</td></tr><tr><td>mknod(name,major,minor)</td><td>创建设备文件</td></tr><tr><td>fstat(fd)</td><td>返回文件信息</td></tr><tr><td>link(f1,f2)</td><td>给f1创建一个新名字（f2）</td></tr><tr><td>unlink(filename)</td><td>删除文件</td></tr></tbody></table></div><p>系统调用的具体讲解我分布在其他博客中了。</p><h4 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a>文件描述符</h4><p>文件描述符是一个整数，它代表了一个进程可以读写的被内核管理的对象。进程可以通过多种方法获得一个文件描述符，如打开文件、目录、设备或者创建一个管道，或者复制已经存在的文件描述符。简单起见，我们常常把文件描述符指向的对象称为“文件”，文件描述符的接口是对文件、管道、设备等的抽象，这种抽象使得它们看上去就是字节流。</p><p>每个进程都有一张表，而xv6内核就是以文件描述符作为这张表的索引，所以每个进程都有一个从0开始的文件描述符空间。</p><p>传统 Unix 风格接口以同步 I/O 语义为主（阻塞、非阻塞、I/O 多路复用都属于同步通知模型）；现代系统也提供异步 I/O 机制，只是接口与使用方式更复杂。</p><p>对于异步IO，异步系统调用会立即返回，不会等待 I/O 操作的完成，应用程序可以继续执行其他的操作，等到 I/O 操作完成了以后，操作系统会通知调用进程。</p><p>对于同步IO：</p><ul><li>阻塞IO，如果IO没有就绪就一直block，直到IO就绪。</li><li>非阻塞IO，</li></ul><h5 id="系统调用read和write"><a href="#系统调用read和write" class="headerlink" title="系统调用read和write"></a>系统调用read和write</h5><p>系统调用<code>read</code>和<code>write</code>从文件描述符所指的文件中读或者写n个字节。<code>read</code>从fd中读最多n个字节，将它们拷贝到buffer中，然后返回读出的字节数。每一个指向文件的文件描述符都和一个偏移量有关，</p><p>我们都知道在linux中有一切皆文件的说法，linux中的所有内容都是以文件的形式保存和管理的，在linux中，文件具体可分为以下几种类型：</p><ul><li><p>普通文件</p><p>类似txt、jpg这种可以直接拿来使用的都是普通文件，linux用户根据访问权限的不同可以对于这些文件进行查看、删除以及更改操作。</p></li><li><p>目录文件</p><p>在linux中，目录文件包含了此目录中各个文件的文件名以及执行这个文件的指针，打开目录等同于打开目录文件。</p></li><li></li></ul><h6 id="“一切皆文件”的利弊"><a href="#“一切皆文件”的利弊" class="headerlink" title="“一切皆文件”的利弊"></a>“一切皆文件”的利弊</h6><p>linux中所有读操作都可以用read函数来进行，几乎所有更改的操作都可以用write函数来进行。不过任何硬件设备都必须跟根目录下某一目录执行挂载操作，否则无法使用。</p><h3 id="第一章"><a href="#第一章" class="headerlink" title="第一章"></a>第一章</h3><p>xv6使用页表（由硬件实现）来为每个进程提供其独有的地址空间。页表将虚拟地址翻译为物理地址。xv6为每个进程维护了不同的页表，这样就能够合理地定义进程的地址空间了，一片地址空间包含了从虚拟地址0开始的用户内存，</p><h3 id="第二章"><a href="#第二章" class="headerlink" title="第二章"></a>第二章</h3><p>这一章细说页表。</p><p>操作系统通过页表机制实现了对内存空间的控制。除了传统的功能——让不同的进程各自的进程空间映射到相同的物理内存上，还能够为不同的进程的内存提供保护，我们还能够通过使用页表来间接实现一些特殊功能。xv6主要利用页表来</p><h3 id="第三章"><a href="#第三章" class="headerlink" title="第三章"></a>第三章</h3><p>运行程序时，CPU一直处于一个大循环中：取指、更新PC、执行，取指。但有些情况下用户程序需要进入内核，而不是执行下一条用户指令，这些情况包括设备信号的发出，用户程序的非法操作，也就是我们所说的用户态进入内核态的三种情况——中断、异常和系统调用。处理这些情况面临三大挑战：</p><ol><li>内核必须使处理器能够从用户态转换到内核态（并且再转回到用户态）。</li><li>内核和设备必须协调好他们并行的活动。</li><li>内核必须知道硬件接口的细节。</li></ol><p>用户程序通过系统调用请求系统服务；硬件产生引起操作系统注意的信号<code>interrupt</code>；非法程序操作产生的<code>exception</code>，都会使得用户态进入内核态。在所有的三种情况下，我们都要保证：系统必须保存寄存器以备将来的状态恢复。</p><h3 id="第四章"><a href="#第四章" class="headerlink" title="第四章"></a>第四章</h3><p>xv6运行在多处理器上，即计算机上有多个单独执行代码的CPU，这些CPU操作同一片地址空间并分享其中的数据结构，xv6必须建立一种合作机制防止它们互相干扰，即使是在单个处理器上，xv6也必须使用某些机制来防止中断处理程序与非中断代码之间的相互干扰。xv6为这两种情况的使用提供了底层概念——锁。</p><h4 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h4><p><code>trap</code>调用<code>syscall</code>，<code>syscall</code>从中断帧中读出系统调用号。</p><h3 id="第五章"><a href="#第五章" class="headerlink" title="第五章"></a>第五章</h3><h4 id="关于进程"><a href="#关于进程" class="headerlink" title="关于进程"></a>关于进程</h4><p>xv6这个版本还没有线程的概念，因此我们要从proc（进程）的视角来看待它的运行和调度过程。</p><blockquote><p>“在大部分情况下，我们认为内核态是一种CPU的特权态，这个特权态下，CPU可以执行这个特权态才允许执行的指令，访问这个特权态才允许访问的资源。这和当前的进程无关。”</p></blockquote><p>上面这句话描述的对象是CPU，从CPU的角度来说什么是内核态。对于CPU来说，用户态——内核态就是硬件上一个标志位的切换，从而可以执行一些特权级才能执行的指令。</p><p>那么我们从进程的角度来说内核态是怎么样的呢？援引陈海波教授《现代操作系统：原理与实现》的说法：</p><blockquote><p>处于进程地址空间最顶端的是内核内存。每个进程的虚拟地址空间里都映射了相同的内核内存。当进程在用户态运行时，内核内存对其不可见；只有当进程进入内核态时，才能访问内核内存。与用户态相似，内核部分也有内核需要的代码和数据段，当进程由于中断或系统调用进入内核后，会使用内核的栈。</p></blockquote><p>这一点在xv6的<code>proc</code>结构也有所体现，我们来介绍一下<code>proc</code>结构，详情见<code>kernel/proc.h</code>：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Per-process state</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spinlock</span> <span class="hljs-title">lock</span>;</span>  <span class="hljs-comment">// p-&gt;lock must be held when using these:</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">procstate</span> <span class="hljs-title">state</span>;</span>        <span class="hljs-comment">// Process state</span>  <span class="hljs-keyword">void</span> *chan;                  <span class="hljs-comment">// If non-zero, sleeping on chan</span>  <span class="hljs-keyword">int</span> killed;                  <span class="hljs-comment">// If non-zero, have been killed</span>  <span class="hljs-keyword">int</span> xstate;                  <span class="hljs-comment">// Exit status to be returned to parent&#x27;s wait</span>  <span class="hljs-keyword">int</span> pid;                     <span class="hljs-comment">// Process ID</span>  <span class="hljs-comment">// wait_lock must be held when using this:</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">parent</span>;</span>         <span class="hljs-comment">// Parent process</span>  <span class="hljs-comment">// these are private to the process, so p-&gt;lock need not be held.</span>  uint64 kstack;               <span class="hljs-comment">// Virtual address of kernel stack</span>  uint64 sz;                   <span class="hljs-comment">// Size of process memory (bytes)</span>  <span class="hljs-keyword">pagetable_t</span> pagetable;       <span class="hljs-comment">// User page table</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">trapframe</span> *<span class="hljs-title">trapframe</span>;</span> <span class="hljs-comment">// data page for trampoline.S</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">context</span> <span class="hljs-title">context</span>;</span>      <span class="hljs-comment">// swtch() here to run process</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> *<span class="hljs-title">ofile</span>[<span class="hljs-title">NOFILE</span>];</span>  <span class="hljs-comment">// Open files</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">cwd</span>;</span>           <span class="hljs-comment">// Current directory</span>  <span class="hljs-keyword">char</span> name[<span class="hljs-number">16</span>];               <span class="hljs-comment">// Process name (debugging)</span>  <span class="hljs-keyword">pagetable_t</span> kernel_pt;&#125;;</code></pre></div><p>在上述代码中，我们可以看到如下成员：</p><ul><li><p><code>int killed</code>，表示进程是否被杀死；</p></li><li><p><code>struct proc *parent</code>，指向父进程；</p></li><li><p><code>uint sz</code>，进程空间的大小；</p><p>……..</p></li></ul><p>我们可以看到有一个<code>uint64 kstack</code>的成员，这就是进程内核栈的地址。当进程由于中断或系统调用进入内核后，会使用内核的栈。类比到 Linux，对应的调度实体是 <code>task_struct</code>，但 <code>proc</code> 并不等于 <code>task_struct</code> 的“改名版”，两者属于不同内核实现中的结构体定义。</p><hr><hr><p>值得注意的是：在 Linux 内核中，进程和线程都用 <code>task_struct</code> 描述，线程本质上是“共享更多资源的 task”。因此它们在统一调度框架下运行，但并不是“没有线程概念”，而是“用统一抽象表示进程/线程”。</p><hr><hr><p>我们来看看进程的process memory是如何分布的，也就是它的虚拟地址空间如何分布，其实在<code>proc</code>结构体中，我们有了<code>pagetable</code>、<code>sz</code>和<code>kstack</code>就可以基本描述我们的虚拟用户空间，如下图所示：</p><p>内核在创建进程的时候，会为进程创建相应的堆栈。每个进程会有两个栈，一个用户栈，存在于用户空间，一个内核栈，存在于内核空间。当进程在用户空间运行时，CPU堆栈指针寄存器里面的内容是用户堆栈地址，使用用户栈；当进程在内核空间时，CPU堆栈指针寄存器里面的内容是内核栈空间地址，使用内核栈。当进程因为中断或者系统调用而陷入内核态时，进程所使用的堆栈也要从用户栈转到内核栈。进程进入内核态后，先把用户态堆栈的地址保存在内核栈中，然后设置堆栈指针寄存器的内容为内核栈的地址，这样就完成了用户栈向内核栈的切换。</p><p>值得注意的是：用户堆是低地址到高地址的，而用户栈是高地址到低地址的。</p><p>进程的上下文包括进程运行时的寄存器状态，其能够用于保存和恢复一个进程在处理器上运行的状态。我们看看context的结构就知道了：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Saved registers for kernel context switches.</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">context</span> &#123;</span>  uint64 ra;  uint64 sp;  <span class="hljs-comment">// callee-saved</span>  uint64 s0;  uint64 s1;  uint64 s2;  uint64 s3;  uint64 s4;  uint64 s5;  uint64 s6;  uint64 s7;  uint64 s8;  uint64 s9;  uint64 s10;  uint64 s11;&#125;;</code></pre></div><p>这里保存的全是寄存器状态。</p><p>进程间切换：进程间的切换可以看成是PCB的切换，也就是地址空间的切换、<code>context</code>的切换以及栈指针的切换。当进程要切换的时候就会把它（now_process)的状态保存在$PCB_1$中，然后将下一个进程先前保存的$PCB_2$取出来，将$PCB_2$的<code>context</code>的状态取出来恢复到对应的寄存器中，然后将用然后将$PCB_2$中的其他信息（pagetable、stack、pid等）写入now_process中，从而切换到该进程执行。</p><h4 id="调度与多路复用"><a href="#调度与多路复用" class="headerlink" title="调度与多路复用"></a>调度与多路复用</h4><p>当进程数&gt;处理器的数量的时候，我们就要考虑如何分享处理器资源，即如何调度资源。通常我们对进程造成一个自己独占处理器的家乡，然后让操作系统的多路复用机制将单独的一个物理处理器模拟为多个虚拟处理器。</p><p>当一个进程等待磁盘请求的时候，xv6使之进入睡眠状态，然后调度执行另一个进程。另外，当一个进程耗尽了它在处理器上的时间片的时候，xv6使用时钟中断强制它停止运行，这样调度器才能调度运行其他进程，这样的多路复用机制为进程提供了独占处理器的假象。</p><p>进程调度的主要功能是按照一定的策略选择—个处于就绪状态的进程，使其获得处理机执行。根据不同的系统设计目的，有各种各样的选择策略，例如系统开销较少的静态优先数调度法，适合于分时系统的轮转法(Round RoLin) 和多级互馈轮转法(Round Robin with Multip1e feedback) 等。这些选择策略决定了调度算法的性能。</p><h5 id="进程切换"><a href="#进程切换" class="headerlink" title="进程切换"></a>进程切换</h5><h6 id="上下文切换"><a href="#上下文切换" class="headerlink" title="上下文切换"></a>上下文切换</h6><p>xv6在底层实现了两种上下文切换：</p><ol><li>从进程的内核线程切换到当前CPU的调度器线程；</li><li>从调度器线程切换到进程的内核线程。</li></ol><h6 id="具体的实现过程"><a href="#具体的实现过程" class="headerlink" title="具体的实现过程"></a>具体的实现过程</h6><p>进程的切换是通过用户态——内核态切换、切换到调度器、切换到新进程的内核线程、最后返回用户态实现的。如下图所示：</p><blockquote><p>好了，然后工程师们就发现，其实一个线程分为 “内核态 “线程和” 用户态 “线程。</p><p><a href="https://learnku.com/articles/41728">《GMP调度器》</a></p></blockquote><p>这其实是一种诙谐的说法，我们在上面看到确实一个线程（进程）是分为用户态和内核态的。</p><p>每个xv6进程都有自己的内核空间以及内核栈以及寄存器集合。每个CPU都有一个单独的调度器线程，这样调度就不会发生在进程的内核线程中，而是在此调度器线程中，线程切换涉及到了保存旧线程的CPU寄存器，恢复新线程之前保存的寄存器。</p><p>当进程让出CPU时，进程的内核线程调用swtch来保存自身进程的上下文然后返回到调度器的上下文中。这也就是第一个swtch，第二个swtch同理，swtch简单的保存和恢复寄存器集合。每个上下文以结构体<code>struct context*</code>表示，实际上就是一个保存在内核栈中的指针，swtch有两个参数，<code>struct context *old</code>，<code>struct context *new</code>。</p><p>回到上面我标成黄色的句子，xv6默认实现里没有 Linux 那样的多线程用户进程模型（每个 <code>proc</code> 对应一个执行流），所以书里说“线程”时更多是在解释调度语义，而不是说 xv6 已经实现了完整的 pthread 风格线程库。Linux 中进程/线程在内核里都是 <code>task_struct</code>，差别主要体现在资源是否共享。</p><p>在 xv6 中，可以把一次系统调用/中断理解为“同一执行流在用户态与内核态之间切换”，其中 <code>kstack</code> 是该执行流在内核态使用的栈地址。当发生切换时：</p><ol><li><p>将对应的用户栈的PC地址和寄存器信息保存起来（xv6中是保存在内核栈），方便后续内核方法调用完毕后，恢复用户方法执行的现场。</p></li><li><p>将CPU的字段改为内核态（硬件标志位），将内核段对应的代码地址写入到PC寄存器中，设置堆栈指针寄存器的内容为内核栈的地址，然后开始执行内核方法。</p></li><li><p>当内核方法执行完毕后，会将CPU的字段改为用户态，然后利用之前用户态的信息来恢复用户栈的执行。</p></li></ol><p>在 Linux 中，线程通常由 <code>clone</code> 系统调用支持实现，线程之间共享地址空间、文件描述符等资源，但拥有各自的执行上下文。<code>vfork</code> 更适合描述“创建后立刻 exec”的子进程场景，它会让父子进程暂时共享地址空间，但不能把它简单等同于线程创建。因此，更准确的说法是：线程是共享同一进程资源的一组执行流，而 xv6 这里讨论的 <code>swtch</code> 关注的是内核调度时如何保存和恢复执行上下文。</p><h3 id="第六章"><a href="#第六章" class="headerlink" title="第六章"></a>第六章</h3><h4 id="文件系统"><a href="#文件系统" class="headerlink" title="文件系统"></a>文件系统</h4><p>正如我们在第〇章所说，xv6的文件系统中使用了类似Unix的文件、文件描述符、目录和路径，并且把数据存储到一块内存，我们通过xv6中的描述来具体看看：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">open</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span>*, <span class="hljs-keyword">int</span>)</span></span>;</code></pre></div><p>第一个参数代表文件路径字符串，第二个参数是<code>flag</code>，表示以何种方式打开文件，返回值是文件描述符。说到这里，我们来说说文件描述符指向的结构：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> &#123;</span> FD_NONE, FD_PIPE, FD_INODE, FD_DEVICE &#125; type;  <span class="hljs-keyword">int</span> ref; <span class="hljs-comment">// reference count</span>  <span class="hljs-keyword">char</span> readable;<span class="hljs-comment">//能读</span>  <span class="hljs-keyword">char</span> writable;<span class="hljs-comment">//能写</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">pipe</span> *<span class="hljs-title">pipe</span>;</span> <span class="hljs-comment">// FD_PIPE</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">ip</span>;</span>  <span class="hljs-comment">// FD_INODE and FD_DEVICE</span>  uint off;          <span class="hljs-comment">// FD_INODE</span>  <span class="hljs-keyword">short</span> major;       <span class="hljs-comment">// FD_DEVICE</span>&#125;;</code></pre></div><h3 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h3><p>我们从一个实例来看看xv6的调度情况：</p><p>如上图所示：我们从一个shell语句开始，例如我们执行<code>read</code>这个语句，我们为了进入内核，要先执行如下代码：</p><div class="code-wrapper"><pre><code class="hljs assembly">read:li a7,SYS_readecallret.global read</code></pre></div><p>在上述代码中，我们会将系统调用号<code>SYS_read</code>放入a7寄存器</p>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MIT 6.S081 Lab 2: System calls</title>
    <link href="/2022/01/10/MIT-6-S081-Lab-2-System-Calls/"/>
    <url>/2022/01/10/MIT-6-S081-Lab-2-System-Calls/</url>
    
    <content type="html"><![CDATA[<p>在上一个实验中我们使用系统调用编写了一些utilities，在这个实验中我们需要为xv6增加一些系统调用，以便我们理解系统调用是如何工作的，并向我们展示xv6内核的一些内部结构。</p><p>我们先来说说什么是系统调用，操作系统作为用户和计算机硬件之间的接口，需要向上提供一些简单易用的服务，主要包括命令接口和程序接口。其中，程序接口由一组系统调用组成。应用程序通过系统调用请求操作系统的服务。系统中的各种共享资源都由操作系统统一掌握，因此在用户程序中，凡是与资源有关的操作(如存储分配、I/O操作、文件管理等)，都必须通过系统调用的方式向操作系统提出服务请求，由操作系统代为完成。</p><p>从某种角度来看，系统调用和普通的函数调用非常相似。区别仅仅在于，系统调用由操作系统核心提供，运行于核心态，而普通的函数调用由函数库或用户自己提供，运行于用户态。</p><p>用户程序执行到系统调用命令时，会发生中断，处理器由用户态变为内核态，操作系统的中断处理程序得到控制权，它根据系统调用的功能号，通过例行子程序入口地址表跳转到相应的例程中去执行，在完成了用户所需要的服务功能后，退出中断，返回到用户子程序的断点继续执行。</p><p>我们来看如下例子，来源于<a href="http://blog.codepiano.com/2017/03/01/read-file-from-disk">博客</a>：</p><hr><p>假设场景是一个 Linux 操作系统中运行的进程 P 需要读取文件 <code>/home/user/text.txt</code>。</p><p>如果进程P需要读取文件，首先要发起系统调用open，传入文件路径和相关参数，执行系统调用后操作系统会从用户态转换到内核态，切换到内核态后，操作系统调用相应的处理器开始处理读取文件的请求。然后内核控制硬件。</p><p>这个时候并不会直接读取磁盘中的文件到进程掌握的内存，而是先进行权限方面的检查，如果进程可以访问这个文件，就根据文件路径去查找文件对应的inode编号，这部分属于文件系统的内容，这里不做说明。</p><p>在获取 inode 以后，操作系统生成一个文件描述符，存储在进程 P 的数据结构中，通过文件描述符可以索引到文件的打开方式和要打开文件的 inode。至此系统调用 <code>open</code> 完成。</p><p>进程在得到文件描述符之后，还要再发起系统调用read，传入文件描述符来读取文件内容，在这里读取操作同样需要切换到内核态由内核代为完成，切换到内核态后，操作系统通过文件描述符找到对应的inode，通过inode来确定文件存储在磁盘的哪个扇区，然后读取这些扇区，把内容读取到内核的地址空间里面。</p><p>磁盘的IO完成之后，磁盘会触发一个中断，CPU会暂时终止当前线程的执行，保存相关的寄存器信息，然后把读取到的内容从内核地址空间拷贝到进程P的地址空间里面，然后进程P的状态设置为runnable，进程P排队等待自己的CPU时间片，被调度器调度以后可以继续执行。</p><hr><h4 id="syscall-c"><a href="#syscall-c" class="headerlink" title="syscall.c"></a>syscall.c</h4><p>syscall.c是系统调用程序，我们先来介绍在syscall中的几个函数。</p><h5 id="argint函数"><a href="#argint函数" class="headerlink" title="argint函数"></a>argint函数</h5><p>argint的函数原型为<code>int argint(int n,int *ip)</code>，其意义为获得第n个32位调用参数，该函数调用了argraw函数，在得到了相应的数据之后，保存在以ip为地址的空间中。</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span>(argint(<span class="hljs-number">0</span>,&amp;pid)&lt;<span class="hljs-number">0</span>)&#123;<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;&#125;</code></pre></div><h5 id="argraw函数"><a href="#argraw函数" class="headerlink" title="argraw函数"></a>argraw函数</h5><p>argraw的函数原型为<code>static uint64 argraw(int n)</code>，其意义为返回相应的寄存器中保存的值。代码如下图所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">switch</span> (n) &#123;<span class="hljs-keyword">case</span> <span class="hljs-number">0</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a0;<span class="hljs-keyword">case</span> <span class="hljs-number">1</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a1;<span class="hljs-keyword">case</span> <span class="hljs-number">2</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a2;<span class="hljs-keyword">case</span> <span class="hljs-number">3</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a3;<span class="hljs-keyword">case</span> <span class="hljs-number">4</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a4;<span class="hljs-keyword">case</span> <span class="hljs-number">5</span>:<span class="hljs-keyword">return</span> p-&gt;trapframe-&gt;a5;&#125;</code></pre></div><h5 id="syscall函数"><a href="#syscall函数" class="headerlink" title="syscall函数"></a>syscall函数</h5><p>syscall函数代码如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">syscall</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">int</span> num;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">p</span> =</span> myproc();  num = p-&gt;trapframe-&gt;a7;  <span class="hljs-keyword">if</span>(num &gt; <span class="hljs-number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num]) &#123;    p-&gt;trapframe-&gt;a0 = syscalls[num]();  &#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d %s: unknown sys call %d\n&quot;</span>,            p-&gt;pid, p-&gt;name, num);    p-&gt;trapframe-&gt;a0 = <span class="hljs-number">-1</span>;  &#125;&#125;</code></pre></div><p>设置一个proc指针，指向myproc，在<code>num = p-&gt;trapframe-&gt;a7;</code>中，将a7寄存器中的值赋值给num，a7寄存器中保存的就是系统调用的调用号，我们可以在user/usys.s中看见相关的代码（以fork系统调用为例）：</p><div class="code-wrapper"><pre><code class="hljs awk">fork: li a7, SYS_fork ecall ret.global <span class="hljs-keyword">exit</span></code></pre></div><p>也就是说，当一个程序需要作系统调用的时候，它将相关参数放进系统调用相关的寄存器。</p><p>然后判断调用号是否正确，如果正确的话就将<code>syscalls[num]()</code>赋值给a0，syscalls中调用号对应sys_xxx函数，也就是将要调用的函数的返回值赋值给a0。代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-title">uint64</span> <span class="hljs-params">(*syscalls[])</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>= &#123;[SYS_fork]    sys_fork,[SYS_exit]    sys_exit,[SYS_wait]    sys_wait,[SYS_pipe]    sys_pipe,[SYS_read]    sys_read,[SYS_kill]    sys_kill,[SYS_exec]    sys_exec,[SYS_fstat]   sys_fstat,[SYS_chdir]   sys_chdir,[SYS_dup]     sys_dup,[SYS_getpid]  sys_getpid,[SYS_sbrk]    sys_sbrk,[SYS_sleep]   sys_sleep,[SYS_uptime]  sys_uptime,[SYS_open]    sys_open,[SYS_write]   sys_write,[SYS_mknod]   sys_mknod,[SYS_unlink]  sys_unlink,[SYS_link]    sys_link,[SYS_mkdir]   sys_mkdir,[SYS_close]   sys_close,[SYS_trace]   sys_trace,&#125;;</code></pre></div><p>同样，我们在syscalls.c中定义了如下的外部函数，部分外部函数的实现在sysproc.c中，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_chdir</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_close</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_dup</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_exec</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_exit</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_fork</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_fstat</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_getpid</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_kill</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_link</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_mkdir</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_mknod</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_open</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_pipe</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_read</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_sbrk</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_sleep</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_unlink</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_wait</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_write</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_uptime</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_trace</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;</code></pre></div><h4 id="sysproc-c"><a href="#sysproc-c" class="headerlink" title="sysproc.c"></a>sysproc.c</h4><p>sysproc.c是系统进程，正如我们在xv6文档看到的那样，起到由用户转到内核的作用，通过syscall，可以进入sysproc执行相应的函数。</p><h5 id="proc数据结构"><a href="#proc数据结构" class="headerlink" title="proc数据结构"></a>proc数据结构</h5><p>proc数据结构表示进程状态，其数据结构如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// Per-process state</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> &#123;</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spinlock</span> <span class="hljs-title">lock</span>;</span>  <span class="hljs-comment">// p-&gt;lock must be held when using these:</span>  <span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">procstate</span> <span class="hljs-title">state</span>;</span>        <span class="hljs-comment">// Process state</span>  <span class="hljs-keyword">void</span> *chan;                  <span class="hljs-comment">// If non-zero, sleeping on chan</span>  <span class="hljs-keyword">int</span> killed;                  <span class="hljs-comment">// If non-zero, have been killed</span>  <span class="hljs-keyword">int</span> xstate;                  <span class="hljs-comment">// Exit status to be returned to parent&#x27;s wait</span>  <span class="hljs-keyword">int</span> pid;                     <span class="hljs-comment">// Process ID</span>  <span class="hljs-comment">// wait_lock must be held when using this:</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">proc</span> *<span class="hljs-title">parent</span>;</span>         <span class="hljs-comment">// Parent process</span>  <span class="hljs-comment">// these are private to the process, so p-&gt;lock need not be held.</span>  uint64 kstack;               <span class="hljs-comment">// Virtual address of kernel stack</span>  uint64 sz;                   <span class="hljs-comment">// Size of process memory (bytes)</span>  <span class="hljs-keyword">pagetable_t</span> pagetable;       <span class="hljs-comment">// User page table</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">trapframe</span> *<span class="hljs-title">trapframe</span>;</span> <span class="hljs-comment">// data page for trampoline.S</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">context</span> <span class="hljs-title">context</span>;</span>      <span class="hljs-comment">// swtch() here to run process</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">file</span> *<span class="hljs-title">ofile</span>[<span class="hljs-title">NOFILE</span>];</span>  <span class="hljs-comment">// Open files</span>  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">inode</span> *<span class="hljs-title">cwd</span>;</span>           <span class="hljs-comment">// Current directory</span>  <span class="hljs-keyword">char</span> name[<span class="hljs-number">16</span>];               <span class="hljs-comment">// Process name (debugging)</span>  uint64 mask;&#125;;</code></pre></div><h4 id="System-call-tracing"><a href="#System-call-tracing" class="headerlink" title="System call tracing"></a>System call tracing</h4><p>这个模块要实现系统调用跟踪，添加一个新的系统调用跟踪功能，即创建一个新的跟踪系统调用来控制跟踪。我们首先分析trace程序，在user/trace.c中可以看到第十七行为<code>if (trace(atoi(argv[1])) &lt; 0)</code>，我们可以得知trace函数的原型为<code>int trace(int n)</code>其中，n为第一个参数，也就是<code>1&lt;&lt;SYS_xxx</code>,这个SYS_xxx是一个整数“掩码”（这么翻译的，感觉翻译得不太好，调用号更合适些），每一个不同的SYS_xxx对应不同的系统调用。我们可以看看trace函数中的代码：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/param.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/types.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;kernel/stat.h&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;user/user.h&quot;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span></span><span class="hljs-function"><span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> *argv[])</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-keyword">int</span> i;  <span class="hljs-keyword">char</span> *nargv[MAXARG];  <span class="hljs-keyword">if</span>(argc &lt; <span class="hljs-number">3</span> || (argv[<span class="hljs-number">1</span>][<span class="hljs-number">0</span>] &lt; <span class="hljs-string">&#x27;0&#x27;</span> || argv[<span class="hljs-number">1</span>][<span class="hljs-number">0</span>] &gt; <span class="hljs-string">&#x27;9&#x27;</span>))&#123;    <span class="hljs-built_in">fprintf</span>(<span class="hljs-number">2</span>, <span class="hljs-string">&quot;Usage: %s mask command\n&quot;</span>, argv[<span class="hljs-number">0</span>]);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;  <span class="hljs-keyword">if</span> (trace(atoi(argv[<span class="hljs-number">1</span>])) &lt; <span class="hljs-number">0</span>) &#123;    <span class="hljs-built_in">fprintf</span>(<span class="hljs-number">2</span>, <span class="hljs-string">&quot;%s: trace failed\n&quot;</span>, argv[<span class="hljs-number">0</span>]);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;    <span class="hljs-keyword">for</span>(i = <span class="hljs-number">2</span>; i &lt; argc &amp;&amp; i &lt; MAXARG; i++)&#123;    nargv[i<span class="hljs-number">-2</span>] = argv[i];  &#125;  exec(nargv[<span class="hljs-number">0</span>], nargv);  <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div><p>在上述程序中，调用了trace函数，trace函数的参数就是在终端输入的trace指令的第一个参数，trace函数通过<code>entry(trace)</code>从用户态转到内核态，执行后续操作。在内核态中会将第一个参数也就是系统调用号保存在myproc-&gt;mask中，后续调用exec函数的时候会通过之前保存的mask值来跟踪相应的系统调用（后续详细讲解）。</p><p>在上述程序中，输入的参数必须大于2个，也就是必须要有一个调用号参数，还要有一个另外的一个指令参数，跟踪的正是这个指令中相应调用号的系统调用。正如上述代码中第三个for循环所示，将从第二个参数开始之后的所有参数复制到nargv，然后调用exec函数执行该指令。</p><p>在实验文档中给出的例子中，执行命令<code>trace 32 grep hello README</code>的意思是，跟踪grep命令中调用号为5（32=1&lt;&lt;5）的系统调用。我们可以在syscall.h中看到这些调用号，如下代码所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-comment">// System call numbers</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_fork    1</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_exit    2</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_wait    3</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_pipe    4</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_read    5</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_kill    6</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_exec    7</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_fstat   8</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_chdir   9</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_dup    10</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_getpid 11</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_sbrk   12</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_sleep  13</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_uptime 14</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_open   15</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_write  16</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_mknod  17</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_unlink 18</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_link   19</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_mkdir  20</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYS_close  21</span></code></pre></div><p>那么32就对应<code>SYS_read=5</code>。也就是说要跟踪read系统调用。</p><p>因为trace本身也是系统调用之一，在user/usys.pl中加入从用户态进入内核态的入口<code>entry(trace)</code>，生成汇编语言代码，如下所示：</p><div class="code-wrapper"><pre><code class="hljs c">trace: li a7, SYS_trace ecall ret</code></pre></div><p>可以看到，将SYS_trace保存在a7寄存器中。系统调用的ecall指令会使用a0和a7寄存器，其中a7寄存器保存的是系统进程号，a0寄存器保存的是系统调用参数，返回值会保存在a0寄存器中（unix中是这样，ecall的实现过程在文章开头给出）。</p><p>（这一部分我是参考了CSDN上的博主@//夜游神的<a href="https://blog.csdn.net/qq_33894666/article/details/121545027">博客</a>)</p><p>在kernel/syscall.h中加入系统进程号<code>#define SYS_trace 22</code>。</p><p>在kernel/proc.h的结构体proc中加入一个新成员<code>uint64 mask</code>。（我看到有的博主写的是<code>uint32 mask</code>，个人认为位数的多少取决于系统调用的数量。到trace为止，系统调用的数量为22）。</p><p>在kernel/sysproc.c中实现<code>uint64 sys_trace(void)</code>函数。</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64 <span class="hljs-title">sys_trace</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;    <span class="hljs-keyword">int</span> mask;<span class="hljs-comment">//这里的int也可以换成长整数</span>    <span class="hljs-keyword">if</span> (argint(<span class="hljs-number">0</span>,&amp;mask) &lt; <span class="hljs-number">0</span>)        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;    myproc()-&gt;mask = mask;    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div><p>在文章开头我们讲过了<code>int argint(int n,int *ip)</code>函数，，trace只有一个参数，放在a0中，在这里我们要将a0的值保存在mask中，即<code>if(argint(0,&amp;mask)&lt;0) return -1;</code>。将mask赋值给myproc()-&gt;mask，作为我们进程的状态结构体的一部分。</p><p>在kernel/syscall.c中补充syscall函数，代码如下如所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span> (p-&gt;mask &amp; (<span class="hljs-number">1</span> &lt;&lt; num))      <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d: syscall %s -&gt; %d\n&quot;</span>, p-&gt;pid, callnames[num - <span class="hljs-number">1</span>], p-&gt;trapframe-&gt;a0);</code></pre></div><p>上述代码中callnamens数组如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">static</span> <span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span>* callnames[] = &#123;<span class="hljs-string">&quot;fork&quot;</span>,<span class="hljs-string">&quot;exit&quot;</span>,<span class="hljs-string">&quot;wait&quot;</span>,<span class="hljs-string">&quot;pipe&quot;</span>,<span class="hljs-string">&quot;read&quot;</span>,<span class="hljs-string">&quot;kill&quot;</span>,<span class="hljs-string">&quot;exec&quot;</span>,<span class="hljs-string">&quot;fstat&quot;</span>,<span class="hljs-string">&quot;chdir&quot;</span>,<span class="hljs-string">&quot;dup&quot;</span>,<span class="hljs-string">&quot;getpid&quot;</span>,<span class="hljs-string">&quot;sbrk&quot;</span>,<span class="hljs-string">&quot;sleep&quot;</span>,<span class="hljs-string">&quot;uptime&quot;</span>,<span class="hljs-string">&quot;open&quot;</span>,<span class="hljs-string">&quot;write&quot;</span>,<span class="hljs-string">&quot;mknod&quot;</span>,<span class="hljs-string">&quot;unlink&quot;</span>,<span class="hljs-string">&quot;link&quot;</span>,<span class="hljs-string">&quot;mkdir&quot;</span>,<span class="hljs-string">&quot;close&quot;</span>,<span class="hljs-string">&quot;trace&quot;</span>&#125;;</code></pre></div><p>p-&gt;pid是进程号，callnamens是系统调用名称，p-&gt;trapframe-&gt;a0是返回值。</p><p>下面就可以好好说说系统调用响应的过程了：</p><p>在命令行输入<code>trace 32 grep hello README</code>指令，然后调用trace程序(如上述代码所示)，首先执行trace函数，待trace函数执行完后执行grep命令。在trace函数中（trace的系统调用只有sys_trece)，通过entry(trace)将sys_trace系统调用号保存在a7寄存器中，调用ecall进入内核态（用户态进入内核态的具体实现会在lab4中体现）。在内核态中首先调用syscall函数，将a7的值赋值给num，由此调用sys_trace函数，将我们的命令的第一个参数保存在<code>myproc()-&gt;mask</code>中，方便后续的命令进行比较。由于trace函数只有trace一个系统调用，num的值为SYS_trace，但是此时的mask为read的调用号，所以不进行打印系统调用的过程。</p><p>执行完trace函数之后通过exec命令执行grep命令，grep命令会用到调用号为5的系统调用read，通过entry(read)进入内核态后，先调用syscall函数，将a7的值赋值给num，然后执行sys_read函数，然后进行判定，<code>p-&gt;mask &amp; (1 &lt;&lt; num)</code>，如果这两个值相等，就可以打印相应系统调用的进程号、名称以及返回值。</p><p>在linux中，提供了一个<code>ptrace</code>系统函数，<code>ptrace</code>提供了一种使父进程得以监视和控制其他进程的方式，它还能改变子进程中的寄存器和内核映像，因而在gdb中用作断点调试和跟踪。</p><h4 id="Sysinfo"><a href="#Sysinfo" class="headerlink" title="Sysinfo"></a>Sysinfo</h4><p>在这个作业中，我们要添加一个系统调用sysinfo，它收集有关正在运行的系统调用的信息。系统调用有一个参数：指向strcut sysinfo的指针。</p><ol><li><p>在Makefile中加入<code>$U/_sysinfotest</code>。</p></li><li><p>在user.h中加入<code>int sysinfo(struct sysinfo*)</code>和<code>struct sysinfo</code>。</p></li><li><p>在usys.pl中加入系统调用入口<code>entry(&quot;sysinfo&quot;)</code>。</p></li><li><p>在kernel/syscall.h中加入sysinfo的系统调用号<code>#define SYS_sysinfo 23</code>。</p></li><li><p>在kernel/syscall.c函数中加入外部函数生命和函数指针，代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">extern</span> uint64 <span class="hljs-title">sys_sysinfo</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span>;<span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-title">uint64</span> <span class="hljs-params">(*syscalls[])</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>= &#123;    .....    [SYS_sysinfo] sys_sysinfo&#125;;</code></pre></div></li></ol><p>在上述完成后我们就将基础工作完成了。</p><p>在正式分析sysinfotest函数之前，我们要先搞懂<code>struct sysinfo</code>数据结构，其定义如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sysinfo</span> &#123;</span>  uint64 freemem;   <span class="hljs-comment">// amount of free memory (bytes)</span>  uint64 nproc;     <span class="hljs-comment">// number of process</span>&#125;;</code></pre></div><p>在sysinfo数据结构中，有两个uint64变量，一个是freemem，代表空闲内存的大小，单位为bytes；另一个是nproc，代表进程的数量。</p><p>现在来具体分析sysinfotest要实现的功能，该系统调用收集正在运行的系统调用的信息。在sysinfotest的主函数如下所示：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span></span><span class="hljs-function"><span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> *argv[])</span></span><span class="hljs-function"></span>&#123;  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sysinfotest: start\n&quot;</span>);  testcall();  testmem();  testproc();  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sysinfotest: OK\n&quot;</span>);  <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);&#125;</code></pre></div><p>在上述主函数中，首先调用了testcall函数，testcall函数如下：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">testcall</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sysinfo</span> <span class="hljs-title">info</span>;</span>    <span class="hljs-keyword">if</span> (sysinfo(&amp;info) &lt; <span class="hljs-number">0</span>) &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;FAIL: sysinfo failed\n&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;  <span class="hljs-keyword">if</span> (sysinfo((struct sysinfo *) <span class="hljs-number">0xeaeb0b5b00002f5e</span>) !=  <span class="hljs-number">0xffffffffffffffff</span>) &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;FAIL: sysinfo succeeded with bad argument\n&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;&#125;</code></pre></div><p>在sysinfo函数中，定义了一个sysinfo变量info。调用sysinfo函数，也就是在内核中调用sys_sysinfo函数，我们在上文中提到，调用sysinfo函数，会在entry中将sys_info的系统调用号赋值给a7（同时将参数赋值给a0），然后进入内核进行调用。那么我们就要来完成sys_sysinfo函数：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function">uint64</span><span class="hljs-function"><span class="hljs-title">sys_sysinfo</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span></span><span class="hljs-function"></span>&#123;uint64 addr;<span class="hljs-keyword">if</span> (argaddr(<span class="hljs-number">0</span>,&amp;addr) &lt; <span class="hljs-number">0</span>)<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sysinfo</span> <span class="hljs-title">sf</span>;</span>sf.nproc = nop();<span class="hljs-comment">//nop 后面加</span>sf.freemem = freemem();<span class="hljs-comment">// 后面加</span><span class="hljs-keyword">if</span> (copyout(myproc()-&gt;pagetable,addr,(<span class="hljs-keyword">char</span>*)&amp;sf,<span class="hljs-keyword">sizeof</span>(sf)) &lt; <span class="hljs-number">0</span>)<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre></div><p>在sys_sysinfo中定义了一个uint64变量addr，sysinfo的参数也就是a0赋值给addr，</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span></span><span class="hljs-function"><span class="hljs-title">testmem</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sysinfo</span> <span class="hljs-title">info</span>;</span>  uint64 n = countfree();    sinfo(&amp;info);  <span class="hljs-keyword">if</span> (info.freemem!= n) &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;FAIL: free mem %d (bytes) instead of %d\n&quot;</span>, info.freemem, n);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;    <span class="hljs-keyword">if</span>((uint64)sbrk(PGSIZE) == <span class="hljs-number">0xffffffffffffffff</span>)&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sbrk failed&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;  sinfo(&amp;info);      <span class="hljs-keyword">if</span> (info.freemem != n-PGSIZE) &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;FAIL: free mem %d (bytes) instead of %d\n&quot;</span>, n-PGSIZE, info.freemem);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;    <span class="hljs-keyword">if</span>((uint64)sbrk(-PGSIZE) == <span class="hljs-number">0xffffffffffffffff</span>)&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sbrk failed&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;  sinfo(&amp;info);      <span class="hljs-keyword">if</span> (info.freemem != n) &#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;FAIL: free mem %d (bytes) instead of %d\n&quot;</span>, n, info.freemem);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;&#125;</code></pre></div><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">testproc</span><span class="hljs-params">()</span> </span>&#123;  <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sysinfo</span> <span class="hljs-title">info</span>;</span>  uint64 nproc;  <span class="hljs-keyword">int</span> status;  <span class="hljs-keyword">int</span> pid;    sinfo(&amp;info);  nproc = info.nproc;  pid = fork();  <span class="hljs-keyword">if</span>(pid &lt; <span class="hljs-number">0</span>)&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sysinfotest: fork failed\n&quot;</span>);    <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;  <span class="hljs-keyword">if</span>(pid == <span class="hljs-number">0</span>)&#123;    sinfo(&amp;info);    <span class="hljs-keyword">if</span>(info.nproc != nproc+<span class="hljs-number">1</span>) &#123;      <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sysinfotest: FAIL nproc is %d instead of %d\n&quot;</span>, info.nproc, nproc+<span class="hljs-number">1</span>);      <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);    &#125;    <span class="hljs-built_in">exit</span>(<span class="hljs-number">0</span>);  &#125;  wait(&amp;status);  sinfo(&amp;info);  <span class="hljs-keyword">if</span>(info.nproc != nproc) &#123;      <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;sysinfotest: FAIL nproc is %d instead of %d\n&quot;</span>, info.nproc, nproc);      <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);  &#125;&#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MIT 6.S081 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go Goroutine 与 GMP 调度器</title>
    <link href="/2021/12/17/Go-Goroutine-and-GMP-Scheduler/"/>
    <url>/2021/12/17/Go-Goroutine-and-GMP-Scheduler/</url>
    
    <content type="html"><![CDATA[<p>Goroutine 是由 Go runtime 管理的轻量级并发执行单元。它和操作系统线程不是一一对应关系：runtime 会把大量 goroutine 复用到少量线程上执行。goroutine 初始栈很小且可按需增长，因此单进程可以承载大量并发任务。</p><p>我在前面的文章<a href="https://databrains.cn/2022/01/10/MIT-6-S081-xv6-Book-Notes/">MIT 6.S081: xv6 实验参考书解析</a>中讲过进程/线程的切换方式，当切换的时候要从用户态-&gt;内核态-&gt;另一个进程/线程的内核态-&gt;另一个进程的用户态，但是在Goroutine中不是这样。通过GMP，可以使得开销尽量小。</p><h4 id="Coroutine"><a href="#Coroutine" class="headerlink" title="Coroutine"></a>Coroutine</h4><p>先来说说协程（Coroutine）：</p><blockquote><p>主流的操作系统都是采用一对一的线程模型，用户态和内核态线程具有一对一关系，可以认为用户态线程的执行完全受到操作系统调度器的管理。但是随着应用程序越来越复杂，与操作系统调度器相比，应用程序对线程的语义和执行状态更加了解，因此可能做出更优的调度策略。在这个背景下，操作系统开始提供更多对用户态线程，即协程的支持。</p></blockquote><p>也就是说，一个线程的执行可以从内核态和用户态两个视角来观察。内核级线程由操作系统调度，用户级协程则由语言运行时或协程库调度。</p><p>如下图所示，我们可以更好地理解协程和线程的关系，<a href="https://juejin.cn/post/6995091405563494431">图片来源</a>。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a22b3af8462e41f8adb601eb34c22bb6~tplv-k3u1fbpfcp-watermark.awebp" alt=""></p><p>协程的上下文切换触发机制与内核态线程存在较大不同。操作系统可以通过中断抢占当前 CPU 并进行上下文切换，这种切换是强制性的，因此称为抢占式调度；协程通常由运行时或协程库调度，协程库一般会提供 <code>yield</code> 接口，让当前协程暂时放弃 CPU，从而使其他协程获得运行机会。</p><hr><p>补充一下常见的面经：</p><h6 id="进程、线程、协程的区别："><a href="#进程、线程、协程的区别：" class="headerlink" title="进程、线程、协程的区别："></a>进程、线程、协程的区别：</h6><p>进程是资源隔离与分配的基本单位，线程是进程内的执行流。线程共享进程地址空间和大部分资源，但有各自的栈与寄存器上下文。不同进程天然隔离，通常通过 IPC 机制通信。</p><p>共享一个进程资源的多线程有自己独有的用户栈和内核栈，有共享的代码段、数据段和堆。线程有自己独有的<code>context</code>，这个<code>context</code>包括栈、栈指针、寄存器和程序计数器。</p><p>协程是用户态调度的并发执行单元，通常由语言 runtime 或协程库管理。它并不等同于“异步机制”，而是另一种并发组织方式；是否抢占、何时切换，取决于具体 runtime 的调度策略。</p><p>进程、线程、协程切换开销的对比：</p><ul><li><h6 id="同步与异步的区别："><a href="#同步与异步的区别：" class="headerlink" title="同步与异步的区别："></a>同步与异步的区别：</h6></li></ul><p>同步是阻塞模式，而异步是非阻塞模式：</p><ul><li><p>同步通常指调用方需要在当前控制流中等待操作完成（或等待其结果可用）后再继续。</p></li><li><p>异步是进程不需要一直等下去，而是继续执行下面的操作，不管其他进程的状态。当有消息返回时系统会通知进程处理，这样可以提高执行的效率。</p><p>知乎上这个<a href="https://zhuanlan.zhihu.com/p/67452727">文章</a>说得很好，分享给大家：</p><blockquote><p>同步异步 ， 举个例子来说，一家餐厅吧来了5个客人，<strong>同步</strong>的意思就是说，来第一个点菜，点了个鱼，好， 厨师去捉鱼杀鱼，过了半小时鱼好了给第一位客人，开始下位一位客人，就这样一个一个来，按<strong>顺序</strong>来相同， <strong>异步</strong>呢，异步的意思就是来第一位客人，点什么，点鱼，给它一个牌子，让他去一边等吧，下一位客人接着点菜，点完接着点让厨师做去吧，哪个的菜先好就先端出来。</p><p>同步的优点是：同步是按照顺序一个一个来，不会乱掉，更不会出现上面代码没有执行完就执行下面的代码， 缺点：是解析的速度没有异步的快；</p><p>异步的优点是：异步是接取一个任务，直接给后台，在接下一个任务，一直一直这样，谁的先读取完先执行谁的， 缺点：没有顺序 ，谁先读取完先执行谁的 ，会出现上面的代码还没出来下面的就已经出来了，会报错。</p></blockquote></li></ul><hr><p>goroutine 来自协程的概念，让一组可复用的函数运行在一组线程之上，即使有协程阻塞，该线程的其他协程也可以被 runtime 调度，转移到其他可运行的线程上。最关键的是，程序员看不到这些底层的细节，这就降低了编程的难度，提供了更容易的并发。</p><p>Go 中，协程被称为 goroutine，它非常轻量，一个 goroutine 只占几 KB，并且这几 KB 就足够 goroutine 运行完，这就能在有限的内存空间内支持大量 goroutine，支持了更多的并发。虽然一个 goroutine 的栈只占几 KB，但实际是可伸缩的，如果需要更多内容，runtime 会自动为 goroutine 分配。</p><h4 id="channel之于Goroutine"><a href="#channel之于Goroutine" class="headerlink" title="channel之于Goroutine"></a>channel之于Goroutine</h4><h5 id="无缓冲channel和有缓冲channel"><a href="#无缓冲channel和有缓冲channel" class="headerlink" title="无缓冲channel和有缓冲channel"></a>无缓冲channel和有缓冲channel</h5><p>无缓冲channel发送动作和接收动作是同时发生的，例如 <code>ch := make(chan int)</code> ，如果没 <code>goroutine</code> 读取接收者<code>&lt;-ch</code> ，那么发送者<code>ch&lt;-</code> 就会一直阻塞，<strong>缓冲</strong> <code>channel</code> 类似一个队列，只有队列满了才可能发生阻塞。</p><h5 id="如何优化channel"><a href="#如何优化channel" class="headerlink" title="如何优化channel"></a>如何优化channel</h5><p>和学长聊到这个问题，深夜无眠，写一下自己的理解。</p><p>channel 的构造语句 <code>make(chan int)</code>，将会被 golang 编译器翻译为 <code>runtime.makechan</code> 函数, 其函数签名如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">makechan</span><span class="hljs-params">(t *chantype, size <span class="hljs-keyword">int</span>)</span> *<span class="hljs-title">hchan</span></span></code></pre></div><p>其中，<code>t *chantype</code> 即构造 channel 时传入的元素类型。<code>size int</code> 即用户指定的 channel 缓冲区大小，不指定则为 0。该函数的返回值是 <code>*hchan</code>。hchan 则是 channel 在 golang 中的内部实现。其定义如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> hchan <span class="hljs-keyword">struct</span> &#123;qcount   <span class="hljs-keyword">uint</span>           <span class="hljs-comment">// buffer 中已放入的元素个数</span>dataqsiz <span class="hljs-keyword">uint</span>           <span class="hljs-comment">// 用户构造 channel 时指定的 buf 大小</span>buf      unsafe.Pointer <span class="hljs-comment">// buffer</span>elemsize <span class="hljs-keyword">uint16</span>         <span class="hljs-comment">// buffer 中每个元素的大小</span>closed   <span class="hljs-keyword">uint32</span>         <span class="hljs-comment">// channel 是否关闭，== 0 代表未 closed</span>elemtype *_type         <span class="hljs-comment">// channel 元素的类型信息</span>sendx    <span class="hljs-keyword">uint</span>           <span class="hljs-comment">// buffer 中已发送的索引位置 send index</span>recvx    <span class="hljs-keyword">uint</span>           <span class="hljs-comment">// buffer 中已接收的索引位置 receive index</span>recvq    waitq          <span class="hljs-comment">// 等待接收的 goroutine  list of recv waiters</span>sendq    waitq          <span class="hljs-comment">// 等待发送的 goroutine list of send waiters</span>lock mutex&#125;</code></pre></div><p>我们可以知道，hchan中定义了悲观锁来进行互斥，channel是一个用于同步和通信的有锁队列。</p><h6 id="channel回比mutex低效吗？"><a href="#channel回比mutex低效吗？" class="headerlink" title="channel回比mutex低效吗？"></a>channel回比mutex低效吗？</h6><p>今晚我被这个问题折磨了很久，查了很多资料才有了一些答案。</p><p>在 Go 中，channel 的发送/接收是并发安全的同步操作；通过它可以建立清晰的 happens-before 关系。常见经验是：如果问题本质是“数据流动与协作”，channel 往往更自然；如果问题本质是“保护共享状态”，mutex 往往更直接。性能与可维护性需要结合场景权衡。</p><div class="table-container"><table><thead><tr><th>操作</th><th>一个零值nil通道</th><th>一个非零值但已关闭的通道</th><th>一个非零值且尚未关闭的通道</th></tr></thead><tbody><tr><td>关闭</td><td>产生恐慌</td><td>产生恐慌</td><td>成功关闭</td></tr><tr><td>发送数据</td><td>永久阻塞</td><td>产生恐慌</td><td>阻塞或者成功发送</td></tr><tr><td>接收数据</td><td>永久阻塞</td><td>永不阻塞</td><td>阻塞或者成功接收</td></tr></tbody></table></div><p><strong><em>更新</em></strong></p><hr><p><a href="https://segmentfault.com/a/1190000017890174">https://segmentfault.com/a/1190000017890174</a></p><p>我在看了上述博文之后对channel和mutex的选择有了一些认识，摘录如下：</p><blockquote><p>面对一个并发问题的时候，应当选择合适的并发方式：channel还是mutex。<strong>选择的依据是他们的能力/特性：channel的能力是让数据流动起来，擅长的是数据流动的场景</strong>，<strong>mutex的能力是数据不动，某段时间只给一个协程访问数据的权限擅长数据位置固定的场景</strong>。</p></blockquote><hr><h6 id="无锁队列ring-buffer"><a href="#无锁队列ring-buffer" class="headerlink" title="无锁队列ring buffer"></a>无锁队列ring buffer</h6><p>按照上述我的想法来看加锁的损耗较高，对于某些特殊的情况，可以采用简单的无锁ring buffer来实现。</p><p>在ring buffer中，设置两个指针，head指向的是下一次读的位置，而tail指向的是下一次写的位置，由于是ring buffer，可以将buffer的最后一个单元不存储数据。所以，如果head == tail，那么说明buffer为空。如果 head == tail + 1 ，那么说明buffer满了。</p><p>在进行读操作时，我们只修改head的值，而在写操作时我们只修改tail的值，在写操作时，我们在写入内容到buffer之后才修改tail的值；而在进行读操作的时候，我们会读取tail的值并将其赋值给copyTail。</p><p>这里依赖的是“按顺序发布与读取索引”的并发协议，而不是“任意赋值都天然原子”。示例代码通过 <code>atomic.Load/Store</code> 保证索引读写的可见性和顺序性，从而让读写双方对可读区间达成一致。</p><p>因此在单生产者/单消费者模型下，tail 到 head 之前的区间可视为可写区间，避免覆盖尚未消费的数据。注意这一定义依赖前面的并发前提；推广到多生产者或多消费者时需要额外同步机制。</p><p>下面是我自己实现的无锁队列ring buffer，大伙看个热闹：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">type</span> RingBuffer <span class="hljs-keyword">struct</span> &#123;data []<span class="hljs-keyword">int</span>head <span class="hljs-keyword">uint64</span>tail <span class="hljs-keyword">uint64</span>&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(r *RingBuffer)</span> <span class="hljs-title">Push</span><span class="hljs-params">(v <span class="hljs-keyword">int</span>)</span> <span class="hljs-title">bool</span></span> &#123;tail := atomic.LoadUint64(&amp;r.tail)next := (tail + <span class="hljs-number">1</span>) % <span class="hljs-keyword">uint64</span>(<span class="hljs-built_in">len</span>(r.data))<span class="hljs-keyword">if</span> next == atomic.LoadUint64(&amp;r.head) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>&#125;r.data[tail] = vatomic.StoreUint64(&amp;r.tail, next)<span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>&#125;<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(r *RingBuffer)</span> <span class="hljs-title">Pop</span><span class="hljs-params">()</span> <span class="hljs-params">(<span class="hljs-keyword">int</span>, <span class="hljs-keyword">bool</span>)</span></span> &#123;head := atomic.LoadUint64(&amp;r.head)<span class="hljs-keyword">if</span> head == atomic.LoadUint64(&amp;r.tail) &#123;<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>, <span class="hljs-literal">false</span>&#125;v := r.data[head]atomic.StoreUint64(&amp;r.head, (head+<span class="hljs-number">1</span>)%<span class="hljs-keyword">uint64</span>(<span class="hljs-built_in">len</span>(r.data)))<span class="hljs-keyword">return</span> v, <span class="hljs-literal">true</span>&#125;</code></pre></div><p>这个例子只适合单生产者、单消费者模型。只要扩展到多生产者或多消费者，就需要额外的 CAS、序号槽位或锁来处理竞争，否则多个 goroutine 可能同时写同一个位置。</p><h6 id="分段锁"><a href="#分段锁" class="headerlink" title="分段锁"></a>分段锁</h6><p>分段锁提高channel效率思想的思想就是<strong>使用带有协调机制的独占锁，这些机制允许更高的并发性</strong>。</p><p>用白话说就是给buffer中的不同数据段上不同的锁，那么当多个Goroutine访问不同数据段的数据时，就不会存在锁竞争，从而可以有效的提高并发访问效率。</p><p>但是这只是ConcurrentHashMap所使用的锁分段技术，我查了很多遍也没有在网上看到有人用这个思路优化channel，这也许只是我个人的脑洞。</p><h6 id="官方大佬的答案"><a href="#官方大佬的答案" class="headerlink" title="官方大佬的答案"></a>官方大佬的答案</h6><p>Go 语言社区也在 2014 年提出了无锁 Channel 的实现方案，该方案将 Channel 分成了以下三种类型：</p><ul><li>同步 Channel — 不需要缓冲区，发送方会直接将数据交给（Handoff）接收方；</li><li>异步 Channel — 基于环形缓存的传统生产者消费者模型；</li><li>chan struct{} 类型的异步 Channel — struct{} 类型不占用内存空间，不需要实现缓冲区和直接发送（Handoff）的语义；</li></ul><p>这个方案可以在一些关键路径上通过无锁提升channel的性能，但这玩意最后被搁浅了，我也不知道为啥。</p><h6 id="channel一些问题"><a href="#channel一些问题" class="headerlink" title="channel一些问题"></a>channel一些问题</h6><p>给未初始化的channel读写数据，会报错：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">var</span> ch <span class="hljs-keyword">chan</span> <span class="hljs-keyword">int</span>ch&lt;<span class="hljs-number">-1</span><span class="hljs-comment">//fatal error: all goroutines are asleep - deadlock!</span></code></pre></div><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">var</span> ch <span class="hljs-keyword">chan</span> <span class="hljs-keyword">int</span>ch&lt;<span class="hljs-number">-1</span><span class="hljs-comment">//fatal error: all goroutines are asleep - deadlock!</span></code></pre></div><p>为什么会报死锁的错误呢？我在网上看到一个解释：</p><blockquote><p>当<code>chan</code>能阻塞的情况下，则直接阻塞 <code>gopark(nil, nil, waitReasonChanSendNilChan, traceEvGoStop, 2),</code> 然后调用<code>throw(s string)</code>抛出错误,其中<code>waitReasonChanSendNilChan</code>。</p><p>没有初始化的channel，相当于nil，这个时候对其进行操作，会直接阻塞，抛出异常<code>waitReasonChanSendNilChan</code>，也就是报错<code>&quot;chan send (nil chan)&quot;</code>，最后呈现给我们的error就是<code>deadlock</code>。</p><p>————————————————<br><a href="https://blog.csdn.net/ilini/article/details/106884674">原文链接</a></p></blockquote><h5 id="Goroutine调度原理"><a href="#Goroutine调度原理" class="headerlink" title="Goroutine调度原理"></a>Goroutine调度原理</h5><h6 id="GMP调度模型"><a href="#GMP调度模型" class="headerlink" title="GMP调度模型"></a>GMP调度模型</h6><ul><li><p>M 对应 runtime 管理的工作线程（映射到底层 OS 线程），负责实际执行 goroutine。</p></li><li><p>G代表一个Goroutine，它有自己的栈，instruction pointer和其他信息。</p></li><li>P 表示调度上下文（可理解为运行 Go 代码所需的“处理器令牌”），它维护本地可运行 goroutine 队列并参与调度。</li></ul><p>也就是说，一个G的执行需要M和P的支持，一个M在与一个P关联后形成一个有效的G的运行环境[内核环境+上下文环境]。每个P都会包含一个可运行的G的队列。</p><p><img src="https://upload-images.jianshu.io/upload_images/10436675-a29a641d8a384787.png?imageMogr2/auto-orient/strip|imageView2/2/w/400/format/webp" alt=""></p><p>从上图中看，有2个物理线程M，每一个M都拥有一个处理器P，每一个也都有一个正在运行的Goroutine。默认的P的数量等于CPU的个数，P的数量可以通过GOMAXPROCS()来设置，它其实也就代表了真正的并发度，即有多少个Goroutine可以同时运行。上图中灰色的代表Goroutine并没有运行，而是处于ready的就绪态，正在等待被调度，P维护这个队列。</p><p>在go语言中，一旦执行go function，runqueue队列就会在其末尾加入一个Goroutine。在下一个调度点，就从runqueue中取出一个goroutine执行。系统中这么多P，具体会加到哪个P中，要看具体的调度策略。（补充：其实还有一个全局队列，如果P中队列满了，就会存放在全局队列中。）</p><h6 id="P-和-M-何时会被创建"><a href="#P-和-M-何时会被创建" class="headerlink" title="P 和 M 何时会被创建"></a>P 和 M 何时会被创建</h6><p>1、P 何时创建：在确定了 P 的最大数量 n 后，运行时系统会根据这个数量创建 n 个 P。</p><p>2、M 何时创建：没有足够的 M 来关联 P 并运行其中的可运行的 G。比如所有的 M 此时都阻塞住了，而 P 中还有很多就绪任务，就会去寻找空闲的 M，而没有空闲的，就会去创建新的 M。</p><h6 id="调度过程"><a href="#调度过程" class="headerlink" title="调度过程"></a>调度过程</h6><ol><li>通过go func()创建一个goroutine；</li><li>新创建的G会保存在P的本地队列中，如果P的本地队列已经满了就会保存在全局的队列中；</li><li>G只能运行在M中，一个M必须持有一个P。M会从P的本地队列中弹出一个可执行状态的G来执行，如果P的本地队列为空，就会向其他的M-P组合中偷取一个可执行的G来执行；</li><li>循环执行；</li><li>当M执行某一个G的时候如果发生了系统调用或者阻塞操作，M会阻塞，如果当前有一个G在执行，runtime会把这个线程M从P中摘除，然后再创建一个新的内核线程来服务于这个P；</li><li>当M系统调用结束后，这个G会尝试获取一个空闲的P执行，并放到这个P的本地队列，如果获取不到P，那么这个线程M就会变成休眠状态，加入到空闲线程中，然后这个G会被放到全局队列中。</li></ol><p>GMP的数量都是有限制的，摘自<a href="https://juejin.cn/post/6947589840187686920">博客</a></p><h6 id="M的限制"><a href="#M的限制" class="headerlink" title="M的限制"></a>M的限制</h6><p>在协程的执行中，真正干活的是GMP中的M，M的默认数量限制是10000，如果超出就会报错。通常只有在Goroutine出现阻塞的时候才会出现这种情况。</p><h6 id="G的限制"><a href="#G的限制" class="headerlink" title="G的限制"></a>G的限制</h6><p>Goroutine的创建数量理论上没有限制，但是我们的内存是有上限的，所以实际是有限制的。</p><h6 id="P的限制"><a href="#P的限制" class="headerlink" title="P的限制"></a>P的限制</h6><p>P的数量受环境变量GOMAXPROCS的直接影响。</p><h6 id="GMP中为什么要有P"><a href="#GMP中为什么要有P" class="headerlink" title="GMP中为什么要有P"></a>GMP中为什么要有P</h6><h6 id="老调度器的缺点"><a href="#老调度器的缺点" class="headerlink" title="老调度器的缺点"></a>老调度器的缺点</h6><ol><li>因为缓存G的只有一个全局队列，而M有多个，这样我们多个M在访问同一个资源的时候就要加锁保持互斥，这样就加大了开销。</li></ol><p>当一个Go程序启动之后会创建一个编号为0的主线程M0，M0负责初始化操作和启动第一个G，之后M0就和其他的M一样了。</p><p>G0是每次启动一个M都会第一个创建的goroutine,G0是仅负责调度的G，其不指向任何可执行的函数，每个M都会有一个自己的G0，在调度或系统调用时会使用G0的栈空间，全局变量中的G0是M0的G0。</p><p>真正决定并行度的是P的数量，我们看如下例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<span class="hljs-keyword">import</span> (<span class="hljs-string">&quot;fmt&quot;</span><span class="hljs-string">&quot;runtime&quot;</span>)<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;fmt.Println(runtime.NumCPU())old := runtime.GOMAXPROCS(<span class="hljs-number">2</span>)fmt.Println(<span class="hljs-string">&quot;old GOMAXPROCS:&quot;</span>, old)fmt.Println(<span class="hljs-string">&quot;new GOMAXPROCS:&quot;</span>, runtime.GOMAXPROCS(<span class="hljs-number">0</span>))&#125;</code></pre></div><p><code>GOMAXPROCS</code> 控制同时执行 Go 代码的 P 的数量。goroutine 可以创建很多，但同一时刻真正并行运行 Go 代码的数量不会超过 P 的数量。I/O 阻塞、系统调用和 runtime 调度会让 M 的数量变化，但并行度的核心旋钮仍然是 P。</p><p>Go 同时支持“共享内存 + 锁”与“通过通信共享内存”两种并发组织方式。CSP 风格在 Go 里主要通过 goroutine + channel 体现，核心是用通信来协调并发执行流。</p><p>开发go程序的时候，时常需要使用goroutine并发处理任务，有时候这些goroutine是相互独立的，而有的时候，多个goroutine之间常常是需要同步与通信的，值得注意的是，有时候主goroutine需要控制它所属的子goroutine。</p>]]></content>
    
    
    <categories>
      
      <category>Go语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Goroutine</tag>
      
      <tag>Go runtime</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go 内存管理与 GC：从逃逸分析到三色标记</title>
    <link href="/2021/12/16/Go-Memory-Management-and-GC/"/>
    <url>/2021/12/16/Go-Memory-Management-and-GC/</url>
    
    <content type="html"><![CDATA[<p>Go 的自动内存管理并不是“程序员完全不用关心内存”。更准确地说，Go runtime 接管了对象生命周期判断和堆内存回收，程序员仍然需要理解对象何时逃逸、哪些分配会给 GC 制造压力，以及写屏障为什么会影响吞吐。</p><h3 id="手动内存管理的问题"><a href="#手动内存管理的问题" class="headerlink" title="手动内存管理的问题"></a>手动内存管理的问题</h3><p>C/C++ 这类语言把内存释放责任交给程序员，灵活性很高，但错误也很隐蔽。最典型的问题有两个：</p><ol><li>悬空指针：内存已经释放，但还有指针指向原来的地址。</li><li>内存泄漏：对象已经不再需要，但程序没有释放它。</li></ol><p>例如：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">char</span> *p = (<span class="hljs-keyword">char</span> *)<span class="hljs-built_in">malloc</span>(<span class="hljs-number">100</span>);    <span class="hljs-built_in">strcpy</span>(p, <span class="hljs-string">&quot;hello&quot;</span>);    <span class="hljs-built_in">free</span>(p);    <span class="hljs-keyword">if</span> (p != <span class="hljs-literal">NULL</span>) &#123;        <span class="hljs-built_in">strcpy</span>(p, <span class="hljs-string">&quot;world&quot;</span>);    &#125;&#125;</code></pre></div><p><code>free(p)</code> 释放的是 <code>p</code> 指向的内存，不会自动把 <code>p</code> 置空。因此 <code>p != NULL</code> 不能证明这块内存仍然可用。这个例子说明：手动内存管理的问题不只是“忘记释放”，还包括释放时机和所有权边界很难在局部代码中判断。</p><h3 id="Go-的内存分配路径"><a href="#Go-的内存分配路径" class="headerlink" title="Go 的内存分配路径"></a>Go 的内存分配路径</h3><p>Go 程序中的对象通常先由编译器决定放在栈上还是堆上。能被证明只在当前函数内使用的对象优先放在栈上；生命周期可能超过当前栈帧，或者大小/类型在编译期难以确定的对象会逃逸到堆上。</p><p>可以用下面命令观察逃逸分析：</p><div class="code-wrapper"><pre><code class="hljs bash">go build -gcflags=<span class="hljs-string">&#x27;-m=2&#x27;</span> ./...</code></pre></div><p>常见逃逸场景包括：</p><ol><li>返回局部变量指针。</li><li>把具体类型装进 <code>interface&#123;&#125;</code> 后跨函数传递。</li><li>闭包捕获外部变量，且闭包生命周期不明确。</li><li>切片、map、channel 等引用类型扩容或被长期持有。</li><li>对象太大，编译器不愿意放在栈上。</li></ol><p>逃逸本身不是 bug。问题在于高频路径上的堆分配会增加 GC 扫描和回收压力。</p><h3 id="Go-堆分配的粗略结构"><a href="#Go-堆分配的粗略结构" class="headerlink" title="Go 堆分配的粗略结构"></a>Go 堆分配的粗略结构</h3><p>Go runtime 的堆分配器采用分层缓存思路。简单理解：</p><ol><li>每个 P 持有本地的 <code>mcache</code>，小对象优先从本地缓存分配，避免频繁加全局锁。</li><li><code>mcache</code> 不够时向 <code>mcentral</code> 申请 span。</li><li><code>mcentral</code> 不够时再向 <code>mheap</code> 申请更大的内存块。</li><li>小对象按 size class 分配，大对象会直接走更靠近 <code>mheap</code> 的路径。</li></ol><p>这种设计的目的很直接：绝大多数小对象分配都应该足够快，尽量不要因为内存分配让 goroutine 频繁竞争全局结构。</p><h3 id="为什么-Go-使用并发标记清除"><a href="#为什么-Go-使用并发标记清除" class="headerlink" title="为什么 Go 使用并发标记清除"></a>为什么 Go 使用并发标记清除</h3><p>引用计数的优点是回收及时，缺点是每次引用变化都要维护计数，并且天然处理不了循环引用。复制算法适合年轻代对象回收，但 Go 长期没有采用传统分代 GC。Go 当前主线思路是并发三色标记清除，并用写屏障维持并发标记期间的正确性。</p><p>三色标记把对象分成三类：</p><ol><li>白色：尚未确认可达。标记结束后仍为白色的对象可以回收。</li><li>灰色：对象本身已确认可达，但它引用的对象还没扫描完。</li><li>黑色：对象已确认可达，并且它引用的对象也已经处理过。</li></ol><p>标记过程从根对象开始。根对象包括全局变量、各 goroutine 栈、寄存器中可见的指针等。GC 从根对象出发，把可达对象染灰，再逐步扫描灰色对象，把它引用的对象继续染灰，最后灰色对象变黑。当灰色队列为空时，仍然白色的对象就是不可达对象。</p><h3 id="写屏障解决什么问题"><a href="#写屏障解决什么问题" class="headerlink" title="写屏障解决什么问题"></a>写屏障解决什么问题</h3><p>并发 GC 的麻烦在于：GC 线程在标记对象时，用户 goroutine 仍在修改指针关系。如果没有额外约束，可能出现黑色对象指向白色对象，而 GC 又永远不会再扫描这个黑色对象，最终把仍然可达的白色对象误回收。</p><p>写屏障就是在指针写入时插入的一段 runtime 逻辑，用来维护三色不变式。它会让并发标记期间的指针变更被 GC 看见，避免“用户代码刚建立引用，GC 却以为对象不可达”的情况。</p><p>写屏障不是免费的。它会增加指针写入的成本，所以 Go GC 会在一个很短的 STW 阶段打开写屏障，并在标记结束后关闭它。</p><h3 id="一次-GC-周期"><a href="#一次-GC-周期" class="headerlink" title="一次 GC 周期"></a>一次 GC 周期</h3><p>可以把 Go 的一次 GC 周期粗略拆成以下阶段：</p><ol><li>Mark setup：短暂停顿，开启写屏障，准备标记任务。</li><li>Concurrent mark：并发扫描根对象和堆对象，大部分工作与用户 goroutine 同时进行。</li><li>Mark assist：当程序分配太快时，分配者需要帮助 GC 做一部分标记工作，避免堆增长失控。</li><li>Mark termination：短暂停顿，确认标记完成，关闭写屏障。</li><li>Sweep：清扫未标记对象，把空闲 span 归还给分配器。</li></ol><p>Go GC 的优化目标不是“完全没有停顿”，而是把 STW 控制在较短范围内，并通过并发标记换取较稳定的延迟表现。</p><h3 id="GC-触发条件与-GOGC"><a href="#GC-触发条件与-GOGC" class="headerlink" title="GC 触发条件与 GOGC"></a>GC 触发条件与 GOGC</h3><p>Go 主要通过堆增长比例触发 GC。<code>GOGC</code> 控制下一次 GC 的目标堆大小，默认值是 100，含义可以近似理解为：当新分配对象让堆大小相对上次存活堆翻倍时，触发下一轮 GC。</p><p>如果 <code>GOGC</code> 调小，GC 更频繁，内存占用更低，但 CPU 消耗更高；如果 <code>GOGC</code> 调大，GC 更少，吞吐可能更好，但峰值内存更高。线上服务需要结合延迟、吞吐和内存预算来调。</p><p>Go 1.19 之后还可以用 <code>GOMEMLIMIT</code> 给 runtime 一个软内存上限。相比只调 <code>GOGC</code>，<code>GOMEMLIMIT</code> 更适合容器环境，因为容器里真正危险的是触碰 cgroup limit 后被 OOM kill。</p><h3 id="怎么降低-GC-压力"><a href="#怎么降低-GC-压力" class="headerlink" title="怎么降低 GC 压力"></a>怎么降低 GC 压力</h3><p>优化 GC 不应该从“关闭 GC”开始，而应该先定位分配来源：</p><div class="code-wrapper"><pre><code class="hljs bash">go <span class="hljs-built_in">test</span> -bench=. -benchmem ./...go tool pprof -alloc_space http://host/debug/pprof/heapgo tool pprof -inuse_space http://host/debug/pprof/heap</code></pre></div><p>常见有效手段包括：</p><ol><li>减少临时对象：热点路径上避免无意义的字符串拼接、切片扩容和装箱。</li><li>预分配容量：对已知规模的 slice/map 使用 <code>make([]T, 0, n)</code> 或 <code>make(map[K]V, n)</code>。</li><li>控制指针数量：指针越多，GC 扫描成本越高。能用值类型表达的数据，不必都拆成指针对象。</li><li>复用缓冲区：例如 <code>bytes.Buffer</code>、<code>sync.Pool</code>，但要避免把 <code>sync.Pool</code> 当作普通缓存。</li><li>避免大对象频繁分配：大对象分配和回收更容易影响堆增长曲线。</li></ol><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>Go 的自动内存管理由编译器逃逸分析、runtime 分配器和并发 GC 共同完成。逃逸分析决定对象更可能在栈上还是堆上；分配器负责让常见小对象分配足够快；GC 负责在低停顿目标下回收不可达对象。理解这些机制之后，再看线上内存问题就不只是看 <code>heap inuse</code>，而是能追问：对象为什么逃逸、是谁在分配、GC 为什么被迫频繁运行，以及调 <code>GOGC/GOMEMLIMIT</code> 是否真的解决了根因。</p>]]></content>
    
    
    <categories>
      
      <category>Go语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Go runtime</tag>
      
      <tag>内存管理</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>浮点数精度丢失：IEEE 754、舍入误差与工程实践</title>
    <link href="/2021/12/13/%E5%AF%B9%E6%B5%AE%E7%82%B9%E6%95%B0%E7%B2%BE%E5%BA%A6%E4%B8%A2%E5%A4%B1%E7%9A%84%E4%B8%80%E7%82%B9%E7%A0%94%E7%A9%B6/"/>
    <url>/2021/12/13/%E5%AF%B9%E6%B5%AE%E7%82%B9%E6%95%B0%E7%B2%BE%E5%BA%A6%E4%B8%A2%E5%A4%B1%E7%9A%84%E4%B8%80%E7%82%B9%E7%A0%94%E7%A9%B6/</url>
    
    <content type="html"><![CDATA[<h3 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h3><p>先看一个很常见的例子：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">float</span> f = <span class="hljs-number">0</span>;<span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>;i&lt;<span class="hljs-number">100</span>;i++)&#123;    f+=<span class="hljs-number">0.1f</span>;&#125;<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%f\n&quot;</span>,f);</code></pre></div><p>数学上 $0.1 \times 100 = 10$，但这段代码可能输出 <code>10.000002</code>。这不是 C 语言算错了，而是 <code>0.1f</code> 本身无法被二进制浮点数精确表示，循环累加又把每一步舍入误差继续传播。</p><p>我们打印小数点后更多位数，能看得更清楚。<code>printf(&quot;%.20f\n&quot;,f);</code>，得到的结果如下：</p><p>我们通过gdb调试看看存储在内存中的数据是什么样子的：</p><p>我们将其转化为二进制是 <code>0100 0001 0010 0000 0000 0000 0000 0010</code>。</p><p>将这个 IEEE 754 标准的二进制浮点数转化为十进制为：<code>10.000001907348633</code>。</p><h3 id="IEEE-754-如何表示浮点数"><a href="#IEEE-754-如何表示浮点数" class="headerlink" title="IEEE 754 如何表示浮点数"></a>IEEE 754 如何表示浮点数</h3><p>以 <code>float32</code> 为例，一个浮点数由三部分组成：</p><ol><li>符号位 sign：1 bit。</li><li>指数 exponent：8 bit，使用偏置编码。</li><li>尾数 fraction：23 bit，规格化数隐含一个最高位 1。</li></ol><p>规格化浮点数的值可以写成：</p><script type="math/tex; mode=display">(-1)^s \times 1.f \times 2^{e-bias}</script><p><code>float64</code> 也是同样思路，只是指数位扩展到 11 bit，尾数位扩展到 52 bit。尾数位数决定了有效精度，因此 <code>float64</code> 不是“精确小数”，只是比 <code>float32</code> 能表示更多二进制有效位。</p><h3 id="为什么-0-1-不能精确表示"><a href="#为什么-0-1-不能精确表示" class="headerlink" title="为什么 0.1 不能精确表示"></a>为什么 0.1 不能精确表示</h3><p>十进制小数能否有限表示，取决于分母质因数是否只包含 2 和 5。比如 $0.5 = 1/2$ 可以有限表示，$0.25 = 1/4$ 也可以。但二进制小数只能有限表示分母质因数只包含 2 的数。</p><script type="math/tex; mode=display">0.1_{10} = \frac{1}{10} = \frac{1}{2 \times 5}</script><p>分母里有 5，所以它在二进制里会变成无限循环小数。硬件只能保存有限位尾数，于是必须舍入。也就是说，程序里写下 <code>0.1</code> 的那一刻，进入寄存器或内存的就已经是最接近 <code>0.1</code> 的某个二进制浮点数。</p><p>类似地，下面这段代码不是特例：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">float</span> f = <span class="hljs-number">0.1f</span>;<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%.18f\n&quot;</span>,f);</code></pre></div><p>上述代码中打印结果如下所示：</p><p>我们来看看内存中如何表示这个数据的：</p><p>将这个二进制数转化为十进制数为：</p><p>可以看到某些浮点数在写入内存的过程中就存在误差，这是二进制浮点表示的自然结果，和十进制中 $1/3$ 无法有限写成小数是同一类问题。</p><h3 id="误差为什么会累积"><a href="#误差为什么会累积" class="headerlink" title="误差为什么会累积"></a>误差为什么会累积</h3><p>浮点运算的每一步通常都会经历“计算出更高精度中间结果，再舍入回目标格式”的过程。一次舍入误差可能很小，但循环累加、减去两个接近的数、不同数量级的数相加，都会放大误差影响。</p><p>典型问题有三类：</p><ol><li>累加误差：反复加一个无法精确表示的小数，例如循环加 <code>0.1</code>。</li><li>大小数相加：<code>1e20 + 1</code> 在 <code>float64</code> 中可能看不到 <code>1</code> 的影响，因为 <code>1</code> 小于当前量级下的间隔。</li><li>灾难性消去：两个非常接近的数相减，有效数字会大量丢失。</li></ol><p>例如：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    <span class="hljs-keyword">var</span> x <span class="hljs-keyword">float64</span> = <span class="hljs-number">1e20</span>    fmt.Println((x + <span class="hljs-number">1</span>) - x)&#125;</code></pre></div><p>直觉上结果是 <code>1</code>，但实际可能是 <code>0</code>。原因不是加法规则变了，而是在 <code>1e20</code> 这个量级附近，<code>float64</code> 可表示数之间的间隔已经远大于 1。</p><h3 id="ULP-与-EPSILON"><a href="#ULP-与-EPSILON" class="headerlink" title="ULP 与 EPSILON"></a>ULP 与 EPSILON</h3><p>比较浮点数时常说的 <code>EPSILON</code> 不是一个固定真理。它应该和业务量级相关。<code>1e-9</code> 对金额可能太大，对物理仿真可能又太小。</p><p>更底层的概念是 ULP（unit in the last place），表示某个浮点数附近相邻可表示数之间的距离。浮点数越大，相邻两个可表示数的绝对间隔通常也越大。因此同一个绝对误差阈值不能适用于所有量级。</p><p>更稳妥的比较方式通常同时考虑绝对误差和相对误差：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">almostEqual</span><span class="hljs-params">(a, b, absTol, relTol <span class="hljs-keyword">float64</span>)</span> <span class="hljs-title">bool</span></span> &#123;    diff := math.Abs(a - b)    <span class="hljs-keyword">if</span> diff &lt;= absTol &#123;        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>    &#125;    <span class="hljs-keyword">return</span> diff &lt;= relTol*math.Max(math.Abs(a), math.Abs(b))&#125;</code></pre></div><p>绝对误差适合接近 0 的比较，相对误差适合大数比较。</p><h3 id="排序和相等判断"><a href="#排序和相等判断" class="headerlink" title="排序和相等判断"></a>排序和相等判断</h3><p>当判断两个浮点数是否“业务上相等”时，可以使用如下思想：</p><div class="code-wrapper"><pre><code class="hljs c"><span class="hljs-keyword">if</span> (<span class="hljs-built_in">fabs</span>(a - b) &lt; EPSILON) &#123;    <span class="hljs-comment">//执行当两个浮点数 a 和 b 相等时的操作</span>&#125;</code></pre></div><p>但要注意：这只适合业务语义下的近似相等，不适合所有场景。如果要做 map key、排序去重、缓存索引，就必须明确使用精确 bit 模式还是归一化后的业务值。</p><h3 id="float-可以作为-map-key-吗"><a href="#float-可以作为-map-key-吗" class="headerlink" title="float 可以作为 map key 吗"></a>float 可以作为 map key 吗</h3><p>从语法上看，Go 语言中只要是可比较的类型都可以作为 key。除开 slice、map、function 这几种类型，其他类型大多可以。浮点数支持 <code>==</code> 和 <code>!=</code>，所以 <code>float64</code> 可以作为 map key。</p><p>问题在于：语法允许不代表业务上合适。</p><p>我们来看如下例子：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    m := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">map</span>[<span class="hljs-keyword">float64</span>]<span class="hljs-keyword">int</span>)    m[<span class="hljs-number">1.4</span>] = <span class="hljs-number">1</span>    m[<span class="hljs-number">2.4</span>] = <span class="hljs-number">2</span>    <span class="hljs-keyword">for</span> k, v := <span class="hljs-keyword">range</span> m &#123;        fmt.Printf(<span class="hljs-string">&quot;[%v, %d] &quot;</span>, k, v)    &#125;    fmt.Printf(<span class="hljs-string">&quot;k: %v, v: %d\n&quot;</span>, <span class="hljs-number">2.400000000001</span>, m[<span class="hljs-number">2.400000000001</span>])    fmt.Printf(<span class="hljs-string">&quot;k: %v, v: %d\n&quot;</span>, <span class="hljs-number">2.4000000000000000000000001</span>, m[<span class="hljs-number">2.4000000000000000000000001</span>])&#125;</code></pre></div><p>程序输出如下：</p><div class="code-wrapper"><pre><code class="hljs go">[<span class="hljs-number">2.4</span>, <span class="hljs-number">2</span>] [<span class="hljs-number">1.4</span>, <span class="hljs-number">1</span>] k: <span class="hljs-number">2.400000000001</span>, v: <span class="hljs-number">0</span>k: <span class="hljs-number">2.4</span>, v: <span class="hljs-number">2</span></code></pre></div><p>我们发现 <code>2.4000000000000000000000001</code> 可以查到 value。原因是这个十进制字面量在编译后被舍入成了和 <code>2.4</code> 相同的 <code>float64</code>。</p><p>Go 可以通过以下函数观察浮点数的 bit 表示：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-comment">// Float64frombits returns the floating point number corresponding</span><span class="hljs-comment">// the IEEE 754 binary representation b.</span><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">Float64frombits</span><span class="hljs-params">(b <span class="hljs-keyword">uint64</span>)</span> <span class="hljs-title">float64</span></span> &#123; <span class="hljs-keyword">return</span> *(*<span class="hljs-keyword">float64</span>)(unsafe.Pointer(&amp;b)) &#125;</code></pre></div><p>也就是 IEEE 754 规定的格式。</p><p>我们再来输出点东西：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<span class="hljs-keyword">import</span> (    <span class="hljs-string">&quot;fmt&quot;</span>    <span class="hljs-string">&quot;math&quot;</span>)<span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;    m := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">map</span>[<span class="hljs-keyword">float64</span>]<span class="hljs-keyword">int</span>)    m[<span class="hljs-number">2.4</span>] = <span class="hljs-number">2</span>    fmt.Println(math.Float64bits(<span class="hljs-number">2.4</span>))    fmt.Println(math.Float64bits(<span class="hljs-number">2.400000000001</span>))    fmt.Println(math.Float64bits(<span class="hljs-number">2.4000000000000000000000001</span>))&#125;</code></pre></div><p>输出结果如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-number">4612586738352862003</span><span class="hljs-number">4612586738352864255</span><span class="hljs-number">4612586738352862003</span></code></pre></div><p>转成十六进制如下：</p><div class="code-wrapper"><pre><code class="hljs go"><span class="hljs-number">0x4003333333333333</span><span class="hljs-number">0x4003333333333BFF</span><span class="hljs-number">0x4003333333333333</span></code></pre></div><p>由此可见，由于精度受限，<code>2.4</code> 和 <code>2.4000000000000000000000001</code> 经过 <code>math.Float64bits()</code> 转换后的结果是一样的。自然，二者在 map 看来就是同一个 key。</p><p>还有两个坑：</p><ol><li><code>NaN != NaN</code>，所以 NaN 作为 key 时很容易出现无法按直觉查询的问题。</li><li><code>+0.0 == -0.0</code> 为 true，但二者 bit 表示不同。不同语言或序列化层如果按 bit 处理，可能得到不同结果。</li></ol><p>工程上如果需要用小数做 key，通常更推荐先归一化成整数。例如金额用“分”或更小单位保存，价格 tick 用整数 tick 数保存，坐标或指标按业务精度放大后取整。</p><h3 id="工程建议"><a href="#工程建议" class="headerlink" title="工程建议"></a>工程建议</h3><ol><li>金额、账务、撮合价格等场景不要直接用二进制浮点数表达精确十进制小数，优先使用整数定点数或 decimal 库。</li><li>科学计算可以使用浮点数，但要关注算法稳定性，例如 Kahan summation、排序后累加、避免相近大数相减。</li><li>比较浮点数时不要盲目使用 <code>==</code>，除非比较的是协议定义的特殊值、枚举式浮点值，或你明确需要 bit 级相等。</li><li>日志和序列化要保留足够有效位，否则打印出来的值可能无法 round-trip 回原来的浮点数。</li><li>map key、数据库索引、缓存 key 中出现 float 时，要先问清楚业务语义：是精确 bit 模式，还是某个精度下的等价类。</li></ol><p>浮点数不是“不准确”，而是“精确地表示了某个二进制近似值”。理解这一点后，很多看似诡异的问题都会变成可解释、可规避的工程取舍。</p>]]></content>
    
    
    <categories>
      
      <category>杂文</category>
      
    </categories>
    
    
    <tags>
      
      <tag>浮点数</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Stanford CS144 Lab 1: Stream reassembler</title>
    <link href="/2021/11/27/Stanford-CS144-Lab-1-Stream-Reassembler/"/>
    <url>/2021/11/27/Stanford-CS144-Lab-1-Stream-Reassembler/</url>
    
    <content type="html"><![CDATA[<p>该lab要求我们实现一个流重组类，可以将Sender发来的带索引号的字节碎片重组成有序的字节写入到byte_stream。接收端从发送端读取数据，调用流重组器，流重组器对数据进行排序，排序好后写入byte_stream。</p><p>值得注意的是，无论是 lab0 中的 byte_stream，还是 lab1 中的重组器，都有 <code>capacity</code> 的概念。<code>capacity</code> 对应可用缓冲上限，它由“重组器中尚未拼接的数据”与“已经写入 byte_stream 但尚未被读取的数据”共同占用。</p><p>所以我们在进行重组和读写操作时，都要根据 <code>capacity</code> 约束维护重组器。为了方便起见，我们依旧使用双端队列作为重组器。</p><p>在重组器类的函数中，push_substring函数完成重组工作。其参数有data(string),index(size_t),eof(bool),data是待排序的数据，index是数据首元素的序号，eof用于判断是否结束。结束后将不再有数据输入。</p><p>在重组的过程中，我们会遇到重复、丢包、重叠、乱序的现象。为了使乱序的数据找到有序的位置，我使用’\0’维护重组器中数据的相对序号，例如，第一次data为make,index为0,第二次data为great,index为13,而处于两组数据中间的数据未知，我们就用’\0’代替，即make\0\0\0\0\0\0\0\0\0great。这样就维护了已经进入重组器的数据的有序。当然，写入的data中也有可能含有\0,这是，我们就需要一个bool双端队列，来记录相应位置的数据是否有序，在上述例子中，队列的bool值为111100000000011111。</p><p>1.我们先判断输入序号是否超过当前可接受窗口（unacceptable 区域之外）。如果超过，说明当前缓冲容量不足以接纳该片段，需要直接丢弃。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(index&gt;_output.<span class="hljs-built_in">bytes_read</span>()+_capacity)&#123;    <span class="hljs-keyword">return</span>;&#125;</code></pre></div><p>2.如果 <code>data</code> 的尾部超出当前可接受范围，我们只保留窗口内可接纳的那一段。写入时常见两种情况：一是 <code>index</code> 在当前重组尾部之后，需要补占位并标记状态；二是 <code>index</code> 落在已有区间内，需要按覆盖规则处理重叠部分。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(index+data.<span class="hljs-built_in">length</span>()&gt;_capacity+_output.<span class="hljs-built_in">bytes_read</span>())&#123;    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=_lens_un+_output.<span class="hljs-built_in">bytes_written</span>();i&lt;_capacity+_output.<span class="hljs-built_in">bytes_read</span>();i++)&#123;        <span class="hljs-keyword">if</span>(i&lt;index)&#123;            _unassembled_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-string">&#x27;\0&#x27;</span>);            _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">false</span>);        &#125;<span class="hljs-keyword">else</span>&#123;            _unassembled_byte.<span class="hljs-built_in">push_back</span>(data[i-index]);            _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">true</span>);        &#125;        _lens_un++;    &#125;&#125;</code></pre></div><p>3.我们要判断index是否等于已经写入byte_stream(_output)中的数据，如果是的，我们就直接将data中的数据写入byte_stream,然后在重组器中弹出data.length()个数据，值得注意的是，当重组器中的数据个数小于data.length()，我们就全部弹出。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(index==_output.<span class="hljs-built_in">bytes_written</span>())&#123;    _output.<span class="hljs-built_in">write</span>(data);    <span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data.<span class="hljs-built_in">length</span>());    _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);    _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);    _lens_un-=temp_len;&#125;</code></pre></div><p>4.我们要判断index是否大于流重组器中的最后一个数据的序号和写入byte_stream中的数据个数之和，如果大于，我们就可以参考1的处理，代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(index&gt;_output.<span class="hljs-built_in">bytes_written</span>()+_lens_un)&#123;    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=_output.<span class="hljs-built_in">bytes_written</span>()+_lens_un;i&lt;index;i++)&#123;        _unassembled_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-string">&#x27;\0&#x27;</span>);        _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">false</span>);        _lens_un++;    &#125;    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> i : data)&#123;        _unassembled_byte.<span class="hljs-built_in">push_back</span>(i);        _lens_un++;        _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">true</span>);    &#125;&#125;</code></pre></div><p>5.我们要判断data中的数据是否已经被写入byte_stream，这个说法有些不准确，准确的说是相应序号的数据被写入，如果data中的所有数据都被写入了byte_stream，我们就直接返回，如果只是部分被写入，我们就将data中未被写入的部分写入。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(index&lt;_output.<span class="hljs-built_in">bytes_written</span>())&#123;    <span class="hljs-keyword">if</span>(_output.<span class="hljs-built_in">bytes_written</span>()&gt;index+data.<span class="hljs-built_in">length</span>())&#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-function">std::string <span class="hljs-title">data_cut</span><span class="hljs-params">(data.begin()+_output.bytes_written()-index,data.end())</span></span>;    _output.<span class="hljs-built_in">write</span>(data_cut);    <span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data_cut.<span class="hljs-built_in">length</span>());    _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);    _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);    _lens_un-=temp_len;</code></pre></div><p>6.在上述特殊情况都被排除之后，剩下的就是index处于流重组器的中间，我们就插入data中的数据到流重组器就行了。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-comment">//在中间插入元素</span><span class="hljs-comment">//先弹出一部分数据保存到栈中</span>std::stack&lt;<span class="hljs-keyword">char</span>&gt; temp;std::stack&lt;<span class="hljs-keyword">bool</span>&gt; temp_check;<span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=<span class="hljs-number">0</span>;i&lt;index-_output.<span class="hljs-built_in">bytes_written</span>();i++)&#123;    temp.<span class="hljs-built_in">push</span>(_unassembled_byte.<span class="hljs-built_in">at</span>(i));    temp_check.<span class="hljs-built_in">push</span>(_check_byte.<span class="hljs-built_in">at</span>(i));&#125;<span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data.<span class="hljs-built_in">length</span>()+index-_output.<span class="hljs-built_in">bytes_written</span>());_unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);_check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);_lens_un-=temp_len;<span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=data.<span class="hljs-built_in">length</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--)&#123;    _unassembled_byte.<span class="hljs-built_in">push_front</span>(data[i]);    _check_byte.<span class="hljs-built_in">push_front</span>(<span class="hljs-literal">true</span>);    _lens_un++;&#125;<span class="hljs-keyword">while</span>(!temp.<span class="hljs-built_in">empty</span>())&#123;    _unassembled_byte.<span class="hljs-built_in">push_front</span>(temp.<span class="hljs-built_in">top</span>());    _check_byte.<span class="hljs-built_in">push_front</span>(temp_check.<span class="hljs-built_in">top</span>());    _lens_un++;    temp.<span class="hljs-built_in">pop</span>();    temp_check.<span class="hljs-built_in">pop</span>();&#125;</code></pre></div><p>在讲完data中数据插入流重组器操作之后，我们就要讲一讲如何讲流重组器中的数据写入byte_stream，其实非常简单，如果流重组器中的front数据的判定值为true,就说明该数据已经排好序了，我们就可以直接写入，依次判定，直到遇到判定值为false的数据。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function">std::string <span class="hljs-title">n</span><span class="hljs-params">(_unassembled_byte.begin(),_unassembled_byte.begin()+i)</span></span>;_output.<span class="hljs-built_in">write</span>(n);_unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+i);_lens_un-=i;_check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+i);<span class="hljs-keyword">if</span>(eof) input_end_index=index+data.<span class="hljs-built_in">length</span>();<span class="hljs-keyword">if</span>(input_end_index==_output.<span class="hljs-built_in">bytes_written</span>()) _output.<span class="hljs-built_in">end_input</span>();</code></pre></div><p>至于unassembled_byte函数，就是返回流重组器中false数据的个数，empty就是一个判空函数，代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">StreamReassembler::unassembled_bytes</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">size_t</span> res=<span class="hljs-number">0</span>;    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">bool</span> i:_check_byte)&#123;        <span class="hljs-keyword">if</span>(i)&#123;            res++;        &#125;    &#125;    <span class="hljs-keyword">return</span> res;&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">StreamReassembler::empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _lens_un==<span class="hljs-number">0</span>; &#125;</code></pre></div><p>stream_reassembler.cc代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;stream_reassembler.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;stack&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;cstring&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;iostream&quot;</span></span><span class="hljs-comment">// Dummy implementation of a stream reassembler.</span><span class="hljs-comment">// For Lab 1, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab1`.</span><span class="hljs-comment">// You will need to add private members to the class declaration in `stream_reassembler.hh`</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;StreamReassembler::<span class="hljs-built_in">StreamReassembler</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity) : _output(capacity), _capacity(capacity), _unassembled_byte(<span class="hljs-number">0</span>),_check_byte(<span class="hljs-number">0</span>),_lens_un(<span class="hljs-number">0</span>),<span class="hljs-built_in">input_end_index</span>(<span class="hljs-number">-1</span>)&#123;&#125;<span class="hljs-comment">//! \details This function accepts a substring (aka a segment) of bytes,</span><span class="hljs-comment">//! possibly out-of-order, from the logical stream, and assembles any newly</span><span class="hljs-comment">//! contiguous substrings and writes them into the output stream in order.</span><span class="hljs-comment">//</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">StreamReassembler::push_substring</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;data, <span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> index, <span class="hljs-keyword">const</span> <span class="hljs-keyword">bool</span> eof)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(data, index, eof);    <span class="hljs-comment">//始终使用绝对索引</span>    <span class="hljs-keyword">if</span>(index&gt;_output.<span class="hljs-built_in">bytes_read</span>()+_capacity)&#123;        <span class="hljs-keyword">return</span>;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(index+data.<span class="hljs-built_in">length</span>()&gt;_capacity+_output.<span class="hljs-built_in">bytes_read</span>())&#123;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=_lens_un+_output.<span class="hljs-built_in">bytes_written</span>();i&lt;_capacity+_output.<span class="hljs-built_in">bytes_read</span>();i++)&#123;            <span class="hljs-keyword">if</span>(i&lt;index)&#123;                _unassembled_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-string">&#x27;\0&#x27;</span>);                _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">false</span>);            &#125;<span class="hljs-keyword">else</span>&#123;                _unassembled_byte.<span class="hljs-built_in">push_back</span>(data[i-index]);                _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">true</span>);            &#125;            _lens_un++;        &#125;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(index==_output.<span class="hljs-built_in">bytes_written</span>())&#123;        _output.<span class="hljs-built_in">write</span>(data);        <span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data.<span class="hljs-built_in">length</span>());        _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _lens_un-=temp_len;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(index&gt;_output.<span class="hljs-built_in">bytes_written</span>()+_lens_un)&#123;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=_output.<span class="hljs-built_in">bytes_written</span>()+_lens_un;i&lt;index;i++)&#123;            _unassembled_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-string">&#x27;\0&#x27;</span>);            _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">false</span>);            _lens_un++;        &#125;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> i : data)&#123;            _unassembled_byte.<span class="hljs-built_in">push_back</span>(i);            _lens_un++;            _check_byte.<span class="hljs-built_in">push_back</span>(<span class="hljs-literal">true</span>);        &#125;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(index&lt;_output.<span class="hljs-built_in">bytes_written</span>())&#123;        <span class="hljs-keyword">if</span>(_output.<span class="hljs-built_in">bytes_written</span>()&gt;index+data.<span class="hljs-built_in">length</span>())&#123;            <span class="hljs-keyword">return</span>;        &#125;        <span class="hljs-function">std::string <span class="hljs-title">data_cut</span><span class="hljs-params">(data.begin()+_output.bytes_written()-index,data.end())</span></span>;        _output.<span class="hljs-built_in">write</span>(data_cut);        <span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data_cut.<span class="hljs-built_in">length</span>());        _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _lens_un-=temp_len;    &#125;<span class="hljs-keyword">else</span>&#123;        <span class="hljs-comment">//在中间插入元素</span>        <span class="hljs-comment">//先弹出一部分数据保存到栈中</span>        std::stack&lt;<span class="hljs-keyword">char</span>&gt; temp;        std::stack&lt;<span class="hljs-keyword">bool</span>&gt; temp_check;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">size_t</span> i=<span class="hljs-number">0</span>;i&lt;index-_output.<span class="hljs-built_in">bytes_written</span>();i++)&#123;            temp.<span class="hljs-built_in">push</span>(_unassembled_byte.<span class="hljs-built_in">at</span>(i));            temp_check.<span class="hljs-built_in">push</span>(_check_byte.<span class="hljs-built_in">at</span>(i));        &#125;        <span class="hljs-keyword">size_t</span> temp_len=std::<span class="hljs-built_in">min</span>(_lens_un,data.<span class="hljs-built_in">length</span>()+index-_output.<span class="hljs-built_in">bytes_written</span>());        _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+temp_len);        _lens_un-=temp_len;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i=data.<span class="hljs-built_in">length</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--)&#123;            _unassembled_byte.<span class="hljs-built_in">push_front</span>(data[i]);            _check_byte.<span class="hljs-built_in">push_front</span>(<span class="hljs-literal">true</span>);            _lens_un++;        &#125;        <span class="hljs-keyword">while</span>(!temp.<span class="hljs-built_in">empty</span>())&#123;            _unassembled_byte.<span class="hljs-built_in">push_front</span>(temp.<span class="hljs-built_in">top</span>());            _check_byte.<span class="hljs-built_in">push_front</span>(temp_check.<span class="hljs-built_in">top</span>());            _lens_un++;            temp.<span class="hljs-built_in">pop</span>();            temp_check.<span class="hljs-built_in">pop</span>();        &#125;    &#125;    <span class="hljs-keyword">size_t</span> i=<span class="hljs-number">0</span>;    <span class="hljs-keyword">while</span>(i&lt;_lens_un)&#123;        <span class="hljs-keyword">if</span>(!_check_byte.<span class="hljs-built_in">at</span>(i))&#123;            <span class="hljs-keyword">break</span>;        &#125;        i++;    &#125;    <span class="hljs-function">std::string <span class="hljs-title">n</span><span class="hljs-params">(_unassembled_byte.begin(),_unassembled_byte.begin()+i)</span></span>;    _output.<span class="hljs-built_in">write</span>(n);    _unassembled_byte.<span class="hljs-built_in">erase</span>(_unassembled_byte.<span class="hljs-built_in">begin</span>(),_unassembled_byte.<span class="hljs-built_in">begin</span>()+i);    _lens_un-=i;    _check_byte.<span class="hljs-built_in">erase</span>(_check_byte.<span class="hljs-built_in">begin</span>(),_check_byte.<span class="hljs-built_in">begin</span>()+i);    <span class="hljs-keyword">if</span>(eof) input_end_index=index+data.<span class="hljs-built_in">length</span>();    <span class="hljs-keyword">if</span>(input_end_index==_output.<span class="hljs-built_in">bytes_written</span>()) _output.<span class="hljs-built_in">end_input</span>();&#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">StreamReassembler::unassembled_bytes</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">size_t</span> res=<span class="hljs-number">0</span>;    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">bool</span> i:_check_byte)&#123;        <span class="hljs-keyword">if</span>(i)&#123;            res++;        &#125;    &#125;    <span class="hljs-keyword">return</span> res;&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">StreamReassembler::empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _lens_un==<span class="hljs-number">0</span>; &#125;</code></pre></div><p>stream_reassembler.hh代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">ifndef</span> SPONGE_LIBSPONGE_STREAM_REASSEMBLER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SPONGE_LIBSPONGE_STREAM_REASSEMBLER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;byte_stream.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;cstdint&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;string&gt;</span></span><span class="hljs-comment">//! \brief A class that assembles a series of excerpts from a byte stream (possibly out of order,</span><span class="hljs-comment">//! possibly overlapping) into an in-order byte stream.</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">StreamReassembler</span> &#123;</span>  <span class="hljs-keyword">private</span>:    <span class="hljs-comment">// Your code here -- add private members as necessary.</span>    ByteStream _output;  <span class="hljs-comment">//!&lt; The reassembled in-order byte stream</span>    <span class="hljs-comment">//capacity是SreamReassembler里面储存的max_index减去在bytestream里面还没有被读取min_index,然后还要加上1</span>    <span class="hljs-keyword">size_t</span> _capacity;    <span class="hljs-comment">//!&lt; The maximum number of bytes</span>    std::deque&lt;<span class="hljs-keyword">char</span>&gt; _unassembled_byte;<span class="hljs-comment">//</span>    std::deque&lt;<span class="hljs-keyword">bool</span>&gt; _check_byte;    <span class="hljs-keyword">size_t</span> _lens_un;    <span class="hljs-keyword">bool</span> _eof;  <span class="hljs-keyword">public</span>:    <span class="hljs-comment">//! \brief Construct a `StreamReassembler` that will store up to `capacity` bytes.</span>    <span class="hljs-comment">//! \note This capacity limits both the bytes that have been reassembled,</span>    <span class="hljs-comment">//! and those that have not yet been reassembled.</span>    <span class="hljs-built_in">StreamReassembler</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity);    <span class="hljs-comment">//! \brief Receive a substring and write any newly contiguous bytes into the stream.</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! The StreamReassembler will stay within the memory limits of the `capacity`.</span>    <span class="hljs-comment">//! Bytes that would exceed the capacity are silently discarded.</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! \param data the substring</span>    <span class="hljs-comment">//! \param index indicates the index (place in sequence) of the first byte in `data`</span>    <span class="hljs-comment">//! \param eof the last byte of `data` will be the last byte in the entire stream</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">push_substring</span><span class="hljs-params">(<span class="hljs-keyword">const</span> std::string &amp;data, <span class="hljs-keyword">const</span> <span class="hljs-keyword">uint64_t</span> index, <span class="hljs-keyword">const</span> <span class="hljs-keyword">bool</span> eof)</span></span>;    <span class="hljs-comment">//! \name Access the reassembled byte stream</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-function"><span class="hljs-keyword">const</span> ByteStream &amp;<span class="hljs-title">stream_out</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _output; &#125;    <span class="hljs-function">ByteStream &amp;<span class="hljs-title">stream_out</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> _output; &#125;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! The number of bytes in the substrings stored but not yet reassembled</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! \note If the byte at a particular index has been pushed more than once, it</span>    <span class="hljs-comment">//! should only be counted once for the purpose of this function.</span>    <span class="hljs-comment">/*unassembled byte是还未传递但是要传递给bytestream的储存在StreamReassembler的字符数，</span><span class="hljs-comment">     * 其中如果不同的字符串有重叠部分的话，重叠部分只算一次</span><span class="hljs-comment">     * */</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">unassembled_bytes</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \brief Is the internal state empty (other than the output stream)?</span>    <span class="hljs-comment">//! \returns `true` if no substrings are waiting to be assembled</span>    <span class="hljs-comment">//判空函数，</span>    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;&#125;;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span>  <span class="hljs-comment">// SPONGE_LIBSPONGE_STREAM_REASSEMBLER_HH</span></span></code></pre></div><p>后记：</p><p>在我面试的时候，面试官问我：</p><p>如果遇到极端情况，当前所需的第一个字节index为i，但包含这个字节的segment迟迟不来，这个时候应该怎么办？</p><p>这个问题其实不应该是流重组器应该考虑的，这个应该是快速重传的问题。当<code>receiver</code>所需的segment迟迟没来的时候，大概是三次冗余ack，<code>sender</code>就会进行快速重传，当然本实验是不需要实现快速重传的，所以从这个角度讲关我流重组器什么是捏。</p><p>大家如果要看快速重传的内容可以到这里下载：</p><p><a href="https://github.com/xiao-nanbei/hust-cs/tree/main/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%AE%9E%E9%AA%8C%E4%BA%8C(%E4%BC%A0%E8%BE%93%E5%8D%8F%E8%AE%AE%EF%BC%89">GBN&amp;SR&amp;TCP</a>）</p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Stanford CS144 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Stanford CS144 Lab 3: TCP sender</title>
    <link href="/2021/11/23/Stanford-CS144-Lab-3-TCP-Sender/"/>
    <url>/2021/11/23/Stanford-CS144-Lab-3-TCP-Sender/</url>
    
    <content type="html"><![CDATA[<p>在这个实验中我们要完成tcp_sender的部分。由于我们实现的是简易的TCP，因此在测试集对于拥塞控制并没有太多要求，且计时器我们也只用实现重传计时器。</p><p>我们先来理解bytes_in_flight，bytes_in_flight是发送但还未确认的数据长度。根据概念，这段长度等于_next_seqno减去base。因此我们定义一个base用来维护基序号，同时也可以完成bytes_in_flight函数。代码如下:</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">TCPSender::bytes_in_flight</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">return</span> _next_seqno-base;&#125;</code></pre></div><h4 id="TCP连接与释放（三次握手与四次挥手）"><a href="#TCP连接与释放（三次握手与四次挥手）" class="headerlink" title="TCP连接与释放（三次握手与四次挥手）"></a>TCP连接与释放（三次握手与四次挥手）</h4><p>在这个实验中我们同样要用到三次握手的知识，为了更好的解释实现过程，我们用以下图示解释。</p><p>在介绍三次握手之前，先要说说TCP头部的一些常用字段（这也是我们实验中要用到的）。</p><ul><li>序号：seqno，占32位，用来标识从发送端到接收端的字节流；</li><li>确认号：ackno，占32位，只有 ACK 标志位为 1 时才有效；它表示“期望收到的下一个序号”，并不总是简单等于 <code>seqno+1</code>；</li><li>标志位：<ul><li>SYN：发起一个连接；</li><li>FIN：释放一个连接；</li><li>ACK：确认序号有效。</li></ul></li></ul><p>我们可以将sender理解为将要发送syn报文的client(事实上，client和server都有sender和receiver，我们将在lab4中具体组装sender和receiver）。至此，我们的lab都是单向通信。发送方只有一个TCPsender，接收方只有一个TCPReceiver，由TCPsender发送的和TCPReceiver返回的都是TCPsegment格式的TCP报文段，只不过由于单向通信，所以发送方的TCPsegment中只包含序列号seqno、 Payload、SYN 和 FIN，接收方返回的TCPsegment中只包含ackno和window size。作为绝对序号，_next_seqno的初始值为0，此时我们必须发送SYN报文建立连接，也就是第一次握手，seqno为 _isn（即上图的x，这是系统赋予的），syn为true，payload置空，报文准备好后，我们将其写入到定义好的队列 _segments_out，等待系统调用，同时，我们为了实现重传，定义了一个 _segments_out_cache,作为缓冲区，用于缓存报文，在特定的情况下将报文重传。当然，我们也要维护GBN中的滑动窗口，因此定义一个curr_window_size，用来维护当前可用窗口的大小。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(_next_seqno==<span class="hljs-number">0</span>)&#123;    TCPSegment seg;    seg.<span class="hljs-built_in">header</span>().seqno=_isn;    seg.<span class="hljs-built_in">header</span>().syn= <span class="hljs-literal">true</span>;    _next_seqno=<span class="hljs-number">1</span>;    curr_window_size--;<span class="hljs-comment">//当前可用窗口减1</span>    _segments_out.<span class="hljs-built_in">push</span>(seg);    _segments_out_cache.<span class="hljs-built_in">push</span>(seg);&#125;</code></pre></div><p>当client接收第二次握手时传入的内容，即ack=x+1，也即ackno_64=1的内容，将会调用ack_received函数，ack_received函数的定义——<code>ack_received(const WrappingInt32 ackno, const uint16_t window_size)</code>，此时ackno=_isn+1，我们会执行如下操作：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(base==<span class="hljs-number">0</span>&amp;&amp;ackno_64==<span class="hljs-number">1</span>)&#123;        base=<span class="hljs-number">1</span>;        _segments_out_cache.<span class="hljs-built_in">pop</span>();        _consecutive_remission=<span class="hljs-number">0</span>;&#125;</code></pre></div><p>将base置为1，也就是将基序号+1；将缓冲队列出队（此时对方已经确认，我们不需要了）；连续重传此时对于sender来说，我们可以进行正常的报文发送了，也就是第三次握手。</p><p>无独有偶，实验中也要用到四次挥手的知识，为了更好的解释该过程，我们用下图解释。</p><p>在该实验中，当数据传输完成时，即 _stream.eof等于1时，我们要进行挥手，此时sender发送fin报文，即fin为1。如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">if</span>(_stream.<span class="hljs-built_in">eof</span>())&#123;    fin_flag=<span class="hljs-literal">true</span>;    TCPSegment seg;    <span class="hljs-comment">//将syn设置为false，将fin设置为true</span>    seg.<span class="hljs-built_in">header</span>().syn= <span class="hljs-literal">false</span>;    seg.<span class="hljs-built_in">header</span>().fin=<span class="hljs-literal">true</span>;    seg.<span class="hljs-built_in">header</span>().seqno= <span class="hljs-built_in">wrap</span>(_next_seqno,_isn);    _next_seqno++;    <span class="hljs-comment">//维护窗口</span>    curr_window_size--;    _segments_out_cache.<span class="hljs-built_in">push</span>(seg);    _segments_out.<span class="hljs-built_in">push</span>(seg);&#125;</code></pre></div><p>在该实验中，我们有两种情况发送fin包，第一种是恰好 _stream.eof为1，即刚好将 _stream中的数据全部读取完毕，这个时候我们发送一个fin值为1，payload为空的报文。第二种情况是发送窗口大于 _stream.size，此时也可以将 _stream中的报文读完。此时我们将最后一个报文(读取 _stream之后 _stream为空）的fin值置为1。值得注意的是，当fin值为1是要占用一个序号。因为有MAX_PAYLOAD_SIZE的限制，我们将用while进行循环读取。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">else</span> &#123;    <span class="hljs-keyword">while</span> (!_stream.<span class="hljs-built_in">buffer_empty</span>()&amp;&amp;curr_window_size&gt;<span class="hljs-number">0</span>)&#123;        <span class="hljs-comment">//这里会有两种状况，一种是_stream为空但窗口不为空；一种是窗口为空但_stream不为空。</span>        <span class="hljs-comment">//我们找到_stream curr_window_size以及payload中的最小值，作为发送报文的长度</span>        <span class="hljs-keyword">uint64_t</span> lens_byte=std::<span class="hljs-built_in">min</span>(_stream.<span class="hljs-built_in">buffer_size</span>(),<span class="hljs-built_in">uint64_t</span> (curr_window_size));        lens_byte=std::<span class="hljs-built_in">min</span>(lens_byte,TCPConfig::MAX_PAYLOAD_SIZE);        TCPSegment seg;        <span class="hljs-comment">//对header中的字段进行赋值</span>        seg.<span class="hljs-built_in">header</span>().seqno= <span class="hljs-built_in">wrap</span>(_next_seqno,_isn);        seg.<span class="hljs-built_in">header</span>().syn=<span class="hljs-literal">false</span>;        seg.<span class="hljs-built_in">payload</span>()=_stream.<span class="hljs-built_in">read</span>(lens_byte);        _next_seqno+=lens_byte;        <span class="hljs-comment">//将curr_window_size减去lens_byte</span>        curr_window_size-=lens_byte;        <span class="hljs-comment">//如果_stream已经空了，那么我们就进行第一次挥手</span>        <span class="hljs-keyword">if</span>(_stream.<span class="hljs-built_in">eof</span>()&amp;&amp;curr_window_size&gt;<span class="hljs-number">0</span>)&#123;            seg.<span class="hljs-built_in">header</span>().fin= <span class="hljs-literal">true</span>;            fin_flag= <span class="hljs-literal">true</span>;            curr_window_size--;            _next_seqno++;        &#125;        _segments_out.<span class="hljs-built_in">push</span>(seg);        _segments_out_cache.<span class="hljs-built_in">push</span>(seg);        <span class="hljs-keyword">if</span>(fin_flag)&#123;<span class="hljs-comment">//开始挥手之后就可以退出</span>            <span class="hljs-keyword">break</span>;        &#125;    &#125;&#125;</code></pre></div><h4 id="TCP重传机制"><a href="#TCP重传机制" class="headerlink" title="TCP重传机制"></a>TCP重传机制</h4><p>在每次发送报文之后，都会启动一个定时器，如果在定时器时间内没有收到，则进行重传。而重传的超时时间，则称为RTO（Retransmission TimeOut），我们就要开始计时，将初始超时重传时间 _time_out设置 _initial_retransmission_timeout（也就是说，我们并不会去动态地计算RTT和RTO，这都是测试程序自动计算的），如果发生超时，将 _time_outx2。这个跟我们在《计算机网络黑皮书》上学到的不一样，是最新的标准。上述过程用time_waiting标志位进行维护。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(!time_waiting)&#123;    _time_out=_initial_retransmission_timeout;    time_waiting=<span class="hljs-literal">true</span>;<span class="hljs-comment">//为true的时候表示我们正在计时</span>    _total_time=<span class="hljs-number">0</span>;&#125;</code></pre></div><p>那么这个tick函数就很好做了，每次调用tick函数，_total_time加上 ms_since_last_tick，如果总时间大于超时时间，那么我们就可以进行重传，并将重传的次数+1，我们的 TCPConnection 将使用此信息来决定连接是否无望（连续重传过多）并需要中止，如果window size不为0，那么我们将RTO的值翻倍，称为指数退避。将总计时时间置为0。</p><div class="code-wrapper"><pre><code class="hljs sqf">void TCPSender::tick(const size_t ms_since_last_tick) &#123;    DUMMY_CODE(ms_since_last_tick);    <span class="hljs-variable">_total_time</span>+=ms_since_last_tick;    <span class="hljs-comment">//如果缓冲队列不为空，停等时间在运行，且总时间大于超时</span>    <span class="hljs-keyword">if</span>(!<span class="hljs-variable">_segments_out_cache</span>.empty()&amp;&amp;time_waiting&amp;&amp;<span class="hljs-variable">_total_time</span>&gt;=<span class="hljs-variable">_time_out</span>)&#123;        <span class="hljs-variable">_segments_out</span>.push(<span class="hljs-variable">_segments_out_cache</span>.front());        <span class="hljs-comment">//重传次数增加</span>        <span class="hljs-variable">_consecutive_remission</span>++;        <span class="hljs-keyword">if</span>(!window_zero)&#123;            <span class="hljs-variable">_time_out</span>*=<span class="hljs-number">2</span>;        &#125;        <span class="hljs-variable">_total_time</span>=<span class="hljs-number">0</span>;    &#125;&#125;</code></pre></div><p>值得注意的是，TCP中有7种定时器，我们在这里只实现了重传定时器：</p><ul><li>建立连接定时器(connection-establishment timer)</li><li>重传定时器(retransmission timer)</li><li>延迟应答定时器(delayed ACK timer)</li><li>坚持定时器(persist timer)</li><li>保活定时器(keepalive timer)</li><li>FIN_WAIT_2定时器(FIN_WAIT_2 timer)</li><li>TIME_WAIT定时器 (TIME_WAIT timer, 也叫2MSL timer)</li></ul><h4 id="ack-received"><a href="#ack-received" class="headerlink" title="ack_received"></a>ack_received</h4><p>1.如果 ackno_64大于 _next_seqno，就会触发SYN_SENT状态，按照常理来说，ackno_64是不应该大于 _next_seqno,如果ackno_64大于 _next_seqno，我们将其定为“<em>stream started but nothing acknowledged</em>“，直接返回即可，等待下一次传入的ackno，至于为什么会出现ackno_64大于 _next_seqno我们并不关心。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp">uint <span class="hljs-keyword">64_t</span> ackno_64= <span class="hljs-built_in">unwrap</span>(ackno,_isn,base);<span class="hljs-keyword">if</span>(ackno_64&gt;_next_seqno)&#123;    <span class="hljs-keyword">return</span>;&#125;</code></pre></div><p>2.对于三次握手中的第二次握手，也就是我们客户端收到确认报文这个情况，对于发送方的窗口而言，我们应当这样实现：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(base==<span class="hljs-number">0</span>&amp;&amp;ackno_64==<span class="hljs-number">1</span>)&#123;       base=<span class="hljs-number">1</span>;       _segments_out_cache.<span class="hljs-built_in">pop</span>();       _consecutive_remission=<span class="hljs-number">0</span>;&#125;</code></pre></div><p>3.如果是正常的接收确认报文，对于窗口而言，我们应当这样实现，代码如下（详解在注释中）：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-comment">//下面代码就是在模拟GBN,根据接收到的ackno，删除窗口中base和ackno之间的数据</span><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!_segments_out_cache.<span class="hljs-built_in">empty</span>() &amp;&amp; ackno_64 &gt;= base + _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>()) &#123;<span class="hljs-comment">//收到的确认号大于base+发送的报文的长度</span>        <span class="hljs-comment">//缓冲队列第一个元素的序号</span>        <span class="hljs-keyword">uint64_t</span> copy_seg_seqno = <span class="hljs-built_in">unwrap</span>(_segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">header</span>().seqno, _isn, base);        <span class="hljs-comment">//缓冲队列第一个元素的长度</span>        <span class="hljs-keyword">uint64_t</span> copy_seg_len = _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();        <span class="hljs-comment">//序号加长度小于等于确认号，就一个一个删除</span>        <span class="hljs-keyword">while</span> (copy_seg_seqno + copy_seg_len &lt;= ackno_64) &#123;            base += _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();            _segments_out_cache.<span class="hljs-built_in">pop</span>();            <span class="hljs-keyword">if</span> (_segments_out_cache.<span class="hljs-built_in">empty</span>()) <span class="hljs-keyword">break</span>;            copy_seg_seqno = <span class="hljs-built_in">unwrap</span>(_segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">header</span>().seqno, _isn, base);            copy_seg_len = _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();        &#125;        _time_out = _initial_retransmission_timeout;        _total_time = <span class="hljs-number">0</span>;        _consecutive_remission = <span class="hljs-number">0</span>;    &#125;</code></pre></div><p>4.对于流量控制，我们需要用接收方的窗口大小来维护发送方的窗口大小，如果说<code>_next_seqno-base&gt;=window_size</code>，那么此时我们肯定是不能发送报文的，在这种情况下，说明我们发送出去但还没有确认的报文的长度大于接收窗口的长度，这种情况下我们如果再次发送报文可能会导致接收方接收出现问题。代码如下所示：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-keyword">if</span>(_next_seqno-base&gt;=window_size)&#123;        curr_window_size=<span class="hljs-number">0</span>;        <span class="hljs-keyword">return</span>;&#125;</code></pre></div><p>这里的curr_window_size就是活动窗口中待发送的长度，这个长度是根据接收窗口维护的。</p><p><code>tcp_sender.hh</code></p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">ifndef</span> SPONGE_LIBSPONGE_TCP_SENDER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SPONGE_LIBSPONGE_TCP_SENDER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;byte_stream.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_config.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_segment.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;wrapping_integers.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;functional&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;queue&gt;</span></span><span class="hljs-comment">//! \brief The &quot;sender&quot; part of a TCP implementation.</span><span class="hljs-comment">//! Accepts a ByteStream, divides it up into segments and sends the</span><span class="hljs-comment">//! segments, keeps track of which segments are still in-flight,</span><span class="hljs-comment">//! maintains the Retransmission Timer, and retransmits in-flight</span><span class="hljs-comment">//! segments if the retransmission timer expires.</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TCPSender</span> &#123;</span>  <span class="hljs-keyword">private</span>:    <span class="hljs-comment">//! our initial sequence number, the number for our SYN.</span>    WrappingInt32 _isn;    <span class="hljs-keyword">uint64_t</span> base&#123;<span class="hljs-number">0</span>&#125;;    <span class="hljs-comment">//! outbound queue of segments that the TCPSender wants sent</span>    std::queue&lt;TCPSegment&gt; _segments_out&#123;&#125;;    std::queue&lt;TCPSegment&gt; _segments_out_cache&#123;&#125;;    <span class="hljs-comment">//! retransmission timer for the connection</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> _initial_retransmission_timeout;    <span class="hljs-comment">//! outgoing stream of bytes that have not yet been sent</span>    ByteStream _stream;    <span class="hljs-comment">//! the (absolute) sequence number for the next byte to be sent</span>    <span class="hljs-keyword">uint64_t</span> _next_seqno&#123;<span class="hljs-number">0</span>&#125;;    <span class="hljs-keyword">uint16_t</span> curr_window_size;    <span class="hljs-keyword">bool</span> fin_flag;    <span class="hljs-keyword">size_t</span> _total_time;    <span class="hljs-keyword">bool</span> time_waiting;    <span class="hljs-keyword">int</span> _consecutive_remission;    <span class="hljs-keyword">size_t</span> _time_out;    <span class="hljs-keyword">bool</span> window_zero;  <span class="hljs-keyword">public</span>:    <span class="hljs-comment">//! Initialize a TCPSender</span>    <span class="hljs-built_in">TCPSender</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity = TCPConfig::DEFAULT_CAPACITY,              <span class="hljs-keyword">const</span> <span class="hljs-keyword">uint16_t</span> retx_timeout = TCPConfig::TIMEOUT_DFLT,              <span class="hljs-keyword">const</span> std::optional&lt;WrappingInt32&gt; fixed_isn = &#123;&#125;);    <span class="hljs-comment">//! \name &quot;Input&quot; interface for the writer</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-function">ByteStream &amp;<span class="hljs-title">stream_in</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> _stream; &#125;    <span class="hljs-function"><span class="hljs-keyword">const</span> ByteStream &amp;<span class="hljs-title">stream_in</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _stream; &#125;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \name Methods that can cause the TCPSender to send a segment</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! \brief A new acknowledgment was received</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ack_received</span><span class="hljs-params">(<span class="hljs-keyword">const</span> WrappingInt32 ackno, <span class="hljs-keyword">const</span> <span class="hljs-keyword">uint16_t</span> window_size)</span></span>;    <span class="hljs-comment">//! \brief Generate an empty-payload segment (useful for creating empty ACK segments)</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">send_empty_segment</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">//! \brief create and send segments to fill as much of the window as possible</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">fill_window</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">//! \brief Notifies the TCPSender of the passage of time</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">tick</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> ms_since_last_tick)</span></span>;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \name Accessors</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! \brief How many sequence numbers are occupied by segments sent but not yet acknowledged?</span>    <span class="hljs-comment">//! \note count is in &quot;sequence space,&quot; i.e. SYN and FIN each count for one byte</span>    <span class="hljs-comment">//! (see TCPSegment::length_in_sequence_space())</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">bytes_in_flight</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \brief Number of consecutive retransmissions that have occurred in a row</span>    <span class="hljs-comment">//连续发生的重传次数</span>    <span class="hljs-function"><span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> <span class="hljs-title">consecutive_retransmissions</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \brief TCPSegments that the TCPSender has enqueued for transmission.</span>    <span class="hljs-comment">//! \note These must be dequeued and sent by the TCPConnection,</span>    <span class="hljs-comment">//! which will need to fill in the fields that are set by the TCPReceiver</span>    <span class="hljs-comment">//! (ackno and window size) before sending.</span>    <span class="hljs-function">std::queue&lt;TCPSegment&gt; &amp;<span class="hljs-title">segments_out</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> _segments_out; &#125;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \name What is the next sequence number? (used for testing)</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! \brief absolute seqno for the next byte to be sent</span>    <span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">next_seqno_absolute</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _next_seqno; &#125;    <span class="hljs-comment">//! \brief relative seqno for the next byte to be sent</span>    <span class="hljs-function">WrappingInt32 <span class="hljs-title">next_seqno</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> <span class="hljs-built_in">wrap</span>(_next_seqno, _isn); &#125;    <span class="hljs-comment">//!@&#125;</span>&#125;;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span>  <span class="hljs-comment">// SPONGE_LIBSPONGE_TCP_SENDER_HH</span></span></code></pre></div><p><code>tcp_sender.cc</code></p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_sender.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;iostream&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_config.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;random&gt;</span></span><span class="hljs-comment">// Dummy implementation of a TCP sender</span><span class="hljs-comment">// For Lab 3, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab3`.</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<span class="hljs-comment">//! \param[in] capacity the capacity of the outgoing byte stream</span><span class="hljs-comment">//! \param[in] retx_timeout the initial amount of time to wait before retransmitting the oldest outstanding segment</span><span class="hljs-comment">//! \param[in] fixed_isn the Initial Sequence Number to use, if set (otherwise uses a random ISN)</span>TCPSender::<span class="hljs-built_in">TCPSender</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity, <span class="hljs-keyword">const</span> <span class="hljs-keyword">uint16_t</span> retx_timeout, <span class="hljs-keyword">const</span> std::optional&lt;WrappingInt32&gt; fixed_isn)    : _isn(fixed_isn.<span class="hljs-built_in">value_or</span>(WrappingInt32&#123;<span class="hljs-built_in">random_device</span>()()&#125;))    ,<span class="hljs-built_in">base</span>(<span class="hljs-number">0</span>)    , _initial_retransmission_timeout&#123;retx_timeout&#125;    , _stream(capacity)    , <span class="hljs-built_in">curr_window_size</span>(<span class="hljs-number">1</span>)    , <span class="hljs-built_in">fin_flag</span>(<span class="hljs-literal">false</span>)    , _total_time(<span class="hljs-number">0</span>)    , <span class="hljs-built_in">time_waiting</span>(<span class="hljs-literal">false</span>)    , _consecutive_remission(<span class="hljs-number">0</span>)    , _time_out(<span class="hljs-number">0</span>)    , <span class="hljs-built_in">window_zero</span>(<span class="hljs-literal">false</span>)    &#123;&#125;<span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">TCPSender::bytes_in_flight</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">return</span> _next_seqno-base;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPSender::fill_window</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">if</span>(curr_window_size==<span class="hljs-number">0</span>||fin_flag)&#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">if</span>(_next_seqno==<span class="hljs-number">0</span>)&#123;        TCPSegment seg;        seg.<span class="hljs-built_in">header</span>().seqno=_isn;        seg.<span class="hljs-built_in">header</span>().syn= <span class="hljs-literal">true</span>;        _next_seqno=<span class="hljs-number">1</span>;        curr_window_size--;        _segments_out.<span class="hljs-built_in">push</span>(seg);        _segments_out_cache.<span class="hljs-built_in">push</span>(seg);    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(_stream.<span class="hljs-built_in">eof</span>())&#123;        fin_flag=<span class="hljs-literal">true</span>;        TCPSegment seg;        seg.<span class="hljs-built_in">header</span>().syn= <span class="hljs-literal">false</span>;        seg.<span class="hljs-built_in">header</span>().fin=<span class="hljs-literal">true</span>;        seg.<span class="hljs-built_in">header</span>().seqno= <span class="hljs-built_in">wrap</span>(_next_seqno,_isn);        _next_seqno++;        curr_window_size--;        _segments_out_cache.<span class="hljs-built_in">push</span>(seg);        _segments_out.<span class="hljs-built_in">push</span>(seg);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">while</span> (!_stream.<span class="hljs-built_in">buffer_empty</span>()&amp;&amp;curr_window_size&gt;<span class="hljs-number">0</span>)&#123;            <span class="hljs-keyword">uint64_t</span> lens_byte=std::<span class="hljs-built_in">min</span>(_stream.<span class="hljs-built_in">buffer_size</span>(),<span class="hljs-built_in">uint64_t</span> (curr_window_size));            lens_byte=std::<span class="hljs-built_in">min</span>(lens_byte,TCPConfig::MAX_PAYLOAD_SIZE);            TCPSegment seg;            seg.<span class="hljs-built_in">header</span>().seqno= <span class="hljs-built_in">wrap</span>(_next_seqno,_isn);            seg.<span class="hljs-built_in">header</span>().syn=<span class="hljs-literal">false</span>;            seg.<span class="hljs-built_in">payload</span>()=_stream.<span class="hljs-built_in">read</span>(lens_byte);            _next_seqno+=lens_byte;            curr_window_size-=lens_byte;            <span class="hljs-keyword">if</span>(_stream.<span class="hljs-built_in">eof</span>()&amp;&amp;curr_window_size&gt;<span class="hljs-number">0</span>)&#123;                seg.<span class="hljs-built_in">header</span>().fin= <span class="hljs-literal">true</span>;                fin_flag= <span class="hljs-literal">true</span>;                curr_window_size--;                _next_seqno++;            &#125;            _segments_out.<span class="hljs-built_in">push</span>(seg);            _segments_out_cache.<span class="hljs-built_in">push</span>(seg);            <span class="hljs-keyword">if</span>(fin_flag)&#123;                <span class="hljs-keyword">break</span>;            &#125;        &#125;    &#125;    <span class="hljs-comment">//开始计时</span>    <span class="hljs-keyword">if</span>(!time_waiting)&#123;        _time_out=_initial_retransmission_timeout;        time_waiting=<span class="hljs-literal">true</span>;        _total_time=<span class="hljs-number">0</span>;    &#125;&#125;<span class="hljs-comment">//! \param ackno The remote receiver&#x27;s ackno (acknowledgment number)</span><span class="hljs-comment">//! \param window_size The remote receiver&#x27;s advertised window size</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPSender::ack_received</span><span class="hljs-params">(<span class="hljs-keyword">const</span> WrappingInt32 ackno, <span class="hljs-keyword">const</span> <span class="hljs-keyword">uint16_t</span> window_size)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(ackno, window_size);    <span class="hljs-keyword">uint64_t</span> ackno_64= <span class="hljs-built_in">unwrap</span>(ackno,_isn,base);    <span class="hljs-keyword">if</span>(base==<span class="hljs-number">0</span>&amp;&amp;ackno_64==<span class="hljs-number">1</span>)&#123;        base=<span class="hljs-number">1</span>;        _segments_out_cache.<span class="hljs-built_in">pop</span>();        _consecutive_remission=<span class="hljs-number">0</span>;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!_segments_out_cache.<span class="hljs-built_in">empty</span>() &amp;&amp; ackno_64 &gt;= base + _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>()) &#123;        <span class="hljs-comment">//缓冲队列第一个元素的序号</span>        <span class="hljs-keyword">uint64_t</span> copy_seg_seqno = <span class="hljs-built_in">unwrap</span>(_segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">header</span>().seqno, _isn, base);        <span class="hljs-comment">//缓冲队列第一个元素的长度</span>        <span class="hljs-keyword">uint64_t</span> copy_seg_len = _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();        <span class="hljs-comment">//序号加长度小于等于确认号，就一个一个删除</span>        <span class="hljs-keyword">while</span> (copy_seg_seqno + copy_seg_len &lt;= ackno_64) &#123;            base += _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();            _segments_out_cache.<span class="hljs-built_in">pop</span>();            <span class="hljs-keyword">if</span> (_segments_out_cache.<span class="hljs-built_in">empty</span>()) <span class="hljs-keyword">break</span>;            copy_seg_seqno = <span class="hljs-built_in">unwrap</span>(_segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">header</span>().seqno, _isn, base);            copy_seg_len = _segments_out_cache.<span class="hljs-built_in">front</span>().<span class="hljs-built_in">length_in_sequence_space</span>();        &#125;        _time_out = _initial_retransmission_timeout;        _total_time = <span class="hljs-number">0</span>;        _consecutive_remission = <span class="hljs-number">0</span>;    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(ackno_64==_next_seqno&amp;&amp;fin_flag)&#123;        base=ackno_64;        _segments_out_cache.<span class="hljs-built_in">pop</span>();    &#125;    <span class="hljs-keyword">if</span>(_next_seqno-base==<span class="hljs-number">0</span>)&#123;        time_waiting= <span class="hljs-literal">false</span>;    &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(_next_seqno-base&gt;=window_size)&#123;        curr_window_size=<span class="hljs-number">0</span>;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">if</span>(window_size==<span class="hljs-number">0</span>)&#123;        curr_window_size=<span class="hljs-number">1</span>;        window_zero= <span class="hljs-literal">true</span>;    &#125;<span class="hljs-keyword">else</span>&#123;        curr_window_size=window_size;        window_zero= <span class="hljs-literal">false</span>;        _consecutive_remission=<span class="hljs-number">0</span>;    &#125;    <span class="hljs-built_in">fill_window</span>();&#125;<span class="hljs-comment">//! \param[in] ms_since_last_tick the number of milliseconds since the last call to this method</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPSender::tick</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> ms_since_last_tick)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(ms_since_last_tick);    _total_time+=ms_since_last_tick;    <span class="hljs-comment">//如果缓冲队列不为空，停等时间在运行，且总时间大于超时</span>    <span class="hljs-keyword">if</span>(!_segments_out_cache.<span class="hljs-built_in">empty</span>()&amp;&amp;time_waiting&amp;&amp;_total_time&gt;=_time_out)&#123;        _segments_out.<span class="hljs-built_in">push</span>(_segments_out_cache.<span class="hljs-built_in">front</span>());        <span class="hljs-comment">//重传次数增加</span>        _consecutive_remission++;        <span class="hljs-keyword">if</span>(!window_zero)&#123;            _time_out*=<span class="hljs-number">2</span>;        &#125;        _total_time=<span class="hljs-number">0</span>;    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> <span class="hljs-title">TCPSender::consecutive_retransmissions</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _consecutive_remission; &#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPSender::send_empty_segment</span><span class="hljs-params">()</span> </span>&#123;    TCPSegment seg;    seg.<span class="hljs-built_in">header</span>().seqno= <span class="hljs-built_in">wrap</span>(_next_seqno,_isn);    seg.<span class="hljs-built_in">payload</span>()=&#123;&#125;;    _segments_out.<span class="hljs-built_in">push</span>(seg);&#125;</code></pre></div><p>做到这里我们就可以理解几个面经的问题了：</p><p>Q1:<strong><em>TCP建立连接可以两次握手吗？为什么？</em></strong></p><p>A1:不可以。有两个原因：</p><ol><li><p>已经失效的连接请求报文段又传到了服务器端。</p><blockquote><p>client发出的第一个连接请求报文并没有丢失，而是在某个网络结点长时间地滞留了，以致于延误到连接释放以后的某个时间才到达server，本来这是一个已经失效的报文段，但server收到此失效的连接请求报文段后，还是向client发出确认报文段，同意建立连接。假设不采用三次握手，那么只要server发出确认，新的连接就建立了，由于此时client不会与server进行通讯，那么server的资源就白白浪费了。</p></blockquote></li><li><p>两次握手无法保证client端正确接收第二次握手的报文，无法保证client和server之间成功互换了初始序列号。</p></li></ol><p>Q2:<strong><em>为什么TCP连接的时候是3次，关闭的时候却是4次？</em></strong></p><p>A2:因为关闭的时候必须要确保通信双方都能通知对方释放连接，假设客户端发送完数据向服务端发送释放连接的请求，客户端并不知道，服务端是否已经发送完数据，所以此次断开的是客户端到服务端的单项连接，服务端返回给客户端确认报文后，服务端还能继续单向给客户端发送数据。也就是说第二次挥手就是服务端向客户端发送确认报文，这之后服务端还能单向给客户端发送数据。当服务端发送完数据后还需要向客户端发送释放连接请求，客户端返回确认报文，TCP才彻底关闭，四次挥手代表的是客户端和服务器端分别向对方发送释放连接的报文和发送确认报文，一共需要四次。</p><p>这里我们来总结一下滑动窗口的实现：</p><p>首先在发送方，我们定义了两个成员变量：<code>base</code>和<code>next_seqno</code>，其中<code>nextseqno-base==byte_in_fly</code>，当我们在<code>ack_res</code>函数中收到返回的<code>segment</code>的<code>ackno</code>和<code>window</code>的时候，我们根据<code>ackno</code>来确定<code>base</code>的大小，将<code>ackno</code>-原<code>base</code>的这些数据出队，注意，这个时候如果<code>nextseqno-base&gt;window</code>，那就说明我们没必要去发送<code>segment</code>，直接返回。然后我们把<code>window</code>赋值给<code>curr_window</code>，这个时候呢，我们将<code>next_seqno+=curr_window</code>，发送长度为<code>curr_window</code>的数据过去，那么这个时候<code>base</code>和原<code>next_seqno</code>的数据我们就交给<code>tick</code>也就是超时重传来处理。</p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Stanford CS144 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Stanford CS144 Lab 2: TCP receiver</title>
    <link href="/2021/11/20/Stanford-CS144-Lab-2-TCP-Receiver/"/>
    <url>/2021/11/20/Stanford-CS144-Lab-2-TCP-Receiver/</url>
    
    <content type="html"><![CDATA[<p>CS144 这个实验并不是“在 UDP 之上封装 TCP”，而是让我们在用户态按 TCP 语义实现发送端与接收端组件（例如 sequence number、ACK、reassembly、window 等），以理解 TCP 协议机制本身。</p><h3 id="Translating-between-64-bit-indexes-and-32-bit-seqnos"><a href="#Translating-between-64-bit-indexes-and-32-bit-seqnos" class="headerlink" title="Translating between 64-bit indexes and 32-bit seqnos"></a>Translating between 64-bit indexes and 32-bit seqnos</h3><p>《自顶向下》中，在讲到GBN协议时有这样一段话可能大家都忽略了，原文如下：</p><blockquote><p>在实践中，一个分组的序号承载在分组首部的一个固定长度的字段中。如果分组序号字段的比特数是k,则该序号范围是[0,2^k]。 在一个有限的序号范围内，所有涉及序号的运算必须使用模2^k 运算。（即序号空间可被看作是一个长度为2^k 的环，其中序号2^k-1紧挨着0）。</p></blockquote><p>序号的大小始终限制在2^k 之内，事实上，我们往往取k为32,这个序号被称为相对序号，与之相对应的还有绝对序号，即没有被取模2^k 序号，其大小范围为2^64,在这个任 务中我们要完成相对序号和绝对序号的转换。</p><p>值得注意的是，相对序号的起始值是一个随机数_isn，这样取的好处是增加报文复杂程度，而绝对序号的起始值为0,具体如下图所示：</p><p>首先是绝对序号转化为相对序号，根据位运算和模运算，我们只需要将绝对序号n加上相对序号_isn,赋值给一个32位数字。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function">WrappingInt32 <span class="hljs-title">wrap</span><span class="hljs-params">(<span class="hljs-keyword">uint64_t</span> n, WrappingInt32 isn)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(n, isn);    <span class="hljs-function">WrappingInt32 <span class="hljs-title">res</span><span class="hljs-params">(n+isn.raw_value())</span></span>;    <span class="hljs-keyword">return</span> res;&#125;</code></pre></div><p>接着是相对序号转化为绝对序号，其中n是待转化的相对序号，_isn是相对序号的起始值，checkpoint是参考值，也就是我们要找到离checkpoint最近的绝对序号。那么如何找到相应的绝对序号呢，代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">unwrap</span><span class="hljs-params">(WrappingInt32 n, WrappingInt32 isn, <span class="hljs-keyword">uint64_t</span> checkpoint)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(n, isn, checkpoint);    <span class="hljs-keyword">uint64_t</span> temp=n.<span class="hljs-built_in">raw_value</span>()-isn.<span class="hljs-built_in">raw_value</span>();    <span class="hljs-keyword">if</span>(checkpoint==<span class="hljs-number">0</span>)&#123;        <span class="hljs-keyword">return</span> temp;    &#125;    <span class="hljs-keyword">uint32_t</span> div=checkpoint/(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    <span class="hljs-keyword">uint32_t</span> res=checkpoint%(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    <span class="hljs-keyword">if</span> (res&lt;=temp) &#123;        temp=(checkpoint-temp-(div<span class="hljs-number">-1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>))&lt;(temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>)-checkpoint)?temp+(div<span class="hljs-number">-1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>):temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    &#125;<span class="hljs-keyword">else</span>&#123;        temp=(checkpoint-temp-div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>))&lt;(temp+(div+<span class="hljs-number">1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>)-checkpoint)?temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>):temp+(div+<span class="hljs-number">1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    &#125;    <span class="hljs-keyword">return</span> temp;&#125;</code></pre></div><h3 id="Implementing-the-TCP-receiver"><a href="#Implementing-the-TCP-receiver" class="headerlink" title="Implementing the TCP receiver"></a>Implementing the TCP receiver</h3><p>这个任务的主要目标是实现tcp的接收部分，在实现receiver之前，我们首先要熟悉segment类，具体可以参考tcp_segment.cc中的内容。TCP头部的一些常用字段（这也是我们实验中要用到的）如下：</p><ul><li>序号：seqno，占32位，用来标识从发送端到接收端的字节流；</li><li>确认号：ackno，占32位，只有 ACK 标志位为 1 时才有效；它表示“期望收到的下一个序号”，并不总是简单等于 <code>seqno+1</code>。</li><li>标志位：<ul><li>SYN：发起一个连接；</li><li>FIN：释放一个连接；</li><li>ACK：确认序号有效。</li></ul></li></ul><p>在后面的实验中我们要用到rst标志位。</p><p>1.在这个任务中我们要用到三次握手的知识，为了更好的解释实现过程，我们用以下图示解释(在下一个实验sender中也会用到该图）。</p><p>我们可以先从“被动接收 SYN 的一端”来理解 receiver：如果收到带 <code>SYN</code> 的 segment，就记录初始序号并进入已同步状态；如果还没看到 <code>SYN</code> 就收到普通数据段，则直接忽略。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(seg.<span class="hljs-built_in">header</span>().syn)&#123;        syn_flag= <span class="hljs-literal">true</span>;    <span class="hljs-comment">//窗口的左端    </span>    ISN=seg.<span class="hljs-built_in">header</span>().seqno;&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(!syn_flag)&#123;        <span class="hljs-keyword">return</span>;&#125;</code></pre></div><p>2.为了调用流重组器函数，我们要计算 <code>data</code>、<code>index</code> 和 <code>eof</code>。其中 <code>data</code> 就是 <code>segment.payload()</code>，<code>index</code> 是 payload 对应的绝对序号起点。由于 <code>SYN/FIN</code> 都会各自占用一个序号，所以在非 <code>SYN</code> 报文上要对序号做相应偏移；<code>eof</code> 则由 <code>FIN</code> 决定。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPReceiver::segment_received</span><span class="hljs-params">(<span class="hljs-keyword">const</span> TCPSegment &amp;seg)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(seg);    <span class="hljs-comment">//代表第一个传过来的seg</span>    <span class="hljs-keyword">if</span>(seg.<span class="hljs-built_in">header</span>().syn)&#123;        syn_flag= <span class="hljs-literal">true</span>;        <span class="hljs-comment">//窗口的左端</span>        ISN=seg.<span class="hljs-built_in">header</span>().seqno;    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(!syn_flag)&#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">uint64_t</span> received_lens=_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>();    <span class="hljs-keyword">size_t</span> index= <span class="hljs-built_in">unwrap</span>(seg.<span class="hljs-built_in">header</span>().seqno,ISN,received_lens);    <span class="hljs-keyword">if</span>(!seg.<span class="hljs-built_in">header</span>().syn)&#123;        index--;    &#125;    <span class="hljs-comment">//进行重组</span>    _reassembler.<span class="hljs-built_in">push_substring</span>(seg.<span class="hljs-built_in">payload</span>().<span class="hljs-built_in">copy</span>(),index,seg.<span class="hljs-built_in">header</span>().fin);&#125;</code></pre></div><p>这样我们就完成了接收操作。然后我们要返回确认报文。</p><p>对于要返回的ackno，如果还未建立连接，我们就返回nullptr，否则，我们分为两种情况，一种是全部数据传输未完成，我们返回写入byte_stream的数据的序号加1，这样一种是全部数据传输完成之后，end_input为true,这个时候我们的返回值应该为写入byte_streamde的数据的序号加2（因为fin标志位要占1个位置）。</p><p>发送方没有收到ACK分为两种情况，一种是发送方发送的数据报丢失，因此接收方并没有向发送方返回ACK；另一种是接收方收到了发送方的数据报，但是返回的ACK丢失了，这种情况发送方重传后，接收方会直接丢弃发送方重传的数据报，然后再次发送ACK响应报文。</p><p>在代码中我们并没有实现这种情况，因为在<code>_reassembler.push_substring</code>中有解决这个问题的相关实现，如果说非要做一个这样的检验是否是重传的数据报的函数，可以增加一个buffer队列，用来缓存收到的segment的header，然后将收到的segment与其进行对比，但是lab并没有要求我们实现，所以没有做。</p><p>代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function">optional&lt;WrappingInt32&gt; <span class="hljs-title">TCPReceiver::ackno</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">if</span>(!syn_flag)&#123;        <span class="hljs-keyword">return</span> std::nullopt;    &#125;<span class="hljs-keyword">else</span>&#123;        <span class="hljs-keyword">if</span>(_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">input_ended</span>())&#123;            <span class="hljs-keyword">return</span> ISN+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+<span class="hljs-number">2</span>;        &#125;<span class="hljs-keyword">else</span>&#123;            <span class="hljs-keyword">return</span> ISN+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+<span class="hljs-number">1</span>;        &#125;    &#125;&#125;</code></pre></div><p>至于滑动窗口大小，就是我们之前提到过的重组器大小，即capacity减去已经写入byte_stream但还没有读取的数据的大小。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">TCPReceiver::window_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">return</span> _capacity-_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_read</span>();&#125;</code></pre></div><p><code>wrapping_integers.cc</code></p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;wrapping_integers.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;cmath&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;iostream&gt;</span></span><span class="hljs-comment">// Dummy implementation of a 32-bit wrapping integer</span><span class="hljs-comment">// For Lab 2, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab2`.</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<span class="hljs-comment">//! Transform an &quot;absolute&quot; 64-bit sequence number (zero-indexed) into a WrappingInt32</span><span class="hljs-comment">//! \param n The input absolute 64-bit sequence number</span><span class="hljs-comment">//! \param isn The initial sequence number</span><span class="hljs-function">WrappingInt32 <span class="hljs-title">wrap</span><span class="hljs-params">(<span class="hljs-keyword">uint64_t</span> n, WrappingInt32 isn)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(n, isn);    <span class="hljs-function">WrappingInt32 <span class="hljs-title">res</span><span class="hljs-params">(n+isn.raw_value())</span></span>;    <span class="hljs-keyword">return</span> res;&#125;<span class="hljs-comment">//! Transform a WrappingInt32 into an &quot;absolute&quot; 64-bit sequence number (zero-indexed)</span><span class="hljs-comment">//! \param n The relative sequence number</span><span class="hljs-comment">//! \param isn The initial sequence number</span><span class="hljs-comment">//! \param checkpoint A recent absolute 64-bit sequence number</span><span class="hljs-comment">//! \returns the 64-bit sequence number that wraps to `n` and is closest to `checkpoint`</span><span class="hljs-comment">//!</span><span class="hljs-comment">//! \note Each of the two streams of the TCP connection has its own ISN. One stream</span><span class="hljs-comment">//! runs from the local TCPSender to the remote TCPReceiver and has one ISN,</span><span class="hljs-comment">//! and the other stream runs from the remote TCPSender to the local TCPReceiver and</span><span class="hljs-comment">//! has a different ISN.</span><span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">unwrap</span><span class="hljs-params">(WrappingInt32 n, WrappingInt32 isn, <span class="hljs-keyword">uint64_t</span> checkpoint)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(n, isn, checkpoint);    <span class="hljs-keyword">uint64_t</span> temp=n.<span class="hljs-built_in">raw_value</span>()-isn.<span class="hljs-built_in">raw_value</span>();    <span class="hljs-keyword">if</span>(checkpoint==<span class="hljs-number">0</span>)&#123;        <span class="hljs-keyword">return</span> temp;    &#125;    <span class="hljs-keyword">uint32_t</span> div=checkpoint/(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    <span class="hljs-keyword">uint32_t</span> res=checkpoint%(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    <span class="hljs-keyword">if</span> (res&lt;=temp) &#123;        temp=(checkpoint-temp-(div<span class="hljs-number">-1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>))&lt;(temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>)-checkpoint)?temp+(div<span class="hljs-number">-1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>):temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    &#125;<span class="hljs-keyword">else</span>&#123;        temp=(checkpoint-temp-div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>))&lt;(temp+(div+<span class="hljs-number">1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>)-checkpoint)?temp+div*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>):temp+(div+<span class="hljs-number">1</span>)*(<span class="hljs-number">1ul</span>&lt;&lt;<span class="hljs-number">32</span>);    &#125;    <span class="hljs-keyword">return</span> temp;&#125;</code></pre></div><p><code>wrapping_integers.hh</code></p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">ifndef</span> SPONGE_LIBSPONGE_WRAPPING_INTEGERS_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SPONGE_LIBSPONGE_WRAPPING_INTEGERS_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;cstdint&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;ostream&gt;</span></span><span class="hljs-comment">//! \brief A 32-bit integer, expressed relative to an arbitrary initial sequence number (ISN)</span><span class="hljs-comment">//! \note This is used to express TCP sequence numbers (seqno) and acknowledgment numbers (ackno)</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">WrappingInt32</span> &#123;</span>  <span class="hljs-keyword">private</span>:    <span class="hljs-comment">//原始32位储存整数</span>    <span class="hljs-keyword">uint32_t</span> _raw_value;  <span class="hljs-comment">//!&lt; The raw 32-bit stored integer</span>  <span class="hljs-keyword">public</span>:    <span class="hljs-comment">//! Construct from a raw 32-bit unsigned integer</span>    <span class="hljs-comment">//显式构造</span>    <span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">WrappingInt32</span><span class="hljs-params">(<span class="hljs-keyword">uint32_t</span> raw_value)</span> : _raw_value(raw_value) &#123;</span>&#125;    <span class="hljs-comment">//访问原始存储值</span>    <span class="hljs-function"><span class="hljs-keyword">uint32_t</span> <span class="hljs-title">raw_value</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _raw_value; &#125;  <span class="hljs-comment">//!&lt; Access raw stored value</span>&#125;;<span class="hljs-comment">//! Transform a 64-bit absolute sequence number (zero-indexed) into a 32-bit relative sequence number</span><span class="hljs-comment">//! \param n the absolute sequence number</span><span class="hljs-comment">//! \param isn the initial sequence number</span><span class="hljs-comment">//! \returns the relative sequence number</span><span class="hljs-function">WrappingInt32 <span class="hljs-title">wrap</span><span class="hljs-params">(<span class="hljs-keyword">uint64_t</span> n, WrappingInt32 isn)</span></span>;<span class="hljs-comment">//! Transform a 32-bit relative sequence number into a 64-bit absolute sequence number (zero-indexed)</span><span class="hljs-comment">//! \param n The relative sequence number</span><span class="hljs-comment">//! \param isn The initial sequence number</span><span class="hljs-comment">//! \param checkpoint A recent absolute sequence number</span><span class="hljs-comment">//! \returns the absolute sequence number that wraps to `n` and is closest to `checkpoint`</span><span class="hljs-comment">//!</span><span class="hljs-comment">//! \note Each of the two streams of the TCP connection has its own ISN. One stream</span><span class="hljs-comment">//! runs from the local TCPSender to the remote TCPReceiver and has one ISN,</span><span class="hljs-comment">//! and the other stream runs from the remote TCPSender to the local TCPReceiver and</span><span class="hljs-comment">//! has a different ISN.</span><span class="hljs-function"><span class="hljs-keyword">uint64_t</span> <span class="hljs-title">unwrap</span><span class="hljs-params">(WrappingInt32 n, WrappingInt32 isn, <span class="hljs-keyword">uint64_t</span> checkpoint)</span></span>;<span class="hljs-comment">//! \name Helper functions</span><span class="hljs-comment">//!@&#123;</span><span class="hljs-comment">//! \brief The offset of `a` relative to `b`</span><span class="hljs-comment">//! \param b the starting point</span><span class="hljs-comment">//! \param a the ending point</span><span class="hljs-comment">//! \returns the number of increments needed to get from `b` to `a`,</span><span class="hljs-comment">//! negative if the number of decrements needed is less than or equal to</span><span class="hljs-comment">//! the number of increments</span><span class="hljs-keyword">inline</span> <span class="hljs-keyword">int32_t</span> <span class="hljs-keyword">operator</span>-(WrappingInt32 a, WrappingInt32 b) &#123; <span class="hljs-keyword">return</span> a.<span class="hljs-built_in">raw_value</span>() - b.<span class="hljs-built_in">raw_value</span>(); &#125;<span class="hljs-comment">//! \brief Whether the two integers are equal.</span><span class="hljs-keyword">inline</span> <span class="hljs-keyword">bool</span> <span class="hljs-keyword">operator</span>==(WrappingInt32 a, WrappingInt32 b) &#123; <span class="hljs-keyword">return</span> a.<span class="hljs-built_in">raw_value</span>() == b.<span class="hljs-built_in">raw_value</span>(); &#125;<span class="hljs-comment">//! \brief Whether the two integers are not equal.</span><span class="hljs-keyword">inline</span> <span class="hljs-keyword">bool</span> <span class="hljs-keyword">operator</span>!=(WrappingInt32 a, WrappingInt32 b) &#123; <span class="hljs-keyword">return</span> !(a == b); &#125;<span class="hljs-comment">//! \brief Serializes the wrapping integer, `a`.</span><span class="hljs-keyword">inline</span> std::ostream &amp;<span class="hljs-keyword">operator</span>&lt;&lt;(std::ostream &amp;os, WrappingInt32 a) &#123; <span class="hljs-keyword">return</span> os &lt;&lt; a.<span class="hljs-built_in">raw_value</span>(); &#125;<span class="hljs-comment">//! \brief The point `b` steps past `a`.</span><span class="hljs-keyword">inline</span> WrappingInt32 <span class="hljs-keyword">operator</span>+(WrappingInt32 a, <span class="hljs-keyword">uint32_t</span> b) &#123; <span class="hljs-keyword">return</span> WrappingInt32&#123;a.<span class="hljs-built_in">raw_value</span>() + b&#125;; &#125;<span class="hljs-comment">//! \brief The point `b` steps before `a`.</span><span class="hljs-keyword">inline</span> WrappingInt32 <span class="hljs-keyword">operator</span>-(WrappingInt32 a, <span class="hljs-keyword">uint32_t</span> b) &#123; <span class="hljs-keyword">return</span> a + -b; &#125;<span class="hljs-comment">//!@&#125;</span><span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span>  <span class="hljs-comment">// SPONGE_LIBSPONGE_WRAPPING_INTEGERS_HH</span></span></code></pre></div><p><code>tcp_receiver.cc</code></p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_receiver.hh&quot;</span></span><span class="hljs-comment">// Dummy implementation of a TCP receiver</span><span class="hljs-comment">// For Lab 2, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab2`.</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">TCPReceiver::segment_received</span><span class="hljs-params">(<span class="hljs-keyword">const</span> TCPSegment &amp;seg)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(seg);    <span class="hljs-comment">//代表第一个传过来的seg</span>    <span class="hljs-keyword">if</span>(seg.<span class="hljs-built_in">header</span>().syn)&#123;        syn_flag= <span class="hljs-literal">true</span>;        <span class="hljs-comment">//窗口的左端</span>        ISN=seg.<span class="hljs-built_in">header</span>().seqno;    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(!syn_flag)&#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">uint64_t</span> received_lens=_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>();    <span class="hljs-keyword">size_t</span> index= <span class="hljs-built_in">unwrap</span>(seg.<span class="hljs-built_in">header</span>().seqno,ISN,received_lens);    <span class="hljs-keyword">if</span>(!seg.<span class="hljs-built_in">header</span>().syn)&#123;        index--;    &#125;    _reassembler.<span class="hljs-built_in">push_substring</span>(seg.<span class="hljs-built_in">payload</span>().<span class="hljs-built_in">copy</span>(),index,seg.<span class="hljs-built_in">header</span>().fin);&#125;<span class="hljs-function">optional&lt;WrappingInt32&gt; <span class="hljs-title">TCPReceiver::ackno</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">if</span>(!syn_flag)&#123;        <span class="hljs-keyword">return</span> std::nullopt;    &#125;<span class="hljs-keyword">else</span>&#123;        <span class="hljs-keyword">if</span>(_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">input_ended</span>())&#123;            <span class="hljs-keyword">return</span> ISN+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+<span class="hljs-number">2</span>;        &#125;<span class="hljs-keyword">else</span>&#123;            <span class="hljs-keyword">return</span> ISN+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+<span class="hljs-number">1</span>;<span class="hljs-comment">//返回当前接收窗口的初始序号</span>        &#125;    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">TCPReceiver::window_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">return</span> _capacity-_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_written</span>()+_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">bytes_read</span>();&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">TCPReceiver::recv_fin</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-keyword">if</span>(_reassembler.<span class="hljs-built_in">stream_out</span>().<span class="hljs-built_in">input_ended</span>())&#123;        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;    &#125;<span class="hljs-keyword">else</span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;    &#125;</code></pre></div><p><code>tcp_receiver.hh</code></p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-meta-keyword">ifndef</span> SPONGE_LIBSPONGE_TCP_RECEIVER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SPONGE_LIBSPONGE_TCP_RECEIVER_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;byte_stream.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;stream_reassembler.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;tcp_segment.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;wrapping_integers.hh&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;optional&gt;</span></span><span class="hljs-comment">//! \brief The &quot;receiver&quot; part of a TCP implementation.</span><span class="hljs-comment">//! Receives and reassembles segments into a ByteStream, and computes</span><span class="hljs-comment">//! the acknowledgment number and window size to advertise back to the</span><span class="hljs-comment">//! remote TCPSender.</span><span class="hljs-comment">//接收重组segments为 ByteStream，并计算确认号和窗口大小以通告回远程 TCPSender。</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TCPReceiver</span> &#123;</span>    <span class="hljs-comment">//! Our data structure for re-assembling bytes.</span>    <span class="hljs-comment">//我们用于重新组装字节的数据结构。</span>    StreamReassembler _reassembler;    <span class="hljs-comment">//! The maximum number of bytes we&#x27;ll store.</span>    <span class="hljs-comment">//容量大小</span>    <span class="hljs-keyword">size_t</span> _capacity;    WrappingInt32 ISN;    <span class="hljs-keyword">bool</span> syn_flag;  <span class="hljs-keyword">public</span>:    <span class="hljs-comment">//! \brief Construct a TCP receiver</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! \param capacity the maximum number of bytes that the receiver will</span>    <span class="hljs-comment">//!                 store in its buffers at any give time.</span>    <span class="hljs-comment">//构造函数，构造一个 TCP 接收器，容量接收器在任何给定时间将存储在其缓冲区中的最大字节数。</span>    <span class="hljs-built_in">TCPReceiver</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity) : _reassembler(capacity), _capacity(capacity),<span class="hljs-built_in">ISN</span>(<span class="hljs-number">0</span>) ,<span class="hljs-built_in">syn_flag</span>(<span class="hljs-number">0</span>)&#123;&#125;    <span class="hljs-comment">//! \name Accessors to provide feedback to the remote TCPSender</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! \brief The ackno that should be sent to the peer</span>    <span class="hljs-comment">//! \returns empty if no SYN has been received</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! This is the beginning of the receiver&#x27;s window, or in other words, the sequence number</span>    <span class="hljs-comment">//! of the first byte in the stream that the receiver hasn&#x27;t received.</span>    <span class="hljs-comment">// 如果没有收到 SYN，则应发送给对等方的 ackno 为空</span>    <span class="hljs-comment">//这是接收器窗口的开始，否则，接收器未接收到的流中第一个字节的序列号。</span>    <span class="hljs-function">std::optional&lt;WrappingInt32&gt; <span class="hljs-title">ackno</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \brief The window size that should be sent to the peer</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! Operationally: the capacity minus the number of bytes that the</span>    <span class="hljs-comment">//! TCPReceiver is holding in its byte stream (those that have been</span>    <span class="hljs-comment">//! reassembled, but not consumed).</span>    <span class="hljs-comment">//!</span>    <span class="hljs-comment">//! Formally: the difference between (a) the sequence number of</span>    <span class="hljs-comment">//! the first byte that falls after the window (and will not be</span>    <span class="hljs-comment">//! accepted by the receiver) and (b) the sequence number of the</span>    <span class="hljs-comment">//! beginning of the window (the ackno).</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">window_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \brief number of bytes stored but not yet reassembled</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">unassembled_bytes</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _reassembler.<span class="hljs-built_in">unassembled_bytes</span>(); &#125;    <span class="hljs-comment">//! \brief handle an inbound segment</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">segment_received</span><span class="hljs-params">(<span class="hljs-keyword">const</span> TCPSegment &amp;seg)</span></span>;    <span class="hljs-comment">//! \name &quot;Output&quot; interface for the reader</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-function">ByteStream &amp;<span class="hljs-title">stream_out</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> _reassembler.<span class="hljs-built_in">stream_out</span>(); &#125;    <span class="hljs-function"><span class="hljs-keyword">const</span> ByteStream &amp;<span class="hljs-title">stream_out</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _reassembler.<span class="hljs-built_in">stream_out</span>(); &#125;    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">recv_fin</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//!@&#125;</span>&#125;;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span>  <span class="hljs-comment">// SPONGE_LIBSPONGE_TCP_RECEIVER_HH</span></span></code></pre></div>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Stanford CS144 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Stanford CS144 Lab 0: Networking warmup</title>
    <link href="/2021/11/20/Stanford-CS144-Lab-0-Networking-Warmup/"/>
    <url>/2021/11/20/Stanford-CS144-Lab-0-Networking-Warmup/</url>
    
    <content type="html"><![CDATA[<p>Stanford大学的CS144 lab要求我们实现一个TCP。CS144的课程我觉得可以配合《自顶向下》来学习，它是目前最好的中文的计算机网络书籍了。《自顶向下》中采用的是五层模型，当然在CS144中介绍了四层模型和七层模型。华科的计算机网络课程是根据《自顶向下》来备课的，如果有空的话，还可以听一听华科的计网mooc。既然都写到这里了，我们在前言来聊聊计算机网络模型：</p><hr><h5 id="四层网络模型"><a href="#四层网络模型" class="headerlink" title="四层网络模型"></a>四层网络模型</h5><p>链路层：定义单个链路如何传输。</p><p>网络层：选择合适的网间路由，确保数据及时到达。常见的协议有IP协议。在internet中连接局域网和广域网是路由器。</p><p>传输层：端到端的通信，负责向两台主机之间提供数据传输服务。传输层的主要协议有传输控制协议TCP和用户数据协议UDP。</p><p>应用层：通过应用进程之间的交互来完成特定的网络作用</p><p><img src="https://img-blog.csdnimg.cn/20190323002502144.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2RqbDgwNjk0MzM3MQ==,size_16,color_FFFFFF,t_70" alt=""></p><h5 id="五层模型"><a href="#五层模型" class="headerlink" title="五层模型"></a>五层模型</h5><p>五层模型把四层模型的link层分为了数据层和物理层。</p><p>数据层：数据层的作用是在物理层提供比特流服务的基础上，建立相连节点之间的数据链路，通过差错控制提供数据帧在信道上无差错的传输。</p><p>物理层：实现相邻计算机节点之间的比特流的透明传输。</p><h5 id="七层模型"><a href="#七层模型" class="headerlink" title="七层模型"></a>七层模型</h5><p>表示层和会话层实际上是和应用层一起实现的。</p><p>为什么网络协议要分层呢？</p><ul><li>层层封装，易于实现和维护；</li><li>有利于协议等标准化的制定。</li></ul><p>今天在连接网络的时候发现一个很有意思的现象，我用设置了全局代理，并用v2ray连接了代理服务器，之后我用ping命令<code>ping google.com</code>，却ping不通，原来v2ray是作用于会话层，而ping是作用于网络层，所以我们即使连接了代理服务器却不能ping通google.com。</p><p>我们常用的v2ray、shadowsocks等客户端位于会话层，只能代理应用层的应用，当然我们也可以通过一些软件去实现真正的全局代理，如：SSTap、Proxifier、tun2sock2、SocksCap等。</p><hr><p><strong><em>Fetch a web page</em></strong></p><p>该部分要求使用telnet手动获取网页，具体步骤如下：</p><p>1.键入<code>telnet cs144.keithw.org http</code>得到如下响应（http可替换为80）：</p><p>2.继续键入GET / hello HTTP / 1.1。 这告诉服务器URL的路径部分。</p><p>3.继续键入Host：<a href="https://link.zhihu.com/?target=http%3A//cs144.keithw.org">http://cs144.keithw.org</a>。这告诉服务器URL的主机部分。</p><p>4.再按一次Enter键 这告诉服务器您已经完成了HTTP请求。</p><p>实验结果如下所示：</p><p>我们知道，telnet是作用于应用层的一个应用，通常用来进行远程请求。当远程请求获取成功，我们通过<code>GET / hello HTTP / 1.1</code>获取超文本文档，这里的HTTP是超文本传输协议。这里我们再来讲讲HTTP不同版本协议的区别：</p><p>HTTP 1.0和HTTP 1.1的区别</p><ul><li><p>长连接</p><p>HTTP 1.1支持长连接和请求的流水线操作。长连接是指不在需要每次请求都重新建立一次连接，HTTP 1.0默认使用短连接，每次请求都要重新建立一次TCP连接，资源消耗较大。请求的流水线操作是指客户端在收到HTTP的响应报文之前可以先发送新的请求报文，不支持请求的流水线操作需要等到收到HTTP的响应报文后才能继续发送新的请求报文。</p></li><li><p>缓存处理</p><p>在HTTP 1.0中主要使用header中的If-Modified-Since,Expires作为缓存判断的标准，HTTP 1.1引入了Entity tag，If-Unmodified-Since, If-Match等更多可供选择的缓存头来控制缓存策略。</p></li><li><p>错误状态码</p><p>在HTTP 1.1新增了24个错误状态响应码</p></li><li><p>HOST域</p><p>在HTTP 1.0 中认为每台服务器都会绑定唯一的IP地址，所以，请求中的URL并没有传递主机名。但后来一台服务器上可能存在多个虚拟机，它们共享一个IP地址，所以HTTP 1.1中请求消息和响应消息都应该支持Host域。</p></li><li><p>带宽优化及网络连接的使用</p><p>在HTTP 1.0中会存在浪费带宽的现象，主要是因为不支持断点续传功能，客户端只是需要某个对象的一部分，服务端却将整个对象都传了过来。在HTTP 1.1中请求头引入了range头域，它支持只请求资源的某个部分，返回的状态码为206。</p></li></ul><p>HTTP 2.0的新特性</p><ul><li>新的二进制格式：HTTP 1.x的解析是基于文本，HTTP 2.0的解析采用二进制，实现方便，健壮性更好。</li><li>多路复用：每一个request对应一个id，一个连接上可以有多个request，每个连接的request可以随机混在一起，这样接收方可以根据request的id将request归属到各自不同的服务端请求里。</li><li>header压缩：在HTTP 1.x中，header携带大量信息，并且每次都需要重新发送，HTTP 2.0采用编码的方式减小了header的大小，同时通信双方各自缓存一份header fields表，避免了header的重复传输。</li><li>服务端推送：客户端在请求一个资源时，会把相关资源一起发给客户端，这样客户端就不需要再次发起请求。</li></ul><p><code>GET / hello HTTP / 1.1</code>就是我们的请求行，请求行包括请求方法，请求URL，HTTP版本协议。那么我们又不得不讲一讲请求头了，请求头包括若干个属性值，服务端据此获取客户端的信息。至于请求体，它承载多个请求参数的数据。再来说说GET和POST的区别：</p><ul><li><p>作用</p><p>GET用于获取资源，POST用于传输实体主体</p></li><li><p>参数位置</p><p>GET的参数放在URL中，POST的参数存储在实体主体中，并且GET方法提交的请求的URL中的数据做多是2048字节，POST请求没有大小限制。</p></li><li><p>安全性</p><p>GET方法因为参数放在URL中，安全性相对于POST较差一些</p></li><li><p>幂等性</p><p>GET方法是具有幂等性的，而POST方法不具有幂等性。这里幂等性指客户端连续发出多次请求，收到的结果都是一样的.</p></li></ul><p>而上述图片返回的则是响应报文。响应报文的响应行包括报文协议及版本，状态吗及状态描述。响应头也是属性值，至于响应体，则是我们请求的需要的数据。我们看到响应代码是200，我们来说说常见的状态码：</p><div class="table-container"><table><thead><tr><th style="text-align:center">状态码</th><th style="text-align:center">类别</th></tr></thead><tbody><tr><td style="text-align:center">1XX</td><td style="text-align:center">信息性状态码</td></tr><tr><td style="text-align:center">2XX</td><td style="text-align:center">成功状态码</td></tr><tr><td style="text-align:center">3XX</td><td style="text-align:center">重定向状态码</td></tr><tr><td style="text-align:center">4XX</td><td style="text-align:center">客户端错误状态码</td></tr><tr><td style="text-align:center">5XX</td><td style="text-align:center">服务端错误状态码</td></tr></tbody></table></div><p><strong><em>Writing webget</em></strong></p><p>利用TCPsocket与Host建立连接，发送请求头，达到获取网页的目的。</p><p>众所周知，请求头为</p><div class="code-wrapper"><pre><code class="hljs http"><span class="hljs-keyword">GET</span> <span class="hljs-string">path</span> <span class="hljs-meta">HTTP/1.1</span><span class="hljs-attribute">Host</span><span class="hljs-punctuation">: </span>host</code></pre></div><p>故write函数里面的字符输入为<code>sock.write(&quot;GET &quot; + path + &quot; HTTP/1.1\r\nHost: &quot; + host + &quot;\r\n\r\n\r\n&quot;);</code></p><p>webget.cc代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">get_URL</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;host, <span class="hljs-keyword">const</span> string &amp;path)</span> </span>&#123;    <span class="hljs-comment">// Your code here.</span>    <span class="hljs-comment">// You will need to connect to the &quot;http&quot; service on</span>    <span class="hljs-comment">// the computer whose name is in the &quot;host&quot; string,</span>    <span class="hljs-comment">// then request the URL path given in the &quot;path&quot; string.</span>    <span class="hljs-comment">// Then you&#x27;ll need to print out everything the server sends back,</span>    <span class="hljs-comment">// (not just one call to read() -- everything) until you reach</span>    <span class="hljs-comment">// the &quot;eof&quot; (end of file).</span>    TCPSocket sock;    sock.<span class="hljs-built_in">connect</span>(<span class="hljs-built_in">Address</span>(host, <span class="hljs-string">&quot;http&quot;</span>));    sock.<span class="hljs-built_in">write</span>(<span class="hljs-string">&quot;GET &quot;</span> + path + <span class="hljs-string">&quot; HTTP/1.1\r\nHost: &quot;</span> + host + <span class="hljs-string">&quot;\r\n\r\n\r\n&quot;</span>);    sock.<span class="hljs-built_in">shutdown</span>(SHUT_WR);    <span class="hljs-keyword">while</span>(!sock.<span class="hljs-built_in">eof</span>())        std::cout &lt;&lt; sock.<span class="hljs-built_in">read</span>();    sock.<span class="hljs-built_in">close</span>();&#125;</code></pre></div><p>实验结果：</p><p><strong><em>An in-memory reliable byte stream</em></strong></p><p>该部分要在计算机的内存中实现一个提供可靠的有序字节流的抽象。该抽象充当运输层与应用层的连接。也就是应用层从socket中读取数据的部分。其中，byte_stream的byte_read函数将byte_stream中的数据发送到应用层，byte_write函数将有序的而数据写入byte_stream。</p><p>该抽象使用deque实现。</p><p>对于write函数，我们将data中的数据写入deque，返回本次实际写入的数据长度。注意 <code>capacity</code> 代表字节流容量，写入时不能超过剩余可用空间。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::write</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;data)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(data);    <span class="hljs-comment">//取其中的较小值，data的长度，bytestream的容量和deque长度的差,保证其不超过bytestream容量</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(data.<span class="hljs-built_in">size</span>(),stream_capacity - byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-comment">//在deque的末尾插入data的数据</span>    byte_stream.<span class="hljs-built_in">insert</span>(byte_stream.<span class="hljs-built_in">end</span>(),data.<span class="hljs-built_in">begin</span>(),data.<span class="hljs-built_in">begin</span>()+lens_byte);    <span class="hljs-comment">//写入的长度增加</span>    byte_written+=lens_byte;    <span class="hljs-keyword">return</span> lens_byte;&#125;</code></pre></div><p>对于read函数，我们只需要弹出len个数据，注意是len和deque.size()的较小值。代码如下：</p><div class="code-wrapper"><pre><code class="hljs cpp"><span class="hljs-function">std::string <span class="hljs-title">ByteStream::read</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-comment">//找到要read的长度byte_stream长度的较小值，尽可能地读</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-comment">//读取数据</span>    <span class="hljs-function">std::string <span class="hljs-title">n</span><span class="hljs-params">(byte_stream.begin(),byte_stream.begin()+lens_byte)</span></span>;    <span class="hljs-comment">//将读取后的数据弹出</span>    <span class="hljs-built_in">pop_output</span>(len);    <span class="hljs-keyword">return</span> n;&#125;</code></pre></div><p>byte_stream.hh代码如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-meta-keyword">ifndef</span> SPONGE_LIBSPONGE_BYTE_STREAM_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SPONGE_LIBSPONGE_BYTE_STREAM_HH</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;string&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;queue&gt;</span></span><span class="hljs-comment">//! \brief An in-order byte stream.</span><span class="hljs-comment">//! Bytes are written on the &quot;input&quot; side and read from the &quot;output&quot;</span><span class="hljs-comment">//! side.  The byte stream is finite: the writer can end the input,</span><span class="hljs-comment">//! and then no more bytes can be written.</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ByteStream</span> &#123;</span>  <span class="hljs-keyword">private</span>:    <span class="hljs-comment">// Your code here -- add private members as necessary.</span>    <span class="hljs-keyword">size_t</span> stream_capacity;<span class="hljs-comment">//容量大小</span>    <span class="hljs-keyword">size_t</span> byte_written;<span class="hljs-comment">//写入</span>    <span class="hljs-keyword">size_t</span> byte_read;<span class="hljs-comment">//读取</span>    std::deque&lt;<span class="hljs-keyword">char</span>&gt; byte_stream;<span class="hljs-comment">//快速插入和删除</span>    <span class="hljs-keyword">bool</span> _end;<span class="hljs-comment">//结束标志</span>    <span class="hljs-comment">// Hint: This doesn&#x27;t need to be a sophisticated data structure at</span>    <span class="hljs-comment">// all, but if any of your tests are taking longer than a second,</span>    <span class="hljs-comment">// that&#x27;s a sign that you probably want to keep exploring</span>    <span class="hljs-comment">// different approaches.</span>    <span class="hljs-keyword">bool</span> _error&#123;&#125;;  <span class="hljs-comment">//!&lt; Flag indicating that the stream suffered an error.</span>  <span class="hljs-keyword">public</span>:    <span class="hljs-comment">//! Construct a stream with room for `capacity` bytes.</span>    <span class="hljs-comment">//构造函数，构造一个带有容量字节空间的流。</span>    <span class="hljs-built_in">ByteStream</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity);    <span class="hljs-comment">//! \name &quot;Input&quot; interface for the writer</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! Write a string of bytes into the stream. Write as many</span>    <span class="hljs-comment">//! as will fit, and return how many were written.</span>    <span class="hljs-comment">//! \returns the number of bytes accepted into the stream</span>    <span class="hljs-comment">//写入字节，返回字节的数量</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">write</span><span class="hljs-params">(<span class="hljs-keyword">const</span> std::string &amp;data)</span></span>;    <span class="hljs-comment">//! \returns the number of additional bytes that the stream has space for</span>    <span class="hljs-comment">//</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">remaining_capacity</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! Signal that the byte stream has reached its ending</span>    <span class="hljs-comment">//结束输入</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">end_input</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">//! Indicate that the stream suffered an error.</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">set_error</span><span class="hljs-params">()</span> </span>&#123; _error = <span class="hljs-literal">true</span>; &#125;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \name &quot;Output&quot; interface for the reader</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! Peek at next &quot;len&quot; bytes of the stream</span>    <span class="hljs-comment">//! \returns a string</span>    <span class="hljs-function">std::string <span class="hljs-title">peek_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! Remove bytes from the buffer</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">pop_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span></span>;    <span class="hljs-comment">//! Read (i.e., copy and then pop) the next &quot;len&quot; bytes of the stream</span>    <span class="hljs-comment">//! \returns a string</span>    <span class="hljs-function">std::string <span class="hljs-title">read</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span></span>;    <span class="hljs-comment">//! \returns `true` if the stream input has ended</span>    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">input_ended</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \returns `true` if the stream has suffered an error</span>    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">error</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _error; &#125;    <span class="hljs-comment">//! \returns the maximum amount that can currently be read from the stream</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">buffer_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \returns `true` if the buffer is empty</span>    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">buffer_empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! \returns `true` if the output has reached the ending</span>    <span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">eof</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//!@&#125;</span>    <span class="hljs-comment">//! \name General accounting</span>    <span class="hljs-comment">//!@&#123;</span>    <span class="hljs-comment">//! Total number of bytes written</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">bytes_written</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//! Total number of bytes popped</span>    <span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">bytes_read</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span></span>;    <span class="hljs-comment">//!@&#125;</span>&#125;;<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span>  <span class="hljs-comment">// SPONGE_LIBSPONGE_BYTE_STREAM_HH</span></span></code></pre></div><p>byte_stream.cc代码如下：</p><div class="code-wrapper"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;byte_stream.hh&quot;</span></span><span class="hljs-comment">// Dummy implementation of a flow-controlled in-memory byte stream.</span><span class="hljs-comment">// For Lab 0, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab0`.</span><span class="hljs-comment">// You will need to add private members to the class declaration in `byte_stream.hh`</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;ByteStream::<span class="hljs-built_in">ByteStream</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity) : <span class="hljs-built_in">stream_capacity</span>(capacity),<span class="hljs-built_in">byte_written</span>(<span class="hljs-number">0</span>),<span class="hljs-built_in">byte_read</span>(<span class="hljs-number">0</span>),<span class="hljs-built_in">byte_stream</span>(<span class="hljs-number">0</span>),_end(<span class="hljs-literal">false</span>),_error(<span class="hljs-literal">false</span>)&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(capacity);&#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::write</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;data)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(data);    <span class="hljs-comment">//取其中的</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(data.<span class="hljs-built_in">size</span>(),stream_capacity - byte_stream.<span class="hljs-built_in">size</span>());    byte_stream.<span class="hljs-built_in">insert</span>(byte_stream.<span class="hljs-built_in">end</span>(),data.<span class="hljs-built_in">begin</span>(),data.<span class="hljs-built_in">begin</span>()+lens_byte);    byte_written+=lens_byte;    <span class="hljs-keyword">return</span> lens_byte;&#125;<span class="hljs-comment">//! \param[in] len bytes will be copied from the output side of the buffer</span><span class="hljs-function">string <span class="hljs-title">ByteStream::peek_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-keyword">return</span> std::<span class="hljs-built_in">string</span>(byte_stream.<span class="hljs-built_in">begin</span>(),byte_stream.<span class="hljs-built_in">begin</span>()+std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>()));&#125;<span class="hljs-comment">//! \param[in] len bytes will be removed from the output side of the buffer</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ByteStream::pop_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>());    byte_stream.<span class="hljs-built_in">erase</span>(byte_stream.<span class="hljs-built_in">begin</span>(),byte_stream.<span class="hljs-built_in">begin</span>()+lens_byte);    byte_read+=lens_byte;&#125;<span class="hljs-comment">//! Read (i.e., copy and then pop) the next &quot;len&quot; bytes of the stream</span><span class="hljs-comment">//! \param[in] len bytes will be popped and returned</span><span class="hljs-comment">//! \returns a string</span><span class="hljs-function">std::string <span class="hljs-title">ByteStream::read</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-function">std::string <span class="hljs-title">n</span><span class="hljs-params">(byte_stream.begin(),byte_stream.begin()+lens_byte)</span></span>;    <span class="hljs-built_in">pop_output</span>(len);    <span class="hljs-keyword">return</span> n;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ByteStream::end_input</span><span class="hljs-params">()</span> </span>&#123;_end= <span class="hljs-literal">true</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::input_ended</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _end; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::buffer_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_stream.<span class="hljs-built_in">size</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::buffer_empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_stream.<span class="hljs-built_in">empty</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::eof</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _end&amp;&amp;byte_stream.<span class="hljs-built_in">empty</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::bytes_written</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_written; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::bytes_read</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_read; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::remaining_capacity</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> stream_capacity-byte_stream.<span class="hljs-built_in">size</span>(); &#125;<span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;byte_stream.hh&quot;</span></span><span class="hljs-comment">// Dummy implementation of a flow-controlled in-memory byte stream.</span><span class="hljs-comment">// For Lab 0, please replace with a real implementation that passes the</span><span class="hljs-comment">// automated checks run by `make check_lab0`.</span><span class="hljs-comment">// You will need to add private members to the class declaration in `byte_stream.hh`</span><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span>... Targs&gt;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">DUMMY_CODE</span><span class="hljs-params">(Targs &amp;&amp;... <span class="hljs-comment">/* unused */</span>)</span> </span>&#123;&#125;<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;ByteStream::<span class="hljs-built_in">ByteStream</span>(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> capacity) : <span class="hljs-built_in">stream_capacity</span>(capacity),<span class="hljs-built_in">byte_written</span>(<span class="hljs-number">0</span>),<span class="hljs-built_in">byte_read</span>(<span class="hljs-number">0</span>),<span class="hljs-built_in">byte_stream</span>(<span class="hljs-number">0</span>),_end(<span class="hljs-literal">false</span>),_error(<span class="hljs-literal">false</span>)&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(capacity);&#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::write</span><span class="hljs-params">(<span class="hljs-keyword">const</span> string &amp;data)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(data);    <span class="hljs-comment">//取其中的较小值，data的长度，bytestream的容量和deque长度的差,保证其不超过bytestream容量</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(data.<span class="hljs-built_in">size</span>(),stream_capacity - byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-comment">//在deque的末尾插入data的数据</span>    byte_stream.<span class="hljs-built_in">insert</span>(byte_stream.<span class="hljs-built_in">end</span>(),data.<span class="hljs-built_in">begin</span>(),data.<span class="hljs-built_in">begin</span>()+lens_byte);    <span class="hljs-comment">//写入的长度增加</span>    byte_written+=lens_byte;    <span class="hljs-keyword">return</span> lens_byte;&#125;<span class="hljs-comment">//! \param[in] len bytes will be copied from the output side of the buffer</span><span class="hljs-function">string <span class="hljs-title">ByteStream::peek_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> <span class="hljs-keyword">const</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-keyword">return</span> std::<span class="hljs-built_in">string</span>(byte_stream.<span class="hljs-built_in">begin</span>(),byte_stream.<span class="hljs-built_in">begin</span>()+std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>()));&#125;<span class="hljs-comment">//! \param[in] len bytes will be removed from the output side of the buffer</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ByteStream::pop_output</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-comment">//其实在实际情况下并不需要求较小值，但是为了通过单个测试，必须要求较小值</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-comment">//删除这些数据</span>    byte_stream.<span class="hljs-built_in">erase</span>(byte_stream.<span class="hljs-built_in">begin</span>(),byte_stream.<span class="hljs-built_in">begin</span>()+lens_byte);    <span class="hljs-comment">//已经读取的数据增加</span>    byte_read+=lens_byte;&#125;<span class="hljs-comment">//! Read (i.e., copy and then pop) the next &quot;len&quot; bytes of the stream</span><span class="hljs-comment">//! \param[in] len bytes will be popped and returned</span><span class="hljs-comment">//! \returns a string</span><span class="hljs-function">std::string <span class="hljs-title">ByteStream::read</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-built_in">DUMMY_CODE</span>(len);    <span class="hljs-comment">//找到要read的长度byte_stream长度的较小值，尽可能地读</span>    <span class="hljs-keyword">size_t</span> lens_byte = std::<span class="hljs-built_in">min</span>(len,byte_stream.<span class="hljs-built_in">size</span>());    <span class="hljs-comment">//读取数据</span>    <span class="hljs-function">std::string <span class="hljs-title">n</span><span class="hljs-params">(byte_stream.begin(),byte_stream.begin()+lens_byte)</span></span>;    <span class="hljs-comment">//将读取后的数据弹出</span>    <span class="hljs-built_in">pop_output</span>(len);    <span class="hljs-keyword">return</span> n;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">ByteStream::end_input</span><span class="hljs-params">()</span> </span>&#123;_end= <span class="hljs-literal">true</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::input_ended</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _end; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::buffer_size</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_stream.<span class="hljs-built_in">size</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::buffer_empty</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_stream.<span class="hljs-built_in">empty</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">ByteStream::eof</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> _end&amp;&amp;byte_stream.<span class="hljs-built_in">empty</span>(); &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::bytes_written</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_written; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::bytes_read</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> byte_read; &#125;<span class="hljs-function"><span class="hljs-keyword">size_t</span> <span class="hljs-title">ByteStream::remaining_capacity</span><span class="hljs-params">()</span> <span class="hljs-keyword">const</span> </span>&#123; <span class="hljs-keyword">return</span> stream_capacity-byte_stream.<span class="hljs-built_in">size</span>(); &#125;</code></pre></div><p>实验结果如下：</p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Stanford CS144 Lab</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
